<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019eb946-848c-7604-ab72-37427ab6e9ab -->
* **cch-seed-check.yml: runner, steps, and extract job structure**: cch-seed-check.yml: all jobs use \`ubuntu-latest\`. \`extract-seed\` steps: checkout → pnpm setup → node 24 → \`pnpm install --frozen-lockfile\` → extract → test → PR creation. No compiler install — GCC available by default. \`scripts/cch-scan.c\` committed to repo. Native scan essential: JS fallback times out (120s+, OOM on 237MB binary); C scanner: 8-byte aligned ~10.8s, 1-byte aligned ~91.1s. \`notify-failure\` dedupes GitHub issues by version marker. CRITICAL: Capture 2+ oracle pairs; scan for seed matching BOTH to eliminate false positives. Algorithm: \`xxHash64(body\_with\_cch=00000, seed) & 0xFFFFF\`. Billing header: \`x-anthropic-billing-header: cc\_version=X; cc\_entrypoint=Y;\[ cc\_workload=Z;] cch=00000;\`. Real cch write at \`0x2e0687e\`; seed confirmed \`0x4d659218e32a3268\` via gdb. TRAP: Use hardware watchpoint on placeholder bytes — software breakpoints at nearby addresses never fire. CRITICAL: \`OraclePair.body\` MUST be raw bytes (\`Uint8Array\`/\`Buffer\`), NOT a string. \`body += chunk\` corrupts multibyte UTF-8 across TCP chunk boundaries. TWO ROOT CAUSES proven: (1) encoding bug corrupts some bodies; (2) the binary hashes a different buffer (\`r14\`) than the wire body — 2-pair byte-perfect sc \[truncated — entry too long]

<!-- lore:019eb931-7257-7ef6-87db-b2dd034c3ea7 -->
* **embedding-worker.ts: self-heal retry for corrupt model — skipped for vendored binaries**: embedding-worker.ts self-heal retry: \`initPromise\` wraps \`loadPipeline()\`; on catch, if \`!vendorModel && isCorruptModelError(msg)\`, calls \`purgeCachedModel()\` then retries once. NEVER post \`{type:'init-error'}\` before the retry — \`embedding.ts\` sets \`localProviderKnownBroken=true\` permanently on init-error. Only the final \`.catch\` may post \`init-error\`. Error classifiers are INLINED — Node ESM resolver cannot map \`./foo.js\` → \`./foo.ts\` for spawned workers. TRAP: \`/load model .\* failed/i\` is too broad — misclassifies 401/403 → purge→redownload→401 loop. \`embedding-worker.ts\` cannot be imported in tests (throws at module top-level if \`!parentPort\`). \`vendorModel=true\`: skip all purge/heal paths (model is read-only inside binary).

<!-- lore:019ebaff-f083-7b04-834a-6f23308561fd -->
* **pipeline.ts: system\[2] (context-bound LTM) construction and cache-bust root causes**: pipeline.ts system\[2] (ltmText) construction and cache-bust root causes: Built in step 6: \`ltmSessionCache\` miss → \`ltm.forSession(excludeCategories:\['preference'])\` → \`formatKnowledge()\` → stored in cache. Reorder-tolerant diff-pinning: compare entry-key sets (\`ltmEntryKeys\`) instead of \`textDiffRatio\`. Pin reused when \`sameEntryKeys(pinned.entryKeys, cachedKeys)\`. \`entryKeys\` persisted as JSON in \`ltm\_pin\_keys\` (migration v39). ROOT CAUSE OF CHURN: \`forSession\` scores entries via vector similarity against per-turn session context (latest distillation + last 10 messages). With ~22 entries/~4417 tokens against ~3400 budget, ~7 entries are dropped each turn as scores shift → selected SET changes → pin busts every turn. Fix A+B (id-tiebreak, key-set comparison) fix reordering but NOT set-membership churn. True fix: stabilize selected set within a session (cache it, only re-select on knowledge change) or add hysteresis. \`ltmSessionCache\` was meant to do this but gets invalidated too aggressively.

### Gotcha

<!-- lore:019eb4d0-6fd9-7ef5-b22f-0f156eeca4b0 -->
* **cli/main.ts OPTIONS map: undeclared flags silently become booleans under strict:false**: Trap: \`parseArgs\` with \`strict: false\` treats any undeclared \`--flag value\` as \`{flag: true}\` with \`value\` falling into positionals — looks correct because \`--flag=value\` (equals syntax) still works. Fix: every flag used in ANY subcommand MUST be declared in the global \`OPTIONS\` map in \`packages/gateway/src/cli/main.ts\` with its correct type (\`string\` or \`boolean\`). Discovered in PR #696: \`--to\`, \`--project\`, \`--dry-run\`, \`--no-children\`, \`--min-confidence\` were missing, causing \`--to /path\` to parse as \`{to: true}\` with \`/path\` as a positional.

<!-- lore:019eb4d0-6fe3-7dc1-b80b-fad31fa29748 -->
* **distillations table schema: NOT NULL columns narrative, facts, source\_ids (not content)**: Trap: \`distillations\` table has no \`content\` column — looks like it should because other tables use \`content\`. Actual NOT NULL columns: \`narrative\`, \`facts\`, \`source\_ids\`, \`generation\`, \`token\_count\`, \`created\_at\`. Also: \`tool\_calls\` table uses \`tool\` (not \`tool\_name\`) and requires \`message\_id\`. Test helpers that insert into these tables must include all NOT NULL columns or get constraint errors. Discovered while writing \`packages/core/test/data-move.test.ts\`.

<!-- lore:019ebb4a-397c-76aa-89c8-93110713f3d4 -->
* **formatKnowledge: title-collision within a category produces non-deterministic render order**: Trap: \`formatKnowledge\` sorts entries within each category by title for byte-stable rendering. If two entries share an identical title, the sort returns 0 and their relative order is non-deterministic — text can swap between turns, busting the prompt cache. This is an edge case (duplicate titles are rare) but worth guarding. Fix: add a secondary sort key (e.g. entry id) as a tiebreak after title comparison, mirroring the id-tiebreak already used in the preference fast-path packing loop.

<!-- lore:019ebb26-22c5-7997-a28a-f6d6d4a9adc1 -->
* **git stash fails on untracked files without -u flag**: Trap: \`git stash\` silently fails for untracked files (\`??\` in status). Fix: use \`git stash -u\` (include untracked) or copy files to /tmp. In worktrees with mixed tracked+untracked changes, prefer committing to a temp branch over stash gymnastics. Also: \`git apply --check\` on an already-patched tree reports 'does not apply' — verify working tree is clean before checking.

<!-- lore:019ebafd-32b7-7468-bdb2-5c77ff6c4ccf -->
* **opencode-lore plugin: legacy export path causes getLegacyPlugins() to be invoked**: Trap: lore's plugin module exports named functions (\`applyLoreProviderConfig\`, \`probeGateway\`, \`LorePlugin\`) plus \`export default LorePlugin\`. OpenCode's \`getLegacyPlugins()\` iterates \`Object.values(mod)\` and treats ANY function as a server plugin (\`isServerPlugin\` = \`typeof === 'function'\`). So \`applyLoreProviderConfig\` and \`probeGateway\` are invoked as plugins; \`applyLoreProviderConfig\` returns \`undefined\` → pushed into \`hooks\[]\` → all dispatch loops (\`config\`, \`event\`, \`dispose\`) crash: \`j.config\` / \`A.event\` / \`j.dispose\` on undefined. Fix: lore's plugin must \`default export { server: LorePlugin }\` (plain object with \`server\` key) so \`readV1Plugin\` takes the v1 path and \`getLegacyPlugins\` is never called. Additionally, do NOT export helper functions (\`applyLoreProviderConfig\`, \`probeGateway\`) from the plugin's top-level index — they leak into the legacy loader.

<!-- lore:019ebb4c-83b8-7080-b1da-8a29f2f3f6a0 -->
* **pipeline.ts: warm-cache/pin DB divergence after recompute-reuse causes spurious re-pin on restart**: Trap: When \`sameEntryKeys\` is true (pin reused), \`ltmDirty=true\` but \`pinDirty=false\`, so DB ends up with \`ltmCacheText=NULL\` while \`ltmPinText\` is populated. On next gateway restart, warm-cache branch hits byte-equality check (\`pinned.formatted === cached.formatted\`) which is now false → spurious cache bust + \`entryKeys: undefined\` wipes the keys. Self-heals after one turn but defeats the 'survives restarts' goal. Fix options: (a) in recompute-reuse branch, also persist \`ltmCacheText = pinned.formatted\` so DB never diverges; or (b) on warm path, recompute \`cachedKeys\` from cached selection and compare keys instead of bytes. DB evidence: sessions with NULL \`ltm\_cache\_text\` but populated \`ltm\_pin\_text\` confirm S1 divergence. See also: \[\[019ebb4a-39be-7bd1-90b2-6ab874f3529a]].

<!-- lore:019eb94d-db25-72d2-ba71-f2b1ae570c51 -->
* **Recurring read failure: not\_found**: Recurring tool failure mitigations: \`read\` fails \`not\_found\` — verify paths with \`ls\`/\`find\` first. \`edit\` fails \`could-not-find-oldstring\` — oldString must match exactly including whitespace/indentation; re-read file immediately before editing. \`webfetch\` returns 404 for raw GitHub URLs — check branch/path. \`write\`/\`bash\`/\`edit\` fail 'aborted' when tool use is dismissed or interrupted — retry or adjust approach. \`question\`/\`plan\_exit\` fail 'user-dismissed-this' when user dismisses prompt. \`write\` fails 'other:the-user-has-specified' when a permission rule blocks the call — check tool permissions. All recur frequently; treat as expected transient errors.

<!-- lore:019eb4d0-6fe8-7e3e-b9dc-46608c201399 -->
* **suggest.ts: RegExp flag deduplication required before appending 'g'**: Trap in \`packages/gateway/src/suggest.ts\`: constructing \`new RegExp(pattern.source, ${pattern.flags}g)\` produces \`'gg'\` if source pattern already has \`g\` flag, causing \`Invalid flags supplied to RegExp constructor\`. Fix: strip \`g\` from \`pattern.flags\` before appending: \`pattern.flags.replace(/g/g, '') + 'g'\`. Applies anywhere a RegExp is reconstructed with forced flags.

<!-- lore:019ebaff-f08a-7740-a3a1-471c3524128c -->
* **system\[2] context-health note appended AFTER diff-pinning — bypasses cache stability**: Trap: diff-pinning logic (pipeline.ts:4382-4394) looks like it stabilizes system\[2] text. But step 7c (lines 4562-4590) appends a context-health note to \`ltmText\` AFTER the pin comparison, varying by \`result.layer\` value (wording: 'compressed' / 'aggressively compressed' / 'emergency compressed'). Even when the pin holds, the final \`system\[2]\` sent to Anthropic differs every turn if \`result.layer\` changes (e.g. layer 1→2→1). Since \`system\[2]\` has no \`cache\_control\` of its own (\`anthropic.ts:419-425\`), any change forces re-processing of \`system\[2]\` + all messages — only \`system\[0]\`+\`\[1]\` survive as cache reads. Fix: move the context-health note into a separate system block, or apply it before diff-pinning so the pin comparison sees the full final text.

### Pattern

<!-- lore:019eb4d0-6fed-71ff-baca-7151d61bf315 -->
* **api.ts resolveProject() priority and parseBody zstd support**: \`packages/gateway/src/api.ts\`: \`resolveProject()\` priority: (1) \`:id\` route param, (2) \`?git\_remote=\`, (3) \`?path=\`. POST dispatch: literal routes first, then parameterized. TRAP: \`moveSessions()\`/\`reassignKnowledge()\` update \`knowledge SET project\_id=?\` but leave \`knowledge\_transfers\` rows pointing to old project. \`moveSessions()\` BFS-expands children before moving; \`sessions\_moved\` return overcounts if BFS includes children from other projects. \`rebindActiveSession\` iterates only user-provided IDs, not BFS-expanded children. \`parseBody\<T>()\` supports optional zstd decompression via \`Content-Encoding: zstd\`.

<!-- lore:019ebb26-22d5-78b3-bb61-e128eea8ba06 -->
* **Branching strategy: create new branch from origin/main, not from feature branch**: When cache-bust or unrelated fixes need a separate PR, create a new branch from \`origin/main\` (not from the current feature branch). Workflow: back up changes to /tmp via \`git diff > /tmp/patch\`, create branch pointing at \`origin/main\`, switch to it carrying working-tree changes, then stage ONLY the relevant files — leave unrelated modified files unstaged. Avoids mixing unrelated commits into the new PR. Always fetch \`origin/main\` first to check if it advanced during the session. \`.lore.md\` changes from the background lore process should be excluded from PRs unless intentional.

### Preference

<!-- lore:019ebafe-90fd-772e-aa97-fc7a70383558 -->
* **Always call plan\_exit to signal completion of the planning phase**: The user consistently enforces the directive to call \`plan\_exit\` at the end of the planning phase. This appears as a repeated red-flag observation across every session, indicating the user treats it as a critical, non-negotiable instruction. When in plan mode, after completing exploration and writing the plan file, the assistant must explicitly call \`plan\_exit\` to signal to the user that planning is done. Failing to do so is treated as a violation. This applies universally across all plan-mode sessions regardless of the task being planned.

<!-- lore:019ebb47-ef8a-7456-a23a-55e653a31dd1 -->
* **Always document behavioral invariants as inline code comments at the point of enforcement**: When the user states a rule or constraint (e.g., 'always keep at least 3', 'never displaces actual search hits', 'always prefer recall over assuming'), they expect that exact directive to appear as an inline comment in the code at the precise location where the invariant is enforced — not just in docs, plans, or commit messages. The comment should be terse and verbatim (or near-verbatim) to the user's wording. This applies to formatting logic, scoring/budget logic, tool descriptions, and any other enforcement point. When implementing such rules, always add the explanatory comment co-located with the enforcement code.

<!-- lore:019eb936-7c9f-7f60-83a0-05442d818862 -->
* **Always enforce lowercase hex literals and symbolic named constants for magic values**: Always enforce lowercase hex literals and symbolic named constants for magic values: (1) lowercase hex throughout (e.g., \`0x4d659218e32a3268n\`, never uppercase), matching Biome/existing style; (2) repeated hex values extracted into named constants — no bare literal appears more than once; (3) generated/applied code must reuse existing named constants when value matches. Applies to hand-written and script-generated code including C source. Violations cause lint/style failures. CCH oracle \`cch\` values are always lowercase 5-hex — C code must produce lowercase (\`%05llx\`) to match.

<!-- lore:019ebb4c-83c4-72ac-872b-b3fdcb1136b1 -->
* **Always request a subagent for final pre-merge code review for objectivity**: Before merging a PR, Burak Yigit Kaya consistently requests a thorough, critical self-review of the code and PR description — and explicitly prefers using a subagent for this review to get more objective results. The review should cover: correctness of the core mechanism, any robustness gaps, consistency nits (e.g. bare literals vs. named constants), and verification of PR-claim accuracy. Only after the review passes (or gaps are fixed) does the user proceed to merge.

<!-- lore:019ebb2a-b7e4-768e-ba35-7ec0d84a1df3 -->
* **Always request read-only reconnaissance with exact file:line references before making changes**: When investigating bugs, understanding architecture, or planning features: always do a thorough read-only exploration phase first — no file modifications. Specify exact files, functions, and topics; responses must include precise file:line references and code snippets. This applies across debugging, feature planning, schema audits, and CLI exploration. Only after root cause is fully traced does the user move toward solutions.

<!-- lore:019ebb4c-83e5-7d30-b4cf-ca0a10d4493e -->
* **Always use MIGRATIONS.length for schema version instead of hardcoded constants**: When updating the database schema version, always reference \`MIGRATIONS.length\` dynamically rather than hardcoding a version number. This applies to any \`UPDATE schema\_version SET version = ...\` statement. The user explicitly stated this as a directive: 'always normalize to MIGRATIONS.length'. This ensures the version number stays in sync with the actual migration array length automatically, preventing drift when migrations are added or removed.

<!-- lore:019eb885-9bfe-7c6a-987c-6edda2862119 -->
* **Prefers recall over assuming you don't have the information**: Always prefer recall over assuming you don't have the information. Check memory BEFORE exploring the filesystem. Applies to: people, services, tools, projects, file paths, past decisions, prior approaches. Key paths: opencode binary source at /home/byk/Code/opencode/packages/opencode/src/ (lore's monorepo depends only on @opencode-ai/plugin SDK). @ai-sdk packages live at /home/byk/Code/opencode/packages/opencode/node\_modules/@ai-sdk/. When investigating, enumerate 4–7 specific targets, read actual files, report findings with exact file:line references. Grep/ripgrep sometimes returns empty despite files existing — fall back to direct file reads. Always verify paths with \`ls\` or \`find\` before reading. Before implementing anything, produce exact file:line references, code snippets, and structural understanding. Only after investigation does the user proceed to design or implementation.
