<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019e54f5-04e0-7b47-8058-65742c17c693 -->
* **agents-file.ts: AGENTS.md pointer section + .lore.md full export, hash-cached writes**: LORE\_FILE=\`.lore.md\` (agents-file.ts:44) always at project root — entirely lore-owned, no section markers needed. agentsFile config default path=\`AGENTS.md\` also at project root. exportToFile() writes a pointer section to AGENTS.md (not full entries) and calls exportLoreFile() as side effect — full entries go to \`.lore.md\`. splitFile() handles 3 known historical start-marker variants (ALL\_START\_MARKERS). exportLoreFile() skips write if content hash matches cached hash (kv\_meta keyed by \`'lore\_file\_cache:' + filepath\`). In hosted mode, config.load() never reads \`.lore.json\` from client-controlled paths (config.ts lines 272–274: \`if (!isHostedMode())\` guard) — prevents crafted config from altering gateway behavior.

<!-- lore:019e1c62-3208-7836-a531-f92d1bb20733 -->
* **Conversation import system: providers, detection, extraction pipeline**: Import system in \`packages/core/src/import/\`. \`AgentHistoryProvider\` interface: \`detect()\`/\`load()\`; providers in \`providers/index.ts\`. Extraction calls curator LLM sequentially per chunk, deduplicating via \`parseOps()\`/\`applyOps()\`. Idempotency via \`import\_history\` table (migration v19). Providers: Claude Code (\`~/.claude/projects/\`), OpenCode (SQLite), Aider (markdown), Codex (\`~/.codex/sessions/\` JSONL), Cline (VS Code globalStorage JSON), Continue (\`~/.continue/sessions/\` JSON), Pi (\`~/.pi/agent/sessions/\` JSONL). Auto-import via \`maybeAutoImport()\` in \`lore run\`. Copilot Chat skipped (opaque leveldb). Split-brain gotcha: \`isImported()\` checks local DB but \`recordImport\` writes to remote.

<!-- lore:019e54f5-04d5-728e-a92e-25bde9b441fe -->
* **curator.ts: consolidation scope, dedup, action tags, and entry size limits**: curator.ts MAX\_ENTRY\_CONTENT\_LENGTH=1200 chars (~400 tokens); entries exceeding this are truncated with \` \[truncated — entry too long]\` suffix in applyOps(). consolidate() excludes cross-project entries (includeCross=false) — only merges/trims project-scoped entries. applyOps() guards against mutating entries owned by a different project\_id. Post-curation dedup sweep runs when result.created > 0; records auto-signals for adaptive threshold calibration. buildActionTagContext() scans distillation observations for \`\[verb-noun]\` action tags across distinct sessions (excludes current), reports tags appearing in 2+ prior sessions. curator.run() uses p-limit(1) per session via curatorLimiter; skips if already busy. maxEntries default=25 (config.ts). AUTO\_SIGNAL\_MIN\_SIMILARITY=0.80, AUTO\_SIGNAL\_MAX\_PAIRS=50, MIN\_CALIBRATION\_SAMPLES=20.

<!-- lore:019e550f-29b4-768d-ade6-4e46c014effe -->
* **DB schema current version: 26 migrations, key columns per migration**: DB schema current version: 27 migrations. Key migrations: v2=distillations.observations; v4=session\_state(force\_min\_layer); v5=distillations.archived; v7=distillation\_fts FTS5; v8=knowledge.embedding+kv\_meta; v10=lat\_sections\_fts+knowledge\_refs; v11=F3b chunk separator; v12=distillations r\_compression+c\_norm; v13=metadata table; v14=projects.git\_remote+path\_aliases; v15=warmup\_histograms; v16=temporal\_messages.embedding; v17=distillations.call\_type; v18=session\_state 10 cost/savings cols; v19=import\_history; v22=projects.last\_import\_at; v23=session\_state LTM cache/pin+consecutive\_text\_only\_turns; v24=session identity+gradient state; v25=dedup\_feedback; v26=session\_state.parent\_session\_id+is\_subagent; v27=knowledge +11 cols + team\_knowledge + team\_config tables. CONFLICT: feat-entity-registry branch also targets v27 — whichever merges second must bump to v28. \`migrate()\` normalizes to MIGRATIONS.length; \`SCHEMA\_VERSION\` constant is dead code.

<!-- lore:019e54f5-051c-7641-aaad-7688451d5021 -->
* **Gateway complete HTTP endpoint inventory**: Gateway HTTP endpoints. LLM Proxy (server.ts): POST /v1/messages (Anthropic), POST /v1/chat/completions (OpenAI Chat), POST /v1/responses (OpenAI Responses API), POST /v1/compact ({project\_path, previous\_summary?}→{summary} or 404/502), GET /v1/models (passthrough). Health: GET /health→{status:'ok',version}. REST API (api.ts, /api/v1/): GET projects/stats/knowledge/sessions/distillations/recall/import-history; DELETE knowledge/:id/sessions/:id/distillations/:id/projects/:id; POST projects/merge (disabled hosted), reindex, import/extract, import/record, projects/:id/clear, projects/:id/dedup. UI: GET+POST /ui/\*. Project resolution in api.ts: 1) :id URL param, 2) ?git\_remote=, 3) ?path=. All pipeline handlers return internal Anthropic-format; translation to OpenAI/Responses format happens after pipeline returns.

<!-- lore:019e458b-fe1a-77fe-886b-37ef1817e7ca -->
* **Gradient tool\_use/tool\_result pairing: reconstruct-after-eviction pattern**: Gradient tool\_use/tool\_result pairing and layer selection: \`tryFit()\` has no pair-keeping logic; safety via: (1) \`resolveToolResults()\` merges tool result data onto assistant tool parts, strips user-side \`tool\_result\` blocks; (2) \`loreMessagesToGateway()\` reconstructs pairs from surviving assistant tool parts; (3) \`removeOrphanedToolResults()\` validates both directions. \`sanitizeToolParts()\` converts pending/running tool parts to error state. Layer 4 NEVER strips tool outputs — causes infinite tool-call loop. Current agentic turn (\`currentTurnStart()\`) is ALWAYS fully included in Layer 4 even if it alone exceeds tail budget — dropping it causes the same infinite loop. Layer 4 tail budget: \`clamp(usable \* 0.25, 2\_000, 8\_000)\`. Pre-pass \`deduplicateToolOutputs()\` runs before layer selection. \`cleanParts()\` applied but tool outputs never stripped. Layers 0–4 defined in COMPRESSION\_STAGES table.

<!-- lore:019e5544-4bc6-7ae6-ba2c-8d4493f6e1cf -->
* **gradient.ts: consecutiveBusts lifecycle — init, mutation, persistence, and DB-restore skip**: \`consecutiveBusts\` in \`gradient.ts\` SessionState: (1) initialized to 0 in \`makeSessionState()\` line 298; (2) ONLY mutated in \`recordCacheUsage()\` line 144 — increments when \`bustRatio > 0.5\`, resets to 0 otherwise; (3) \`recordCacheUsage()\` called from \`pipeline.ts:postResponse()\` after each successful API response; (4) persisted to DB via \`saveGradientState()\` repurposing dead \`dynamicContextCap\` column (diagnostics only); (5) NEVER restored from DB on \`getSessionState()\` — lines 329-333 explicitly skip it with comment: stale values from prior process (different cache state) cause false \`unsustainable\` warnings. \`unsustainable\` is derived ONLY from \`consecutiveBusts >= 5\` across all 4 return paths in \`transformInner()\`. Per-session isolation via \`Map\<string, SessionState>\` — no cross-session sharing.

<!-- lore:019e54f5-04be-7a9f-9e62-177d65d5db9f -->
* **LTM forSession() budget, phases, and safety-net constants**: \`forSession()\` (ltm.ts): budgets: \`budget.ltm\`=0.05, \`budget.preferenceLtm\`=0.02 (200k ctx → ~7,650/~3,060 tokens). Phase 1: architecture entries up to ARCH\_BUDGET\_FRACTION=0.2. Phase 2: remaining entries by score descending. Phase 3: .lore.md sections. HEADER\_OVERHEAD\_TOKENS=15 subtracted upfront. Hard cap: \`if (used + cost > maxTokens) continue\`. PROJECT\_SAFETY\_NET=5: top 5 project entries by confidence always included. NO\_CONTEXT\_FALLBACK\_CAP=10. Preference-only fast path skips relevance scoring. IF-2: global entries (pid=null) must be cross\_project=1. \`vectorSearch()\` has no project\_id filter (intentional); dedup threshold for Nomic v1.5 is 0.935. pipeline.ts injects LTM as 3 system prompt blocks: \[0]=host prompt (no cache), \[1]=stable preferences (1h cache\_control), \[2]=context-bound LTM (no cache, diff-pinned, deferred to turn 2+). Turn 1 only injects preferences. \`ltmSessionCache\` always deleted and replaced unconditionally on Layer 4 refresh.

<!-- lore:019e1de2-7650-76c3-9416-40d905dd998f -->
* **OpenAI streaming translation: stateful SSE translators in stream/openai.ts**: OpenAI streaming translation — stateful SSE translators in \`stream/openai.ts\` and \`stream/openai-responses.ts\` consume Anthropic SSE and emit OpenAI-format SSE. \`effectiveProtocol\` in \`UpstreamResult\` dispatches to the right translator. All translators must implement: (1) \`cancelled\` flag + \`cancel()\` aborting upstream via AbortController; (2) \`safeEnqueue()\` no-ops if cancelled; (3) error catch emitting protocol-appropriate terminal event. Pipeline always returns internal Anthropic-format — translation happens after pipeline returns. Parsing traps: (1) \`parseOpenAIRequest\` coerces non-string content to \`""\` — array-format system/developer messages silently dropped; (2) \`buildOpenAIMessages\` pushes tool\_result blocks before flushing textParts/toolUses; (3) \`parseOpenAIResponsesRequest\` reads system from top-level \`instructions\`; developer/system role messages in input array treated as user messages.

<!-- lore:019e4586-82db-796f-a3cd-d65f790276bf -->
* **OpenCode x-session-affinity is a per-process nanoid — not stable across restarts**: (architecture) OpenCode x-session-affinity is a per-process nanoid — not stable across restarts. The Lore plugin (\`packages/opencode/src/index.ts\`) never touches this header. \`input.sessionID\` in plugin hooks is OpenCode's persistent DB \`Session.id\` — stable across restarts. When OpenCode restarts, new nanoid causes Tier 1 in \`identifySession()\` to create a brand-new Lore session, orphaning prior distillations/gradient state. Fix: inject \`input.sessionID\` as \`x-lore-session-id\` in the \`chat.headers\` hook. Tier 1 upgrade-path trap: when plugin starts sending \`x-lore-session-id\` alongside \`x-session-affinity\`, prior sessions indexed under \`x-session-affinity\` are invisible — Tier 1b must check both header names.

<!-- lore:019e1952-398c-753f-b92c-4a5fa5ecf15f -->
* **Pi plugin: which providers can be proxied through the gateway**: Pi plugin gateway proxy compatibility by wire protocol. \*\*Proxiable\*\*: \`anthropic\` → \`/v1/messages\`: \`anthropic\`, \`fireworks\`, \`github-copilot\`; \`openai-completions\` → \`/v1/chat/completions\`: \`deepseek\`, \`xai\`, \`groq\`, \`cerebras\`, \`openrouter\`, \`huggingface\`, \`opencode\`, \`opencode-go\`; \`openai-responses\` → \`/v1/responses\`: \`openai\`, \`azure-openai-responses\`, \`openai-codex\`, \`azure-openai\`, \`lm-studio\`, \`ollama\`. \*\*Cannot proxy\*\*: \`google\`, \`google-vertex\`, \`amazon-bedrock\`, \`mistral\`. \`registerProvider(name, { baseUrl })\` overrides base URL. Gateway routes by URL path only.

<!-- lore:019e4c7d-bfae-7c96-a491-e57cef38d84d -->
* **pipeline.ts: per-turn auth binding, billing prefix capture, and session bootstrap sequence**: pipeline.ts key behaviors: (1) \`extractAuth()\`→\`setLastSeenAuth\`+\`setSessionAuth\` every request; (2) \`captureBillingPrefix(sessionID, req.system)\` per-session; (3) new sessions: \`fingerprintMessages()\`, \`learnHeaders()\` Tier 2 bootstrap, \`tryImportKnowledge()\`; (4) \`stripContextWarnings()\` before forwarding; (5) 1h TTL→\`effectiveCacheWriteCost×2\`. Background worker 401: \`AUTH\_ERROR\_CODES={401,403}\`; \`markAuthStale()\`→re-resolve→retry once. distillation.ts: \`run()\` serialized per-session via \`distillLimiter\`; \`resetOrphans()\` resets orphaned distilled=1 messages in batches of 500; \`skipMeta\` skips meta-distillation when upstream cache warm; urgent bypasses batch queue. TTL downgrade hysteresis: 1h→5m requires 3 consecutive short-gap turns (\`ttlDowngradeStreak\`); blocks if >50% tokens cached; resets on long-gap turn. Core test isolation: \`packages/core/test/setup.ts\` (preload) creates isolated temp DB via \`mkdtempSync\`, sets \`LORE\_DB\_PATH\`, \`afterAll\` calls \`close()\`+\`rmSync\`. Test project paths use namespaced strings like \`/test/ltm/ranking\`. \`ensureProject()\` 4-tier resolution: exact path→alias→git remote→create. \`LORE\_HOSTED\_MODE=1\`: \`getGitRemote()\` returns null, FS-touching func \[truncated — entry too long]

<!-- lore:019e4a07-0161-7795-961a-38dd0ef8ac69 -->
* **Sentry initialization and error routing in gateway**: Sentry in gateway: init in \`packages/gateway/instrument.ts:75-88\`. Two capture paths: (1) direct \`Sentry.captureException()\` in \`llm-adapter.ts:490\` for worker retry exhaustion; (2) any \`log.error(..., errorObj)\` auto-forwards via \`sink.captureException()\` (wired at instrument.ts:91-96). To filter transient errors (EPIPE, ZlibError, TimeoutError, 429/502 retries): add \`beforeSend\` callback to \`Sentry.init()\` in instrument.ts — single interception point. \`sentryEnabled\` guard controlled by \`SENTRY\_ENABLED\` env var.

<!-- lore:019e56c3-c6af-78fd-a312-8ed73905680b -->
* **Team mode knowledge promotion: both explicit sharing and auto-suggest; approval workflow**: Team mode knowledge promotion supports explicit sharing (user-initiated) and auto-suggest (system-initiated), with an approval workflow preventing noisy/wrong entries from reaching team DB. Turso sync lives in the gateway layer (not core/db.ts — synchronous bun:sqlite incompatible with async @tursodatabase/sync). Team topology: Personal DB = local SQLite synced to Turso cloud; Team DB = Turso cloud only. Same schema with nullable team columns for backward compat. Cross-project knowledge stays personal. Personal entries get score boost in forSession(). Sensitivity column is a product hint for auto-promotion, not a security boundary. Local NLP for entity detection REJECTED (heavy deps, underperforms on technical entities, duplicates curator LLM work). compromise.js also rejected. Current approach (curator detects → alias lookup → FTS5 fuzzy match) covers ~80% of cases. Entity clustering (issue #462) approved as path forward: reuses Nomic v1.5/Voyage/OpenAI embeddings.

<!-- lore:019e4bbd-ade4-7355-8fb1-87df54c8ea69 -->
* **X-Lore-\* headers: project path injection and resolution priority**: (architecture) X-Lore-\* headers: project path injection and resolution priority: Project path resolution priority in \`packages/gateway/src/config.ts\` lines ~246-274: 1) \`X-Lore-Project\` header (absolute path, max 1024 chars, strips trailing slashes/control chars), 2) inferred from system prompt (\`cwd\` JSON key, \`Working directory:\` prefix, \`.lore.md\` path, generic \`/home/\`/\`/Users/\` paths), 3) \`process.cwd()\` fallback. Source tagged as \`ProjectPathSource\`: \`"header" | "inferred" | "cwd"\`. Pipeline warns on \`cwd\` source (dedup via \`cwdWarned\` Set). \`agents.ts\` \`envVars()\` injects \`X-Lore-Project: \<cwd>\` + \`X-Lore-Git-Remote: \<remote>\` into \`ANTHROPIC\_CUSTOM\_HEADERS\`. Other headers: \`x-lore-session-id\`, \`x-lore-no-store\`, \`x-lore-upstream-url\`, \`x-lore-recall-invoked\` (response). \`lore run\` always runs locally; remote mode delegates to existing remote gateway but computes git remote locally.

### Gotcha

<!-- lore:019e1c27-967c-7eb4-bd0e-afb195823970 -->
* **Bun NAPI crash on process.exit() — use safeExit() via libc \_exit()**: Bun NAPI crash on process.exit() with fastembed — use safeExit(): Loading fastembed (onnxruntime NAPI bindings) causes a C++ panic on \`process.exit()\` because Bun runs NAPI teardown destructors that throw. Fix: \`packages/gateway/src/cli/exit.ts\` exports \`safeExit(code)\` — uses \`\_exit()\` from libc via \`bun:ffi\` under Bun, falls back to \`process.exit()\` under Node.js. All gateway exit paths must use \`safeExit()\`. Do NOT call \`embedding.resetProvider()\` in test teardown \`resetPipelineState()\` — move \`resetProvider()\` to \`shutdown()\` in \`start.ts\` only. \`resetPipelineState()\` must preserve the 'fastembed unavailable' cached state.

<!-- lore:019e54e4-fe50-71e7-ab69-49ac97cf32f3 -->
* **consumeSSE retries counter never resets after successful reconnection — permanent failure after 10 total disconnects**: Trap: \`retries\` in \`consumeSSE\` (src/commands/local/server.ts:469) increments on each disconnect but never resets on successful reconnection. After 10 total disconnections (even with successful reconnects between them), the client permanently stops. Looks correct because 10 retries sounds like a per-attempt limit. Fix: reset \`retries = 0\` on successful connection (first message or \`open\` event) so it tracks only consecutive failures. Also: initial SSE connection should fail fast on \`'error'\` result rather than retrying — fixed in commit d967edd51.

<!-- lore:019e50e7-6250-779e-a842-b1b5baf87295 -->
* **ONNX OOM error code 284432024: single oversized text bypasses batch budget guard**: (gotcha) ONNX OOM (Nomic v1.5): pads ALL batch texts to longest sequence — one oversized text → huge tensor → OOM. Fix chain: (1) \`LOCAL\_MAX\_CHARS=8192\` in embedding.ts:38; (2) worker OOM-retry loop: attempt=0 full texts; on OOM, \`maxTokens = 4096 >> attempt\` (4096→2048→1024→512), \`OOM\_MAX\_RETRIES=3\`; (3) \`truncateTexts()\` skips texts ≤ maxTokens chars; (4) \`nextBatch()\` adaptive batching caps \`batch\_size×max\_tokens\` at \`MAX\_BATCH\_TOKEN\_AREA=4096\`, always includes ≥1 item; (5) \`safeLocalTruncate()\` backs up on high surrogate boundary. \`isOomError()\` matches \`/^\d{6,}$/\`. Known codes: 284432024, 287180544, 144786472.

<!-- lore:019e1de4-cada-77cf-ac3b-79ab96b963e4 -->
* **Query expansion disabled by default AND LLM client not wired in gateway (IF-5 fix)**: Two co-required fixes for query expansion (IF-5): (1) \`queryExpansion\` default in \`packages/core/src/config.ts\` must be \`true\` in BOTH the field-level \`.default()\` AND the section-level \`.default({})\` — section-level overrides field-level in Zod. (2) \`executeRecall()\` in \`packages/gateway/src/recall.ts\` must accept an optional \`LLMClient\` param and pass it to \`runRecall()\` — without it, \`if (searchConfig?.queryExpansion && llm)\` is always false. Both \`pipeline.ts\` call sites (streaming and non-streaming) must pass \`getLLMClient(config)\`.

<!-- lore:019e2e20-95b3-7a9d-ab38-77d87eafecc4 -->
* **splitSegments() infinite recursion on oversized single messages**: (gotcha) distillation.ts splitSegments() infinite recursion on oversized single messages: recurses infinitely when a single message exceeds maxSegmentTokens (16384). findSplitIndex() returns messages.length (=1), so left = messages.slice(0, 1) produces an identical recursive call. Triggered on large tool outputs (~49KB+). Fix: add base case after the totalTokens <= maxTokens guard — \`if (messages.length <= 1) return \[messages]\`. The oversized message becomes an indivisible segment.

<!-- lore:019e49d5-15c4-78d1-8bbc-f43fde34d9f1 -->
* **startServer() returns plain object, not Promise — use startGateway() for programmatic async use**: (gotcha) Gateway startup API traps: (1) \`startServer()\` returns \`{ stop, port, hosts }\` synchronously — callers doing \`.then()\` get TypeError. Use \`startGateway()\` from \`cli/start.ts\` (returns \`Promise\<GatewayHandle>\`, handles port fallback, port file writing, existing-gateway detection). (2) Under Node.js, EADDRINUSE fires as async \`'error'\` event, never caught by try/catch, bypassing port fallback chain (3207→5673→0). Fix: wrap \`server.listen()\` in a Promise. (3) OpenCode/Pi plugins use \`startInProcess()\`; in monorepo Bun resolves \`@loreai/gateway\` to \`src/index.ts\`, npm resolves to \`dist/index.cjs\` (only post-build). Fix: use variable indirection \`const mod = '@loreai/gateway'; await import(mod)\`. Pi plugin must externalize \`@loreai/gateway\` in esbuild. Use port \`0\` in tests.

<!-- lore:019e49f0-1d65-7737-96da-cb4b04122681 -->
* **vitest-evals requires vitest and tinyrainbow as direct devDependencies**: (gotcha) vitest-evals traps: (1) \`vitest-evals\` requires \`vitest\` and \`tinyrainbow\` as explicit devDependencies (peer deps). (2) \`buildLoreContext(turns, contextWindow=200\_000)\` — second arg is a token count number, NOT a model string; passing a model string yields NaN → empty context. (3) \`createJudgeHarness\` \`run\` callback receives positional args \`(input, options)\` — destructuring \`{ input }\` yields undefined; fix: \`run: async (input, \_options) => { /\* use input.system, input.prompt \*/ }\`. (4) \`inflateScenario(scenario, opts?)\` — opts is \`{ targetTokens?, excludeKeywords? }\`, not positional args.

### Pattern

<!-- lore:019e56d6-9cca-7a77-aa1a-a5850daef395 -->
* **Gateway test patterns: unit vs integration vs subprocess isolation**: Gateway tests in \`packages/gateway/test/\`. Three patterns: (1) Unit: import functions directly, build fixtures inline — \`makeRequest()\`/\`makeUserMsg()\` factories in recall.test.ts and pipeline-tools.test.ts. (2) Integration: \`createHarness()\` starts real server on random port with replay interceptor, provides \`chat()\` + \`queryDB()\`. (3) Subprocess isolation: \`mock.module()\` calls isolated in subprocess files to avoid global cache pollution. DB isolation via root \`bunfig.toml\` preloading \`packages/core/test/setup.ts\`. \`GatewayRequest\` minimal required fields: \`protocol, model, system, messages, tools, stream, maxTokens, metadata, rawHeaders\`. Use port \`0\` in tests.

### Preference

<!-- lore:019e558d-a998-77aa-9a31-568c3339c3b5 -->
* **Always follow an explicit plan approval workflow before executing file changes**: Always follow an explicit plan approval workflow before executing file changes. Plans written to \`.opencode/plans/\<timestamp>-\<slug>.md\` before any file modifications, detailing design decisions, migration specifics, type changes, and implementation steps. Execution only begins after explicit user approval. During execution: numbered todo list with priority levels, incremental progress notes per step, final typecheck + full test suite verification (report pass/skip/fail counts, expect() calls, duration, bun version). Plans reference specific file locations, line numbers, and exact changes. Every modification must trace back to an approved plan item. For Sentry issues: plan → execute → submit non-draft PR → wait for CI → address review comments → repeat until clean → merge. File implementation plans as GitHub issues in BYK/loreai repo before starting work. Before writing new code, always explore existing patterns first: find relevant test files, read how similar constructs are built/mocked, understand framework choices (bun:test), and identify setup/isolation patterns.

<!-- lore:019e2168-2fa4-77bd-a557-9d6dbcb40d81 -->
* **Prefer WASM backend over native onnxruntime-node for compiled binaries**: \`binaryExternalsPlugin\` in esbuild redirects \`onnxruntime-node\` → \`onnxruntime-web\` via \`onResolve\` (static imports only — does NOT redirect dynamic \`import()\` calls) and patches transformers.js CDN fallback via \`onLoad\` to read \`wasmPaths\` from \`globalThis.\_\_LORE\_VENDOR\_WASM\_PATHS\_\_\` (object form \`{ mjs, wasm }\` with exact hashed \`$bunfs\` filenames — directory strings fail). WASM files embedded as Bun \`{ type: 'file' }\` assets. For npm/CJS builds, \`onnxruntime-node\` stays external. WASM is ~2x faster on batches than native. Importing \`onnxruntime-web\` explicitly alongside the redirect creates two ort instances — 'cannot register backend cpu using priority 10' error.
