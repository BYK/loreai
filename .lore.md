<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:01a08126-4de0-7a17-acbb-36db061a13de -->
* **#1711 Responses ingress boundary**: Chose streaming only \`/v1/responses\` and \`/v1/codex/responses\` over converting every JSON ingress route, because their shared OpenAI item normalizer can process \`input\` incrementally. Anthropic, Chat, Gemini, and compaction routes require distinct full-protocol parsing or intentionally retain exact body text for fallback replay. This reduces compressed/decoded/raw-tree peak allocations, but deliberately does not solve #1710’s post-parse full-history semantic replay: normalized \`GatewayRequest.messages\` are still complete.

<!-- lore:01a0266e-04b2-791c-b2be-02d2f63d969a -->
* **captureRequestUpstream requestOrder precedence**: Chose capturing session routing/policy with the synchronous \`requestOrder\` assigned in \`handleRequest\` over allowing asynchronous turn processing to overwrite session state on completion. Concurrent requests can finish out of order; completion-order writes look natural but let an older turn replace a newer route/policy decision. \`captureRequestUpstream()\` must preserve newest-request-wins semantics.

<!-- lore:01a0465f-5b87-78dd-b68a-c231f8d1675e -->
* **closeServer parser sockets**: Chose \`trackServerSockets()\` plus deadline-driven \`closeIdleConnections()\`, \`closeAllConnections()\`, and explicit tracked-socket destruction over \`server.close()\` alone because partial HTTP headers and upgraded sockets may never become an \`IncomingMessage\` and can keep shutdown pending forever. Track every connection until its \`close\` event; keep closure bounded and idempotent.

<!-- lore:01a03650-2683-7c63-a472-1bb84c8d8d67 -->
* **Consolidation opposing-rule invariant**: Consolidation never creates entries, never merges opposing rules, never deletes contradictions, never double-fires, and never busts the v45 prefix. Chose curator \`update\`/\`delete\` application with cooldowns over creating replacement entries or treating contradiction detection as deduplication, because opposing rules require user resolution and new entries can cause recreation/thrashing.

<!-- lore:01a03427-8813-7df7-8d2a-78d13cbdd483 -->
* **Conversation cache layout, TTL, and LTM pin invariants**: Use the 1h conversation-cache TTL; 5m causes avoidable churn. The distilled prefix always occupies transformed-array positions \[0,1], durable knowledge deltas stay near the tail, and orphan removal never touches the prefix. Meta passthrough bypasses buildAnthropicRequest and remains uncached. Persisted ltmCacheText must equal ltmPinText across restart and re-pin; the pin is baseline-only metadata and is never sent upstream. ltm.forSession() must not bust system\[2] when the selected entry set is unchanged, except for byte-identical re-anchoring. Moving deltas forward, caching passthrough, or independently rendering cache and pin text destabilizes cached bytes and can cause prefix drift or cache spirals.

<!-- lore:01a06451-eb10-7e6f-8b5d-dba33ed07367 -->
* **db() raw initialization order**: Chose raw SQLite setup followed by delayed tracing/singleton publication over wrapping or assigning immediately because migrations and TEMP-trigger setup must never be traced/re-entrant, and failed migration must retry. Register \`lore\_current\_tenant\`, preflight FTS5, migrate, install sync capture, load sqlite-vec, recover authoritative vec0, then trace and publish.

<!-- lore:01a03b4c-373a-719a-9844-2113a4fb29e2 -->
* **derivedMessageId temporal identity**: Chose tenant/project/session/source-derived \`temporal\_messages.id\` over treating caller \`source\_id\` as globally unique because sync, vector rows, distillation links, and multiple tenants require stable collision-free storage keys. Durable temporal jobs must key the derived storage ID; \`source\_id\` lookup is non-unique and must remain scoped by tenant, project, and session. Caller values resembling Lore IDs are still hashed as source input.

<!-- lore:019f1a70-b05d-7756-aa08-d9d57fb9cbb0 -->
* **Embedding token batching and temporal chunk cap**: Use embedInTokenBatches for all embedding paths, including temporal messages, to prevent ONNX padding all inputs to the longest sequence and causing OOM. Batch by token area (MAX\_BATCH\_TOKEN\_AREA 4096), but always admit the first item so the loop progresses. Preserve input order and call storeTemporalChunks once. Cap temporal chunks at 64: retain the first 63 and join the remainder into one tail; an oversized tail may occupy its own batch and is truncated downstream.

<!-- lore:01a03afa-0af5-774e-9bc4-d8d479a37edf -->
* **Embedding worker CJS/ESM resolution**: Chose \`\_\_filename\` for CJS and \`import.meta.url\` for ESM over one universal worker-relative path because Bun/tsx ESM workers must resolve from the module URL while bundled CJS always defines its filename. Retain the CJS shim so source remains natural; do not replace both branches with \`import.meta.url\` or runtime cwd resolution.

<!-- lore:01a05a11-ca8f-7c85-ada1-0bd58524ef63 -->
* **Embedding worker retirement barrier**: Chose a confirmed worker-exit barrier before every replacement—including \`respawnForWasm()\`—over clearing \`worker\`/\`initPromise\` before termination because concurrent \`ensureWorker()\` can spawn WASM while native inference still owns the model. Keep retirement visible to every request until exit; never issue a second query during the shutdown window. If bounded termination cannot confirm exit, latch the provider unavailable rather than overlap models.

<!-- lore:01a05a11-cad7-7b7a-8ced-e799b0e057e8 -->
* **embedding worker stdio drain**: Chose silently calling \`resume()\` on owned embedding-worker stdout/stderr over logging sanitized or rate-limited lines because native dependencies can emit model paths, provider diagnostics, IDs, and process state, while every core log reaches persistent storage and \`LogSink\`. Streams must still drain continuously with no payload-observing \`data\` listeners so full pipes cannot block workers; retain only fixed categorical lifecycle messages.

<!-- lore:01a03ae6-3bb6-76d0-a294-c2bbd20efff7 -->
* **embedding-units 64-chunk cap**: Chose structural \`"\n\x1f"\` boundaries and a 64-chunk temporal cap over splitting on tool-looking text, dropping overflow, or allowing unlimited vec0 rows because tool output may mimic headers and unbounded fan-out expands storage and KNN widening. Keep units dependency-free; preserve prose/reasoning verbatim and reduced tool envelopes. Units \`0..62\` stay separate, while index \`63+\` is joined into the final chunk so evidence is retained. Embed through token-area-bounded \`embedInTokenBatches(..., "document")\`, then atomically store the complete vector set.

<!-- lore:01a05e22-e32b-701d-914e-9a031caa54bd -->
* **EmbeddingProviderError privacy boundary**: Chose one fixed, cause-free \`EmbeddingProviderError\` at remote, worker, and custom-provider boundaries over rethrowing raw failures because fetch diagnostics, malformed bodies, getters, and custom errors can contain provider-controlled content. Keep its own properties non-enumerable; \`String(error)\`, serialization, and \`cause\` must expose no diagnostics. Voyage may expose HTTP status, but never response bodies or \`statusText\`.

<!-- lore:01a05a11-c9de-7235-9324-5c8b17c720f7 -->
* **ensureVec0Store per-table repair**: Chose savepoint-atomic per-table vec0 repair over rebuilding all four indexes because one missing or malformed derived table must not delete compatible sibling vectors. Preserve recognized same-width tables, recreate only missing/incompatible siblings, infer width from healthy physical DDL, persist metadata, and roll back the entire repair on failure; genuine configured-width changes still rebuild every fixed-width index.

<!-- lore:01a01a58-ed37-74c7-903c-0317eddc20d1 -->
* **Entity mutation and deduplication dry-run boundary**: Entity mutation APIs dry-run by default; applying requires explicit ?apply=true. CLI dedup follows the same rule, using --yes only as the explicit apply switch. Preview must remain non-mutating and non-interactive even with --yes or --interactive: resolve projects read-only and never call ensureProject(), create rows, or reconcile lazy git remotes. Only apply mode may ensure a project. Test unknown paths and unsettled remotes so preview-safety regressions are observable.

<!-- lore:01a0266e-047c-7336-b58b-15792dc7246e -->
* **Foreground cancellation and hostile callback settlement**: Race recall, follow-up setup, provider work, and response-body reads against the foreground abort/deadline signal; cancellation must not wait for hostile or non-settling callbacks. On abort, return promptly and asynchronously roll back late recall results or cancel/release late readers without awaiting potentially hung cleanup. If setup wins immediately before abort, release its reader before throwing. Use the same combined signal throughout, stop scheduling new work, suppress cleanup rejection, and always remove listeners, timers, locks, and admission ownership. Test already-aborted requests, stalled bodies, handoff races, and non-settling cleanup with bounded settlement assertions.

<!-- lore:01a0247f-e975-7854-8997-50d4964ee51c -->
* **Gateway and upstream credential boundaries**: Gateway authentication uses a dedicated \`x-lore-gateway-token\`; provider bearer/API-key headers never authorize Lore. Remote access fails closed without an exact configured token match. Remove the gateway token before parsing and never forward, log, store, expose in URLs, or pass it via CLI arguments. Administrator \`LORE\_UPSTREAM\_EXTRA\_HEADERS\` are bound only to configured Anthropic, OpenAI, and worker bases. Caller-selected origins receive no credentials and must match the normalized-HTTPS \`LORE\_CALLER\_UPSTREAM\_ALLOWLIST\`, which rejects credentials, paths, queries, fragments, wildcards, and normalized duplicates; an unset allowlist denies all caller-selected origins.

<!-- lore:01a0247f-e8d0-79cc-80dd-38c1122f8b2d -->
* **Gateway startup, process-boundary, and daemon ownership invariants**: CLI startup alone owns StartOptions.processBoundary because signals and shutdown affect the host process; embedded plugins never request lifecycle control. Quiet in-process callers use { quiet:true, local:true }, route notices through file logging, and must not write lifecycle text into full-screen TUIs. Local mode clears hosted and remote-gateway state before validation so process-wide defaults cannot disable local discovery, imports, configuration, or watching. Daemon records contain pid, processIdentity, and an owner-only token: PID reuse makes numeric ownership insufficient. Reuse, termination, and discovery cleanup require the exact recorded generation, and records are removed only while every identity field still matches.

<!-- lore:01a03836-4f0b-724f-9a4c-a606620bfc78 -->
* **GlobalBus workspace scope**: OpenCode’s application event architecture uses the global event stream and passes workspace context with events. Chose globally scoped events with \`workspace\` metadata over isolated/local bus delivery because TUI, HTTP, sync, and control-plane consumers span instance boundaries; without workspace context, subscribers cannot safely filter or attribute events. \`EventV2Bridge\` is the compatibility boundary that publishes EventV2 values to \`GlobalBus\`.

<!-- lore:01a02957-7cc3-7f5f-84f4-e0828c99f3b3 -->
* **handleCompaction project binding and keepalive streaming**: Before \`initIfNeeded()\`, restore the persisted project path; never initialize compaction from the incoming path-less probe, which can misattribute the session to gateway \`cwd\` or the unattributed bucket. Compaction does not change project binding because its preceding normal turn already persisted it. Open an Anthropic SSE stream immediately and send periodic \`ping\` keepalives while \`generateCompactionSummary()\` runs, including during 429 retries. Translate that stream for OpenAI, Responses, and Gemini clients through the established protocol translators rather than constructing native streams that may lose keepalive behavior.

<!-- lore:01a0266e-04ff-7eb2-903e-9283051212eb -->
* **handleRequest validation and special-route ordering**: \`handleRequestForTenant()\` and \`handleRequestInner()\` must reject missing or header-less input with HTTP 400 \`Malformed request: missing headers\` before authentication, routing, or admission; direct callers and fuzzers may omit \`rawHeaders\`. After validation, process gateway-owned \`/lore:\*\` commands before Claude Code side-channel passthrough, and side channels before structural-compaction detection. Commands must never reach upstream, while side-channel requests must bypass LTM injection, distilled prefixes, and compaction classification to avoid corrupting auto-mode and triggering Claude Code fallback behavior.

<!-- lore:01a042a2-c9cb-7776-a9ac-26a0fd3ad24c -->
* **hosted .lore.json exclusion**: Chose reading \`\<directory>/.lore.json\` only outside hosted mode over honoring client-selected project configuration everywhere because hosted request paths are client-controlled. A crafted file could change gateway budgets, models, or thresholds; hosted mode must use trusted server configuration and never inspect configuration beneath a client-provided directory.

<!-- lore:01a0265e-a7c0-71e6-837e-4e0abd13bf32 -->
* **identifySession remote credential binding**: Chose credential-scoped, tenant-bound remote resolution with distinct \`/\_\_lore\_unattributed\_\_/\<session>\` provisional buckets over header-only identity or gateway-cwd fallback because null fingerprints and pathless requests must never merge unrelated clients or projects. Provisional validation cannot mutate adopted state until identity is proven; remote rows must be fully tenant-bound, known unhydrated headers cannot fall through to aliases, and re-attribution must roll back when the turn commit fails.

<!-- lore:01a06451-eb49-784f-bcc8-1573666cec0e -->
* **installSyncCapture TEMP triggers**: Chose connection-local TEMP triggers and TEMP \`\_sync\_applying\` depth state over persistent triggers with shared suppression because gateway and CLI processes share one DB; a shared flag could suppress another process’s legitimate outbox capture. \`lore\_current\_tenant()\` must resolve live AsyncLocalStorage state on every statement, and trigger installation runs on every connection initialization.

<!-- lore:019edfbc-edd5-780d-ac81-07a35390121b -->
* **Knowledge storage, versioning, meta, pruning invariants**: pruneDeadEntries, pruneDeadEntriesAllProjects, and evictLowestValue MUST never delete cross\_project=1 or promoted rows (promoted\_at IS NOT NULL). This is a hard invariant: only unpromoted, project-scoped entries are eligible for eviction. DEAD\_CONFIDENCE\_FLOOR=0.2. Global sweep in idle.ts runs on first tick then hourly, gated on loreConfig().knowledge.enabled, wrapped in try/catch. Repeated directive, 34+ sessions.

<!-- lore:01a03ae6-3b47-7655-a3e0-ab86e2c0985e -->
* **LocalProvider memory-gated pool**: Chose lazy, container-aware embedding-pool growth over eagerly loading multiple ONNX workers because each model costs about 680 MB and host \`freemem()\` overstates capacity inside constrained containers. Worker 0 remains available; extra workers require concurrent demand and \`clampFreeToContainerLimit(...)\` headroom. \`memDivisor\` shares the memory budget, and upward re-probes never reach a prior OOM ceiling.

<!-- lore:01a01eab-4426-7e1e-9bc4-d20e33d16f8f -->
* **management peer authorization and remote access policy**: Authorize management and data-plane access from the numeric node:http socket peer before CORS preflight, lazy imports, body reads, authentication, session/storage work, or upstream calls. Never trust Host, Origin, Forwarded, or X-Forwarded-For for peer authorization. Non-loopback peers receive bodyless 404s unless remote management is enabled; enabled remote access requires exact normalized Host/port matching and rejects loopback browser Origins. Use origin scheme only for default-port normalization. Apply this fail-closed policy to exported startServer(), native upgrades, /, /api, and /ui.

<!-- lore:01a03afa-0b2e-7b09-90fb-bef2a3debf6b -->
* **nativeIntraOpThreads cgroup quota**: Chose cgroup-aware \`availableParallelism()\` limits only when it is below \`cpus().length\` over always setting ONNX Runtime threads to logical CPU count. Native ORT is cgroup-blind and can create host-core-sized pools in a 1-vCPU container, wasting RSS and causing context-switch thrash. Unconstrained hosts return \`undefined\`, preserving ORT’s physical-core default instead of raising it to the hyper-thread count.

<!-- lore:01a055af-aa91-72c3-9e96-26624a2b161c -->
* **normalEmbed FIFO retained-node bound**: Chose a removable FIFO capped at 64 retained normal-embedding waiters over a promise-tail chain because canceled callers can leave chain nodes after their numerical quota is released. The active owner is not counted; canceled waiters are physically removed, and release transfers ownership to the oldest live waiter. Normal and recall work receive a finite default provider deadline, while single-text recall bypasses only FIFO admission.

<!-- lore:01a020e7-aac7-76f8-a48a-4802c85e2bbd -->
* **OpenCode runtime dependency and packaging boundaries**: Keep runtime dependencies one-way: Schema → Core and Protocol; Core and Protocol → Server; Client runtime → Schema and Protocol only; \`sdk-next\` composes Client, Core, and Server. Never import Core or Server from Client. \`@loreai/core\` must support bundled Node-compatible CJS for gateway and raw TypeScript for OpenCode/Pi. Only \`packages/core/src/db/driver.node.ts\` may import \`node:sqlite\`; all other source uses \`#db/driver\` because Bun lacks \`node:sqlite\`. Test both bundled and raw-TypeScript consumers since either path can hide failures in the other.

<!-- lore:01a037e2-9268-7664-a445-ab01a3d18f28 -->
* **OpenCode v2 durable settlement**: OpenCode v2 separates durable terminal outcomes from ephemeral progress. Chose exactly one durable settlement event over reconstructing state from deltas because replay must remain bounded and deterministic: a newer turn supersedes stale incomplete rows, and the system must never resume an older assistant projection. \`session.tool.success\`/\`failed\` and \`session.compaction.ended\` settle replayable state; reasoning and compaction deltas remain live-only progress.

<!-- lore:01a080ba-7df8-7459-b527-3286546eb39a -->
* **parseOpenAIResponsesRequestChunks bounded spool**: Chose a 256 KiB native-parse spool plus \`Tokenizer\`/\`TokenParser\` large-body path over always buffering or always using a JS parser, because normal requests need exact \`JSON.parse\` behavior while large Responses/Codex transcripts must release decoded bytes. Below \`STREAMING\_PARSE\_SPOOL\_BYTES\`, use \`JSON.parse(Buffer.concat(...).toString("utf8"))\`; after crossing it, replay the bounded prefix once and stream \`$.input.\*\` with \`keepStack:false\`. Share \`createInputItemsBuilder()\` with native parsing so tool coalescing and reasoning provenance remain identical. A decoded-string accumulator or full raw \`input\` tree defeats the memory bound.

<!-- lore:019eeb85-6440-7760-912a-30c1cdc31a9f -->
* **Pipeline urgent-background tracking and reset drain**: Urgent distillation bypasses the background pause gate and limiter because prolonged degraded context is worse than one API call. Track those promises in inFlightBackground using finally cleanup. A non-fast reset must drain both limiter active tasks and a bounded settlement of inFlightBackground; draining either alone leaves work running. boundedSettle snapshots promises, resolves on timeout rather than rejects, and always clears its timer. Clear queued limiter work before settling active tasks.

<!-- lore:01a03ceb-4bc5-7274-a491-126925b7db40 -->
* **repartitionVec0Project atomic move**: Chose exact-primary-key DELETE→INSERT inside one transaction over \`UPDATE\` or partially committed batches because sqlite-vec cannot update partition keys and \`gcVec0DanglingRows()\` removes only orphans, never live mis-partitioned rows. Any failure must roll back the whole move. Keep \`REPARTITION\_BATCH = 500\`, chunk session scopes to 900 IDs, and no-op for equal projects, explicit empty scopes, or non-vec0 mode.

<!-- lore:019f9e12-5625-7f1b-ac14-bce720f527e5 -->
* **Repository mutation, validation, and adversarial review workflow**: resolveTrustedRemote: if disk has a git remote, ALWAYS use the on-disk remote value. A client-supplied x-lore-git-remote header can never override on-disk truth, preventing remote-param tampering. In local mode with no disk remote, drop the client remote entirely — prevents the git-remote magnet bug where a stale header incorrectly selects a remote.

<!-- lore:01a03e37-b468-7dda-98f9-e9707c6b721d -->
* **resolveReadMode storage authority**: Chose \`resolveReadMode()\` physical-layout authority over trusting \`vec.storage\_mode\` because forged \`blob\` metadata can target embedding columns already removed by committed vec0 cutover. Inspect physical state even inside transactions: committed vec0 and the connection latch override \`blob\`; transaction-local provisional state remains rollback-safe. If metadata or inspection fails, return blob mode only after proving base embedding columns exist—never use a catch-all blob fallback.

<!-- lore:01a0809c-124b-7e4e-ae9a-c8ba083c88de -->
* **Responses previous\_response\_id stateless replay**: Chose full transformed-history \`input\` replay over forwarding OpenAI Responses \`previous\_response\_id\`, because Lore is a stateless proxy that rewrites history for compression and recall; upstream continuation prepends server history, duplicates context, and defeats those edits. Codex sends full input items every turn and never relies on upstream item storage. Drop and warn on \`previous\_response\_id\` and unresolvable \`item\_reference\` rather than erroring; do not retain upstream items. The translated request intentionally omits \`store\`; the upstream builder authoritatively forces it to \`false\`.

<!-- lore:01a020f6-0b09-7c5b-9896-1f6d39cd6f56 -->
* **Responses recall stream isolation and depth handling**: Apply every parsed principal and continuation Responses event to its accumulator before delivery suppression; \`postResponse\` requires this authoritative internal state even for hidden recall, sparse-tool, and failed-chain events. Chose separate principal/follow-up accumulators over forwarding-only suppression because output indices collide and suppressed events still affect terminal reconstruction and byte accounting. Follow-up lifecycle events remain hidden while the principal owns one rebuilt terminal; at depth exhaustion replace recall blocks with marker text and preserve buffered visible output.

<!-- lore:019f04c9-26e4-794e-8d94-dd107b7bf454 -->
* **safeDeltaInsertIndex tail boundary**: Prompt-delta insertion and compression re-anchoring must never split an assistant \`tool\_use\` from the immediately following user \`tool\_result\`, including parallel tools and tail re-anchoring. Inserting at the requested index looks harmless, but Anthropic rejects separated pairs; walk backward to a safe boundary, preserve surfaced mutation history during re-anchor, and place delta blocks before assistant tool calls.

<!-- lore:019f324d-a433-76ef-a7d3-c9acc21c6335 -->
* **scope\_keys wrapped DEK immutability**: The scope\_keys trigger must reject updates that change wrapped\_dek without increasing key\_epoch (SQLSTATE 23514). Metadata-only updates and higher-epoch DEK rotations remain allowed. Do not make account\_escrow immutable: it must change on passphrase rotation and is recoverable, whereas same-epoch wrapped-DEK clobber can orphan ciphertext across devices. PostgreSQL BEFORE triggers fire alphabetically, so guard naming must preserve intended ordering.

<!-- lore:01a039c5-3a47-7ce4-b75c-a43a3ebb3ac0 -->
* **Sentry NODE\_ENV=test hard-off**: Chose \`NODE\_ENV === "test"\` as an unconditional Sentry hard-off over allowing \`SENTRY\_ENABLED=1\` to override it because test processes must never ship telemetry. Sentry transport uses \`globalThis.fetch\`; enabling it races mocked fetches, inflates call counts, and clobbers captured initialization. Worker threads are also disabled to prevent duplicate events and fetch races.

<!-- lore:01a020e7-ab76-741c-a37f-6ecdceabe20d -->
* **SessionV2 durable input, execution, and persistence invariants**: \`SessionV2.prompt()\` durably admits exactly one \`session\_input\` row before advisory \`SessionExecution.wake(sessionID)\`; \`resume:false\` is admit-only. The serialized runner promotes inbox rows into visible user messages and makes one explicit \`llm.stream(request)\` call per provider turn after reloading projected history. Never bridge through legacy \`SessionPrompt.loop(...)\`. Preserve project isolation and persist matching, parent-child, and replay state so restarts do not change behavior. Keep frozen prompt/LTM bytes and persisted delta positions cache-stable; bound or coalesce updates. Malformed persisted values require safe fallbacks rather than exceptions.

<!-- lore:01a03836-4ed4-788d-aae8-3a8687b29e92 -->
* **Sync EventV2 authority and persistence requirements**: Sync events must flow exclusively through EventV2 publication and replay, not ad hoc Bus.publish calls or sync-owned database writes. EventV2 is the authoritative boundary for sequencing, ownership/claiming, persistence, replay, and projections. \`events.persist=false\` only mitigates event-log storage growth: live publication and sequence assignment continue, but durable replay, duplicate-ID checks, EventTable-backed sync, and recovery are unavailable for omitted rows. Keep persistence enabled for remote workspace sync or replay participants; use sequence-only mode only for local-only instances until checkpoint/snapshot recovery exists.

<!-- lore:019edff0-4709-74cc-98eb-ad3bd46163c1 -->
* **Sync payload and versioning invariants**: Push rows as toRemoteRow(pickSyncColumns(table,row)), with is\_deleted:false and content\_hash/revision only for versioned tables. Never push scope\_id; the server fills it from auth.uid(). Conflict targets are scope\_id plus table ID columns. Strip remote-only columns before local apply and local-only columns before push. Soft-delete with update({is\_deleted:true}).match(decomposeId(...)). SyncTableMeta.versioned defaults true; knowledge\_entity\_refs is non-versioned and must omit content\_hash/revision or PGRST204 leaves the push cursor stuck.

<!-- lore:019f1fde-f17e-7835-a964-dfb1b4c697f5 -->
* **Temporal backfill queue admission**: PR #1111 (\`fix/temporal-backfill-pool-gate\`) replaces the session-activity idle-gate (PR #1108) with an embed-pool capacity gate. Root cause of old gate: 5 active sessions kept backfill parked continuously; cursor never advanced during 8+ min window. New signal: module-level \`\_recallEmbedsInFlight\` counter in \`embedding.ts\`. Incremented in \`embed()\` when \`inputType === "query" && texts.length === 1\`; decremented in \`finally\`. Exported as \`recallEmbedsInFlight()\` + \`\_setRecallEmbedsInFlightForTest()\` seam. Gate factory \`makeTemporalBackfillGate(deps)\` parks when \`isPaused() || isEmbedBusy()\`. Wired in \`pipeline.ts\` as \`isEmbedBusy: () => embedding.recallEmbedsInFlight() > 0\`. Backfill always uses \`"document"\` inputType → \`isRecall\` always false → no self-gating. Throwing gate predicate caught with \`break\` in \`awaitBackfillIdle\` — never wedges walk.

<!-- lore:01a042a2-c98e-76d5-9cc8-8064f2e62130 -->
* **temporal\_messages sync subset**: Chose syncing only \`temporal\_messages.id ∈ ⋃ distillations.source\_ids\` over syncing every temporal message because undistilled observations must never leave the device. The table is append-only, unversioned, has no capture trigger, is enqueued by distillation fanout, and encrypts content/metadata on the wire. \`distilled\` and \`restored\_at\` remain local-only: remote inserts are archival, while conflicts preserve native residency state.

<!-- lore:01a039c5-3a7d-7a85-ba93-6faaa5170889 -->
* **upstreamFetch Bun node:https**: Chose \`node:https.request()\` under Bun and timeout-disabled \`undici.fetch\` under Node over one shared fetch implementation because Bun native fetch has an unavoidable ~300-second inactivity timeout and real undici streaming hangs under Bun 1.3.14. The type-only \`Dispatcher\` import is erased and never bundles undici; \`getUndici()\` is Node-only. Tests use \`setUpstreamDispatcherForTest()\` because \`upstreamFetch\` bypasses the global dispatcher.

<!-- lore:01a03836-4f42-7294-9232-d1d312ccf5db -->
* **Value-delta sync checkpoints**: OpenCode sync should use value-delta synchronization rather than instruction checkpoints. Chose value deltas over checkpointing instruction state because deltas represent the actual changed values and compose with event-sourced replay; instruction checkpoints couple sync to a derived snapshot and can overwrite or diverge when updates arrive from multiple instances. This is the design direction established by commit \`96a9731947\`.

<!-- lore:01a03e37-b4a0-7f60-8b62-4edf872c7bbe -->
* **vec0 latch transaction frames**: Chose helper-owned nested latch frames plus shared \`connectionInTransaction()\` over a global provisional flag or one driver property because raw/nested rollbacks can publish stale vec0 state and Node/Bun expose different indicators. Check \`isTransaction\`, then \`inTransaction\`, then probe \`BEGIN DEFERRED; ROLLBACK\`; nested commits merge upward, only the outer commit publishes, rollback discards its frame, and state resets only on DB close/swap.

<!-- lore:01a03650-26f7-7567-bf8a-8bbbfbff81aa -->
* **Vector recall worker isolation and safe pool fallback**: Vector recall/LTM work must run off-thread so a slow vec0 SQLite search in one session never blocks another session's stream or the combined event loop. Test through \`\_setTestVectorWorkerFactory\`, never real node workers. Pool dispatch/search/read APIs must never reject: setup or dispatch failure resolves as unavailable, pool unavailability may use equivalent in-process work, and timeouts return an empty/sentinel result without rerunning an uninterruptible scan on the main thread. A slow timeout is not grounds to latch the pool broken; one wedged worker must degrade recall safely rather than hang streaming.

<!-- lore:01a03fde-4891-7b32-ae2f-b3038f34570f -->
* **warmupEmbedding startup race**: Chose a local-only fire-and-forget throwaway \`warmupEmbedding()\` during the startup-backfill delay over waiting for the first real embedding because local ONNX cold load takes about 21 seconds. Lazy loading looks cheaper, but a short session can race gateway teardown and never persist its \`distillation\_vec\` row; warmup moves provider readiness ahead of that lifecycle boundary.

<!-- lore:01a03650-2854-7dd2-8a73-f5b0dd747897 -->
* **worker retry policy 429**: Urgent and background worker calls share one Claude-Code-like retry policy: honor \`Retry-After\`, otherwise use jittered exponential backoff from 500ms through 32s with up to 10 retries. Chose one policy over a fast-failing urgent/background split because OAuth and Claude-Max 429s commonly need 30–60 seconds; quick failure only piles background work onto the same rate limit.

### Decision

<!-- lore:01a03cbb-3a4b-7668-afce-d7d37baf9392 -->
* **Adopted only when it is at least**: adopted only when it is at least as long as the existing content; otherwise only confidence is refreshed.

<!-- lore:01a03b12-a7fe-7da0-8f7c-d75722ba89e2 -->
* **Adopted or resumed sessions**: adopted or resumed sessions instead use a restored pin.

<!-- lore:01a020f6-0b7b-7928-b605-3a1b43a920b9 -->
* **Codex Responses compatibility and terminal validation**: Use observed OpenCode behavior as the compatibility reference for ChatGPT Codex Responses, while retaining Lore’s stricter public validation. Streamed lifecycle events build authoritative state. Under \`validation === "codex"\`, an explicit terminal \`response.output: \[]\` may retain streamed state only when every accumulated item is done. Reject empty snapshots with unfinished items, nonempty contradictory snapshots, and public-mode empty snapshots. Omitted Codex snapshots remain sparse-lifecycle compatible. Do not copy OpenCode’s unconditional terminal acceptance, which would weaken malformed or contradictory snapshot protection.

<!-- lore:01a0416b-355e-72d6-bafd-0a5488cf665b -->
* **embed L2 normalization**: Chose L2 normalization at the exported \`embed()\` chokepoint over normalizing in individual callers because JavaScript recall uses a bare dot product while sqlite-vec normalizes cosine distance internally. Caller-specific normalization looks flexible but permits divergent rankings; zero or non-finite vectors remain unchanged, while valid vectors are copied and normalized.

<!-- lore:01a0603d-5704-73f1-88b3-11ccc0e7995a -->
* **embedding fingerprint compatibility**: Chose versioned SHA-256 digests over raw \`provider:model:dimensions\` because model values may expose paths, URLs, credentials, or private metadata. \`checkConfigChange()\` must recognize only the exact equivalent legacy plaintext fingerprint, replace it with the digest without clearing vectors, and treat other legacy values as real changes; temporal policy remains independently versioned.

<!-- lore:01a03e22-9484-791c-804a-66467cb5baff -->
* **flock advisory locking**: Chose \`flock\` advisory locking over file-based lock ownership because the operating system automatically releases \`flock\` when the process or descriptor exits. File locks look portable and explicit, but stale lock files survive crashes and can wedge later operations.

<!-- lore:01a0809c-120a-77f5-8f92-cd3f3f20b7f6 -->
* **mayReencodeUpstream route trust**: Chose route-and-origin-aware upstream re-encoding over preserving every client \`Content-Encoding\`, because auto-translated or same-protocol foreign-provider routes can reject the client codec (for example OpenAI→DeepSeek). Re-encode only native passthrough with identical protocol and origin, or explicit \`X-Lore-Provider\`/\`X-Lore-Upstream-URL\` overrides; Vertex and Bedrock are provider-tagged. Empty, absent, and \`identity\` encodings remain uncompressed, and forwarded Content-Length must match actual emitted bytes rather than the client header.

<!-- lore:01a05255-1e78-77a5-99b6-3868ef877d47 -->
* **storeEmbeddingVec0 DELETE INSERT**: Chose exact-key \`DELETE\` then \`INSERT\` in \`storeEmbeddingVec0()\` over \`INSERT OR REPLACE\` or \`ON CONFLICT DO UPDATE\` because the pinned sqlite-vec build supports neither replacement form. Re-read ownership first and skip writes when the base row disappeared. The sub-millisecond crash gap may omit one vector without corrupting FTS: startup backfill repairs knowledge/entity/distillation omissions, while temporal omissions wait for the next content re-embed.

<!-- lore:01a042a2-c951-7976-9b64-77b1e8da4239 -->
* **temporal mixed-drain commit barrier**: Chose one drain-wide commit barrier before every per-message mutation over committing empty jobs before provider work because a later provider failure, abort, or malformed vector would partially retire a mixed drain. When any embedding job exists, complete inference, abort checks, cardinality checks, and full vector validation before committing empty or embedding jobs; only all-empty drains use the provider-independent fast path. Regression must mix empty and normal jobs, inject malformed output, and prove both queue rows and exact prior vector bytes survive.

<!-- lore:01a03ea0-8559-7bf3-80bd-5f7594f4ac17 -->
* **temporal\_vec project-only partition**: Chose \`project\_id\` as \`temporal\_vec\`’s sole partition key over \`(project\_id, session\_id)\` because sqlite-vec allocates a chunk per partition; 3,730 sparse session partitions caused about 10.5 GB of over-allocation. Keep \`session\_id\` as auxiliary metadata and \`chunk\_size=64\`; the project-only layout preserves partition pushdown while reducing each sparse partition from roughly 3 MB to 192 KB.

### Gotcha

<!-- lore:01a039c5-3af1-7d3f-9caf-e676e6988d29 -->
* **@sentry/bun complete Vitest mock**: Trap: partially importing and spreading real \`@sentry/bun\` exports looks faithful, but ESM namespace exports cannot be reliably redefined and eager SDK behavior can leak into tests. Fix: install a complete mock before importing the subject, with controllable \`isInitialized()\` and exact \`captureMessage\` assertions; this keeps tests deterministic and proves telemetry remains non-throwing.

<!-- lore:01a0603d-5668-7631-8f96-908be9b77341 -->
* **abort-ignoring provider quarantine**: Trap: releasing admission when the public cancellation race rejects looks bounded, but an abort-ignoring custom, Voyage, or OpenAI request remains alive and repeated calls accumulate detached work. Fix: quarantine the exact provider until its underlying promise settles; only cooperative internally controlled local workers may bypass this quarantine.

<!-- lore:01a055af-ab07-71c6-ad3b-b4b55f80438d -->
* **acquireNormalEmbedAdmission abort races**: Trap: checking cancellation only around FIFO admission looks sufficient because canceled waiters are removed, but recall bypasses admission and cancellation can occur during handoff; invoking the provider anyway hides an unauthorized call, while throwing after grant can leak ownership. Fix: check abort immediately before every provider invocation, release the exact acquired token on every post-grant abort, and assert exact provider-call counts for already-aborted recall and normal handoff races.

<!-- lore:01a02480-de4c-79a4-b9ae-efb97f6e35f3 -->
* **applyLoreProviderConfig headerTimeout override**: For Lore-routed openai and openai-codex providers, set options.headerTimeout = false authoritatively. OpenCode has already merged its inherited 60-second timeout before Lore's hook; Lore owns the foreground deadline. Configure routing through cfg.provider and options.baseURL, not provider-specific base-URL environment variables. Create an openai provider entry when absent so routing applies universally.

<!-- lore:01a03fde-48ca-7c59-a509-70f802d3d278 -->
* **applyRemoteTemporal archived distillation**: Trap: resetting remotely restored temporal rows to active looks consistent with new content, but archival rows are never re-distilled and active local rows must never become archival. Fix: \`applyRemoteTemporal()\` inserts with \`distilled=1\` and \`restored\_at=now\`, preserves local \`distilled/restored\_at\` on conflict, and atomically upserts the base row plus content-safe queue admission under sync suppression. Changed content invalidates stale vectors; unchanged content with a valid vector is not re-enqueued.

<!-- lore:01a041f0-5084-7f47-94bf-de467822c115 -->
* **applyRemoteTemporal ownership collision**: Trap: \`ON CONFLICT(id)\` looks safe because temporal IDs appear globally derived, but a colliding remote ID can rewrite an existing row’s project/session ownership, content, vector, and queue state. Fix: inside the \`apply\_remote\_temporal\` savepoint, read existing ownership before upsert and require exact incoming \`project\_id\` and \`session\_id\`; fail before every mutation and retain the post-upsert changes guard.

<!-- lore:01a042f4-a6bb-727a-ba5c-965399f5c792 -->
* **backfillTemporalEmbeddings NUL eligibility**: Trap: \`length(content) >= 50\` looks like the scheduler eligibility check, but SQLite TEXT length stops at U+0000, so eligible legacy rows can be omitted from the finite migration snapshot forever. Fix: share \`length(CAST(content AS BLOB)) >= TEMPORAL\_EMBEDDING\_MIN\_CONTENT\_LENGTH\` across maximum-rowid, backlog, total, and page queries; regress with \`x\0\` plus 100 \`a\` characters and prove admission, cursor/done progress, and \`0/1\`→\`1/1\` counts.

<!-- lore:019fdd65-c18a-7781-93fd-a7a8070ca14a -->
* **Codex worker streaming and SSE health classification**: ChatGPT Codex Responses workers require stream:true; stream:false returns 400. Use stream:true and store:false, parse LF and CRLF SSE framing, and validate buffered terminal events before projection. Failed, malformed, cancelled, or unterminated streams are upstream-error, never recordEmptyWorkerResponse.

<!-- lore:01a03fde-4859-7194-a921-caecf892cb4a -->
* **computeInitRetryDelayMs zero failures**: Trap: computing retry backoff with \`failures - 1\` looks like standard exponential delay, but callers can report zero failures and produce a negative exponent. Fix: clamp the failure count below 1 before exponentiation; \`computeInitRetryDelayMs(0, 30\_000)\` must return the 2,000 ms base delay.

<!-- lore:019ecab6-f928-7927-a39e-d601fb850af6 -->
* **computeMaxTokens must reserve thinking budget**: Anthropic max\_tokens covers thinking plus visible output. The EMA floor alone can permit all budget to be consumed by reasoning, producing finish:length with no text or tool part and silently ending the agent loop. For non-Claude-Code clients, floor at thinkingBudget + THINKING\_OUTPUT\_HEADROOM (8192), clamped by modelOutput. Claude Code is excluded; OpenCode does not send x-claude-code-session-id.

<!-- lore:01a03b4c-3702-7d19-b1be-94369f82d411 -->
* **configFingerprint omits text policy**: Trap: changing temporal text or projection policy looks sufficient to trigger \`checkConfigChange()\`, but \`configFingerprint()\` contains only provider, model, and dimensions. Fix: version temporal policy independently in queue fingerprints (currently \`temporal-embedding-policy-v2\`) and deliberately re-arm legacy admission only when required. Never blindly reset completed re-chunk metadata: unchanged-policy upgrades would invalidate and enqueue the whole corpus.

<!-- lore:01a04787-d229-7093-92b2-5d553d7ce452 -->
* **Continuation incomplete classification boundary**: Trap: removing the negation in \`incompleteRecallIndices = contRecallIndices.filter(index => !contPending.some(...))\` looks right because \`contPending\` sounds like unfinished work. Fix: keep the negation—\`contRecallIndices\` contains every recognized declaration, while \`contPending\` contains only done items with parsed arguments, so the expression intentionally selects declared-but-unfinished recalls. Reversing it exempts completed recalls from lifecycle validation. Classify \`follow\_up\_incomplete\_arguments\` only after all unrelated protocol validation succeeds; malformed JSON remains \`follow\_up\_protocol\`.

<!-- lore:01a06451-ebc9-7a9e-9c80-db5c02fc69f7 -->
* **curate log JSONL parsing**: Trap: parsing \`/lore:curate\` output with whole-file \`JSON.parse()\` looks correct for JSON, but the log is JSONL, so multiple records always fail and can hide errors or abort evals. Fix: parse non-empty lines independently, surface the first \`type:"error"\`, ignore malformed auxiliary lines, and derive score validity from primary result/session artifacts.

<!-- lore:01a042a2-ca0c-7ee0-b34a-4f7435d88e46 -->
* **db.close bounded cleanup**: Trap: implementing \`close()\` or cache invalidation through \`db()\` looks reusable, but the accessor opens SQLite during cleanup; retaining the normal 5-second busy timeout for \`wal\_checkpoint(TRUNCATE)\` can also stall shutdown behind a reader. Fix: inspect the singleton directly and no-op when absent; close with \`busy\_timeout=0\`, best-effort TRUNCATE checkpoint, then clear connection/path and reset vec state. A missed checkpoint is recoverable on reopen.

<!-- lore:01a080b7-607b-70c8-bd1a-920c0a0595ce -->
* **decodedCompressedRequestChunks drain listener leak**: Trap: awaiting only \`drain\` after \`decoder.write() === false\` looks like correct backpressure handling, but malformed replayed compressed input can emit \`error\` before \`drain\`, leaving the foreground decode hung; racing one-shot listeners also leaks losing listeners. Fix: route both decoder-replay and source writes through \`waitForDecoderDrain()\`, which settles on \`drain\`, \`error\`, or \`close\`, removes every listener on settlement, rejects closed decoders, and races the wait against abort. Regression-test a malformed 64 KiB gzip first chunk that enters backpressure, then assert prompt rejection, source cancellation, and an unlocked reader.

<!-- lore:01a080b7-603b-71f6-8638-243de182f1ab -->
* **decodedCompressedRequestChunks pending source.next cleanup**: Trap: relying on an async generator’s \`source.return()\` for cleanup after decoder, decompressed-limit, or downstream parse failure looks sufficient, but \`return()\` queues behind a pending \`source.next()\`. A never-closing upload can leave the source stalled/locked and delay the foreground 400 indefinitely. Fix: independently cancel or destroy the underlying request reader and decoder without awaiting hostile source settlement; generator return must not be the cancellation mechanism. Test a valid gzip fragment followed by invalid JSON or a non-settling source, and assert prompt failure plus released/cancelled source.

<!-- lore:01a04813-c829-7aa6-8528-6fb5b18ef4e1 -->
* **Deferred sync encryption scope**: Trap: retaining remote ciphertext when decryption, key resolution, scope lookup, or encryption mode fails looks privacy-preserving because plaintext is never written. Fix: on wrong/tampered ciphertext, corrupt or missing wrapped DEKs, absent scope keys, or encryption OFF, defer the affected table, freeze its cursor, and store nothing; advancing or storing ciphertext would strand undecryptable rows or expose ciphertext as knowledge content.

<!-- lore:01a06675-1e23-7f94-b0d7-d75de380c364 -->
* **embed() Float32 validation**: Trap: validating source numbers or trusting typed provider output looks sufficient, but finite doubles can overflow to \`Infinity\`, while local/custom providers can return wrong cardinality, width, type, \`NaN\`, or infinity. Fix: at the shared \`embed()\` chokepoint require exactly one configured-width \`Float32Array\` per input with finite converted entries, then L2-normalize; reject violations with fixed \`EmbeddingProviderError\`.

<!-- lore:01a055af-ab43-7f01-af1b-3863ef343e5a -->
* **embedding concurrency fixtures use query**: Trap: using unresolved \`"document"\` embeds in embedding-pool concurrency or memory-cap fixtures looks representative, but normal document work is intentionally serialized and one unresolved request leaks admission into later tests. Fix: use single-text \`"query"\` embeds only for fixtures whose contract requires simultaneous provider calls; keep dedicated scheduler tests proving production document serialization.

<!-- lore:01a04231-9cb3-767c-9c6a-d513039fb126 -->
* **embedding coverage log.info**: Trap: emitting startup embedding coverage with \`log.info()\` looks correct because it records the complete one-line statistics, but \`log.info()\` reaches stderr only when \`LORE\_DEBUG=1\`. Fix: use an always-visible reviewed stderr path for coverage while retaining embedded/TUI stderr silencing, because coverage failures must remain visible without debug mode.

<!-- lore:01a0603d-56c0-7346-8ebb-2f9c6f8b2d91 -->
* **embedding response body bounds**: Trap: a fetch deadline plus byte-capped response reader looks bounded, but after successful headers a hostile body can leave \`reader.read()\` pending forever; byte limits only help streams that emit data. Fix: race every body read against caller/provider cancellation, cancel fire-and-forget, release ownership without awaiting the hostile source, and retain the dimension-aware byte cap and strict payload validation.

<!-- lore:01a03afa-0abb-740d-8083-6f7004b99e4a -->
* **EmbeddingAbortError hostile-reason boundary**: Trap: inspecting \`AbortSignal.reason\` with \`in\`, getters, or direct property reads looks safe because normal reasons are \`DOMException\`, but hostile proxies/getters can throw private diagnostics through the sanitization boundary. Fix: catch every inspection failure and return categorical \`EmbeddingAbortError\` without \`cause\`; race provider settlement against cancellation, and give an elapsed deadline deterministic precedence over generic provider abort ordering.

<!-- lore:01a0416b-3526-7157-bf66-b93f3c77cfde -->
* **embeddingStatus live recall-load predicate**: \`embeddingStatus()\` must remain side-effect free and report \`retrying\` until a real embed succeeds; health checks never show false \`ok\` before recovery. Recall load counts only single-text query embeds through the shared worker predicate; write-time and backfill document embeds are excluded so backfill never gates against itself. Admission reads the live counter, not a snapshot, and every provider disappearance, OOM, timeout, or rejection releases ownership in \`finally\`.

<!-- lore:01a0436d-6c82-7db6-a3ac-09f4113fe417 -->
* **enqueueTemporalEmbedding transition result**: Trap: invalidating a temporal vector and incrementing \`scheduled\` before \`enqueueTemporalEmbedding()\` looks atomic inside the migration savepoint, but an identical queue row makes the upsert a no-op while still deleting a valid vector and inflating progress. Fix: enqueue first; only invalidate and increment when \`result.changes > 0\`. Always advance the cursor so identical queued work preserves vectors and restart progress.

<!-- lore:01a055af-aaca-77db-92f7-1b6e178c4dad -->
* **ensureEmbeddingReady provider retirement**: Trap: retaining admission until an unbounded readiness warmup settles looks safe because cooperative providers eventually release it, but a permanently stuck local worker wedges readiness and document admission forever. Fix: re-read provider identity after admission, propagate the readiness guard into the pool request, and retire the assigned worker when the finite readiness deadline fires; map pool cancellation back to fixed readiness \`EmbeddingAbortError\` without accepting late output.

<!-- lore:01a03fde-47b3-76e4-9b05-4c78c3e5acb3 -->
* **ensureVec0Store temporal markers**: Trap: resetting only \`lore:temporal\_rechunk.done\` and \`.cursor\` when recreating \`temporal\_vec\` looks sufficient, but stale \`.max\_rowid\` preserves the old snapshot ceiling, so newer messages are never re-admitted. Fix: when—and only when—\`temporal\_vec\` is created or recreated, reset done, cursor, and max\_rowid together; compatible shipped layouts retain all markers for later rebuild.

<!-- lore:01a01a58-ecf6-7c98-9641-92dff74d6673 -->
* **Entity CLI and merge safety invariants**: Enforce entity safety at every Stricli and legacy \`\_cli()\` dispatch boundary. \`delete\`, \`merge\`, dedup apply, alias removal, and relation removal require \`--yes\`; reject \`--json --interactive\`. Only dedup supports \`--dry-run\`, which is non-mutating, non-interactive, overrides \`--yes\`, and must not forward it. \`dedup --yes\` is automatic; \`dedup --interactive --yes\` remains prompt-driven. Help must show explicit \`lore entity dedup --dry-run\`; capture output through the CLI boundary, not \`console.log\`. Core \`entities.merge()\` must reject incompatible entity types before \`BEGIN IMMEDIATE\`, and dedup suggestions must use the same-type gate. Preserve the intentional self↔person exception.

<!-- lore:01a05c91-20c8-75d7-b80f-4257c0a67fea -->
* **FakeWorker exit acknowledgement**: Trap: a test \`FakeWorker\` whose \`terminate()\` resolves but never emits \`exit\` looks like a complete shutdown mock, yet production retirement settles on the exit event, so tests wait for force-timeout and distort retry ordering. Fix: ordinary worker fixtures emit \`exit\` on cooperative shutdown and termination; withhold exit only in the dedicated delayed-retirement regression.

<!-- lore:01a047be-ff82-7d19-833f-a557400f85ed -->
* **finishReason truncation authority**: Trap: accepting a semantic-judge response whenever its text is non-empty and parseable looks robust, but provider completion metadata may say the answer was truncated. Fix: treat \`finishReason\` as authoritative and reject length truncation with \`IncompleteWorkerResponseError\`, because partial structured output can parse successfully while omitting decisive content.

<!-- lore:01a03dc8-cc4b-7a0d-9da3-90612ac0db42 -->
* **fossilize entry.src ignored**: Trap: \`{ file: "ort-manifest.json", src: "ort-manifest-\<target>.json" }\` looks like a normal asset alias, but fossilize 0.10.1 never reads \`entry.src\`; \`entry.file\` is both the SEA key and source path. Fix: write target-specific manifests, copy the current one to shared \`ort-manifest.json\`, then serially await each \`fossilize()\` call so targets cannot race on the shared path.

<!-- lore:019f19db-f73c-74d1-865d-116cf01c0100 -->
* **Gateway auto-cross-routing trust predicate**: Auto-cross-routing must distrust same-protocol reroutes to a different origin as well as protocol translation. Use: !hasUpstreamUrlOverride && !hasProviderOverride && (effectiveProtocol !== ingressProtocol || effectiveOrigin !== ingressOrigin). A protocol-only predicate can replay compressed traffic, such as zstd, to a foreign host.

<!-- lore:01a03d4a-6990-7c39-b9db-f328249930c2 -->
* **gcVec0DanglingRows idle paging**: Trap: running whole-table \`gcVec0DanglingRows()\` anti-joins at startup or after bulk deletes looks like immediate hygiene, but synchronous writer scans block the gateway event loop. Fix: leave orphans for global idle maintenance; page lean candidates by canonical \`id\`/\`chunk\_id\` on a read worker, advance through backed-only pages, and guarded-delete exact returned keys. Timeout must retire the read worker, return \`READ\_JOB\_TIMED\_OUT\`, preserve the cursor, and never rerun synchronously. Reset a table cursor after its final short page so newly orphaned lower keys are eventually rescanned; mis-partitioned rows require repartition repair.

<!-- lore:01a02581-6142-7e06-8844-9c27b0b2c6d0 -->
* **generate:docs backfillCpuDuty semantics**: Trap: \`pnpm run check:docs\` looks like semantic validation, but it only proves generated pages match source comments; \`generate-env-docs.ts\` uses JSDoc at direct \`process.env\` access or project-wide JSDoc naming the variable, not the config schema description. Fix: align every \`LORE\_BACKFILL\_CPU\_DUTY\` source, including \`config.ts\` and \`embedding-cap.ts\`, name the variable in the legacy description, run \`pnpm run generate:docs\`, then \`pnpm run check:docs\`.

<!-- lore:019eea52-a1f7-757a-b0ae-6b749bbcda18 -->
* **Gradient escalated compression budget cap**: For compression stages above layer 1, apply rawFrac and distilled budgets to min(usable, maxLayer0Tokens) when a layer-0 cap exists. Applying fractions to full usable can make escalated windows larger than the cap that triggered compression. Do not clamp layer-1's base rawBudget (usable \* 0.4). Use isEscalated = stageLayer > 1.

<!-- lore:01a080ba-7eba-77b7-8336-01a0281bf01e -->
* **item\_reference.id log injection**: Trap: interpolating \`item\_reference.id\` into \`log.warn\` seems useful for diagnosis, but Responses clients control that value, so it can leak arbitrary content or inject newlines into durable logs. Fix: emit fixed categorical warning text with no raw ID, consistent with the Lore privacy boundary \[\[01a0416d-9d99-7211-98ea-7082af224013]]. Regression-test a distinctive newline-bearing ID and assert it is absent from the warning.

<!-- lore:01a0815d-dda6-7df4-96a9-915c8eb7468e -->
* **jj worktree pnpm node\_modules**: Trap: a new \`jj workspace add\` checkout can resolve source through the parent yet lack its own pnpm executable links and workspace dependency graph, so invoking Vitest fails with missing commands or packages such as \`sqlite-vec\`. Fix: run an offline lockfile-pinned \`pnpm install\` in the isolated workspace before validation; this links dependencies without downloading or changing the lockfile. Run Vitest from workspace root with repository-root-relative test paths, since package-local filtering can match no configured files.

<!-- lore:01a03b4c-36bb-7a36-931b-19960c8805a7 -->
* **LocalProvider cold-backlog pinning**: Trap: least-busy worker assignment looks self-balancing, but the pool starts with one slot and grows only after that slot completes a real embed and becomes healthy. A cold-start burst is therefore pinned to worker 0; later growth never rebalances it, and unbounded parent/worker queues retain every payload and closure. Fix: bound admission before pool dispatch and batch across temporal messages rather than relying on pool growth.

<!-- lore:01a03eaa-13dc-75df-a0d9-978b78c950d9 -->
* **localStackMissing permanent latch**: Trap: permanently latching every local-provider fatal error, floor OOM, exhausted init retry, or non-zero worker exit looks safe because retries may repeat costly failures. Fix: only \`localStackMissing\` is permanent because recovery requires reinstalling the optional embedding stack. WASM-fatal failures, OOM, transient-init exhaustion, and worker exits receive fresh-worker self-heal probes every \`SELF\_HEAL\_INTERVAL\_MS\` (6 hours); \`lastSelfHealAt = 0\` means no probe has occurred and the first tick is primed.

<!-- lore:019f98cf-e495-7847-9227-ce6117a8a97e -->
* **malformed\_chain poison: transient HTTP failures must not classify as chain corruption**: Trap: catching a 5xx/timeout/connection-reset on chain-manifest fetch and treating it as \`malformed\_chain\` looks like a safe failure-handling improvement. Fix: transient HTTP failures MUST NOT trigger a \`malformed\_chain\` poison alert — that classification is reserved for genuinely unparseable/malformed bodies. User directive (Jul 25 2026): this is a permanent invariant of the chain-fetch code path. 5xx/timeout/connection-reset must fall through to the existing retry/reclassification path (chain-unavailable on persistent failure), never poison the chain marker. Confirmed in binpatch PR #7 review (Jul 27 2026): ghcrSource.resolveNightlyChain catches fetch errors per-tag setting fetchFailed=true, reports \`fetchFailed ? "network" : "malformed\_chain"\` if any manifest missing. Poisoned tag's manifest fetches fine (HTTP 200) and is caught by validateNightlyChain — fetch failure classified as "network" so it never triggers a false \`malformed\_chain\` poison alert.

<!-- lore:01a03ae6-3ad8-751f-9156-a984d817ac5e -->
* **OpenCode V8 heap OOM**: Trap: treating 5–9 GiB RSS, high CPU, and empty 502s as host-memory saturation looks right because native ONNX, mmap, and file cache dominate process totals. Fix: inspect V8 Mark-Compact logs and \`heap\_used\_bytes\`; repeated collections near the heap ceiling prove JavaScript retention and OOM risk. CPU-heavy paths may amplify GC but do not identify the retained object graph—capture a heap snapshot before naming its owner.

<!-- lore:01a03ec2-c17c-7925-9712-6ae86bfc4bb1 -->
* **packages/core/test/setup isolation**: Trap: relying on \`NODE\_ENV=test\` or shared module state looks safe, but an inherited/default \`LORE\_DB\_PATH\` can mutate the real Lore database, while cached project mappings and stderr suppression leak across tests. Fix: \`packages/core/test/setup.ts\` must use a temporary \`LORE\_DB\_PATH\`; an unset test path makes \`db()\` throw. After every test, call \`silenceStderr(false)\` and \`invalidateProjectIdCache()\` so reused paths and logging state never cross test boundaries.

<!-- lore:01a03261-970d-7a1b-a99a-18edcb936d20 -->
* **pipeline-streaming producer-consumer sequencing**: Trap: awaiting a queued \`handleRequest()\` or producer before consuming a predecessor response looks like natural sequencing, but admission or upstream fixtures may wait for body consumption and deadlock. Fix: retain pending request promises, consume/release controlled predecessor bodies before awaiting dependents, and use explicit gates for producer/consumer ordering. Ordinary \`Response\` construction is harmless; only body-gated interceptors or unread streams create the deadlock.

<!-- lore:019f43a4-4ea3-752b-bd99-f2ef59d93db6 -->
* **Project-scoped distillation vector-search hydration**: vectorSearchDistillations is not project-scoped, unlike vectorSearchTemporal. Any caller must hydrate with archived = 0 AND project\_id = ?, over-fetch roughly limit \* 4, then cap to limit after filtering. Without this, distillations from other projects can leak and consume result slots. Prefer adding a natively project-scoped distillation vector-search API.

<!-- lore:01a04813-c7bb-708c-9ed3-3366c5c08de9 -->
* **Provider error envelope classification**: Trap: classifying a worker response containing an upstream provider error envelope as worker incapability looks reasonable because no usable answer was produced. Fix: classify it as \`upstream-error\`, because the worker successfully contacted the provider and returned its failure; incapability learning would incorrectly disable a functioning worker path.

<!-- lore:01a04989-1018-7cdb-98e0-de1c00847380 -->
* **Public Responses terminal snapshots**: Trap: using the \`completed()\` helper without an exact terminal \`output\` snapshot looks valid because streamed items already contain the recall call, but public validation rejects the principal before continuation starts, making an expected continuation diagnostic remain empty. Fix: include a one-to-one snapshot of every completed streamed item in the principal terminal and keep the continuation terminal protocol-valid while isolating the intended unfinished lifecycle.

<!-- lore:01a06675-1e8d-7dd0-a945-4d707345a283 -->
* **Reader vec0 smoke test**: Trap: running the writable vec0 temporary-table smoke test on every reader looks like consistent capability validation, but reader connections use \`query\_only=TRUE\`, so a working extension throws \`readonly database\` and is falsely demoted to JavaScript fallback. Fix: prove vec0 KNN once on the writable main connection; readers loading the same host binary only require successful \`vec\_version()\`, because read-only \`MATCH … k = ?\` works under query-only mode.

<!-- lore:01a0330c-7b4c-751d-92f8-a9ad89169fb0 -->
* **readForegroundBody stalled error body**: Do not await a non-OK upstream body to EOF: a hostile body can remain open and stall the foreground response. Race a bounded diagnostic read against foreground abort and timeout, then always cancel/release the reader. Return a sanitized response preserving status regardless. Test a non-settling body whose source observes cancellation/unlocked reader, and a far-future HTTP-date Retry-After clamped to 300 seconds; near-future dates do not prove the cap.

<!-- lore:01a03ae6-3bef-7517-8b59-aca1bcdacba2 -->
* **ReadJob Float32Array boundary**: Trap: routing embeddings, BLOB columns, or other large values through generic \`ReadJobSpec\` looks like reusable worker offload, but structured cloning bloats messages and encourages brute-force vector scans through the wrong RPC. Fix: keep \`read-job.ts\` leaf-level and limited to lean parameterized SQL returning structured-cloneable plain rows; callers supply the connection. Use the dedicated vector-query path for embeddings, and clone rather than transfer query vectors because transfer detaches the buffer needed by in-process fallback.

<!-- lore:01a05255-1e2a-73ea-a05a-3b5f5941d23f -->
* **readPhysicalVecDimension authority**: Trap: trusting \`vec.dimension\`, requiring all four canonical tables, or using configured width after one malformed sibling looks safe, but it can rebuild healthy vectors destructively. Fix: physical recognized DDL is authoritative; infer the common width from healthy siblings, ignore missing/malformed siblings as repairable, reject disagreement, and recognize shipped legacy temporal layouts. Vec tables alone never establish vec0 while blob columns remain authoritative.

<!-- lore:01a032d2-0791-7d74-8f7e-6f9f64c5c9c9 -->
* **Recall continuation resource limits**: Trap: applying \`maxDeferredBytes\` to every continuation buffer looks uniformly safe, but it is the 1 MiB cap for untrusted unresolved tool input; it rejects already-validated client-visible output below retained-state and stream limits. Fix: keep deferred/hidden candidate caps separate, but reserve validated transactional continuation chunks against \`maxTransactionalBytes\` (numerically \`maxRetainedStateBytes\`). Held visible chunks reserve immediately, promoted sparse candidates transfer deferred→transactional accounting, flush never double-charges, and transport-retry rollback restores every spool counter/baseline. Preserve request-wide stream/frame/retained-state limits and map every overflow to \`RecallContinuationFailure("resource\_limit")\`.

<!-- lore:01a03950-1518-76db-bbae-93ccbef87298 -->
* **RecallContinuationFailure telemetry isolation**: Trap: clearing cloned Sentry scopes looks detached, but inherited SDK processing metadata and trace/request state can still reach the envelope. Fix: capture \`Recall continuation failed\` through brand-new current/isolation scopes, pin them as captured scopes, then reconstruct the event in \`beforeSend\` and the transport to only fixed warning metadata and one runtime-allowlisted category. Report at most once after an attempted continuation, including post-delivery failures; never report aborts or let telemetry affect the response. Verify with a real initialized SDK envelope seeded with private request/trace state and fail-first evidence against the broken revision.

<!-- lore:01a07711-ff5a-7386-95cb-3f0e3d080a99 -->
* **reduceBlob paragraph reassembly**: Trap: concatenating adjacent kept segments looks compact, but it fuses distinct paragraphs and changes user meaning. Fix: preserve a \`"\n"\` separator between distinct adjacent paragraphs; fuse without a separator only for contiguous hard-window pieces originating from the same line.

<!-- lore:01a06e0c-193a-741f-94ce-3a8024c94878 -->
* **Remote fallback fixture dimensions**: Trap: using the configured local dimension \`768\` in Voyage/OpenAI fallback payload tests looks consistent, but width validation rejects them before the intended parser guard runs, creating false-positive malformed-payload tests. Fix: use Voyage \`1024\` and OpenAI \`1536\`; mutation-check that removing post-Float32 validation then returns vectors instead of \`EmbeddingProviderError\`.

<!-- lore:01a08126-4ec3-7af5-a906-cfac93e9b9eb -->
* **Responses parser error drain**: Trap: cancelling a Node request reader as soon as streamed JSON parsing fails aborts the socket before \`/v1/responses\` or \`/v1/codex/responses\` can return HTTP 400. Fix: return the fixed \`invalid\_request\_error\` / \`Invalid JSON body\` envelope while draining best-effort—but bind that drain to a bounded abort/connection-close scope. An unbounded detached \`iterator.next()\` drain looks safe because it preserves the 400, yet after the response disposes foreground abort state a client can leave a chunked body open forever, retaining reader, decoder, promise, and socket. Never expose parser, decoder, or encoding diagnostics \[\[01a0416d-9d99-7211-98ea-7082af224013]].

<!-- lore:01a080ba-7e38-7884-bbf3-f4129ab5e319 -->
* **Responses streaming JSON.parse parity**: Responses streaming and native parsing must retain an identical public contract across \`STREAMING\_PARSE\_SPOOL\_BYTES\`. Trap: passing raw bytes directly to \`@streamparser/json\` looks equivalent to \`Buffer.toString("utf8")\` plus \`JSON.parse\`, but the tokenizer rejects malformed UTF-8 where Node replaces it with U+FFFD. Fix: retain native parsing below 256 KiB; above it preserve raw prefix/BOM checks, decode incrementally with non-fatal \`TextDecoder\`, tokenize \`Buffer.from(text, "utf8")\`, and flush the decoder before \`tokenizer.end()\`. Regression-test threshold crossings, malformed UTF-8, BOM, roots, duplicate keys, trailing data, split escapes, and split multibyte characters.

<!-- lore:01a03d4a-6a0e-78c2-8274-7e4e49be32e3 -->
* **schema\_version post-migration normalization**: Trap: trusting each migration to leave \`schema\_version.version\` correct looks standard, but special migration handlers can advance the version while later migrations do not update it. Fix: after all migrations, always normalize the stored version to \`MIGRATIONS.length\`; keep the schema-version contract assertion synchronized with registry additions.

<!-- lore:01a03950-1551-721d-8192-ae1187e2b8e3 -->
* **Sentry conversation sessionID**: Trap: searching Sentry conversations with OpenCode’s \`x-lore-session-id\` looks correct because that is the client-visible session identifier, but \`setSentryRequestContext()\` uses Lore’s internal session ID as the conversation ID. Fix: map \`session\_state.header\_session\_id\` to \`session\_state.session\_id\`, then query Sentry with the internal ID; otherwise valid conversation spans appear missing.

<!-- lore:01a04a74-33af-7ce3-aa34-ed76fe4624cc -->
* **Sentry.BunClient Bun global**: Trap: initializing a real \`Sentry.BunClient\` under Node/Vitest looks sufficient, but default Bun integrations execute \`Bun.serve\` setup and throw \`ReferenceError: Bun is not defined\`. Fix: real-SDK privacy tests must pass \`integrations: \[]\`, temporarily install the minimal reviewed \`globalThis.Bun\` version/revision shim, and restore it in \`finally\`; this exercises envelope processing without activating Bun server instrumentation.

<!-- lore:01a06451-eb84-7344-999d-0181504ef81d -->
* **SQLite EQP covering SCAN**: Trap: \`not.toContain("SCAN t")\` looks like a heap-scan guard, but SQLite also reports valid index-only plans as \`SCAN t USING COVERING INDEX …\`. Fix: use \`not.toMatch(/SCAN t(?! USING)/)\` so tests reject true heap scans without rejecting covering-index walks.

<!-- lore:019fe15e-6f5d-7412-8c4d-3a5964091e04 -->
* **SQLite reader lifetime and VACUUM INTO safety**: Trap: leaving a \`ReaderConnection\` open looks harmless because it can never write and the main thread owns SQLite’s sole writer. A long-lived read mark prevents WAL truncation and caused a 5.4 GB \`-wal\` file. Fix: callers own the reader lifetime and MUST close it during worker shutdown.

<!-- lore:01a03e37-b42d-77e9-8298-da1c71af5a7d -->
* **storeTemporalChunks vec0 replacement**: Trap: treating \`storeTemporalChunks(\[])\` as replacement looks consistent with collection APIs, but DELETE-then-zero-INSERT wipes valid chunks. Fix: after resolving vec0 mode, return \`0\` before validation, lookup, savepoint, or DELETE; outside vec0 mode also no-op. Nonempty replacement validates limits/cardinality before atomically looking up ownership, deleting canonical keys, and inserting all vectors in one savepoint. Delete contiguous \`message\_id#N\` keys in 64-key OR batches while the next boundary exists; scans look simpler but violate exact-key cleanup. Shipped writers assign sequential ordinals, so gaps imply external corruption and require separate repair.

<!-- lore:01a080ba-7e79-76c0-abf1-0c5235a7a6a5 -->
* **Streaming request decoding, parser validity, and cleanup**: Streaming JSON decoding must preserve native-route error privacy and lifecycle behavior. Require exactly one root value: successful tokenizer/parser end calls do not make whitespace-only input valid. Map decode, decompression, unsupported-encoding, and parser failures to the fixed 400 \`invalid\_request\_error\` envelope; never reflect codec or parser diagnostics. Normalize gateway-owned Content-Encoding and never forward a client encoding that does not match transmitted bytes. On source failure after streaming handoff, immediately destroy the decoder and perform bounded cleanup; do not cancel a background body drainer prematurely after syntax failure. Regress malformed codecs, root shape, compressed-size breaches, cancellation, and source cleanup.

<!-- lore:01a03d4a-69c7-7dd4-9599-4d487ce5ee7a -->
* **TaskTool masks assistant errors**: Trap: \`runTask()\` treating any returned assistant message as success looks valid because normal replies always contain text, but failed children also return assistant messages with \`result.info.error\`; empty text then becomes \`state="completed"\` and an empty \`\<task\_result>\`. Fix: fail the task when \`result.info.error\` exists and propagate a sanitized categorical failure so foreground, background, new, and resumed tasks render an error rather than false completion.

<!-- lore:01a03b4c-3685-7ba4-aaeb-9315748f0227 -->
* **temporal queue NUL-safe CAS**: The local-only temporal queue is the sole temporal-inference path: concurrent drains share one \`activeDrain\`, admit at most 8 rows/256 KiB, and commit only after fingerprint, full-content hash, authoritative content, storage-mode, cardinality, and vector validation. Trap: a whole-drain timeout looks bounded but consumes deadline while queued and may retire healthy workers. Fix: start each sub-batch deadline after normal admission; settlement timeout only stops waiting and never clears \`activeDrain\` or cancels provider work.

<!-- lore:01a04325-a999-7ed5-84df-2c74cc3301b4 -->
* **temporal\_vec chunk\_id presence**: Trap: checking vec0 presence through auxiliary \`temporal\_vec.message\_id\` looks natural because the question is whether a message has vectors, but that auxiliary-column predicate can force a scan. Fix: query the canonical indexed \`chunk\_id\` key, because shipped temporal chunks have deterministic exact keys and ordinary admission must remain bounded.

<!-- lore:01a04077-e44a-7561-b143-f0515eaa3806 -->
* **temporal.test shared PROJECT**: Trap: ordered \`temporal.test.ts\` cases can share \`PROJECT\` and assume earlier rows, counts, or distilled flags, which passes normally but fails under shuffled execution. Fix: give count/order-sensitive contracts dedicated projects and seed every precondition locally; auxiliary fixtures such as \`/test/temporal/lean-cols\` must never perturb cumulative tests.

<!-- lore:01a04787-d265-7fad-a666-3afa09ab64f7 -->
* **terminalDelivered delivery reporting**: Trap: returning immediately from the continuation catch when \`terminalDelivered\` is true looks correct because emitting \`response.failed\` would contradict an already delivered terminal, but it also suppresses diagnostics when \`finish()\` or \`onComplete\` fails afterward. Fix: roll back and report one \`delivery\` category before the terminal-delivered early close, while still emitting no contradictory public failure. Failed or incomplete continuations never become completed, and continuation accumulation remains separate from principal state.

<!-- lore:01a03650-272f-7b14-a4ee-d9cb20a86d5a -->
* **tool\_calls composite conflict target**: Trap: \`ON CONFLICT(call\_id)\` looks correct because call IDs historically appeared globally unique and the old schema used a single-key identity. Fix: use \`ON CONFLICT(project\_id, session\_id, call\_id)\` because the current \`tool\_calls\` primary key is composite; SQLite rejects the standalone target and every tool-call seed fails, causing error/Sentry/database churn.

<!-- lore:01a0235f-dd58-73b3-b697-e65fd0989b23 -->
* **tracedDatabase query seam**: Trap: monkeypatching \`db().query\` or tracing \`prepare()\` looks like it observes all SQL, but \`tracedDatabase()\` proxies only \`.query()\` statement \`get\`, \`run\`, and \`all\`; raw \`prepare\`, \`exec\`, migrations, and transaction control intentionally bypass tracing. Fix: SQL-count tests register \`log.registerSink({ withDbSpan })\` and execute through \`.query()\`. The wrapper must call the operation exactly once without changing arguments or return values; core stays free of Sentry dependencies.

<!-- lore:01a04813-c783-7ee1-8769-56a9e0e72066 -->
* **Unsupported-parameter learning**: Trap: permanently marking \`temperature\` or \`thinking\` unsupported after stripping it from one matching 400 looks like useful capability learning, but regex matches can come from unrelated provider errors. Fix: defer learning until the retried request proves that removing the parameter resolves the failure; otherwise a false positive permanently degrades later requests.

<!-- lore:01a04077-e40c-7b97-990d-17aeb2e4b839 -->
* **v85 temporal queue migration**: Trap: resetting \`lore:temporal\_rechunk.done\` and its cursor in migration v85 looks necessary because the walk now schedules durable work, but v84 vectors already use the same text policy and a reset can synchronously invalidate hundreds of thousands of valid rows. Fix: v85 only creates \`temporal\_embedding\_queue\` and its \`(enqueued\_at,message\_id)\` index; \`recoverMissingObjects()\` recreates missing schema without changing progress metadata.

<!-- lore:01a06e0c-19b8-7aab-9b55-d70dc93d699d -->
* **Vendored model purge boundary**: Trap: purging and redownloading the model after every load/download failure looks self-healing, but the shipped in-binary model is read-only and transient \`401\` or \`ETIMEDOUT\` failures create destructive purge→redownload→fail loops. Fix: never delete/redownload the vendored model; reserve purge only for proven corrupt or partial external downloads.

### Pattern

<!-- lore:019f1cc8-b65e-7a6e-97ae-f48d1a7d542b -->
* **forProjectOffloaded timeout fallback**: All offloaded variants of frozen-baseline builders (\`ltm.forProjectOffloaded\`, \`entities.forProjectOffloaded\`, \`entities.entitiesForSessionOffloaded\`) must fall back to the synchronous in-process scan on \`READ\_JOB\_TIMED\_OUT\` — never return \`\[]\`. Trap: returning \`\[]\` on timeout looks safe (same as \`forSession\` degrade). Fix: unlike \`forSession\` (reruns every turn), these results are frozen into \`stableLtmCache\` + \`saveSessionTracking\` for the entire session lifetime. A spurious empty baseline permanently breaks the knowledge catalog for that session. \`forSession\` can safely return \`\[]\` because it reruns every turn. Mutation test: force timeout branch to return \`\[]\` → timeout test must fail.

<!-- lore:01a03e44-1d13-7a8f-ac02-97f6be0fa251 -->
* **jj-create-pr exact-change workflow**: Steps: 1. Resolve the intended stable change ID, defaulting to \`@-\` only after proving it has a nonempty diff and no trunk bookmark. 2. Inspect \`jj diff -r \<change>\` and derive a conventional title from its description. 3. Rebase onto current upstream, then verify \`jj st\`, \`jj log\`, conflicts, and the exact file scope—jj records conflicts without failing. 4. Create or identify the change’s bookmark and push only that bookmark; stop on push or branch-detection failure. 5. Open the PR against \`gh repo view\`’s default branch with assignee \`@me\`, \`## Summary\`, and 3–5 impact-focused \`## Changes\` bullets. 6. Run required validation and correctness/security review on the exact pushed head; address failures before merge. Gotchas: - An empty child above the patch makes \`@\` the wrong PR target; identify the parent explicitly. - Rebasing rewrites commit hashes but preserves stable change IDs. Verify: - \[ ] PR diff contains only the intended change. - \[ ] CI and required reviews pass on the final head.

<!-- lore:01a01eab-4463-721b-a380-46df306f3290 -->
* **shutdownVectorPoolAsync deadline**: Steps: 1. Snapshot workers, set \`shuttingDown=true\`, and reject in-flight requests — dispatch must stop first. 2. Post \`{type:"shutdown"}\` and await each exit — workers close reader connections cooperatively. 3. Race exits against an unrefed termination timer; treat \`postMessage\` failure as completion. 4. Make \`finish()\` idempotent, clear its timer, and always resolve. Gotchas: - A later \`const killTimer\` looks tidy, but an early throw invokes \`finish()\` in the TDZ; declare \`let\` first. - Immediate termination looks sufficient, but live reader WAL read-marks can prevent the writer’s TRUNCATE checkpoint and strand \`-wal\`; graceful gateway shutdown requires \`shutdownVectorPoolAsync()\`, not \`shutdownVectorPool()\`. Verify: - \[ ] Empty, repeated, and concurrent shutdowns resolve. - \[ ] Cooperative exit, forced deadline, and throwing \`postMessage\` resolve. - \[ ] Writer close occurs only after reader shutdown.

<!-- lore:01a06e0c-19f4-774b-94d2-219ee0714ed4 -->
* **Vitest exhaustive sharding**: Steps: 1. Run the normal full suite first — preserves the standard gate \[\[01a03e22-93bd-721c-ae79-e5484d4c6a5a]]. 2. If aggregate runtime or teardown obscures the result, confirm no test workers survived. 3. Partition every configured test file deterministically and run all shards with the repository’s 300s test timeout, \`hanging-process\`, and \`--printConsoleTrace\`. 4. Aggregate results and prove each included file ran exactly once. Gotchas: - Quoted globs look shell-safe, but Vitest treats them as literal filters and reports no files; use directories or explicit file lists. - A 30s diagnostic timeout looks bounded, but valid pipeline tests can need about 35s; retain the repository timeout. Verify: - \[ ] Every configured test file belongs to one shard. - \[ ] Every shard exits cleanly. - \[ ] The changed test file also exits cleanly under handle diagnostics.

### Preference

<!-- lore:01a038dd-e5e5-764a-a1f0-da0889dff60f -->
* **Always commit agents file**: Directive (Burak Yigit Kaya): always commit the agents file; soften auto-maintained wording rather than omitting the commit. Corresponds to commit ptxqqusnzpnr "fix(agents-file): always commit agents file, soften auto-maintained wording". Standing repo convention — the agents file is tracked in version control even when partially auto-generated.

<!-- lore:01a08167-3a8e-7a7f-81b5-ff298c66fa5e -->
* **Always enforce bounded, cancellation-safe streaming lifecycles**: When implementing or reviewing streaming request, response, decoder, and abort paths, ensure every reader, socket, drain, deferred operation, and cleanup path has bounded lifetime and explicit ownership. Add focused regressions for malformed, stalled, backpressured, aborted, or incomplete streams—especially raw-socket tests where ordinary fetch behavior hides lifecycle leaks. Preserve protocol and native-parser semantics while avoiding unbounded buffering. Cleanup must cancel/release resources, observe late settlements, and suppress cleanup-originated unhandled rejections; it must not silently leave background drains or transports alive after an error response.

<!-- lore:01a08168-6be5-7eeb-843c-5135fea2690f -->
* **Always inspect recall-stream edge-case behavior end-to-end**: When working on gateway recall-aware streaming, trace the full continuation lifecycle across implementation and tests: stream-flag selection, client-visible event framing, hidden recall suppression, follow-up failure/depth-limit behavior, warning injection, cancellation, and transaction cleanup. Verify that no-recall passthrough remains live and unchanged, continuation streams follow the established protocol contract, and terminal/failure paths finalize exactly once. Treat empty-completion and warning-banner paths as composable and non-throwing, and keep follow-up behavior aligned with the same condition used to determine continuation streaming.

<!-- lore:01a03426-3ac8-7efa-a762-ebc817c120a7 -->
* **Always preserve cache-stable, tool-safe durable prompt-delta placement**: Keep durable session prompt deltas near the message tail, never inside the cached system prefix or between an assistant tool\_use and its matching user tool\_result. Compute placement with tool-pair awareness at creation time, persist and reuse the position for cache stability, and re-anchor it when compression or cache-warm reshuffling changes the message layout. Apply deltas after orphan-safety processing and retain a fallback that prevents invalid tool sequences, preferring safe degradation over a hard 400. Keep LTM in its separate cacheable system block and avoid rewriting the stable system prefix.

<!-- lore:01a0815a-df98-7592-ad3e-dd32cc5e5a02 -->
* **Always require failing-first regression tests for bug fixes**: For defect fixes, create deterministic regression tests that reproduce the real failure on the base branch and pass only with the fix. Do not accept tests that merely exercise a newly added guard; verify they fail if that guard is removed. Treat “never” behavioral guarantees—especially around cancellation, failed continuations, persistence, and public error behavior—as explicit test requirements. Validate fixes thoroughly, including repeated stability runs and the relevant broader suite, rather than trusting implementation reasoning alone.

<!-- lore:01a03e04-7161-7701-982c-5f40412ec393 -->
* **Always run full CI unconditionally (never path-gated, never gate unrelated PRs)**: Always run full CI unconditionally on every push/PR — never path-gate or filter by changed files. Critical compatibility guards (bash 3.2 compat for the macOS install script, actionlint workflow correctness) must be always-on, mirroring the existing unconditional actionlint job. Never gate unrelated PRs; bash-compat/workflow bugs must never slip through regardless of which files changed. When adding a new guard (e.g. a bashisms linter), wire it as an unconditional CI job, not a path-triggered one.

<!-- lore:01a08162-9fac-7604-914c-a3081ef53a56 -->
* **Always state and preserve explicit edge-case invariants**: When reviewing or implementing gateway and parser behavior, identify precise invariants for edge cases and treat them as non-negotiable contracts. Preserve parity between alternate paths (such as buffered versus streaming parsing, request versus probe URL construction), avoid unsafe asynchronous failure modes (such as unhandled late-settlement cleanup failures), and retain intended stateless or normalization semantics. Encode these conclusions as clear rules and ensure implementations and regressions cover the boundary conditions rather than relying on assumed behavior.

<!-- lore:01a04a84-4cb4-7c88-ad20-4301bfc48ca6 -->
* **cmdEnable cancellation disables sync**: Always disable sync when encryption setup is canceled, even when sync was already enabled. Preserving the previous state looks conservative, but it can keep pushing plaintext after encryption becomes mandatory; cancellation must perform no sync and require an explicit re-enable with a passphrase.

<!-- lore:01a01a58-ecaf-7824-8a8f-0d8210016aa5 -->
* **Craft version CHANGELOG ownership**: Never manually bump versions or edit CHANGELOG; Sentry Craft owns release versioning and changelog generation for BYK/loreai releases via GitHub Actions. Manual edits look harmless locally but conflict with Craft's generated release state and can be overwritten or produce inconsistent artifacts.

<!-- lore:01a03ceb-4c03-7d8c-b142-8a82e2395dbd -->
* **embedding startup reconciliation order**: Always run provider-independent \`checkConfigChange()\` before the provider-availability return and before \`maybeCutoverToVec0()\`; otherwise unavailable embeddings can skip physical vec0 repair, while reversing cutover order can seed vec0 from stale blobs. Pending counts must share exact mode-aware predicates: knowledge \`confidence > 0.2\`, distillations \`archived = 0 AND observations != ''\`, and presence from each index table; emit coverage through always-visible stderr.

<!-- lore:019fcb4b-74bf-7225-8fee-326b27db3db6 -->
* **Eval matrix launch and failure-handling invariants**: Curate-log parsing must NEVER abort an eval. \`/lore:curate\` emits JSONL, so whole-file \`JSON.parse()\` looks natural but always fails on multi-line output and silently hides errors. Parse non-empty lines independently, surface the first \`type:"error"\`, ignore malformed lines, and catch outer parsing failures; score validity from primary artifacts (\`result.json\`, session traces), not this auxiliary log.

<!-- lore:01a019e8-3b49-7057-99a9-a26b1f2014ce -->
* **Evidence-first tracing before code changes or conclusions**: Before changing code or concluding behavior, investigate read-only and treat user invariants plus current runtime evidence as authoritative. Verify the active checkout, worktree, loaded configuration, dependency links, schemas, artifacts, binaries, processes, services, and affected callers, caches, lifecycle, concurrency, failure, and cleanup paths; do not rely on memory, documentation, or another checkout. Cite exact paths, lines, symbols, predicates, revisions, timestamps, and correlated telemetry. Separate facts, hypotheses, and gaps, and never infer causation or mutate state without authorization. Preserve explicit contracts for identity, scoping, atomicity, compatibility, routing, privacy, failure, recovery, and graceful degradation. Prefer narrow responsible-boundary fixes, live state, bounded work, and shared predicates. Add focused deterministic regressions for success, failure, races, aborts, restarts, teardown, compatibility, and degraded modes.

<!-- lore:019fe0d7-d7b0-720b-ae7c-d73dfc90e1f4 -->
* **External judge and judge-output diagnostics**: Directive: when \`JUDGE\_API\_KEY\` is set, the judge MUST run on Anthropic (Sonnet by default) via the real provider. NEVER allow a weak or non-Anthropic answering model to grade its own output. Self-judging looks convenient for cost/latency but produces biased scores and invalid eval signals; always use an external, stronger judge.

<!-- lore:01a047be-fee0-7411-a0fa-f73c107bd22d -->
* **Fan-out registry batteries**: Always run the registry’s contract battery when extending a fan-out list: \`SYNCED\_TABLES\` through \`sync-registry-contract.test.ts\`; \`MIGRATIONS\` through the \`db.test.ts\` schema-version assertion plus migration-specific tests; and \`AGENTS\` through \`agents.test.ts\`, covering \`name\`, \`displayName\`, constant-placeholder \`binary\`, and \`detect()\`. A focused implementation test looks sufficient but misses consumers that silently depend on complete registry metadata.

<!-- lore:01a03dc8-cc13-7a1f-8ae4-6cf8581205e7 -->
* **fossilize version update**: Every fossilize upgrade uses branch \`chore/fossilize-{version}\` from \`origin/main\`: update the dependency, remove functionality fossilize now handles natively, verify the build, commit \`chore: update fossilize to X.Y.Z\`, then open a PR. Chose an isolated upgrade over mixing compatibility code into feature work because native replacements and packaging regressions require a reviewable, reproducible build boundary.

<!-- lore:01a04813-c751-7367-8f44-775d429896ef -->
* **Frozen syncedColumns cache**: Always freeze shared memoized arrays such as the \`syncedColumns\` PRAGMA cache. A mutable shared array looks cheaper and conventional, but one caller can silently corrupt every later result; freezing makes accidental mutation throw \`TypeError\` in strict mode and exposes the defect immediately.

<!-- lore:01a04989-0fe0-72dd-9249-0f7780343946 -->
* **Incomplete Responses recall execution**: Never execute recall from an incomplete principal response or chained continuation, and never convert a failed continuation into completed. Salvaging partial output looks helpful, but incomplete or failed terminals do not establish authoritative arguments or lifecycle state; emit the fixed public recall failure while preserving rollback and the original terminal integrity.

<!-- lore:019fcab7-8956-7867-8b39-3c7efb4426bc -->
* **Jujutsu .jj workflow**: Git workflow (Burak Yigit Kaya): ALWAYS run \`git add .\` before committing — partial staging looks safer but omits related changes (tests, fixtures, config), breaking CI or non-reproducible commits. Before committing: format → lint → typecheck → tests on the current base; check for a stray \`opencode.json\` (untracked opencode config in repo root — BYK/loreai has none; it contaminates cache/auth tests). \`.lore.md\` is auto-managed by the background lore process: on stash-pop conflicts resolve to 'ours' (origin/main), never hand-edit — it re-applies session memory on the next cycle; its regenerated changes are committed via \`git add .\` like everything else. Aug 7 2026: \`.lore.md\` on \`main\` carries auto-managed unstaged local changes that BLOCK \`git checkout \<other-branch>\` and \`git pull\`/ff — \`git stash --keep-index --include-untracked\` + checkout to another branch fails; resolve via \`git checkout -- .lore.md\` (discard local) or stash + drop, then ff. Confirmed Aug 7 PR #1598 merge: main ff \`bf8ffa02 → c1edc4fb\` brought .lore.md (167 lines), embedding-worker.ts (24 lines), new embedding-worker-stderr-routing.test.ts (98 lines).

<!-- lore:01a0619e-0a9e-7bff-8be9-6b1bc0b6c53c -->
* **LocalProvider remote fallback**: Never automatically switch a broken local embedding provider to Voyage or OpenAI; remote embeddings are opt-in. Automatic fallback looks resilient, but it silently changes the privacy, credential, cost, and provider boundary, so local failure must remain \`LocalProviderUnavailableError\` unless remote use was explicitly configured.

<!-- lore:01a039c5-3b2a-7098-8a9b-167eb13d8737 -->
* **log.error durable visibility**: Error logs must always reach persistent file logging and registered \`LogSink\` consumers after credential redaction. Chose durable visibility over dropping errors because failures must remain diagnosable; embedded TUI mode may silence all stderr process-wide to avoid corrupting the display, but must not suppress file or sink delivery.

<!-- lore:01a03dc8-cbdd-7266-b6fd-e970b5dc8837 -->
* **LORE\_NO\_DB\_TRACING singleton**: When a fresh \`db()\` singleton is created with \`LORE\_NO\_DB\_TRACING="1"\`, always return the raw connection and never invoke \`withDbSpan\`. Wrapping first and suppressing spans per query looks equivalent, but it leaves tracing machinery on the hot path and does not prove the raw-connection contract; tests must create a fresh singleton and assert zero tracer calls.

<!-- lore:01a039c5-3aba-798e-8bd6-92e1ea483150 -->
* **meta-distillation warm cache bust**: A warm meta-distillation cache bust is always a real avoidable loss, not an ordinary re-warm. Treat a cache-bust spiral beyond the cold-start grace period as a high-severity Sentry alert tied to issue #797; dismissing it as normal cache churn looks plausible but conceals an upstream caching bug. Debounce alerts once per \`(session, episode)\`.

<!-- lore:01a020df-68b2-7846-a6d6-10b3127b63e6 -->
* **Never alias imports**: Never alias imports.

<!-- lore:01a020df-68d5-7176-bfd2-2c21e12cef8b -->
* **Never use star imports**: User stated never to use star imports,

<!-- lore:01a03ae6-3b0e-78ed-94ed-4f7535176381 -->
* **No free-form content in logs or Sentry export**: Never log or export free-form application content (message text, prompts, paths, commands, entity names, tool payloads, vectors, SQL parameters, credentials, provider bodies) in BYK/loreai or to Sentry. Logging/exporting looks useful for diagnosis but violates the privacy boundary. Emit only fixed categorical errors, reviewed metrics, sanitized spans, and bounded allowlisted metadata with no content-derived values. For Sentry specifically, keep enableLogs:false and drop breadcrumbs/logs; retain only fixed-message error telemetry.

<!-- lore:01a03e37-b4e5-799c-8230-9400dd244f1e -->
* **OpenCode running service isolation**: Never mutate, signal, kill, or restart the running OpenCode/Lore service without Burak Yigit Kaya’s explicit approval; it hosts active work. Touching it looks useful for validating a fix but can disrupt unrelated sessions. Use read-only health and resource checks, and continue development in an isolated workspace against a separate executable or fixture.

<!-- lore:01a03dc8-ccbb-7ab7-8acb-db64c345eaff -->
* **OpenCode skill metadata**: OpenCode skill names must match their folder using lowercase hyphen-separated text of at most 64 characters. Every skill needs a description or it is filtered out before reaching the model. Descriptions should front-load concrete trigger keywords and filenames, use third-person wording such as \`Use when...\`, and say \`Use ONLY when...\` where adjacent topics could activate it incorrectly.

<!-- lore:01a03dc8-ccf1-7334-9117-65b1cf9cbaca -->
* **OpenCode SSE subscriber bounds**: Always bound queued events and bytes per OpenCode SSE subscriber, with explicit tested overflow behavior that preserves ordering and never silently loses terminal events. Reconnects must never exceed one active stream per TUI; disconnect, cancellation, and failure must release the HTTP response, listener, queue, and retained payloads. An unbounded queue looks lossless, but slow or orphaned subscribers retain cloned high-volume payloads and can drive V8 OOM.

<!-- lore:01a03dc8-cc84-7489-934d-1d99f458d67b -->
* **opencode.json model MCP schema**: OpenCode \`model\` values always include the provider prefix, such as \`anthropic/claude-sonnet-4-6\`. Every \`mcp\[name]\` requires \`type\`, and \`command\` is always an array of strings, never one shell string. Bare model names and string commands look ergonomic, but they violate the configuration contract and create ambiguous provider or argument parsing.

<!-- lore:01a03e22-941c-7289-9c82-f097a391d7f5 -->
* **pnpm engine-strict workflow**: Use pnpm exclusively in BYK/loreai; \`.npmrc\` enforces \`engine-strict=true\`. Always use \`pnpm add\` for dependencies, never \`npm install\` or \`yarn add\`, because alternate package managers bypass the repository’s lockfile and engine guarantees.

<!-- lore:019fe14a-f9d4-7f87-947e-3c9730d3faef -->
* **Pre-commit full validation**: User expects a complete validation cycle on changes before marking work complete: bundle the gateway package, run the full vitest suite (or relevant subset), run oxfmt format check, and run oxlint. Bundle debug ID injection and SENTRY\_AUTH\_TOKEN absence are noted as expected. Test failures or warnings must be surfaced with concrete counts. When tests fail or processes hang, the user wants the agent to diagnose root cause (e.g., orphaned processes, reporter config errors) rather than just retry. Completion is signaled only after all checks pass or are explicitly accounted for, with diffs and commit hashes reported. PR readiness protocol (Aug 8 2026, PR #1605): when CI is all-green (mergeStateStatus=CLEAN, mergeable=MERGEABLE, Seer Code Review PASSED, no inline comments), the agent reports the PR is ready at the URL with a CI summary and merge state — but does NOT auto-merge. Standing workflow directive (Burak Yigit Kaya, Aug 5 2026, triple emphatic): 'Always always always commit your work. Then create a PR if applicable.' When user only says 'fix this issue' (not 'merge'), agent defers the merge decision to the user rather than merging unilaterally.

<!-- lore:01a080ce-a153-761e-acd9-ef8effba27e4 -->
* **Prefers dedicated CLI commands over raw API calls**: Prefer dedicated CLI commands over raw API calls,

<!-- lore:01a020df-688c-7348-b9b4-cd7eef2bc595 -->
* **Prefers dot notation over preserve context**: prefers dot notation over unnecessary destructuring to preserve context,

<!-- lore:01a05fd4-7996-76be-a21a-b5ed5b6843b6 -->
* **Prefers stable letter-based change IDs over hexadecimal commit IDs**: prefer stable change IDs over hexadecimal commit IDs.

<!-- lore:01a0815e-dcb0-79a2-a7c1-5c337cd2990d -->
* **Preserve transparent recall marker translation**: When implementing or modifying gateway recall behavior, keep recall tool calls transparent to clients: replace outbound recall tool\_use blocks with human-readable marker text, then expand those markers back into tool\_use/tool\_result pairs on subsequent inbound requests. Apply this consistently across protocols and streaming paths, while keeping recall.ts decoupled from pipeline.ts through injected follow-up context to avoid circular imports.

<!-- lore:019fdd09-a475-7bab-a140-f51be6e986d2 -->
* **recall synchronous-result rule**: Always prefer recall over assuming information is unavailable, especially for prior decisions, preferences, paths, and approaches. Trap: invoking synchronous \`recall\` as the final action looks like deferred background research, but the turn ends before its result is processed and creates a false searching loop. Fix: consume the result and continue within the same turn.

<!-- lore:01a08152-77d4-78ee-8f94-36bc154dc760 -->
* **Recall-aware streaming lifecycle, state, and output invariants**: Recall-aware streaming is transactional: accumulate every valid upstream event into authoritative internal state before filtering, deferring, or projecting it to clients. Commit deferred recall mutations only after explicit transaction commit; roll back on callback, delivery, disconnect, abort, follow-up, or validation failure. Cancellation must settle promptly even when callbacks ignore aborts, and late work must be cancelled or rolled back without starting more work. Keep continuation output transactional until validated; emit only one protocol-correct terminal lifecycle, never convert failed continuation into completion, leak hidden recall details, or emit raw/unscaled usage. Diagnostics are strictly observational: defensively invoke and swallow their failures so they cannot affect response behavior. Add focused deterministic regressions for lifecycle, cancellation, limits, retries, state accumulation, and public-error sanitization.

<!-- lore:01a07711-ff1f-7059-b4e6-8ad7662ba943 -->
* **reduceBlob pinned directives**: Never truncate user signal in distillation. \`reduceBlob()\` must force-keep pinned directives through junk classification, tight budgets, and overlapping hard windows; sampling or low lexical variety looks like a valid reason to drop them, but it can erase explicit user constraints.

<!-- lore:01a032f9-2cf0-7490-b3a4-277ccaf75363 -->
* **Sanitize upstream failures and diagnostics**: Sanitize upstream, malformed-input, and provisional-identity failures to canonical \`{ type: "server\_error", message: "Gateway request failed" }\` payloads, preserving only upstream status and bounded allowlisted retry metadata. Never relay, log, persist, or expose upstream bodies, status text, URLs, credentials, request content, provider diagnostics, malformed successful text, or arbitrary parser/fetch errors; routing logs may record only an auth scheme. Read diagnostics privately only under a bounded timeout, then cancel or release readers. Before identity proof and durable commit, resolution content is never client-visible or stored; retain only fixed allowlisted internal log detail. Add regressions for response sanitization, retry-header clamping, stalled-body cancellation, and log redaction.

<!-- lore:01a038dd-e565-7b58-8951-99ed8e425adc -->
* **Sentry CLI workflow and safety**: Steps: 1. Run dedicated \`sentry\` commands directly — authentication and org/project detection are automatic. 2. Use \`--json\` with \`jq\` — keeps investigation output machine-readable. 3. Before further mutations, verify the detected org/project; override with explicit \`\<org>/\<project>\` only when detection is wrong. 4. Prefer dedicated commands; use \`sentry schema\` for endpoint discovery rather than raw API calls. Gotchas: - \`project delete\` and \`trial start\` always require user confirmation. - Never store or log tokens; the CLI owns credentials. - Pre-authentication looks cautious but adds secret-handling risk and unnecessary context lookup.

<!-- lore:01a0465f-5bc1-7eae-b69b-0ea8265c13ee -->
* **startServer unavailable-host warning**: Always emit an always-visible warning when an explicitly configured host is skipped for \`EADDRNOTAVAIL\` or \`EADDRNOTFOUND\`. Debug-only logging looks quieter, but silently degrading to loopback-only surprises operators; this event is rare and actionable. Continue when another configured host binds, but fail startup when none are available.

<!-- lore:01a04a84-4cea-737a-85ec-e2701d24335d -->
* **streamResponsesRecallAware setup cancellation**: Never await non-settling \`onRecall\`, follow-up setup, or hostile continuation cancellation in \`streamResponsesRecallAware\`. Waiting looks like orderly cleanup, but it wedges abort and disconnect paths; settle cancellation independently, roll back late results, cancel/release late readers, and never let an abort-ignoring callback commit.

<!-- lore:01a0265e-a82d-76c8-aefe-7140a0ab6fb1 -->
* **Synthetic resolution temporal-storage exclusion**: Synthetic resolution and refcheck artifacts are never exposed to the relevant consumer and must always be excluded from temporal storage. Persisting them looks useful for diagnostics or future recall, but it stores internal probe output as user-visible conversational evidence and contaminates temporal memory.

<!-- lore:01a0266e-056f-75ee-a388-1e90d4fd26a0 -->
* **Telemetry and best-effort maintenance must be fail-safe and non-disruptive**: Treat telemetry, diagnostics, and best-effort maintenance as non-functional concerns on request, response-read, streaming, and idle paths. They must never throw, block, stop schedulers, delay terminal delivery, or alter application behavior. Disabled or uninitialized backends are clean no-ops; contain hook, logging, formatting, metric, worker, and maintenance failures. Reject invalid samples such as negative, non-finite, or clock-anomalous durations. Preserve required cancellation semantics and the underlying operation’s normal fallback and recovery behavior when instrumentation or background work fails or times out. Keep cleanup bounded and idempotent, schedule expensive post-response accounting after terminal delivery when applicable, and add deterministic regressions proving failures cannot escape, disabled telemetry emits nothing, and valid captures remain sanitized.

<!-- lore:01a03e22-9453-7678-a057-cd1003c0e001 -->
* **TypeScript export conventions**: BYK/loreai uses strict TypeScript: never use \`any\`; accept \`unknown\` and narrow with type guards. Use named exports only, kebab-case file and directory names, and structured errors shaped as \`{ error: { code, message } }\` without stack traces. Chose these over permissive typing, default exports, and raw errors because explicit contracts keep callers and public failures predictable.

<!-- lore:01a04813-c7f2-7e39-84cf-581e88112b19 -->
* **Uncooperative reader cancellation**: Never await cancellation from an uncooperative stream source. Awaiting \`reader.cancel()\` looks like safer cleanup, but hostile or broken sources may never settle and can wedge abort, timeout, or client-disconnect paths; fire-and-forget cancellation, suppress rejection, and release the reader lock immediately with a later retry when needed.
