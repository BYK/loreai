<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:01a0cdce-4777-72f9-a9b5-ffe5958989e7 -->
* **buildLoreContext independent budgets**: \`buildLoreContext()\` reserves output and overhead first, then allocates separate budgets for archived-excluded distillations (25%) and the recent raw tail (40%). Chose independent budgets over one shared pool because the gradient contract keeps distillation from displacing the raw tail; LTM is omitted because the gateway injects it separately, and including it would duplicate context.

<!-- lore:01a0c3ea-a054-7e11-8d64-d692d01672bf -->
* **buildOpenAIUpstreamRequest cache shape**: When \`cacheConversation\` is enabled, \`buildOpenAIUpstreamRequest\` must emit every text conversation message as array-form content, while placing exactly one \`cache\_control\` breakpoint on the last eligible non-tool message. Tool-role content remains a string, and the breakpoint walks backward past tool results or tool-call-only messages. Chose stable array-form history over preserving plain strings because the moving breakpoint otherwise flips historical message bytes between turns and destroys provider cache hits.

<!-- lore:01a0c1e0-d6a3-7c2d-990f-ce3b1a0b14c1 -->
* **captureRequestUpstream requestOrder**: Chose the synchronous \`requestOrder\` assigned in \`handleRequest\` as routing-policy authority over asynchronous completion order because an older concurrent request may finish last and overwrite a newer session decision. \`captureRequestUpstream()\` must enforce newest-request-wins for routing and policy state.

<!-- lore:01a0c1e0-d886-7245-a38a-119bd8e24156 -->
* **checkpointWal nonblocking TRUNCATE**: Chose \`PRAGMA wal\_checkpoint(TRUNCATE)\` with temporary \`busy\_timeout=0\` over PASSIVE checkpointing or blocking waits because persistent readers can hold WAL read marks indefinitely. Restore the 5,000 ms timeout afterward and report \`busy\` promptly. Measure reclaimed bytes from the WAL file-size delta, not SQLite’s post-TRUNCATE \`log/checkpointed\` columns, which may be \`0/0\` after successful reclamation.

<!-- lore:01a0d597-8429-71b7-8039-3033be86d0a9 -->
* **Codex follow-up streaming boundary**: Codex/ChatGPT Responses follow-ups must use streaming because \`/backend-api/codex/responses\` rejects \`stream: false\`; standard Responses and Chat Completions JSON follow-ups retain non-streaming mode. Chose endpoint-specific transport over one universal follow-up mode because the Codex backend has a stricter wire contract.

<!-- lore:01a0d87c-522f-77ab-a0b5-94961babcc0a -->
* **Commitment-based cache warming**: Use a commitment-based warming model instead of per-window survival heuristics. Chose commitments over window survival because warming should reflect expected future reuse and economic value, not merely whether a request survives each individual window; preserve cost-aware decisions and avoid warming that is net-negative.

<!-- lore:01a03427-8813-7df7-8d2a-78d13cbdd483 -->
* **Conversation cache, system-delta, anchoring, TTL, and LTM pin invariants**: Use the 1h conversation-cache TTL. Keep stable harness rules in system\[0] and a session-frozen LTM baseline/cache-write anchor in system\[1]; put context changes in user-to-assistant tail deltas and re-pin only when material. The distilled prefix occupies transformed positions \[0,1], durable deltas remain near the tail, and orphan removal never touches the prefix. Keep CACHE\_ANCHOR\_MIN\_MESSAGES >= 2 \* CACHE\_ANCHOR\_STEP or the intermediate anchor never fires. Persisted ltmCacheText must equal ltmPinText; pin metadata is never sent upstream. ltm.forSession() must not change system bytes when selected entries are unchanged, except byte-identical re-anchoring. Do not independently render cache and pin text, move deltas forward, or cache meta passthrough.

<!-- lore:01a03ceb-4bc5-7274-a491-126925b7db40 -->
* **Database schema, initialization, vec0, tracing, and conflict invariants**: Physical schema is authoritative for vector reads: resolveReadMode() must inspect it even in transactions; committed vec0 and the connection latch override stale or forged storage\_mode metadata, and blob fallback is allowed only when base embedding columns exist. Repartition vec0 rows by exact primary key using DELETE→INSERT in one transaction; keep batch 500, 900-ID scopes, and no-op equal projects, empty scopes, or non-vec0 mode. Normalize schema\_version to MIGRATIONS.length. Tool-call upserts target (project\_id, session\_id, call\_id). A fresh db() with LORE\_NO\_DB\_TRACING="1" returns the raw connection and never invokes withDbSpan.

<!-- lore:01a0c44d-47c7-71af-ad3c-e4284eb9095a -->
* **db.ts project-ID cache invalidation**: Cache project path→ID resolutions per live SQLite connection and tenant, recording PRAGMA data\_version and memoizing only stable resolutions outside transactions. Detect transactions using runtime-specific state (Node isTransaction, Bun inTransaction, or the legacy probe), and memoize only after commit so failed or automatic rollbacks cannot leak mappings. Invalidate before project creation, merge, deletion, git\_remote backfill, and mutation callbacks; use data\_version to detect external changes. invalidateProjectIdCache() must inspect the singleton directly and never open a database.

<!-- lore:01a0bc06-312b-7da1-a793-ceb4afd03340 -->
* **DemandAwareWriter ownership boundary**: Use a shared DemandAwareWriter for duplicated backpressure mechanics while leaving abort, readers, protocol terminals, accounting, transactions, rollback, and ownership with each producer. Serialize ordinary writes FIFO; wake all waiters on demand or settlement; transform immediately before enqueue; invoke afterWrite once after successful writes; and settle close or error exactly once. tryWrite() succeeds only with immediate demand and no queued ordinary write, so keepalives remain droppable. Before migration, map state transitions and resource ownership, migrate simple translators first, and preserve prompt cancellation, backpressure, exact-once settlement, FIFO delivery, and post-delivery side effects through focused adversarial tests.

<!-- lore:01a0c1e2-9609-73a7-8ae6-463032f9065f -->
* **Embedding worker module location**: Chose \`\_\_filename\` for bundled CommonJS and \`import.meta.url\` for ESM worker resolution over one universal module-location expression or \`cwd\`. Bundled CJS always defines its filename, while Bun/tsx ESM workers resolve relative to the module URL; collapsing the branches can resolve workers from the wrong installation or invocation directory.

<!-- lore:01a03ae6-3b47-7655-a3e0-ab86e2c0985e -->
* **Embedding worker pool health, priority, residency, and OOM replacement**: Chose lazy pool growth and monotonic \`EmbedSlot.healthy\` over eager workers or clearing health on each \`LocalProvider\` generation, because host memory overstates container capacity and an OOM replacement’s temporary absence must not release proven residency for concurrent growth. A slot starts historically unhealthy, becomes healthy after its first successful embed, and never becomes historically unhealthy again; track current-generation residency/readiness separately. Count constructing, active, retiring, and replacement generations until confirmed exit, reserving full residency through reload. Unknown constrained headroom fails closed, while the primary keeps separate OOM backoff. Failure hooks and telemetry remain best-effort; never repeat timed-out scans on the event loop. \`lastSelfHealReprobeMs=0\` means never and primes the first tick.

<!-- lore:01a0b71e-3f83-7672-9416-51d3a11dda96 -->
* **EmbeddingPool memory divisor**: Chose per-worker token sizing from \`free / ceiling\`, resident-budget pool admission, and per-request reclamping over giving each worker full free memory or sizing only at construction because aggregate native ONNX residency can trigger an uncatchable cgroup \`SIGKILL\`, bypassing OOM backoff. Reclamp ordinary dispatch and direct OOM-respawn resubmission to current free memory; keep worker-growth reserve separate from active sequence-cap sizing so the pool never multiplies memory pressure.

<!-- lore:01a0c1e0-d647-78d9-bbdc-a68b72325f7b -->
* **EmbeddingProviderError privacy boundary**: Every remote, worker, and custom-provider failure must become one fixed, cause-free EmbeddingProviderError. Guard hostile Proxy and instanceof inspection, keep properties non-enumerable, and expose no body, statusText, diagnostics, exception text, or cause; Voyage may retain HTTP status only. Chose reconstruction of allowlisted control errors over rethrowing provider failures because provider objects and getters can leak sensitive data through inspection, serialization, or logging.

<!-- lore:01a0bfdd-18ad-7e5c-a170-376374a67fce -->
* **ensureUiAssetsStaged no-build**: Chose fully replacing \`UI\_STAGE\_DIR\` and using \`stageUiAssets({ build: "never" })\` from \`ensureUiAssetsStaged()\` over incremental staging or triggering Vite because stale assets must never survive beside a new set and test global setup must not generate UI builds. If \`packages/ui/dist\` is absent, leave \`/ui\` unavailable with HTTP 503 rather than building implicitly.

<!-- lore:01a0ce39-4ccf-7413-aca1-a63ad82e8306 -->
* **Eval AbortSignal propagation**: Eval cancellation must propagate one AbortSignal from owned-root signal handling through startup, gateway health/chat, EvalLLMClient.prompt(), limiter admission and backoff, retries, compaction, judges, replay, embedding backfills, and provider fetches. Check before every lifecycle boundary, including after awaited root creation; startup must not invoke run or start a gateway after cancellation. Pass signals as fetch options, never JSON. Sanitize abort reasons at public error boundaries because raw AbortSignal.reason can contain credentials or provider content. Chose end-to-end cancellation plus sanitized errors over cleanup-only cancellation or raw reason propagation because teardown alone leaves active work running, while raw reasons leak data.

<!-- lore:01a0ce39-4d2b-7f9f-a2a3-a9f7e8330690 -->
* **Eval compaction synthetic summary**: Eval baseline compaction replaces the conversation prefix with one synthetic assistant summary turn and retains the newest tail. Chose replacement over deleting the prefix or summarizing in place because the tail must preserve recent turn structure while the summary carries file paths, symbols, decisions, errors, configuration, goals, and progress across compaction passes.

<!-- lore:01a0d87c-5291-7221-adec-59b58d2fc715 -->
* **Fingerprint session tracking**: Track sessions with fingerprints instead of content markers. Chose fingerprint-based identity over embedded content markers because markers couple tracking to mutable prompt text and can collide with user content; fingerprints provide a stable session signal without injecting tracking tokens into content.

<!-- lore:01a0a99a-1bde-70bd-9fdb-2b77126909bc -->
* **forceWasm init-needs-wasm respawn**: Chose one fresh worker with sticky \`WorkerInitData.forceWasm=true\` over in-process backend retry or model purge because ONNX Runtime commits backend selection on first load, and Bun/native-addon incompatibility is not model corruption. Keep the request pending, suppress retiring-generation errors, confirm worker exit, then resubmit unchanged; preserve \`forceWasm\` across OOM respawns and treat a second \`init-needs-wasm\` as terminal. Never overlap native and WASM model instances because their combined residency can exceed memory.

<!-- lore:01a0266e-047c-7336-b58b-15792dc7246e -->
* **Foreground cancellation and bounded hostile I/O cleanup**: Race recall, follow-up setup, provider work, and every body read against foreground abort/deadline; byte caps cannot bound a reader that never settles. On cancellation, return promptly and asynchronously roll back late results or cancel/release late readers without awaiting hostile cleanup. \`readForegroundBody\` must bound non-OK diagnostic reads by timeout and abort, always release the reader, sanitize content, preserve status, and clamp far-future HTTP-date \`Retry-After\` to 300 seconds. Suppress cleanup failures. Cancellation tests must use an upstream stream that does not close naturally: cancel the downstream reader after initial data and assert the upstream \`cancel()\` callback runs. Test stalled bodies, handoff races, late results, and non-settling cleanup.

<!-- lore:01a0d87c-52f0-7534-ac1b-12f512796253 -->
* **forSession FTS5 BM25 ranking**: Use FTS5 BM25 for \`forSession()\` scoring instead of the prior custom scoring heuristic. Chose SQLite’s native BM25 ranking over duplicated application scoring because it centralizes text relevance behavior, uses the existing FTS5 index, and avoids maintaining divergent ranking heuristics.

<!-- lore:01a0465f-5b87-78dd-b68a-c231f8d1675e -->
* **Gateway access, startup ownership, daemon identity, and bounded shutdown**: Chose numeric socket-peer authorization and generation-bound daemon ownership over trusting request headers or PID alone because headers are forgeable and PID reuse can target a successor. Authorize before CORS, imports, body reads, auth, storage, or upstream calls; reject non-loopback peers unless exact remote-management checks pass. CLI startup alone owns signals; embedded callers use quiet local mode. Daemon records require pid, processIdentity, generation, and owner token. Revalidate generation immediately before \`SIGKILL\` and never signal a successor. Track every socket and use deadline-driven close, force-close, and destruction so shutdown remains bounded and idempotent.

<!-- lore:01a0247f-e975-7854-8997-50d4964ee51c -->
* **Gateway authentication, routing authority, credentials, and re-encoding boundaries**: Authenticate Lore only with an exact configured x-lore-gateway-token match, then strip it before parsing; never forward, log, store, expose, or pass it via CLI. Use one immutable UpstreamSnapshot as authority for URL, caller selection, protocol, provider, model, headers, and options. Reject unknown protocols before serialization or network activity. Extra administrator credentials apply only to configured provider/worker bases; caller origins require the normalized HTTPS allowlist and receive none. Never intercept gateway-bound or local-host requests, though gateway requests still call observeRequestHeaders(). Auto-cross-routing without an explicit override is untrusted when protocol or origin changes: parse and re-encode instead of replaying client compression, and recompute Content-Length. Preserve native encoding only for same-protocol, same-origin passthrough or explicit provider/upstream overrides.

<!-- lore:01a0266e-04ff-7eb2-903e-9283051212eb -->
* **Gateway request classification, compaction, and SSE lifecycle**: Reject missing or headerless requests with HTTP 400 before authentication, routing, or admission. Process \`/lore:\*\` commands, Claude Code side channels, structural compaction, then normal turns. Commands and compaction never reach upstream; side channels and meta requests bypass LTM, distilled prefixes, and compaction classification. Restore a persisted project path before compaction initialization rather than binding a pathless probe to gateway cwd. Open Anthropic SSE immediately and emit protocol-safe pings during summary generation and 429 retries, translating that canonical stream for other clients. Successful or null live compaction emits a complete Anthropic lifecycle; rejected summary generation emits an errored stream without \`message\_stop\`, never a successful empty turn that could wipe client context.

<!-- lore:01a0b3d5-1939-7c82-a435-f6727ee5fbbf -->
* **GatewayResponse optional usage**: Chose optional \`GatewayResponse.usage\` over a required field because vLLM and partial provider responses may omit runtime usage. Preserve validated usage when available, but absent \`prompt\_tokens\_details.cache\_write\_tokens\` must remain \`undefined\`, never synthetic zero, so analytics and cost tracking cannot mistake missing data for a reported zero write.

<!-- lore:01a0c3cb-949a-7a46-b507-159b478891ae -->
* **Gemini and Bedrock protocol invariants**: Native Gemini ingress always remains on the Gemini wire protocol; do not downgrade it to OpenAI compatibility merely because a provider route advertises OpenAI. Bedrock Mantle always uses the Anthropic wire path and self-builds its regional URL, with model remapping confined to the Anthropic branch. Chose these explicit invariants over generic provider-protocol selection because SDK semantics and special URLs differ; normal and snapshot paths must share \`isBedrockMantleDispatch\`.

<!-- lore:01a0d254-4aea-79e3-b502-8fbc2a4e412f -->
* **Gemini thought-signature part boundaries**: Gemini stream aggregation preserves provider part order and keeps same-frame parts distinct across thought-signature boundaries; a newly signed part is never merged into an earlier block. Chose signature-safe separation over text-only coalescing because merging signed parts changes provider provenance and can invalidate reasoning continuity.

<!-- lore:01a0c1e0-d823-768d-b725-a0c8d08b36ee -->
* **harness queryDB fresh reader**: Harness queryDB() must use a fresh read-only SQLite connection for every assertion and close it in finally, so assertions observe the latest committed state without sharing writer lifecycle or cached connection state. Returning \[] on absent or failed databases keeps cleanup paths bounded. Reusing the core singleton looks cheaper, but can observe stale state or interfere with the server’s writer ownership.

<!-- lore:01a0c464-98e2-79ca-979d-f2a49e779628 -->
* **Hosted gateway filesystem isolation**: Hosted gateways never share a filesystem with clients and must not attribute path-less requests to the gateway’s own \`process.cwd()\`. Use the unattributed remote path namespace and disable client-controlled filesystem operations in hosted mode. Treating the server working directory as a convenient default looks harmless, but it can expose or mutate host files across the client boundary.

<!-- lore:01a0c1a7-10ad-7178-bce2-1b71f36a7bc9 -->
* **isCurrentDatabase identity-only check**: Chose \`isCurrentDatabase(connection)\` as the read-only \`instance === connection\` identity check over reopening or resolving the current database path because deferred derived-cache writes only need to confirm singleton ownership. Reopening looks like stronger validation but introduces I/O and can target a path changed after the original connection; this function must never open a database.

<!-- lore:019edff0-4709-74cc-98eb-ad3bd46163c1 -->
* **Knowledge synchronization, encryption, replay, privacy, and eviction invariants**: Knowledge sync invariants: Sync only through EventV2 publication/replay, which owns sequencing, claiming, persistence, recovery, and projections. The relay receives only end-to-end encrypted approved entries, never plaintext or participant keys; sharing remains reversible. Do not send scope\_id; derive scope and table IDs server-side. Remote apply rejects ownership collisions, atomically upserts and queues under suppression, preserves local confidence/decay/distillation state, invalidates vectors only for changed content, and strips boundary columns. Restored archival messages are distilled=1 and use restored\_at for pruning. Evict only unpromoted project knowledge below confidence 0.2; never evict promoted or cross-project rows.

<!-- lore:01a0d597-84f3-7605-9510-2b8fb1602f7b -->
* **local mode disables remote gateway**: \`--local\` and in-process \`lore run({ local: true })\` always disable remote mode, even when the bind address is non-loopback or \`LORE\_REMOTE\_GATEWAY=1\` is set. Chose explicit local-mode authority over environment or bind heuristics because local execution must not unexpectedly cross into remote management or remote gateway behavior.

<!-- lore:01a0c1f1-682d-768a-b324-30f39ede9ca0 -->
* **lookupProviderRoute nonblocking refresh**: Chose immediate fresh-or-stale cache lookup plus background \`fetchModelData()\` refresh over awaiting models.dev because provider-route resolution is on the request hot path and must never block on network availability. \`ensureModelDataReady()\` may wait only under its explicit bounded timeout; failed or timed-out prewarms must remain retryable.

<!-- lore:01a0d87c-5162-7775-a265-6f3f1dc8360e -->
* **Loops waitlist integration**: Use Loops for the waitlist form instead of maintaining a local waitlist implementation. Chose the external Loops integration over a repository-owned form because it removes duplicated subscription and email-delivery plumbing; preserve the integration boundary rather than recreating provider behavior locally.

<!-- lore:01a0a4bd-c8e0-7fc0-ab23-19af177bddb5 -->
* **Lore closed-loop memory, contradiction, and fused retrieval architecture**: Lore uses experience → distillation → consolidation → retrieval → selective injection because compaction can erase retrieval cues, storage without injection is only archival, and similarity alone cannot establish truth. Preserve source history and temporally grounded observations. Rank exact and semantic evidence with recency, provenance, deduplication, and supersession. Retain unresolved contradictory entries, rank them by confidence, and surface them for resolution; retrieval alone never raises confidence. For scope="all", fuse session, project, and opted-in cross-project knowledge in one ranking, downweighting knowledge BM25 by 0.6 when session evidence exists. Reconstruct only budgeted working context.

<!-- lore:01a08d30-7d77-76fc-a430-a38e228e60a5 -->
* **Lore framing markers and project knowledge catalog IDs**: Lore framing notes must retain their protocol-significant leading marker. Knowledge-delta notes start with the exact stable prefix "\[Lore knowledge update —" because parsing, cache assertions, and legacy migration matching depend on startsWith(). Reserve bracketed Lore system markers for system blocks; never place them in tool descriptions. Project catalogs must state that Lore knows what project knowledge exists and can recall it, preserve full canonical k:/xk: IDs for exact ltm.get(id) lookup, and never advertise tombstoned entries.

<!-- lore:01a0a117-675d-7bc6-b654-83131df04706 -->
* **Lore temporal ground truth and observation logs**: Keep complete local temporal messages in SQLite as the authoritative ground truth, with full-text search. Distillations, curated knowledge, entities, and vectors are derived, lossy, and revisable, so they must never replace source history. For observer/reflector processing, use plain-text timestamped observation logs rather than rigid JSON summaries; dated event logs preserve chronology, changing decisions, and cross-session relationships that structured summaries tend to erase.

<!-- lore:01a0ce65-d168-7774-806d-c8ad049a3e11 -->
* **lore-memory-only baseline injection**: The memory-only eval baseline must disable knowledge, LTM, and curator injection; recall may execute for behavior coverage, but its results must never enter the model context window. Chose no injection over including retrieved memory because this baseline measures behavior without memory-derived context; injecting results would contaminate the comparison and make it indistinguishable from a memory-enabled baseline.

<!-- lore:01a0cb68-4ba9-72f1-9657-f9940b8ef09e -->
* **node:sqlite driver boundary**: The driver is the sole boundary for node:sqlite imports: db.ts and higher-level core code must use the driver rather than importing SQLite directly. Chose one driver boundary over allowing tests, gateway code, or helpers to instantiate node:sqlite because runtime selection, tracing, initialization, and future bundling must remain centralized; scattered imports can bypass those invariants and accidentally bind stale or incompatible implementations. In-memory tests may use the approved driver abstraction, not direct node:sqlite imports.

<!-- lore:01a0bf59-4911-7517-8859-353dca02c27a -->
* **onnxruntime-web pthread boundary**: Chose shipping only the current single-thread \`onnxruntime-web\` WASM path over bundling pthread worker assets because the shipped runtime never calls its pthread \`new Worker(...)\` branch. If runtime configuration or dependency behavior enables that branch, revisit \`bundle.ts\` immediately; assuming the copied WASM module is sufficient looks safe today but becomes a latent production crash when worker resolution starts.

<!-- lore:01a0c3ea-a0d8-7142-bd9a-bdcdf7a878e9 -->
* **parseOpenAIRequest tool coalescing**: \`parseOpenAIRequest\` must coalesce each consecutive group of OpenAI \`role: "tool"\` messages into one internal user message containing all \`tool\_result\` blocks. Chose internal coalescing over preserving OpenAI message granularity because downstream Anthropic-shaped pairing expects every result for one assistant tool-call turn in the immediately following user message; separate tool messages appear orphaned and trigger tool-use removal. A later assistant turn starts a separate group.

<!-- lore:01a0d7d1-6d59-7063-bec8-015ca7db8a56 -->
* **pipeline parallel recall guard**: Anthropic and Vertex effective wire paths disable parallel tool calls when the injected \`recall\` tool is present by forcing \`tool\_choice.disable\_parallel\_tool\_use: true\`; OpenAI Responses uses \`parallel\_tool\_calls: false\`. Preserve an explicit client \`tool\_choice: { type: "none" }\` unchanged rather than replacing it with the disable-parallel form, because removing \`none\` re-enables tools and can unexpectedly expose \`recall\`. Bedrock Mantle is excluded because its Anthropic-compatible endpoint rejects the field. Chose provider-capability gating plus explicit-none preservation over a universal guard because Bedrock would return HTTP 400 and client tool intent must remain authoritative.

<!-- lore:01a0d1d1-ee34-71b7-9d6b-a50df03e5806 -->
* **pipeline snapshot synchronous commit**: Transfer callbacks and related snapshot state must be captured and invoked inside one synchronous commit, never across an \`await\`. An asynchronous snapshot appears easier to sequence, but another stream transition can mutate pending transfers before capture, producing lost or duplicated delivery; commit must establish the immutable batch first, then invoke it synchronously.

<!-- lore:019f04c9-26e4-794e-8d94-dd107b7bf454 -->
* **Prompt-delta, provenance, and structured recall replay invariants**: Prompt-delta and replay invariants: Never insert a delta between assistant tool\_use and its following user tool\_result or into the true tail. safeDeltaInsertIndex() caps before the tail and walks backward across tool calls; keep the memory closer as a separate assistant message. Normalize provider tool\_result strings or block arrays into GatewayContentBlock\[], preserving multimodal output. Keep provenanceContent replay-only and out of visible or temporal content. Recall follow-ups use structured tool\_use/tool\_result pairs. Preserve Anthropic thinking/signatures and opaque Responses output while excluding function calls, outputs, and item references from replay. When coalescing assistants, retain leading thinking blocks.

<!-- lore:01a0cdce-46bc-7e9a-979c-127fc1092bdb -->
* **promptOpenAI Copilot endpoint headers**: Chose backend-specific OpenAI-compatible request construction over one generic \`/v1/chat/completions\` path because GitHub Copilot serves \`/chat/completions\` without \`/v1\` and expects \`copilot-integration-id: vscode-chat\` plus \`x-github-api-version\`. Preserve standard \`/v1/chat/completions\` for OpenAI; silently sharing the path or omitting Copilot headers causes intermittent malformed-request failures.

<!-- lore:01a08daf-ae7e-72a6-89bb-2c59d44753ab -->
* **Provider and client gateway translation invariants**: Assistant responses must never contain \`tool\_result\`; tool results belong to user-side request or replay messages. A streaming client receiving a buffered routed response needs a complete client-native stream including thinking, opaque blocks, and \`tool\_use\`. For Vertex, strip Anthropic headers; use OAuth, body \`anthropic\_version\`, and GA \`cache\_control\`. Treat \`req.system\[0]\` as the sole authoritative billing header. Round-trip Gemini reasoning summaries as reasoning, not visible answer text. For Codex, force \`store:false\` and send full-conversation \`input\`: ChatGPT Codex rejects \`store:true\`, while \`previous\_response\_id\` would duplicate history Lore already supplies. Do not retain client \`store\` as dead gateway state.

<!-- lore:01a08d30-7d23-7fc3-8144-43a85f84a048 -->
* **Recall recovery transaction and replay invariants**: Chose recovery-specific transfer-only commit over the ordinary full recall commit because anchorless recovery has no replayable marker; storing its pending replay entry creates an orphan and consumes recall-store capacity. Buffered and streaming recovery must commit accepted transfers only after downstream EOF, persist no replay entry, and roll back on cancellation, delivery, callback, or postResponse failure.

<!-- lore:01a0265e-a7c0-71e6-837e-4e0abd13bf32 -->
* **Remote session and client-controlled project identity authority**: Chose credential-scoped sessions and on-disk Git identity over client project headers because forged or stale metadata can cross project boundaries. \`resolveTrustedRemote\` must always prefer an existing disk remote; in local mode with no disk remote, drop \`x-lore-git-remote\` rather than adopting it, preventing remote tampering and the stale-header git-remote magnet. Identity proof must never expose upstream response content.

<!-- lore:01a0c1e2-9764-78d9-af29-d1a8fe775bec -->
* **resetPipelineState background drain**: Chose explicit \`inFlightBackground\` ownership and bounded settlement over trusting limiter idleness because urgent work may outlive the limiter’s active-task count. Track each promise with \`finally\` removal; a non-fast reset must clear queued limiter work first, drain active tasks, then boundedly settle background promises. \`boundedSettle\` must always clear its timer so reset cannot leak handles.

<!-- lore:01a0c3d9-e0ee-7040-b021-f5be7d6aed01 -->
* **resolveLastSeenProvider avoids WRONG tags**: Provider tagging must never produce a WRONG tag. Explicit provider headers take precedence over URL inference; user-specific upstream URLs, including routes whose static mapping is null, remain unknown or agnostic rather than matching a static provider. Chose conservative unknown tagging over best-effort URL classification because a false provider label corrupts routing/accounting evidence and is harder to detect than a missing label.

<!-- lore:01a08126-4de0-7a17-acbb-36db061a13de -->
* **Responses ingress streaming boundary and JSON.parse parity**: Use incremental ingress parsing only for /v1/responses and /v1/codex/responses, whose shared normalizer can consume input items incrementally. Below 256 KiB, preserve exact JSON.parse behavior; above it, replay once and stream $.input.\* through createInputItemsBuilder(). Streaming and native paths must agree across the threshold. Decode UTF-8 non-fatally with ignoreBOM:true, reject U+FEFF outside JSON strings, then tokenize re-encoded text; this handles the parser library accepting a BOM after whitespace when JSON.parse rejects it. Test threshold crossings, malformed UTF-8, duplicate keys, trailing data, roots, and split escapes or multibyte characters. This bounds gateway parsing amplification only; it cannot recover memory already spent when OpenCode constructs and serializes the full body.

<!-- lore:01a020f6-0b09-7c5b-9896-1f6d39cd6f56 -->
* **Responses raw-before-project accumulation**: Chose raw-before-project accumulation over projection-first or suppression-first handling because \`postResponse\`, lifecycle reconciliation, and terminal shorthand require every accepted provider coordinate. Apply each principal or continuation event exactly once before fallible projection, callbacks, limits, or suppression—including response-side \`item\_reference\`; references remain private and never enter wire output or completed public projections.

<!-- lore:01a0ce75-6de6-7792-a2fd-24b663289af2 -->
* **scoreRetrieval omits vacuous zero**: Objective retrieval scoring is omitted when no ground-truth anchors exist, and fixture-mode retrieval remains undefined. Chose omission over scoring an empty hypothesis as zero because scenarios without expected facts cannot distinguish failure from an unscorable case; including them would dilute aggregate results with a vacuous penalty.

<!-- lore:01a039c5-3a47-7ce4-b75c-a43a3ebb3ac0 -->
* **Sentry telemetry, tracing, and test-isolation invariants**: Sentry telemetry and test isolation: NODE\_ENV === "test" unconditionally disables Sentry before any other enablement logic. Tests must not initialize transports or worker threads; Sentry uses globalThis.fetch and initialization can race mocked fetches and alter captured state. Outside tests, label production builds "production" and explicitly enabled development builds "development". Use one tracing owner: install httpServerSpansIntegration without the superseded httpIntegration to avoid duplicate events and overlapping instrumentation.

<!-- lore:01a0ce6e-4b70-780b-9250-22c9bc7c8ea9 -->
* **server.ts dedicated upgrade listener**: HTTP-only gateway WebSocket rejection must be handled by a dedicated \`upgrade\` listener, not the ordinary request handler. An upgrade request does not enter the HTTP/1.1 request path, so placing the rejection in request handling looks complete but leaves upgrades unhandled. Return the protocol-specific status through \`socket.end()\` so clients receive the response instead of an \`ECONNRESET\`.

<!-- lore:01a020e7-ab76-741c-a37f-6ecdceabe20d -->
* **SessionV2 durable execution and monotonic state**: SessionV2 durable execution: prompt() durably admits exactly one session\_input row before advisory execution wake; resume:false is admit-only. The serialized runner promotes inbox rows into visible user messages and makes one explicit llm.stream(request) call per provider turn after reloading projected history; do not bridge through legacy SessionPrompt.loop(). Preserve project isolation, matching, parent-child relationships, replay, and stable identity across restarts. Reject stale turn completions rather than using last-writer-wins. Keep frozen prompt/LTM bytes and persisted delta positions cache-stable, bound or coalesce updates, and safely default malformed persisted values.

<!-- lore:01a0a45d-35e2-7f79-8f5c-06b34bcedeec -->
* **Source checkpoint leases and nonblocking SQLite writes**: Use a lease on one accepted-request checkpoint because \`sourceCount\` represents request reconstruction, not an observation watermark. Keep \`claim()\` and \`publish()\` inside the response’s temporal savepoint so stale requests cannot replace newer checkpoints or resurrect deleted owners; release temporary transcript arrays on completion. Foreground source-window creation must not wait on SQLite writers: temporarily set \`PRAGMA busy\_timeout=0\`, attempt insertion nonblockingly, and restore the prior timeout in \`finally\`. Checkpoint caching is only an optional foreground optimization.

<!-- lore:01a0c3cb-94ff-734f-a872-1e90088d01b2 -->
* **SSE field-first framing**: An SSE stream must begin with a \`data:\` or \`event:\` field before any event payload is parsed. Treating arbitrary JSON or body text as an SSE event looks tolerant, but it masks protocol mismatches and turns provider error bodies into misleading malformed-stream failures. Keep SSE detection separate from JSON-body handling and validate framing before event translation.

<!-- lore:01a0bdef-75b5-718a-b9d3-8c375ba12468 -->
* **startServer stopPromise retryability**: Keep the upstream fetch runtime split strict: Node may lazily import Undici, but Bun must never import, evaluate, or include Undici; Bun uses the node:http/node:https bridge. Gateway upstreamFetch() bypasses patched global fetch, uses the explicit dispatcher seam in Node, disables runtime body/header timeouts, and blocks redirects. A shared implementation looks simpler, but Undici evaluation is incompatible with Bun and global interception would make upstream behavior non-authoritative. Transport tests must unmock fetch and dynamically load the module after selecting the runtime.

<!-- lore:01a0bf7a-1722-745e-8818-2d082978fe9b -->
* **startSyncScheduler shutdown serialization**: Chose awaiting the current periodic sync cycle before the shutdown push over running both concurrently because overlapping pushes can corrupt sync cursors. Stop clears both timers, waits for \`inflight\`, rechecks mode and sync enablement, then performs one best-effort final push; failures must receive the original \`Error\` object so file logging keeps the stack and Sentry can capture it.

<!-- lore:01a0bc06-307b-780e-805e-8c1b39bd42de -->
* **streamResponsesRecallAware decomposition**: Chose staged transition owners over a wholesale rewrite because the 4,666-line closure amplifies defects, but missing atomic ownership and wrong ordering are the root causes; mixing repair with restructuring destroys fail-first evidence. Extract pure helpers first, then one lifecycle tracker, \`VisibleOutputProjection\`, separate principal/continuation buffers, and transaction/recovery/delivery last. Principal output may already be public, while continuation output is shifted and transactional, so never merge those buffers.

<!-- lore:01a03ae6-3bb6-76d0-a294-c2bbd20efff7 -->
* **Temporal embedding storage, startup, and scoped retrieval invariants**: Build temporal embedding units on structural newline/unit-separator boundaries, preserving prose, reasoning, and reduced tool envelopes. Cap temporal chunks at 64 by retaining the first 63 and joining overflow into chunk 63. Embed every path in token-bounded batches with MAX\_BATCH\_TOKEN\_AREA = 4096, always admitting the first item and preserving order, because ONNX pads batches to their longest sequence. Empty replacement is an immediate no-op. For nonempty vec0 replacement, validate first, then atomically delete canonical message\_id#N keys and insert all vectors in one savepoint, deleting in 64-key batches. Partition temporal\_vec only by project, retain session as metadata, and use chunk\_size=64.

<!-- lore:01a0c1e2-9711-7b4e-8722-a1f3a597826c -->
* **Temporal queue activeDrain**: Chose one shared \`activeDrain\` with local-only sub-batches capped at 8 rows/256 KiB over concurrent drains or a whole-drain deadline because queued time can expire healthy provider work and duplicate inference. Start each deadline only after normal admission; validate fingerprint, full-content hash, authoritative content, storage mode, cardinality, and vector before commit. A settlement timeout may stop waiting but must never clear \`activeDrain\` or cancel provider work.

<!-- lore:01a0d87c-51d1-7a03-9059-8619d02d64e1 -->
* **Tier-based context cost model**: Use tier-based, cost-aware decisions instead of an EMA-driven context cap. Chose explicit cost tiers over a moving EMA threshold because tier policy is explainable and stable across workloads, while EMA-driven limits can drift with recent traffic and make behavior difficult to predict or audit.

<!-- lore:01a0cde6-c660-7bbc-8fd9-75d9f702ad6f -->
* **upstreamFetch runtime-neutral override**: Use a runtime-neutral test-only fetch override before transport selection in \`upstreamFetch()\`, and restore it only when the expected active owner still matches. Chose this over a universal Undici MockAgent because Bun uses the Node \`http\`/\`https\` path and must never load or evaluate \`undici\`; the seam keeps models.dev interception offline in both runtimes without bundling Undici into Bun.

<!-- lore:01a0b71e-3fda-799a-a8b6-69dc63a46779 -->
* **VectorWorkerPool isolation contract**: Chose off-thread vector recall/LTM execution over main-thread vec0 scans because one slow SQLite search must never block another session’s stream or the combined event loop. Dispatch/search/read APIs resolve unavailable or empty sentinel results rather than reject; timeout must not rerun an uninterruptible scan in-process or permanently latch the pool broken. Tests inject workers through \`\_setTestVectorWorkerFactory\`, never real Node workers, so failure and timeout behavior remains deterministic.

<!-- lore:01a0bf7a-16cb-7b5e-86ca-b01db1362f1e -->
* **warmupEmbedding startup cold-load**: Chose startup \`warmupEmbedding()\` over lazy first-use initialization because the roughly 21-second ONNX cold load can otherwise let the first real distillation embed race gateway teardown and lose its \`distillation\_vec\` write. Keep warmup in process initialization before real embedding demand while preserving background pause and shutdown ownership.

<!-- lore:01a03650-2854-7dd2-8a73-f5b0dd747897 -->
* **Worker retry policy and provider-failure classification**: Urgent and background worker calls share one Claude-Code-like retry policy: honor \`Retry-After\`, otherwise use jittered exponential backoff from 500ms through 32s with up to 10 retries. OAuth and Claude-Max 429s commonly require 30–60 seconds, so a fast-failing path only compounds rate limits. If a worker successfully contacts the provider and returns an upstream provider error envelope, classify it as \`upstream-error\`, not worker incapability; otherwise capability learning can incorrectly disable a functioning worker path.

<!-- lore:01a0c4dd-8fdd-777d-a701-44c0011eb080 -->
* **worker-model provider pricing keys**: Store models.dev pricing by provider-qualified key (\`providerID/modelID\`) rather than bare model ID, because multiple providers can publish the same model and last-write-wins silently charges the wrong route. Then apply canonical first-party provider metadata as an explicit second pass for capability fields; \`.set()\` preserves insertion order while correcting aggregator overrides.

### Decision

<!-- lore:01a0c4ae-c718-7c72-8265-0c59e2a6689e -->
* **BYK/loreai squash-only merges**: Chose squash merging over repository merge commits because BYK/loreai rejects merge commits at the GitHub API boundary. When a merge attempt reports “Merge commits are not allowed,” retry with squash only after rechecking the exact PR head, required checks, reviews, mergeability, and working-tree state; do not interpret a failed merge-method attempt as a failed PR. Verify the resulting squash commit and PR state after merging.

<!-- lore:01a0b48e-d684-73e2-87f3-c0401be45e2f -->
* **codex-rate-limits.ts request-wide bounds**: Chose exact Codex quota schemas, adjacent-only deduplication, and re-sanitizing each combined principal/continuation list over independently capped segments because concatenation bypasses the request-wide 64-event and 16 KiB limits. Preserve reviewed fields in order and stop at the first shared bound. Tests must cross both limits across multiple continuations and use \`Q1 → Q2 → Q1\` to kill global-dedup mutants.

<!-- lore:01a0c1a0-e06c-7cb2-8471-9bb6ad31f85e -->
* **createTestDatabasePath run ownership**: Chose \`createTestDatabasePath()\`/\`createTestDatabaseDirectory()\` beneath the module-captured file-owned \`LORE\_TEST\_DB\_ROOT\` over ad hoc \`/tmp\`, \`mkdtempSync()\`, timestamps, random paths, or rereading the environment because run cleanup cannot own external allocations and tests could redirect mutable environment state. Validate the root in an initializer so the captured binding is concretely typed as \`string\`; runtime narrowing alone looks sufficient but TypeScript does not preserve it inside exported functions.

<!-- lore:01a0c1e2-97ba-77e3-8a0e-36bebb8c1fdb -->
* **Encryption cancellation disables sync**: Chose disabling sync whenever encryption setup is canceled over preserving an already-enabled state because encryption becoming mandatory makes continued pushes potentially plaintext. Cancellation performs no sync and requires explicit re-enabling with a passphrase; retaining prior enablement looks user-friendly but violates the encryption boundary.

<!-- lore:01a0c470-1c2c-7bd0-8191-1134e7cad418 -->
* **per-file root identity capture**: Chose chain budgets over the fixed recall cutoff because long sessions need bounded but productive recall chains rather than an arbitrary global stop. The selected approach is implemented by merged PR #1750 (\`75c5dd1b6dbb3e3f7df0f969406ff9fb5109ea8e\`); future recall work should preserve per-chain budgets, exhaustion handling, and diagnostics instead of restoring a fixed cutoff.

<!-- lore:01a042a2-c951-7976-9b64-77b1e8da4239 -->
* **Project-ID cache cross-test invalidation**: Embedding scheduling, validation, ranking, and vector GC: Backfills measure byte length with length(CAST(content AS BLOB)) because SQLite TEXT length stops at NUL. Enqueue before invalidation; increment scheduled only when enqueue changes a row, while always advancing the cursor. Mixed drains validate inference results and cardinality before committing; failures preserve queue rows and prior vectors. Normalize valid vectors at embed() so JavaScript and sqlite-vec cosine rankings agree, copying inputs and leaving zero or non-finite vectors unchanged. GC pages canonical keys on a read worker and guarded-deletes only returned keys. Jobs must be parameterized and structured-cloneable; timed-out workers are retired and cursors preserved.

<!-- lore:01a0bc06-30d4-7fd0-99dc-ef669c187f22 -->
* **prompt retry transition reducer**: Chose immutable typed retry state plus a pure transition reducer over one enum-style FSM because routing, corrections, fallback, transport, breaker effects, and cleanup form a combinatorial state space. Keep \`semanticContentConsumed\` attempt-local; persist other correction latches across attempts. Protocol/model changes retain transient budget, fallback shrinks candidates and resets only temperature/thinking/length, capability learning follows successful recovery, abort blocks dispatch, rebuilds reapply active credentials and the 4 MiB cap, and health settles once.

<!-- lore:01a0bf7a-1672-721d-8bb7-05fe20e8f11c -->
* **runStartupBackfill predicate parity**: Chose mode-aware \`missingEmbeddingSql()\` backlog counts and pre-cutover stale-blob clearing over generic blob-null counts or direct blob→vec0 copying because startup metrics must match the rows the loops will process, and stale model/dimension vectors must never seed vec0. Never scan the full vector corpus on the startup writer; orphan discovery belongs to idle read-worker maintenance. Always emit embedding coverage statistics to stderr so degraded coverage remains visible.

<!-- lore:01a0cb68-4c01-7832-9dad-42d144815310 -->
* **SQLite edge-binary datastore**: Chose SQLite as Lore’s datastore of record over PostgreSQL because the product ships as a single self-contained edge binary and must not depend on an external database process. PostgreSQL looked stronger for concurrent server workloads, but its separate service lifecycle conflicts with the deployment model; SQLite WAL, FTS5, and controlled connection ownership provide the required local durability and concurrency.

<!-- lore:01a0a0f5-7eac-789d-b0be-2ea39c772780 -->
* **Temporal persistence, noStore, and user-resumption gaps**: Inter-turn gap tracking excludes requests after \`prevStopReason === "tool\_use"\`, because automated tool continuations distort session-survival estimates. Record local and global gaps only for genuine user resumptions, then update \`lastUserTurnTime\`. In \`postResponseForTenant\`, \`noStore\` suppresses temporal persistence and cached request-body snapshots, not LTM, recall, or gradient processing. Tool results following a no-store \`tool\_use\` intentionally become no-ops because no seed row was persisted.

<!-- lore:01a09733-6f47-736d-bff6-2d383f484c83 -->
* **Upstream routing transport, encoding, and authorization**: For auto-routed upstreams, serialize uncompressed JSON instead of replaying the client's Content-Encoding; preserve encoding only for native passthrough or explicit X-Lore-Upstream-URL/X-Lore-Provider routes. OpenAI-protocol upstream requests must always use Authorization: Bearer ${cred.value}, regardless of the caller's scheme. Remote gateway mode must reject every http: upstream before forwarding credentials or bodies, including manual configs and compact fallbacks; allowlists authorize only exact HTTPS origins.

<!-- lore:01a0b35c-ce09-7246-8c06-afd1da6558d9 -->
* **Worker incapability evidence**: Chose evidence-based capability learning over permanent classification from malformed or heuristic failures because bare \`error\`/\`response.error\`, contradictory incomplete-status SSE, transient envelopes, and unrelated HTTP 400 text prove request/upstream failure—not model support. Classify these as \`upstream-error\` without \`recordEmptyWorkerResponse()\` or \`markWorkerIncapable()\`; learn stripped \`temperature\`/\`thinking\` only after a successful 2xx retry, and treat HTTP-200 data-policy envelopes as incapability only for an explicit embedded 404 that matches the policy.

<!-- lore:01a0b36c-7643-7854-a55f-ef888809a50a -->
* **Worker protocol resolution**: Chose explicit protocol and route-table precedence over provider-family fallback because mapping OpenAI-family traffic to Anthropic selects the wrong parser, while collapsing Codex to Chat Completions loses Responses semantics. Preserve explicit Gemini; map Anthropic→Anthropic, OpenAI/DeepSeek→OpenAI, and \`openai-codex\` always→\`openai-codex-responses\`; only protocol-less OpenCode and unknown providers default to Anthropic.

### Gotcha

<!-- lore:01a0c1e0-d764-7301-a693-9fdf36161c53 -->
* **/lore:curate JSONL parsing**: Trap: parsing a \`/lore:curate\` log with one whole-file \`JSON.parse()\` looks correct for JSON output, but the artifact is JSONL and multiple records can abort evals or hide an error. Fix: parse non-empty lines independently, surface the first record with \`type: "error"\`, ignore malformed auxiliary lines, and derive score validity from primary result/session artifacts.

<!-- lore:01a0ba5a-6030-7a4d-9b2d-fec6eabf723f -->
* **assertTerminalOutputMatches quadratic scan**: Trap: calling \`expected.findIndex()\` from zero for each terminal item looks simple and correct for ordered output, but makes high-cardinality terminal reconciliation quadratic; tests that omit terminal output never exercise it. Fix: reconcile with an identity/index map or monotonic iterator and enforce an operation bound using a full ordered terminal snapshot.

<!-- lore:01a0cb28-0c38-7eef-93b8-ca9c78ceffe6 -->
* **Atomic marker rename watcher**: Trap: watching only filesystem \`change\` events looks sufficient because the marker file is written during fixture setup, but atomic \`.pending\` → final \`renameSync\` publication may emit \`rename\` rather than \`change\`; the harness can miss readiness and time out. Fix: watch the event types produced by atomic publication, or poll/read the final marker with a bounded deadline, and test the actual rename path.

<!-- lore:01a0a48f-d5c8-7064-9683-9165737ce0ab -->
* **Bounded V8 heap-snapshot capture, analysis, and RSS attribution**: Never treat one V8 snapshot, a growing JavaScript cohort, or falling RSS as whole-process memory proof. Reconcile every isolate with RssAnon + VmSwap, allocator arenas, WASM/direct mappings, external stores, workers, and cgroup anon/file/kernel/swap; an RSS fall offset by swap is paging. Snapshot capture can retain profiler workspace and cgroup-charged file cache. Freeze process identity and hashes, avoid production mutation, and analyze large snapshots outside the service with bounded streaming C/C++ rather than wholesale JS/Python loading. Use fixed-size node records, indexed strings, and externally sorted reverse edges; count weak edges but exclude them from retaining paths. Prove allocator retention only with isolated pre/post capture, malloc\_trim(0), and a matching no-snapshot control.

<!-- lore:01a0ba3c-fc40-707c-8cdf-05cf16134047 -->
* **Buffered Responses refusal compatibility**: Trap: applying strict stream projection directly to legacy buffered refusals looks consistent, but non-string refusal values historically mean unusable empty content rather than protocol failure. Fix: in non-stream accumulation, normalize nonempty string refusals to text and treat empty, whitespace, \`undefined\`, or numeric refusal values as unusable empty content; still reject structurally invalid refusal placement or malformed message content.

<!-- lore:01a0cd24-bdf0-7080-91df-39cec6831d4d -->
* **Bun models.dev offline interception**: Trap: Node’s undici MockAgent makes fixture mode look offline, but Bun uses node:http(s) and ignores the undici dispatcher override, so fetchModelData() can reach https://models.dev/api.json for real. Fix: provide a runtime-neutral upstream transport seam or Bun-specific interceptor with the same ownership contract; never load undici under Bun.

<!-- lore:01a0a58e-ae99-7673-8170-cdb30fdb25ac -->
* **cgroup-memory.ts stable hierarchy sampling**: Trap: accepting a sole root-mismatched mount as namespace proof or stopping at a subtree mount looks valid because the proc path resolves, but unrelated bind mounts and hidden ancestors can overstate headroom. Fix: require positive namespace identity, prefer the broadest containing direct mount, prove global-root completeness before tolerating absent root controls, and otherwise fail closed. Recheck proc topology plus directory device/inode, double-sample controls using maximum usage and minimum limit, and require every descendant boundary readable. Explicit v1 memory membership blocks v2 fallback. Compute \`min(limitᵢ - usageᵢ)\`; never sum ancestor usage.

<!-- lore:01a0ba5a-5fd2-7bdd-9bf6-4f15872a8a5a -->
* **Codex unfinished message lifecycle**: Trap: accepting a completed Codex terminal with omitted output looks safe when the recall call is complete, but prior message deltas may remain unfinished and be silently omitted by materialization, allowing recall from partial state. Fix: before recall or recovery, require every observed item, content part, and delta lifecycle to complete, even when terminal output is absent.

<!-- lore:01a0c1e0-d703-7e61-8784-8e0ac1095a15 -->
* **computeInitRetryDelayMs zero failures**: Trap: using \`failures - 1\` directly looks like standard exponential-backoff indexing, but callers may report zero and produce a negative exponent. Fix: clamp failures to at least 1 before exponentiation; \`computeInitRetryDelayMs(0, 30\_000)\` must return the 2,000 ms base delay.

<!-- lore:01a0bba4-e032-7c32-ac92-8a63b951e273 -->
* **context-1m worker stripping**: Trap: preserving \`context-1m\` for capable workers looks correct, but bounded background segments on subscription-auth accounts can enter permanent 429 exhaustion, trip breakers, and stop work. Fix: builders always strip only \`context-1m\` while preserving OAuth, cache, and tool-streaming betas. The later \`betaStripped\` retry guard currently appears unreachable because it specifically requires a beta removed before dispatch; retain only as a tested safety net or remove deliberately.

<!-- lore:01a0cd24-be9a-7a7f-b5ef-d02521214372 -->
* **createHarness invalid-port cleanup**: Trap: validating a requested port and throwing after server startup looks harmless, but the exceptional path can leave the database, pipeline, interceptor, auth state, and SQLite sidecars behind; checking only port <= 0 also admits NaN, fractions, and values above 65535. Fix: validate finite integer ports in range, route every startup failure through common cleanup-and-aggregate handling, and inject startServer for deterministic stop-rejection tests.

<!-- lore:01a0c312-9b9e-72ae-9c6d-6fbeb407869c -->
* **createTestDatabaseDirectory label containment**: Trap: joining a descriptive test label beneath the captured root looks contained, but absolute paths, \`..\`, and either path separator can escape or create nested allocations. Fix: reject absolute, traversal, slash, and backslash labels before joining; all test database allocations must remain beneath the immutable \`LORE\_TEST\_DB\_ROOT\`.

<!-- lore:01a0cbcb-d07d-7dc1-8a36-f43837c438cb -->
* **Direct live gateway XDG isolation**: Trap: direct live gateway tests appear isolated because CLI or Vitest wrappers already set XDG\_DATA\_HOME, but programmatic callers can inherit the developer’s real XDG tree and write logs or configuration outside the owned eval root. Fix: startLiveGateway() must capture, set, and restore XDG\_DATA\_HOME beneath the owned root alongside LORE\_TEST\_DB\_ROOT and LORE\_DB\_PATH; restore it only after teardown and confirmed resource shutdown.

<!-- lore:01a0a4f0-901d-71ef-9f22-55347ac1bddc -->
* **Embedding cancellation and body-read ownership**: Trap: caller cancellation looks like permission to free a pool slot, retire the worker, or await reader cleanup, but inference or a hostile response-body read may still be running or never settle. Fix: reject/remove queued work, race every body read against the combined signal, perform reader cancellation fire-and-forget, and keep executing work charged until completion or confirmed worker exit; caller settlement and execution ownership are separate.

<!-- lore:01a0a4f1-7881-7184-98a3-171a7832696f -->
* **Embedding WASM cap persistence**: Trap: merging OOM evidence with \`Math.max(previousBadCap, failedCap)\` looks conservative, but a later lower-cap OOM proves a tighter ceiling and the higher value permits unsafe re-probes after restart. Fix: atomically retain the lowest positive \`knownBadCap\`, cap all upward logic at \`knownBadCap - 1\`, and respawn after every downward cap because WASM memory never shrinks. Persist total free memory plus the learning divisor/per-worker share; malformed state fails closed, and tests must isolate persisted OOM state to avoid order-dependent results.

<!-- lore:01a055af-ab07-71c6-ad3b-b4b55f80438d -->
* **Embedding worker runtime, admission, execution, transactions, and privacy invariants**: Check abort immediately before every embedding-provider invocation, including recall bypass and admission handoff; release the exact acquired token on post-grant abort. Track vector-state changes with helper-owned nested transaction frames and shared connection-in-transaction detection: nested commits merge upward, only the outer commit publishes, and rollback discards its frame. At every remote, worker, or custom-provider boundary, replace raw failures with a fixed cause-free EmbeddingProviderError. Reconstruct only allowlisted control errors, guard hostile Proxy and instanceof inspection, keep properties non-enumerable, and never expose provider bodies, statusText, diagnostics, or causes.

<!-- lore:01a01a58-ecf6-7c98-9641-92dff74d6673 -->
* **Entity preview mutation safety**: Entity mutation APIs are preview-only by default and require \`?apply=true\`; CLI dedup applies only with \`--yes\`. Chose read-only preview over eagerly resolving state because \`ensureProject()\` or lazy remote reconciliation can create rows during an alleged dry run. Preview stays non-mutating and non-interactive even with \`--yes\` or \`--interactive\`; only apply mode may ensure projects. Destructive CLI operations require \`--yes\`, \`--json --interactive\` is invalid, and core merge rejects incompatible types before \`BEGIN IMMEDIATE\`.

<!-- lore:01a0ce6e-4b15-7c51-bfca-2ad5d69310cc -->
* **Eval limiter backoff grantNext**: Trap: checking \`backoffUntil\` only in new \`acquire()\` calls looks sufficient because fresh requests are delayed, but cancellation or completion can call \`grantNext()\` and release queued work during an active 429 backoff. Fix: enforce the backoff barrier in every queue-grant path, retain queued abort listeners only until settlement, and clear delay timers on cancellation; otherwise retries bypass provider throttling and leak listeners or handles.

<!-- lore:01a0cdce-4835-7e11-9264-a164d4d1c64f -->
* **eval LLM 429 retry boundary**: Trap: applying the gateway's no-retry 429 policy to eval LLM clients looks consistent, but eval scoring may use rate-limited external APIs and needs bounded recovery. Fix: keep retries confined to \`llm-backend.ts\` and gateway QA evaluation calls, with exponential backoff and explicit attempt caps; do not infer from this eval resilience that production gateway traffic should retry provider 429s.

<!-- lore:01a09eef-0e1e-71bf-96af-c57b17d8266a -->
* **Failed output accounting privacy**: Failed or cancelled terminals must canonicalize to sanitized failure with empty public output; provisional provider text, errors, and partial output are never forwarded or sent to accounting hooks. Preserve only independently validated usage privately. Treating accumulated output as useful seems natural because streams contain fragments, but failed output is not authoritative and can leak sensitive data or corrupt accounting.

<!-- lore:01a0a789-4891-78a5-98b8-20ad1a68f391 -->
* **Final text replaces streamed deltas**: Treat provider terminal fields such as output\_text.done, refusal.done, and function\_call\_arguments.done as authoritative replacements for accumulated streamed deltas. Appending terminal text looks natural because streams usually deliver fragments, but it duplicates or preserves stale provisional content; keep raw lifecycle snapshots unchanged and refresh only the corresponding projected item.

<!-- lore:01a0cd1d-2f5d-7927-89b0-2c021a883af5 -->
* **fixture descendant registration barrier**: Trap: coordinator exit, marker PID, or launching terminate() without awaiting it looks sufficient, but delayed registration, PID reuse, token loss, or detached descendants can leave a process alive or target an unrelated process. Fix: finalize readiness and descendant registration before cleanup resolves; require positive identity evidence when available, retain process-group cleanup for ordinary children, explicitly clean detached descendants, attempt every target, await bounded termination, and verify exit. Cleanup failures must remain visible.

<!-- lore:01a03261-970d-7a1b-a99a-18edcb936d20 -->
* **Gateway body cancellation sequencing**: Never await a queued request or producer before consuming a predecessor body when admission may depend on consumption; retain promises, release streams, and use explicit gates. Malformed or stalled bodies return sanitized errors without awaiting EOF; bound reads and drains by timeout, abort, or close, cancel readers without awaiting hostile cleanup, and settle backpressure on drain/error/close/abort. A byte cap alone looks bounded but cannot stop a reader that never settles.

<!-- lore:01a0c4f7-1ca8-7a43-8d00-0dc3db1d9d94 -->
* **gateway tests out-of-root SQLite paths**: Every test-owned gateway SQLite database must be allocated through createTestDatabasePath() or createTestDatabaseDirectory() beneath LORE\_TEST\_DB\_ROOT, including session-search, paging, smoke-like, and child-process tests; use WAL for concurrent access and keep sidecars under that root. A unique /tmp filename or source-local fixture directory looks isolated because tests clean it afterward, but forced termination, leaked workers, or skipped teardown bypasses ownership and leaves artifacts outside the run cleanup sweep. Scripts outside Vitest need their own owned root and must set LORE\_TEST\_DB\_ROOT, LORE\_DB\_PATH, and throwaway XDG/HOME before importing gateway modules.

<!-- lore:01a0c312-e298-7128-868c-6f5da2873b67 -->
* **gh placeholder upstream repository**: Trap: \`gh pr view \<branch>\` looks authoritative because the branch is local, but \`gh\` may infer the placeholder \`template/starter\` upstream and report that the repository or PR is missing. Fix: query \`BYK/loreai\` explicitly with \`--repo BYK/loreai\`, then match the PR head SHA and branch to the frozen Jujutsu candidate before any PR or VCS mutation.

<!-- lore:01a0d7d7-692d-7ab7-95b7-39f06a5edcac -->
* **GitHub GraphQL review-comment fields**: Trap: querying \`PullRequestReviewComment.isResolved\` or \`inReplyTo\` looks natural when inspecting review threads, but GitHub's \`PullRequestReviewComment\` GraphQL type does not define either field and rejects the whole query at validation time. Fix: use fields supported by the current schema and model resolution/reply relationships through the appropriate review-thread or comment connection types rather than guessing fields on the comment node.

<!-- lore:01a0c1e2-954a-7f66-b1b7-84ca8041f1b5 -->
* **global-setup signal cleanup ownership**: Trap: withOwnedDatabaseRoot() async try/finally looks sufficient because normal eval completion reaches finally, but SIGINT, SIGTERM, or process.exit can terminate the CLI before cleanup, leaking the owned root and SQLite test.db, -wal, and -shm files. Fix: register synchronous ownership-checked cleanup when the root is created, handle signals and exit, remove handlers after cleanup, and preserve the original signal status; async-only exit cleanup is insufficient.

<!-- lore:01a0b378-709d-79e0-be41-b7ad3865a5ab -->
* **knowledge is\_current append ordering**: Trap: inserting a replacement knowledge version before demoting the old row looks natural, but it violates \`idx\_knowledge\_one\_current\` and a read-before-transaction creates a concurrent-append TOCTOU race. Fix: inside one transaction, resolve and demote the current row first, then insert the forward-copied replacement so each \`logical\_id\` always has at most one \`is\_current=1\` row.

<!-- lore:01a0c4dd-8f20-7ad2-9f2b-a5c7e9d7174f -->
* **live gateway teardown retry ownership**: Live eval teardown is staged and retryable: stop the server first; if stopping fails, retain the live server, database, pipeline, owned root, and environment because later cleanup could delete active state. Retry failed teardown from the owning cleanup path, aggregate startup and cleanup failures, and restore the caller environment in a nested finally even when owned-root removal fails. Do not report teardown success until every owned resource is verified closed or removed.

<!-- lore:01a0bfd9-c503-71ba-89e7-f84f7497f0be -->
* **LORE\_TEST\_DB\_ROOT child identity**: Trap: verifying only the run-root device/inode before recursively deleting a per-file tmp directory looks contained, but a test can replace the child path while preserving the parent root, causing cleanup to delete replacement data. Fix: capture and verify device/inode identity for each destructive child directory as well as the immutable run root; refuse deletion on mismatch while still closing database handles.

<!-- lore:019f98cf-e495-7847-9227-ce6117a8a97e -->
* **malformed\_chain poison: transient HTTP failures must not classify as chain corruption**: Chain-manifest transport failures must never be classified as \`malformed\_chain\`. Track fetch failures separately from validation failures: HTTP 5xx responses, timeouts, connection resets, and similar failures follow retry/reclassification and become \`chain-unavailable\` if persistent. Reserve \`malformed\_chain\` poison alerts for successfully fetched bodies that are genuinely unparseable or structurally invalid, so transient network errors cannot poison a tag or chain marker.

<!-- lore:01a0c19b-e595-72de-85ba-c2536aeeca26 -->
* **models.dev dispatcher lease ownership**: Trap: installing the module-level offline MockAgent in fixture evals looks safe because it prevents accidental models.dev network access, but setUpstreamDispatcherForTest() is process-wide and survives gateway teardown, so later live evals fail through the network-disabled dispatcher. Fix: fixture setup must capture and restore the prior dispatcher (or explicitly clear it) in finally, including startup failure and nested ownership cases; live evals must retain their real upstream dispatcher.

<!-- lore:01a0b35c-ce62-7927-a31e-837fbed1d717 -->
* **NonStreamCompletionError accounting**: Trap: returning content or usage parsed from an unsuccessful non-stream envelope looks useful because numeric usage may still be recoverable. Fix: throw \`NonStreamCompletionError\`; reuse the same parser on usage-only fields so numeric and cache-consistency validation remains authoritative, let invalid usage throw, and retain only validated usage for unsuccessful accounting. Malformed, failed, incomplete-required, or contentless envelopes never become successful responses.

<!-- lore:01a0980b-b430-7c93-9d04-c204e0ecc0a1 -->
* **OpenAI/Codex Responses transactional streaming and projection**: OpenAI Responses streaming is transactional. Before mutation, validate allowlisted shapes, UTF-8, identities, ordering, transitions, usage, terminal consistency, and bounded output/content/summary indices. Accumulate accepted events in private bounded state before callbacks or projection. Reconcile terminal arrays with lifecycle state, preserve validated holes in raw output, and skip holes only during semantic flattening. Public output contains reviewed fields only and excludes references, recall data, diagnostics, credentials, and extensions. A valid terminal settles once; final done text replaces deltas. Malformed, contradictory, cancelled, incomplete, or unterminated streams roll back and emit one sanitized failure with empty accounting.

<!-- lore:01a0cd24-be46-780d-bc05-a453cdb178ce -->
* **owned-root signal cleanup ordering**: Trap: synchronous SIGINT/SIGTERM cleanup that recursively removes the owned root before stopping the gateway and closing SQLite looks reliable because it runs outside async finally, but open test.db-wal/test.db-shm handles can make removal fail on Windows and restoring environment variables early can redirect late writes to the real database. Fix: abort work, stop resources, reset pipeline, close readers and DB, then remove the root; keep environment and retry/reaper ownership until cleanup is complete before re-raising the signal.

<!-- lore:01a0b11a-7b48-755d-bc78-7c5f8a79da14 -->
* **OwnedRetirements preserves unknown liveness**: Trap: removing a rejected retirement from \`active\` looks correct because the promise settled, but rejection means worker exit is unconfirmed and its model/heap may remain resident; redispatch can over-admit and trigger cgroup \`SIGKILL\`. Fix: retain a capacity/liveness reservation until positive exit or termination confirmation, while still aggregating errors and deduplicating retirement owners.

<!-- lore:01a0d7d1-6db7-75f4-a916-0f5dd56a7e41 -->
* **parallel\_recall aborts before execution**: Trap: treating \`RecallContinuationFailure("parallel\_recall")\` as an ordinary recall failure looks correct because the response contains recall tool calls, but Anthropic rejects the turn before any recall executes and the client receives HTTP 200 SSE with no content or terminal event. Fix: prevent the shape upstream with effective-wire parallel-tool guards; retain the hard abort for endpoints or replayed bodies that strip or omit the guard until multi-recall execution exists.

<!-- lore:01a0d7d7-68cb-7018-998c-5788cda10e42 -->
* **pipeline optional metadata guard**: Trap: reading \`modifiedReq.metadata.tool\_choice\` directly looks safe because normal parsed requests usually include metadata, but replayed or transformed requests—especially \`tool\_choice: none\` regression inputs—may omit it and throw before the provider call. Fix: use optional chaining when inspecting request metadata, preserving the fail-closed tool guard without assuming every intermediate request shape includes \`metadata\`; add a real absent-metadata regression even if the current integration path normalizes metadata earlier.

<!-- lore:01a0c470-1b72-73f1-97f2-09fa442a045c -->
* **pipeline sparse output mapping**: Trap: adding custom\_tool\_call to the public Responses allowlist looks complete, but isValidResponsesOutputItemStatus() currently rejects every custom-tool status before consulting the phase matrix, so valid in\_progress, completed, and incomplete events fail validation. Fix: route custom\_tool\_call through the phase-aware public status rules and test added, delta, done, and terminal lifecycle events.

<!-- lore:01a0cffe-41f3-7b2d-91d9-ef43f0b2c59b -->
* **Recall cancellation nonsettling cleanup**: Trap: awaiting recall callbacks, follow-up setup, recovery, tee cleanup, or \`reader.cancel()\` looks correct because it confirms resource release, but hostile or broken streams can never settle and make client cancellation hang indefinitely. Fix: race owned operations against the request AbortSignal, rethrow the caller’s abort reason, suppress late operation failures, and make late-reader, tee-branch, and mismatch-body cleanup best effort without delaying the caller.

<!-- lore:01a08ccd-205c-7e62-a4d5-4d984bdbdf08 -->
* **Recall dependency, coverage, progress, and privacy invariants**: Keep recall independently testable: inject forwarding, parsing, cancellation, and protocol behavior through \`RecallFollowUpCtx\` rather than importing \`pipeline.ts\` and creating a dependency cycle. A detail response is complete only when its completion marker is non-zero; text alone is insufficient. Count zero-length coverage only for a complete item whose length is zero. Only retained duplicate coverage keys prove no progress; undefined, oversized, unknown, or FIFO-evicted coverage resets the counter. Stop after the third consecutive proven no-progress result. \`RecallCoverage\` is private in-process metadata: callers may receive it from \`runRecallWithMetadata()\`, but rendered output and telemetry must never expose revisions, keys, source identities, or related fields.

<!-- lore:01a07711-ff5a-7386-95cb-3f0e3d080a99 -->
* **reduceBlob meaning and pinned-directive preservation**: reduceBlob must never discard pinned user directives, even under junk classification, tight budgets, low lexical variety, sampling, or overlapping hard windows. During reassembly, preserve a newline between distinct adjacent paragraphs; concatenate without a separator only contiguous hard-window pieces from the same original line. These rules prevent distillation from erasing explicit constraints or changing paragraph meaning.

<!-- lore:01a0af08-189c-7373-9505-a7568449989a -->
* **resolveMemoryHeadroom tagged unknown state**: Trap: collapsing verified-unbounded and unavailable cgroup accounting into \`undefined\` or \`0\` looks compatible with Node APIs, but unavailable Linux accounting can then fall back to host RAM and over-admit workers. Fix: preserve distinct finite, verified-unbounded (\`Infinity\`), and unavailable-sentinel states; unavailable maps to the safe floor even without a hard limit, while verified-unbounded may use host memory. Finite \`process.availableMemory()\` remains valid without a hard limit. Source and tests must import the same exported sentinel; a missing export may appear equal to \`undefined\` in Vitest while typecheck fails.

<!-- lore:01a0bbbe-aad1-728c-b7a8-4dc2bf4fde0c -->
* **responseAgainstAbort reader ownership**: Trap: \`responseAgainstAbort()\` appears able to clean up every abort-ignoring parser because it cancels a late \`Response\`, but it never owns a reader locked inside that parser and cannot release the lock. Fix: require parsers to release owned readers and test with lock-taking abort-ignoring doubles. Generalize late-response cleanup with a response selector only for the three equivalent late \`{response,effectiveProtocol}\` results; tee branches and current-response parser cancellation retain separate ownership.

<!-- lore:01a0c470-1bd1-747c-9f66-b2bad33c09db -->
* **Responses hosted-tool lifecycle allowlist**: Trap: validating only the familiar message/function/reasoning events looks safe, but hosted-tool streams emit legitimate lifecycle events such as response.code\_interpreter\_call\_code.delta; rejecting them causes synthetic response.failed output or aborts before accumulation and callbacks. Fix: every item in PUBLIC\_RESPONSES\_OUTPUT\_ITEM\_TYPES needs projection and validation for its lifecycle events, or unsupported item types must be removed from the public allowlist.

<!-- lore:01a0ba9c-5882-7358-940a-63c4e241d32c -->
* **Responses item\_reference protocol split**: Trap: calling \`consumeReferenceEvent()\` before \`applyResponsesEvent()\` looks safe because references are private and must not reach clients, but accepted reference lifecycle events then bypass \`rawItems\`, \`projectedItems\`, accepted-event observers, and \`postResponse\`. Fix: accumulate and validate response-side \`item\_reference\` events first in both principal and continuation paths, then suppress their public projection; tests must inspect authoritative state, not only absence from wire output.

<!-- lore:01a0ba3c-fbaf-79bc-b0ac-23b5684a8eca -->
* **Responses SDK-shape projection**: Trap: sharing one logprob projector between terminal output and streamed events looks correct because both represent text probabilities, but OpenAI SDK 6.26 defines incompatible schemas. Fix: terminal \`ResponseOutputText.Logprob\` requires \`token\`, \`bytes\`, \`logprob\`, and \`top\_logprobs\`; streamed logprobs require only \`token\` and \`logprob\`, forbid \`bytes\`, allow absent \`top\_logprobs\`, and permit optional nested fields. Keep separate strict projectors and test an SDK-valid stream with no \`bytes\`.

<!-- lore:01a0d1d1-ed80-7c63-884a-ed8b79fc4e24 -->
* **runPrincipalAttempt missing response body**: Trap: throwing a plain Error when \`runPrincipalAttempt()\` receives a response without \`body\` looks like a valid malformed-upstream failure, but the pre-output recovery path retries only \`SSEStreamTransportError\` read failures. Fix: classify the missing body as \`SSEStreamTransportError("read", ...)\` so documented principal transport recovery runs; add a bodyless-response regression beside the existing recovery test.

<!-- lore:019fe15e-6f5d-7412-8c4d-3a5964091e04 -->
* **SQLite initialization, vec0 repair, sync capture, and shutdown**: Initialize raw SQLite fully before tracing or publishing the singleton so failed migrations cannot expose a partial handle. Register \`lore\_current\_tenant\` before migrations and trigger installation; it must resolve live AsyncLocalStorage state per statement, while connection-local TEMP capture triggers and suppression depth prevent stale or cross-process tenant state. Repair vec0 savepoint-atomically from recognized DDL and reset temporal rechunk metadata together when rebuilding. On shutdown, close readers without blocking, inspect the singleton without \`db()\`, set \`busy\_timeout=0\`, checkpoint best-effort, and clear DB/vector state.

<!-- lore:01a0bfd9-c4a2-7275-b38b-b938bbef4811 -->
* **startFixture coordinator containment**: Trap: inheriting the parent environment, buffering complete output, or awaiting only the direct child looks convenient, but leaks secrets and lets noisy or pipe-owning descendants hang the harness. Fix: start behavior deadlines after readiness, pass an explicit environment allowlist, continuously drain stdout/stderr while retaining at most 256 KiB per stream, kill the full process tree, bound settlement, and propagate cleanup failures.

<!-- lore:01a0c1e2-95a4-7519-b4e9-3ae42a64fc69 -->
* **startFixture Windows descendant cleanup**: Trap: treating \`taskkill.exe\` completion or coordinator exit as cleanup success looks bounded, but nonzero status or an already-exited coordinator can leave registered descendants alive. Fix: reject nonzero \`taskkill /T /F\`, retain every known descendant PID, attempt all targets even if one fails, and verify each exits within a bound before reporting success.

<!-- lore:01a0d1d1-eddb-72ac-9fd5-abe0a006e438 -->
* **statusless Codex recovery items**: Trap: requiring raw Responses recovery items to explicitly set status="completed" looks safer, but valid completed Codex items may omit status and upstream lifecycle validation already permits omission. Fix: after successful completed-terminal and lifecycle validation, accept an absent status and normalize it to "completed" for projection; reject only an explicitly non-completed status.

<!-- lore:01a0b3d5-18df-7725-a3d0-82332ba436e9 -->
* **systemTTL breakpoint precedence**: Trap: applying the conversation cache breakpoint after the system breakpoint looks like ordinary last-write-wins translation, but it can overwrite the \`systemTTL\` boundary and change cache semantics. Fix: preserve the system breakpoint established by \`systemTTL\`; conversation caching may add a breakpoint only without replacing that authority, and explicit \`systemTTL:false\` must survive recovery and downgrade paths.

<!-- lore:01a03d4a-69c7-7dd4-9599-4d487ce5ee7a -->
* **TaskTool masks assistant errors**: \`TaskTool.runTask()\` must not treat every returned assistant message as success: failed children may return an assistant envelope with \`result.info.error\` and empty text. Inspect that error, fail with a sanitized categorical message, and ensure foreground, background, new, and resumed tasks never render \`state="completed"\` with an empty \`\<task\_result>\`.

<!-- lore:01a0235f-dd58-73b3-b697-e65fd0989b23 -->
* **tracedDatabase query seam**: Trap: monkeypatching \`db().query\` or tracing \`prepare()\` appears to observe all SQL, but Lore’s \`tracedDatabase()\` Proxy shadows query replacement and traces only \`.query()\` statement operations (\`get\`, \`run\`, and \`all\`), causing false counts. Raw \`prepare\`, \`exec\`, migrations, and transaction control intentionally bypass tracing. Fix: SQL-count tests must use \`log.registerSink({ withDbSpan })\`, count spans at the actual tracing boundary, and execute through \`.query()\`. The wrapper must invoke each operation exactly once without changing arguments or return values, and core must remain free of Sentry dependencies.

<!-- lore:01a0c425-e63f-7b67-a3c3-ebd47cfdafb8 -->
* **useTestConfigDir isolateProjectRoot**: Trap: useTestConfigDir() without isolateProjectRoot looks sufficient because the directory is temporary, but findProjectRoot() can walk upward to the repository .git directory, causing detectDsn(cwd) to scan source and trigger mocked-network timeouts. Fix: always pass { isolateProjectRoot: true } when the test exercises project-root or configuration discovery; the nested .git boundary prevents repository traversal.

<!-- lore:01a03ec2-c17c-7925-9712-6ae86bfc4bb1 -->
* **Vitest loreTestRoot cleanup ordering**: Trap: deleting \`loreTestRoot\` during global teardown looks final, but Vitest closes workers afterward and they can recreate DB/WAL/SHM files. Fix: defer cleanup to coordinator \`exit\`. For signals, determine at delivery time whether another persistent handler remains; a setup-time snapshot becomes stale after Vitest removes its listeners and can swallow termination. Remove the exact owned root and re-raise only when no other handler remains. Path-based identity-check-then-\`rm\` is replacement-racy, so never claim concurrent replacement safety without identity-safe deletion and a synchronized regression.

<!-- lore:01a08722-2343-75f0-bfc7-840036c0a51b -->
* **vitest.config.ts alias scope**: Trap: placing \`@loreai/core\` or \`@loreai/gateway\` aliases under \`test.resolve.alias\` looks like the natural Vitest scope, but silently resolves stale \`dist\` builds and masks source failures. Fix: keep aliases in the configuration location used by \`vitest.config.ts\` so tests resolve \`packages/\*/src\` directly.

<!-- lore:01a0b35c-cdb3-72d7-a6cd-86d204c4c606 -->
* **Worker prompt retry and capability-learning invariants**: Trap: normalizing malformed bare \`response.error\` to \`{type:"error"}\` looks equivalent because both names mean provider failure, but principal and continuation parsers validate the synthesized type against the original SSE event name and misclassify the alias as a protocol error. Fix: preserve the exact alias (\`type: event\`) through every duplicated parser and parameterize recall-aware tests over both \`error\` and \`response.error\`.

### Pattern

<!-- lore:01a0c3d9-e1ba-7754-962e-3b51d30f1bd1 -->
* **generate-env-docs LORE variables**: When adding or changing a \`LORE\_\*\` environment variable or generated configuration setting, update source JSDoc and regenerate the checked-in website references with \`tsx scripts/generate-config-docs.ts && tsx scripts/generate-env-docs.ts\`; verify freshness with \`pnpm check:env-docs\` or the repository’s equivalent checks. Editing generated pages alone looks faster, but generators overwrite them and CI detects stale output.

<!-- lore:01a0cdce-47d9-7d45-9959-fdecaf53d83e -->
* **judge fixture neutral scores**: Fixture-mode \`judge()\` returns score 3 for every rubric criterion, composite 3.0, and zero tokens when no LLM client is supplied. Chose neutral regression baselines over fabricated quality judgments because fixture mode verifies behavior and replay determinism, while absolute scoring belongs to an external judge such as the configured Anthropic evaluator \[\[01a0c1e0-d7c5-7271-afbb-a3649d7cf934]].

<!-- lore:01a03e44-1d13-7a8f-ac02-97f6be0fa251 -->
* **Jujutsu-safe workflow and tests-first validation**: In \`.jj/\` workspaces, use non-interactive Jujutsu and stable change IDs; inspect the intended nonempty change, parent, bookmark, diff, and conflicts before mutation. Preserve unrelated work and never rewrite immutable commits. For substantive fixes, add deterministic real-path regressions that fail on the base or under targeted guard removal, then restore and verify the candidate exactly. Run focused tests, required affected suites, typechecking, and production bundling where applicable; inspect repository Vitest configuration first and rebuild stale artifacts. Treat killed, timed-out, or skipped runs as inconclusive. Before pushing only the intended bookmark, verify \`jj st\`, \`jj log\`, exact file scope, and the pushed head. Report commands, exact pass/fail/skip totals, warnings, residual risks, and PR scope.

<!-- lore:019f1cc8-b65e-7a6e-97ae-f48d1a7d542b -->
* **Recall execution, privacy, admission, validation, and operator access**: Consume synchronous recall results in the same turn; recall as the final action creates a false search loop. Admission is liveness-only: productive chains are limited by the foreground deadline with synthesis reserve or proven consecutive no-progress coverage, not arbitrary token, item, byte, or execution quotas. Evicted coverage keys become unknown, never stall evidence. Frozen-baseline builders fall back to synchronous scan on \`READ\_JOB\_TIMED\_OUT\`; \`forSession\` may return \`\[]\` because it reruns each turn. Recall diagnostics use request-local SHA-256 sets, bounded to 64 rounds, emit only aggregate counters, and clear all sets on finish. For assistant access to prior knowledge, do not invoke the built-in recall tool; use read-only \`lore recall "\<query>"\` or the local search UI.

<!-- lore:01a0cdce-4718-7490-8f54-5032ea389397 -->
* **replaySession cumulative scripted history**: Fixture evals always start an isolated gateway with an offline replay interceptor and deterministic cumulative scripted history. Scripted assistant responses must never contain recall tool\_use blocks because that would trigger follow-up loops and nondeterministic interceptor calls. Live mode is separate: it owns isolated storage but uses real upstream APIs and no replay interception.

### Preference

<!-- lore:01a0d1d1-ee8e-78f6-b6e8-77633c3126bd -->
* **.lore.md stash prohibition**: Never use \`git stash\` on \`.lore.md\`. Stashing it looks like a convenient way to obtain a clean worktree, but \`.lore.md\` is user-owned working state and must remain visible and untouched during repository work; preserve the current checkout and report conflicts or unrelated modifications instead.

<!-- lore:01a0d78e-a20b-715c-9843-a11d41dca413 -->
* **Always add isolated regression tests for protocol-specific gateway edge cases**: When fixing or investigating gateway behavior, add focused regression tests that exercise each relevant protocol and ingress path, including cross-protocol routing where applicable. Capture and assert the exact upstream request fields that enforce the fix, not merely the client-facing status. Use temporary per-test project/session state and verify unaffected requests remain unchanged. Include provider-specific coverage for supported routes and explicitly test edge cases such as injected tools, parallel tool-use prevention, continuation failures, and response rewriting.

<!-- lore:01a0d7dd-33b3-7388-89a1-82eb7244aace -->
* **Always keep semantic lint advisory unless explicitly gated**: Treat the semantic linter as non-blocking by default: it may report findings through annotations, summaries, or reviews, but must not fail or prevent merging a PR. Only make it blocking when the trusted repository setting explicitly enables gate mode, with failures handled according to that gate configuration. When reviewing CI, distinguish semantic-lint failures, cancellations, skipped jobs, and in-progress checks from failures in required validation jobs, and verify the exact PR head and remaining required checks before considering a change ready to merge.

<!-- lore:01a0d879-55f3-7aec-a142-583f7183c5c5 -->
* **Always preserve provenance and verify exact state before destructive changes**: Before deleting branches, rewriting bookmarks, pushing changes, or evaluating a candidate, first classify references by active PRs, workspaces, ancestry, and remote state. Preserve \`main\`, open-PR branches, workspace-linked bookmarks, and commit targets; use dry runs or bounded batches for destructive operations, and adapt to tool or hosting constraints. Keep candidate trees and servers untouched when instructed, avoid polling or duplicate work, and maintain provenance across retries. After each operation, verify the exact commit or PR head, remote state, conflicts, residual references, and working-copy cleanliness. Treat stale or conflicted references conservatively and make no further changes once the requested boundary is satisfied.

<!-- lore:01a0d797-ff02-79cb-bfee-1da7b5ecaa13 -->
* **Always prioritize gateway controls in Anthropic request translation**: When reviewing or modifying Anthropic request construction, forward client headers first, then overlay gateway-managed headers so content type and Anthropic version cannot be overridden. Reapply extracted API-key or OAuth credentials using the correct authentication scheme. Preserve the established caching semantics: caching is opt-in, stable system content and conversation boundaries receive the appropriate breakpoints and TTLs, and metadata/body fields remain intact. Treat these precedence, authentication, and cache behaviors as intentional invariants.

<!-- lore:01a0d77b-f7e8-7142-8924-294a8c2e6670 -->
* **Always prove fixes with fail-first tests and exhaustive validation**: For bug fixes and non-trivial changes, first create a deterministic regression test that exercises the real failing precondition and confirm it fails on the base implementation. Apply the fix, then verify the test passes and run the required broader gates (tests, typecheck, lint, format, builds, and relevant property tests). Check exact exit codes, conflicts, worker/database isolation, artifact or diff hashes, and final working-tree cleanliness. Treat adversarial correctness review as mandatory where behavior, lifecycle, synchronization, security, or resource limits are involved, and report explicit PASS/FAIL/CONCERN/MUST-FIX evidence rather than trusting an unverified green result.

<!-- lore:01a0d785-9b2d-73cc-ae9c-d2de4e6e4654 -->
* **Always restore locked workspace dependencies before validation**: Before running tests, builds, or review gates, materialize dependencies across all workspace projects with pnpm v10.28.0 using the existing lockfile and avoid changing dependency resolution. Confirm the install completes without lockfile changes, allow the gateway postinstall to generate development shims, and distinguish expected warnings—missing production bin.cjs files and ignored dependency build scripts—from actual installation failures. Proceed with validation only after the locked dependency set is available.

<!-- lore:01a0d772-b38e-72b6-837a-42507c789b1f -->
* **Always rigorously verify pull requests before merging**: Before merging, inspect the exact PR head and base, confirm relevance and independence from recent changes, review every unresolved comment and automated finding, verify required CI checks (including lint and review jobs), and assess conflicts and merge order. Merge only a fully reviewed, green head. Resolve comments only when the current code proves they are fixed; otherwise create a focused follow-up PR. After merges, update the local test candidate to the resulting main branch and run the requested local validation.

<!-- lore:01a0d7a1-3d55-7839-b0ee-b7953d80b26b -->
* **Always run repository-wide TypeScript diagnostics**: The user consistently performs broad lint/type-check validation across the monorepo, including after changes, and reviews the resulting TypeScript warnings. Pay particular attention to recurring \`no-non-null-assertion\` warnings, replacing \`!\` with optional chaining where appropriate, and \`unbound-method\` warnings, using arrow functions or \`this: void\` annotations when valid. Report existing warnings and truncated output clearly rather than treating them as unnoticed failures.

<!-- lore:01a0d7e6-b02f-791e-b7aa-e8d2ee61a88e -->
* **Always run typechecks and tests after changes**: After modifying code or completing a task, consistently validate the result by running the package typecheck with \`tsc --noEmit\` and the relevant Vitest test suite. Confirm that both complete successfully with no type errors or test failures before considering the work done. When applicable, also run formatting checks and report their successful completion.

<!-- lore:01a0d7c7-fbc9-794d-ae94-9e351d92f5af -->
* **Always run validation gates before committing**: After implementing or reviewing changes, run the relevant test suites first, then complete typechecking, linting, and formatting checks on the exact final tree. Include bundle or package-specific gates when applicable, verify the aggregate results and expected warnings, and freeze or confirm the final diff before committing. Do not commit until all required checks are green.

<!-- lore:01a0d7b7-8f01-7eb8-aca6-f3e60d275f6c -->
* **Always validate the full suite under deterministic conditions before judging changes**: Run the bundle, aggregate tests, typechecks, and other relevant validation gates, then inspect failures rather than treating any nonzero result as a code regression. Distinguish genuine failures from inherited environment variables, stale generated artifacts, timeouts, missing external services, model or infrastructure problems, and pre-existing baseline issues. When the environment invalidates the result, rerun the unchanged candidate with the repository’s isolated settings or use focused/sharded checks until the outcome is meaningful. Record warnings and unresolved failures explicitly, and only freeze or assess the work after the validation result is trustworthy.

<!-- lore:01a0d7f9-86d2-7b2d-b873-3d84ba77f003 -->
* **Always verify every PR gate before merging**: Before merging a pull request, re-check the exact head commit, required CI checks, review decisions, unresolved threads, and mergeability. Do not merge while any check or review gate is pending, failing, or unclear; schedule or perform a follow-up verification when needed. Proceed only after the PR is confirmed clean and mergeable, then verify the final merged and replacement/closed PR states.

<!-- lore:01a0d7d1-20d6-7946-addb-864fe8e44093 -->
* **Always wait for every required PR check to pass before merging**: The user repeatedly monitors pull requests at the exact commit head and treats a PR as not merge-ready while any required check—especially lint—is still in progress, cancelled without a successful rerun, or otherwise incomplete. Even when the PR is technically mergeable and all other tests, reviews, coverage, and smoke checks are green, they wait for the remaining gate to complete successfully before merging or closing/replacing related PRs. Report the outstanding checks clearly and do not recommend merging until the full required merge gate is clean.

<!-- lore:01a0d7d2-7370-7f4b-8f1e-2fbe240d62a3 -->
* **Always write tests alongside implementation**: Behavioral pattern detected across 238 sessions (action: requested-tests). The user consistently demonstrates this behavior.

<!-- lore:01a0c4dd-9039-7d82-ad59-376f6cdb8951 -->
* **apiPath query URL construction**: Always build UI and gateway request paths with \`apiPath()\` and \`query()\` from \`packages/ui/src/contracts/url.ts\`; never hand-encode segments or query values with template strings, \`encodeURIComponent\`, or \`URLSearchParams\`. Manual construction looks equivalent for simple values but diverges on escaping and contract changes, producing malformed or inconsistent paths.

<!-- lore:01a03426-3ac8-7efa-a762-ebc817c120a7 -->
* **Cache-stable safeDeltaInsertIndex**: Always preserve cache stability. Use immutable knowledge-delta blocks at a safe message-tail boundary rather than rewriting system prefixes or shifting prior deltas, because prefix movement breaks caches. \`safeDeltaInsertIndex\` and compression re-anchoring must walk backward and place deltas before assistant tool calls, never between \`tool\_use\` and the immediately following user \`tool\_result\`, including parallel tools and tail re-anchoring. Preserve surfaced mutation history and keep at most two cache breakpoints: one interior and the tail.

<!-- lore:01a0be1b-c7a6-7674-bdae-6083ce3b226c -->
* **copyUsageLimitHeaders allowlist**: Always copy provider quota headers through an explicit allowlist: standard rate-limit families, reviewed primary/secondary bucket fields, Codex limit/credit fields, and provider limit names only when their matching primary-used-percent header exists. Copying all similarly named upstream headers looks compatible but can forward cookies, framing headers, or credentials. Parse \`connection\` into lowercase hop-by-hop names and exclude every named header even when it otherwise matches the allowlist.

<!-- lore:01a0c4dd-909b-7b64-b3ab-2aadb84a8846 -->
* **Craft release versioning**: Never manually bump package versions or edit \`CHANGELOG\`. Releases use Sentry Craft through GitHub Actions (\`release.yml\`, version \`auto\` or an explicit version); Craft runs \`scripts/bump-version.sh\`, generates the changelog, creates the release branch, and publishes. Manual edits look faster but bypass the repository’s release automation and can create inconsistent generated state.

<!-- lore:01a0b474-f88f-7942-b5a8-ea04752a5322 -->
* **Ensure cancellation never waits for non-settling recovery**: Always make stream cancellation complete promptly even when recall recovery has started and returns a promise that never settles. Cancellation must abort or detach the recovery work rather than await it, perform exactly one rollback, perform no commit, and resolve the reader’s cancellation without hanging. Preserve this invariant with a regression test that synchronizes on recovery startup, leaves recovery permanently pending, races \`reader.cancel()\` against the next event-loop turn, and expects cancellation to win.

<!-- lore:01a0c19d-d78a-7f8a-a1d0-3f679f8073ff -->
* **Eval shared gateway ownership**: Never reset an active shared gateway between eval cases. Chose case-owned cleanup over blanket per-case reset because a shared gateway’s lifecycle spans cases; resetting it looks like stronger isolation but disrupts active or concurrent evaluations. Reset only a gateway the case created and exclusively owns, while isolating case-local state around shared instances.

<!-- lore:01a019e8-3b49-7057-99a9-a26b1f2014ce -->
* **Evidence-first investigation, implementation, and review**: Ground investigations, implementations, reviews, and conclusions in the exact checkout, repository state, runtime path, and generated or deployed artifacts. Before editing, search the entire relevant repository for exact symbols, call sites, tests, dependencies, configuration, manifests, exports, history, migrations, bundles, binary provenance, failure paths, security rules, integrations, and nested schemas. Report absent or failed searches, exact match counts, file:line references, fixture values, and direct versus incidental matches; recover truncated output rather than sampling. Inspect surrounding ownership, validation, projection, lifecycle, concurrency, retries, cancellation, cleanup, authorization, persistence, limits, and fallbacks. Treat established protocol tests as authoritative. Never guess provider fields or broaden schemas without reviewed definitions and focused compatibility coverage. Distinguish fact from inference and static proof from unmeasured behavior. Preserve privacy and compatibility, make the smallest purpose-built change, prove it with deterministic adversarial boundary and lifecycle tests, and report unresolved gaps.

<!-- lore:019fe14a-f9d4-7f87-947e-3c9730d3faef -->
* **Exact-candidate validation, adversarial review, and merge workflow**: For every candidate review, freeze and record the exact workspace, immutable revision/change, parent, ancestry, dirty state, conflicts, changed paths, diff, relevant hashes, tool versions, and pre/post integrity. Read repository guidance first. Work strictly read-only: do not edit, format, install, mutate VCS or services, access production data, or use the network. Run only authorized validation with isolated temporary resources; execute the prescribed aggregate suite exactly once unless deterministic disjoint shards are needed to resolve inconclusive results. Never count timeouts as passing or reuse results unless identity, state, hashes, and artifacts match exactly. Audit every changed line and relevant call path for correctness, security, privacy, lifecycle, concurrency, and resource risks. Cite current file:line evidence and report commands, results, warnings, cleanup, residual gaps, classified findings, and the requested definitive verdict. Never merge without Burak Yigit Kaya’s authorization.

<!-- lore:01a0266e-056f-75ee-a388-1e90d4fd26a0 -->
* **Fail-safe private telemetry, diagnostics, and maintenance**: Telemetry, diagnostics, warning hooks, tee branches, embedding failure hooks, and best-effort maintenance must never throw, block, reject, replace delivery, stop schedulers, or alter authoritative behavior. Disabled backends are no-ops. Isolate synchronous throws, rejected promises, hostile values, formatting and logging failures, and cleanup errors, including around optional embedding callbacks. Keep work bounded, idempotent, and preferably post-response. Emit only reviewed fixed-message events, sanitized aggregates, and bounded allowlisted metadata; never expose raw content, identities, provider errors, exception payloads, private results, worker output, or inherited request context. Clear request-local state at finalization and add focused regressions proving that throwing observers leave embedding and other operations unchanged.

<!-- lore:01a09d40-d22d-7c8e-a1d0-7ed6c8a81b5e -->
* **Fan-out registry contract batteries**: Every fan-out registry change requires centralized contract coverage. \`SYNCED\_TABLES\` must pass shape, capture, composite-key, and versioning contracts; every \`AGENTS\` entry needs \`name\`, \`displayName\`, constant-placeholder \`binary\`, and \`detect()\`. \`MIGRATIONS\` requires migration tests plus \`db.test.ts\`; after all migrations always normalize stored \`schema\_version.version\` to \`MIGRATIONS.length\`, because special handlers can advance it while later migrations fail to persist the final value. Freeze memoized shared arrays so mutation fails immediately.

<!-- lore:01a0c1e2-9864-7395-81f8-0903016d0ac4 -->
* **Fixture eval replay gateway isolation**: Fixture eval mode must always start an isolated gateway with a replay interceptor and an owned database root. Reusing a shared or live gateway looks simpler, but provider behavior and mutable gateway state make fixture results non-hermetic and non-reproducible. Eval teardown must await server.stop() before closing the database and resetting pipeline state; failed startup still cleans the owned root.

<!-- lore:01a0d77e-b13d-79c5-9807-c1de3894693e -->
* **Follow the established git workflow (branch, PR, review)**: Behavioral pattern detected across 407 sessions (action: enforced-workflow). The user consistently demonstrates this behavior.

<!-- lore:01a0c3cb-9594-74ee-9685-deca1f416304 -->
* **Gateway 429 no-retry policy**: The gateway never retries an HTTP 429. Report the provider rate limit once with a bounded, privacy-safe hint when the protocol and credential scheme justify it; do not imply that Lore retried or caused the limit. Retrying looks like standard resilience, but it increases quota pressure and obscures that the upstream subscription or provider rejected the request.

<!-- lore:01a0c4a8-16a7-77ab-b93f-42afe3037005 -->
* **Gateway Bearer authentication**: The gateway must always use Bearer authentication at its authentication boundary. Do not silently accept alternate schemes or infer credentials from malformed input; validate the scheme and token before routing, body reads, storage, or upstream activity. Alternate authentication can look convenient for compatibility, but it weakens one auditable security contract.

<!-- lore:01a0cffe-4284-7c68-a4e5-03109e885fba -->
* **Gateway fail-closed response handling**: Always fail closed at gateway lifecycle and protocol boundaries: validate request fields before dereferencing them, return 400 for malformed direct invocations, 502 for sanitized upstream failures, and reserve 503 for defined lifecycle, capacity, reset, or generation conditions. Never expose or log arbitrary parser, fetch, URL, credential, provider, or response-body text; use fixed safe messages and allowlisted diagnostics. \`/lore:\*\` commands are intercepted locally. A permissive fallback looks resilient but can leak data, misclassify lifecycle state, or turn malformed input into upstream activity.

<!-- lore:01a0d7d1-6ed3-7b62-959a-7ecb9d9f4e4f -->
* **Gateway-managed headers precedence**: Gateway-managed headers always override forwarded client headers. Forward non-managed headers first, then overlay protocol/content-type headers and finally the extracted authenticated credential headers; accepting client values last looks flexible but permits stale or conflicting credentials and protocol metadata to control the upstream request.

<!-- lore:01a0c4b9-71b3-7242-bbea-58f8362565cf -->
* **Harness x-lore-project default**: Harness-based gateway tests set \`x-lore-project\` to the configured \`projectPath\` by default so the synthetic project-resolution probe is never triggered. Override the header with an empty string only when a test intentionally exercises a path-less session; allowing the probe by default adds unrelated resolution behavior and can make tests nondeterministic.

<!-- lore:01a0cd24-bef8-7a35-96be-9774be915685 -->
* **Import-reuse credential parser**: The import-reuse credential parser must parse JSON, honor XDG environment variables, and never throw: return \[] for any malformed input, missing file, or other error. Propagating parser failures looks more transparent, but surrounding credential-import code relies on best-effort isolation and must not turn optional reuse into a request failure.

<!-- lore:01a0c1e0-d7c5-7271-afbb-a3649d7cf934 -->
* **JUDGE\_API\_KEY external judge**: When \`JUDGE\_API\_KEY\` is set, evals must use a real external, stronger Anthropic judge, Sonnet by default. Letting a weak or non-Anthropic answering model grade its own output looks convenient but creates biased scores and invalid evaluation signals.

<!-- lore:01a09483-f4d1-7ba7-91e1-b4b891f40892 -->
* **Jujutsu conflict-preserving workflow**: In \`.jj/\` repositories, use non-interactive Jujutsu while Git remains read-only, always pass \`-m\`, use stable change IDs, never rewrite immutable commits, and run \`git add .\` before commits when Git staging is required. jj does not fail on conflicts: it records conflicts in resulting commits, which are resolved later; do not treat a conflicted result as an operation failure. Never use \`git stash\`; verify with \`jj st\` and \`jj log\`, then move bookmarks explicitly after jj commits. Recover with \`jj undo\`, \`jj op log\`, \`jj op restore\`, or \`jj workspace update-stale\`.

<!-- lore:01a0b35c-cd4b-74bd-8505-2c9ddf26c053 -->
* **Lore recall fallback and session continuity**: Do not use BYK/loreai’s built-in recall tool, including from subagents, unless the user confirms it is repaired. Use \`lore recall "query"\` or the read-only UI at \`http://127.0.0.1:3207/ui/search\`. Give CLI recall a generous timeout; after interruption inspect saved output or retry with a narrower query and longer timeout. Never treat a timeout, stall, context error, or empty result as successful completion or proof that no context exists. Resume from preserved state and repository evidence. After interruption or restart, continue the exact task in its original workspace and session, first verifying candidate, parent, scope, status, diff, hashes, and revisions. Preserve restrictions, run only authorized validation, and return substantive evidence and the required verdict or summary. If blocked, report the precise blocker, command, and error.

<!-- lore:01a0a451-bd6b-7b75-8c59-8d73f7159484 -->
* **LTM prompt-cache, pin, and meta-distillation stability**: Keep stable preference/entity LTM in system\[1] byte-identical throughout a session, including idle resumes; defer newly curated stable knowledge until the next session. Carry changing context through durable deltas at a frozen safe tail instead of rewriting stable prefixes. Treat pins as ID/content diff baselines regardless of order, and keep persisted ltmCacheText synchronized with ltmPinText. Preserve caches only when isCacheWarm is true; expired caches are cold. Avoid re-pinning unchanged entry sets and preserve uncertain warm state. Run meta-distillation only at a cold idle-resume boundary, tracking warm-prefix rewrites separately.

<!-- lore:01a0ac0f-8522-74ee-96e9-1af1f6229c99 -->
* **Native ONNX resolution and bounded embedding fallback**: Prefer native ONNX Runtime on supported hosts. Resolve and clean up non-throwingly only from release-owned packages or the embedding worker package’s direct exact-version dependency—never ancestors or workspaces—and validate identity, version, target, libc, N-API layout, canonical containment, ownership, and file format. Return \`null\` when unavailable. On absence, incompatibility, or loader failure, emit only an allowlisted data-free \`init-needs-wasm\` reason and respawn exactly once in a fresh worker with sticky \`forceWasm\`; never retry native. Preserve pending requests, switch watchdogs on \`started\`, ignore retired-worker events, bound initialization and OOM retries, and latch FTS-only on exhaustion. Classify failures narrowly. Apply \`numThreads=1\` and \`proxy=false\` only to WASM; preserve native threading, supported targets, resolution order, lazy Node dependencies, and Bun isolation. Continuously drain stdout/stderr with \`resume()\` despite logger failures and expose no free-form diagnostics. Integrity-check cached models before purging; never purge vendored models. Test absence, containment, fallback continuity, and hostile diagnostics.

<!-- lore:01a03ae6-3b0e-78ed-94ed-4f7535176381 -->
* **No free-form content in logs or Sentry**: Never log, persist, export, or transmit free-form application, provider, exception, callback, native-loader, or worker diagnostic content. Treat worker stdout, stderr, errors, and arbitrary fields as untrusted and potentially sensitive. Continuously drain both worker streams for their entire lifetime with \`resume()\` without observing payloads, and ensure logger or sink failures cannot interrupt draining. Report actionable failures through structured parent-worker protocol handlers. Native-to-WASM fallback signals must be data-free or use an allowlisted categorical reason; consumers must ignore unexpected fields and emit only a fixed parent-generated debug message. Keep original exceptions private. Add regressions proving sustained output cannot block embeds and hostile or legacy fields never reach logs, warnings, errors, or exception sinks.

<!-- lore:01a03dc8-ccbb-7ab7-8acb-db64c345eaff -->
* **OpenCode configuration, routing, events, SSE, and diagnostics**: Skill names match lowercase-hyphenated folder names, stay within 64 characters, and use trigger-focused descriptions; use \`Use ONLY when...\` for narrow activation. In \`opencode.json\`, model names include provider prefixes and MCP commands are string arrays. Route providers explicitly through \`${gatewayBase}/v1\` via \`options.baseURL\`, deep-merge options, avoid \`/v1/v1\`, create a missing OpenAI entry, and force \`headerTimeout=false\` for Lore-routed OpenAI/Codex. Global events carry workspace metadata; EventV2Bridge is the compatibility boundary, and durable state receives exactly one terminal settlement. Keep project path and remote in one project-ID map. Filter SSE before enqueue, bound queues by bytes/events, preserve terminal events, and release retained resources on every exit. Diagnose repeated GC near V8’s ceiling before assigning ownership.

<!-- lore:01a0c3cb-942a-79ff-8c0d-9d1022f10140 -->
* **OpenRouter provider sort scope**: OpenRouter \`provider: { sort: "price" }\` is for background worker calls only; never apply it to the live conversation. Live OpenRouter traffic must retain OpenRouter’s default load balancing for reliability. Applying price sorting globally looks efficient, but it changes interactive routing and can select less reliable providers.

<!-- lore:01a0cdd9-a2fd-750c-b164-690078b58cf6 -->
* **Prefers explicit over implicit and keep responses concise**: prefer explicit over implicit and keep responses concise.

<!-- lore:01a0b357-a2a7-7625-85a7-364037ce838e -->
* **Prefers property-based tests over unfaithful mocks**: prefer property-based tests over faithful mocks,

<!-- lore:01a04989-0fe0-72dd-9249-0f7780343946 -->
* **Recall continuation usability and strict failure semantics**: Never execute recall from an incomplete principal or chained recall from an incomplete continuation. Recovery succeeds only with a protocol-complete usable answer, refusal, or non-recall tool; every explicit raw output-item status must be \`completed\`. Failed/incomplete tools, malformed identity/role/content/usage, private reasoning-only output, repeated recall, exhaustion, or cancellation return the fixed failure with rollback and no persistence.

<!-- lore:01a0ba5a-5f6f-7daf-ae4b-a4db9f2d1804 -->
* **Responses authoritative accounting finalization**: Always finalize authoritative Responses accounting exactly once, independently of downstream demand; cancellation may stop delivery and persistence, never erase known provider usage. Recovery-accounting overflow must synchronously take the fixed recall-failure path, retain only previously validated accounting, and never become completion. Tying \`finish()\` to enqueue or continuing after an invalid merge looks convenient but lets backpressure suppress usage or converts an accounting failure into success.

<!-- lore:01a0bb9e-10a7-74d3-b1f0-7484cc1c4717 -->
* **Responses visible-output projection**: Always buffer recall interception so injected \`tool\_use\`, references, and other non-semantic provider frames never reach clients. Classify privacy before accumulation or projection, track sparse provider indices separately from contiguous public positions, and record source→public mappings only after successful enqueue; forwarded positions never change. Hidden events still update private lifecycle state for \`postResponse\`, but unsuccessful responses expose empty output.

<!-- lore:01a0d89f-a1bf-7c59-bae2-04b9573e529f -->
* **Review code before committing**: Behavioral pattern detected across 382 sessions (action: requested-review). The user consistently demonstrates this behavior.

<!-- lore:01a0d87c-5102-7e8a-8f6b-0e5647e81f61 -->
* **Revset alias exactly-one commit**: Every configured Jujutsu revset alias must resolve to exactly one commit. Reject zero-result and multi-result resolutions rather than choosing a convenient match, because ambiguous aliases can delete, rebase, or push the wrong history.

<!-- lore:01a0bfc0-b751-78d7-95bf-2f08ea2d9a09 -->
* **setup.ts shared gateway isolation reset**: Test setup must reset mutable isolation state on both sides of every test boundary, including list-ordered hooks: restore NODE\_ENV, LORE\_TEST\_DB\_ROOT, LORE\_DB\_PATH, and XDG\_DATA\_HOME; unsilence stderr; invalidate the project-path cache; and close database ownership before a path is reused. Cache invalidation must inspect the existing singleton directly and return when no connection exists; it must never force a database open. Dual-sided reset repairs file-local teardown that runs before the next test under list ordering, while exit reset protects stack ordering. A cached path→ID mapping must never cross a test boundary or survive mutation.

<!-- lore:01a0c4f7-1d07-7d6d-972a-d96ef3771382 -->
* **SQLite WAL concurrent access**: Always use SQLite WAL mode when concurrent readers and writers are expected. Rollback-journal mode looks simpler and can pass serialized tests, but it increases reader/writer blocking and makes concurrent gateway or harness behavior unreliable.

<!-- lore:01a0bb6f-a289-794d-9c37-4e525441b15a -->
* **sync.property.test sequence battery**: Always preserve the sync sequence-property battery: assert invariants after every operation, prune-floor progress, eventual convergence, no ping-pong, and correct \`currentTier\` across enable/disable/push/pull sequences. Example-only tests look simpler but previously missed multiple ordering-dependent engine defects; run these properties 10 times during adversarial review.

<!-- lore:01a0c3de-2d77-7bac-bbd6-aba66812bf1d -->
* **test-shards.ts lightest-shard placement**: When assigning the next test file to a shard, place it on the currently lightest shard rather than using fixed or round-robin placement. Greedy balancing reduces skew and prevents one shard from becoming the bottleneck; validate the resulting shard distribution with the existing test-shard checks.

<!-- lore:01a0ce6e-4bd8-702c-9be7-f95e4e4fb69e -->
* **UI\_STAGE\_DIR stale asset purge**: Always remove the complete UI staging directory before publishing a new asset set. Retaining old files looks harmless because the new manifest points at current assets, but stale files can still be served or embedded through fallback paths. Stage only the fresh set, then write its manifest and compressed variants.

<!-- lore:01a0cd24-bf48-726b-aebe-5dfc685ab164 -->
* **Uninstall purge generation binding**: Uninstall purge operations must quarantine and verify the exact preflight-checked directory before deletion; preserve a replacement raced into the same pathname. Preserve custom XDG\_DATA\_HOME/LORE\_CONFIG\_DIR locations, external or relative LORE\_DB\_PATH, unverified package executables, unrelated profile content, and never open or modify preserved databases. On Windows, retain a running standalone executable and receipt for manual deletion; hosted uninstall may remove only the exact installer-owned PATH block.

<!-- lore:01a0b6f6-7ef2-7c57-988c-96d81be5543a -->
* **Worker shutdown and generation handoff**: Always bound the complete \`awaitWorkerShutdown(worker, timeoutMs)\` operation by \`timeoutMs\`, including a \`worker.terminate()\` promise that never settles. Starting termination at the deadline looks bounded but can still hang forever without an \`exit\`; reject or detach at the deadline while retaining unknown-liveness ownership so no replacement worker is admitted.
