<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019ee004-037c-70e6-a910-eef98c0778aa -->
* **A1 quota trigger: counter-based enforce\_row\_quota — only INSERTs and revivals grow live-row count**: Post-A1 \`enforce\_row\_quota()\` (migration 0007): reads O(1) from \`user\_table\_usage(scope\_id, table\_name, row\_count, byte\_count)\`. Advisory lock keyed on \`hashtextextended(scope\_id::text || '|' || tg\_table\_name, 0)\` — MUST be taken before \`exists\_pk\` check (0005 ordering); 0007 moved \`exists\_pk\` outside lock — SHOULD-FIX. Revival is UPDATE with \`d\_bytes=0, d\_rows=0\` — guard returns early, never gated. Tombstone INSERTs counted+gated (physical accounting). \`user\_table\_usage\` has NO DML grant to \`authenticated\` — written ONLY by SECURITY DEFINER triggers. Raises errcode 23514 on violation. Self-DoS: user creating+deleting ~500 entries can exhaust free cap with zero live rows; no client-side reclaim path until A2 hard-DELETE reaper.

<!-- lore:019fc563-11fb-79e0-9117-b5414a2dcdf9 -->
* **Anthropic SSE filter incomplete content\_block events**: Invariant: incomplete \`content\_block\_start\` / \`content\_block\_delta\` events are NO-OPs and must NEVER be forwarded to clients. Forwarding partial blocks looks like preserving stream fidelity but breaks downstream parsers (e.g., Pi) and yields malformed content. Only forward when block/delta is a valid non-null object.

<!-- lore:019f1fde-f17e-7835-a964-dfb1b4c697f5 -->
* **backfillTemporalEmbeddings: pool-gate on recall embeds in flight (PR #1111)**: PR #1111 (\`fix/temporal-backfill-pool-gate\`) replaces the session-activity idle-gate (PR #1108) with an embed-pool capacity gate. Root cause of old gate: 5 active sessions kept backfill parked continuously; cursor never advanced during 8+ min window. New signal: module-level \`\_recallEmbedsInFlight\` counter in \`embedding.ts\`. Incremented in \`embed()\` when \`inputType === "query" && texts.length === 1\`; decremented in \`finally\`. Exported as \`recallEmbedsInFlight()\` + \`\_setRecallEmbedsInFlightForTest()\` seam. Gate factory \`makeTemporalBackfillGate(deps)\` parks when \`isPaused() || isEmbedBusy()\`. Wired in \`pipeline.ts\` as \`isEmbedBusy: () => embedding.recallEmbedsInFlight() > 0\`. Backfill always uses \`"document"\` inputType → \`isRecall\` always false → no self-gating. Throwing gate predicate caught with \`break\` in \`awaitBackfillIdle\` — never wedges walk.

<!-- lore:019fbea5-1d44-7c84-a301-abd5bd66c72a -->
* **background tasks: activeTasks excludes queued**: Invariant: queued-but-not-started background tasks are NEVER present in activeTasks. This separation prevents false concurrency accounting and incorrect shutdown/drain logic. Including queued items looks simpler but breaks invariants like 'drain active tasks' and can cause premature shutdown or missed work accounting.

<!-- lore:019fd191-7eba-7a4e-bc2f-74f3fe061d4d -->
* **BEDROCK\_RUNTIME\_PATH\_RE + route + issue #1567 resolution**: AWS Bedrock Runtime passthrough at packages/gateway/src/translate/bedrock-runtime.ts:49. Path shape: \`/v1/model/{modelId}/{verb}\` (POST route in server.ts). modelId class \`\[a-zA-Z0-9.\_:-]+\` (colon added for AWS catalog version suffixes like \`anthropic.claude-opus-4-5-20251101-v1:0\`). Verb allowlist exact: \`converse|converse-stream|invoke|invoke-with-response-stream\` — BEDROCK\_RUNTIME\_VERBS constant. Anchored ^...$ so \`/v1/models\` (plural) and \`/v1/messages\` cannot match. Colon unambiguous to WHATWG URL parser (only port separator immediately after host, not in path segments) — mirrors AWS SDK v3 smithy-typescript. Issue #1567 resolved: PR #1575 (proxy+route+14 tests, commit 49523c08) + PR #1578 (regex widen+JSDoc+7 boundary tests, commit b40c6e56). 22 tests total.

<!-- lore:019fa8e0-d884-7d07-a793-537e38938425 -->
* **binpatch website Pages config: gh-pages branch + custom domain + https\_enforced flow**: binpatch website Pages setup (BYK/binpatch, chore/website-rebrand branch, 2026-07-28): (1) Custom DNS: CNAME \`binpatch.p.byk.im\` → \`byk.github.io.\` at Cloudflare, proxy OFF (grey cloud). (2) Pages config (\`gh api repos/BYK/binpatch/pages\`): \`build\_type:"legacy"\`, \`source:{branch:"gh-pages", path:"/"}\`, \`cname:"binpatch.p.byk.im"\`, \`https\_enforced:true\`. Enable via \`gh api repos/BYK/binpatch/pages -X PUT --input -\` with JSON body — boolean type errors (422) come from \`--field\` flag. (3) \`pages.yml\` deploys on \`push:branches:\[main]\` (NOT PR) using \`JamesIves/github-pages-deploy-action\` to push gh-pages. (4) \`docs-preview.yml\` PR preview uses \`pr-preview-action\` with \`DOCS\_BASE\_PATH: /\_preview/pr-${NUMBER}/\` (no \`/binpatch/\` prefix). Astro \`base: '/'\` and \`site: 'https://binpatch.p.byk.im'\`. (5) Production deploy from PRs requires manual \`workflow\_dispatch\` because \`pages.yml\` filter is \`main\` only — \`git remote -v\` from a PR branch never re-triggers production.

<!-- lore:019fbea5-1d0e-7cea-a6b8-99970c95d75c -->
* **boundedSettle Promise.allSettled timer race invariant**: boundedSettle() races Promise.allSettled against a timeout that ONLY resolves (never rejects), and the timer is always cleared in finally. Chose resolve-only timer over reject because rejection would mask underlying task errors and change semantics of allSettled; resolve preserves outcome collection while bounding time. Any implementation must keep finally-clear to avoid leaked timers and cross-test interference.

<!-- lore:019f9e12-567c-7d0b-bbf6-1077ea0a3820 -->
* **checkpointWal uses TRUNCATE in quiet window, never blocks indefinitely**: checkpointWal in db.ts: the WAL auto-checkpoint can never obtain the 'no reader below the mark' instant needed to reset the log. So checkpointWal uses TRUNCATE instead of checkpointing, executed only in a quiet window (no concurrent operations). It never blocks indefinitely because it only runs when the write-ahead log can be safely truncated without waiting on readers. This prevents startup stalls on large WAL files.

<!-- lore:019ef0c0-02e8-701a-824e-329ffb264c14 -->
* **CLI migration: symbol locations in inventory/start/portfile modules**: Two identities per entry: logical\_id (stable entry identity) + version (immutable snapshot keyed by UUIDv7). Invariant: version rows are immutable except is\_current — content, title, category, is\_deleted, version, logical\_id NEVER mutate after insert. Invariant: never resurrect a logical\_id whose latest version has is\_deleted=1 — no separate tombstones table needed. All non-FTS reads use knowledge\_current view (is\_current=1 AND is\_deleted=0). FTS5 indexes only current+live rows; superseded version's FTS row removed on append. Knowledge outbox keyed by logical\_id (not version id) so deletes always match their remote row.

<!-- lore:019fd322-32b8-71b0-862b-03253b8b1ac3 -->
* **earlyFlushStreamingResponse response.failed canonical envelope**: PR #1580 \`earlyFlushStreamingResponse\` (pipeline.ts ~L10937-11026) wraps handleConversationTurn to flush SSE headers ≤10s for codex/Responses. Error semantics: once 200 SSE committed, upstream 4xx/5xx become in-band \`response.failed\` events. Canonical envelope per stream/openai-responses.ts:446-470: outer \`{ type: "response.failed", response: { id, object: "response", created\_at, model, status: "failed", output: \[], usage: null, error: { type: "server\_error", message } } }\`. Original minimal \`{ type, error }\` was a protocol mismatch fixed in PR #1580 review. Scope: openai-responses only (opencode's 10s timeout at provider.ts:35 is openai-provider-scoped); anthropic/gemini/openai Chat Completions clients not wrapped.

<!-- lore:019f1a70-b05d-7756-aa08-d9d57fb9cbb0 -->
* **embedding.ts: embedInTokenBatches + nextBatch — sub-batching invariants for temporal chunk fan-out**: \`embedInTokenBatches(texts, type)\` loops \`nextBatch\` (generic over \`{text:string}\`, \`MAX\_BACKFILL\_CHUNK=8\`, \`MAX\_BATCH\_TOKEN\_AREA=4096\`, \`CHARS\_PER\_TOKEN=4\`). Key invariant: area guard fires only when \`batch.length > 0\` → first item always added unconditionally → loop always terminates. Vectors accumulate in input order; \`storeTemporalChunks\` called exactly once (all-or-nothing). \`MAX\_TEMPORAL\_CHUNKS\_PER\_MESSAGE=64\`: keeps first 63 verbatim, folds \`slice(63)\` into one merged chunk via \`join("\n")\` → exactly 64 texts. Oversized merged tail goes alone in its own batch; worker \`truncateTexts()\` + \`truncation:true\` handle downstream. PR #1075 (\`a2d71daa\`). Root cause of #1072: \`embed()\` forwarded entire \`texts\` array in ONE call — ONNX pads all texts to longest sequence, creating oversized tensor (OOM). Fix: \`embedInTokenBatches\` was already used by knowledge/distillation/entity backfill — gap was \`embedTemporalMessage\` did not use it. Mutation A (revert sub-batch) and Mutation B (revert cap fold) each independently kill their respective tests.

<!-- lore:019f9e12-5651-7d66-be6f-265b77ee4fc6 -->
* **embeddingColumnExists uses PRAGMA table\_info to avoid dropped column reads**: embeddingColumnExists never uses direct SELECT on potential embedding columns — a dropped column would cause a runtime error (v55 boot-loop lesson). Instead, it queries PRAGMA table\_info on the table and checks column existence in the result set. This is the pattern for any runtime column-existence check in the migration layer.

<!-- lore:019fd188-2c41-7ef4-905e-b9bdf7e56c98 -->
* **finalizeWithRecall: shared buffered recall loop (MAX\_RECALL\_DEPTH 10)**: \`finalizeWithRecall\` (pipeline.ts:9680) is the buffered recall-interception loop SHARED by the non-streaming path AND the openai/openai-responses streaming paths — both accumulate upstream SSE into the same internal Anthropic-format response, so the recall loop is protocol-agnostic. Without it, a gateway-injected \`recall\` tool\_use leaks to the client ("Model tried to call unavailable tool 'recall'"). Loop: while hasRecallToolUse(currentResp) && recallDepth < MAX\_RECALL\_DEPTH (10, recall.ts:72); cumulativeUsage accumulates across iterations; alreadyInLtmIds = buildAlreadyInLtmIds(stableLtmText, pendingKnowledgeDelta) snapped ONCE per request (Seer 15623149/1). Mixed-tools branch: store result in recallStore, markerResp.usage = cumulativeUsage, postResponse, return nonStreamHttpResponse with header 'x-lore-recall-invoked': 'true'. requestEnablesLongContext(req) gates the client-usage cap so 1M-capable models clamp below the ~167K auto-compact threshold (#910 regression).

<!-- lore:019f43a4-4edb-7066-b799-932b9ee9b165 -->
* **forSession packing loop uses continue (not break) on over-budget entries**: In \`ltm.ts\` packing loop (line ~2358): \`if (used + cost > maxTokens) continue;\` — NOT \`break\`. This means over-budget entries are skipped but smaller subsequent entries can still be packed. The loop never exceeds total budget but does not stop at first overflow. This is intentional: allows smaller high-value entries to fill remaining budget after a large entry is skipped.

<!-- lore:019fc52b-ba09-7bc5-8b95-decca8ddf993 -->
* **Gateway as source of truth; plugin is dumb relay**: Invariant: the Gateway is the single source of truth; the plugin must act as a dumb relay. Adding logic to the plugin looks like latency/UX optimization but creates split-brain behavior and breaks consistency with gateway-enforced contracts.

<!-- lore:019fceb9-a92b-7691-9a3c-b0ea94f9bdfc -->
* **Gateway server.ts route order matters: bedrock-runtime path must match before /v1/models**: Gateway route dispatch in server.ts is order-sensitive. Bedrock Runtime path \`POST /v1/model/{modelId}/{verb}\` (modelId = \`\[^/]+\`, verbs allowlisted: converse/converse-stream/invoke/invoke-with-response-stream) must be wired AFTER \`/v1/compact\` handling but BEFORE \`/v1/models\` GET passthrough — otherwise a single-character overlap with \`/v1/models\` swallows Bedrock Runtime requests as 404s. The path regex is distinct from \`/v1/models\` (plural list endpoint). Both \`bedrock\` and \`amazon-bedrock\` opencode provider IDs route via this dispatch.

<!-- lore:019fc52b-ba6c-73b9-957f-699eaf2284d8 -->
* **GatewayContentBlock\[] normalization + opaque block preservation**: Invariant: all content is normalized to GatewayContentBlock\[] (array of blocks). Opaque/thinking blocks must be preserved unmodified and in position. Flattening or rewriting blocks looks simpler but corrupts semantics and breaks provider round-trips.

<!-- lore:019f9e12-55cd-794c-84e2-48899d873deb -->
* **gcVec0DanglingRows only handles orphaned rows, not mis-partitioned**: gcVec0DanglingRows is designed exclusively for orphaned rows (base row missing in the relational table). It never repairs mis-partitioned rows — rows that exist but have a wrong partition key. A mis-partitioned row is mis-partitioned, not orphaned, so gcVec0DanglingRows never touches it. Mis-partitioning must be fixed by repartitionVec0Project or the session-coalescing rebuild, not the dangling-row sweep.

<!-- lore:019f1890-9144-77d2-98f3-57e2fa2dfc68 -->
* **http-body.ts: encodeUpstreamBodyForRoute — single chokepoint for re-compression scoping**: PR #1059 adds \`encodeUpstreamBodyForRoute(serializedBody, rawEncoding, route: UpstreamRouteContext)\` as the single chokepoint for upstream re-compression decisions. PR #1064 (\`de7bbd79\`, 2026-06-30) extends with origin-aware distrust: \`mayReencodeUpstream(route)\` returns false when \`autoCrossRouted = !hasUpstreamUrlOverride && !hasProviderOverride && (effectiveProtocol !== ingressProtocol || effectiveOrigin !== ingressOrigin)\`. F3 root cause: protocol-only predicate trusted same-protocol re-routes to different hosts. \`buildUpstreamRouteContext\` accepts options object with \`ingressUpstreamBase\`/\`effectiveUpstreamBase\`. Vertex/Bedrock never reach origin comparison — both require explicit \`X-Lore-Provider\` → short-circuit to trusted. Codex always carries \`x-lore-provider\` → trusted. \`fetch.ts\` gets \`setUpstreamDispatcherForTest\` seam. Mutation-verified: removing origin clause → 4 tests RED.

<!-- lore:019ef387-4ae0-7676-b7b5-140c85ad2627 -->
* **knowledge\_meta table (v55): architecture, rationale, invariants, migration gotcha**: Migration v55 moves \`confidence\` and \`last\_reinforced\_at\` off the \`knowledge\` table into a new convergent register table: \`knowledge\_meta (logical\_id TEXT PRIMARY KEY, confidence REAL NOT NULL DEFAULT 1.0, last\_reinforced\_at INTEGER, updated\_at INTEGER NOT NULL DEFAULT 0)\`. \`knowledge\_current\` view exposes both via LEFT JOIN: \`SELECT k.\*, COALESCE(m.confidence, 1.0) AS confidence, m.last\_reinforced\_at FROM knowledge k LEFT JOIN knowledge\_meta m ON m.logical\_id = k.logical\_id WHERE k.is\_current=1 AND k.is\_deleted=0\`. Reading/writing confidence on the base \`knowledge\` table now errors with 'no such column'. For v1 rows: logical\_id == id. \`ltm.create()\` creates matching knowledge\_meta row automatically. Schema version bumped 54→55. \`markInjected()\` updates \`knowledge\_meta.last\_reinforced\_at\` but does NOT bump \`updated\_at\` — injection stays sync-silent. Content-only \`update()\` also must NOT bump \`meta.updated\_at\`.

<!-- lore:019fa414-5f3b-77ce-afb6-debe2c0365bd -->
* **Knowledge-delta pair design: user-payload + assistant-closer**: Knowledge deltas use a user→assistant pair: payload on USER (ambient), inert ASSISTANT closer. Putting payload on assistant caused visible history spam and premature loop exits; omitting closer leaves an open user turn.

<!-- lore:019fd7d9-bc88-7317-8703-02f64547fee8 -->
* **lint CLI stdio contract: exit 0 even with findings in advisory mode**: \`packages/gateway/src/cli/commands/invariant-check.ts\` legacy handler (\`commandInvariantCheck\`) uses \`console.log(JSON.stringify(...))\` at lines 207-208 to emit the JSON result envelope. Diagnostic logs use \`console.error\` (lines 60, 84, 92, 135). Lore lint Stricli adapter (\`lint.ts\`) sets \`toJson: (data) => extractTrailingJsonObject(data) ?? { output: data }\`. Process exit code contract: advisory mode exits 0 EVEN ON FINDINGS (default lore lint behavior); only \`process.exit(2)\` paths signal policy violations. Mixing exit codes with \`console.log\` data is the contract — exit code is the signal, not the JSON body.

<!-- lore:019edfbc-edd5-780d-ac81-07a35390121b -->
* **ltm.ts: confidence lifecycle — decay, reinforcement, eviction, and cross-project protection**: pruneDeadEntries, pruneDeadEntriesAllProjects, and evictLowestValue MUST never delete cross\_project=1 or promoted rows (promoted\_at IS NOT NULL). This is a hard invariant: only unpromoted, project-scoped entries are eligible for eviction. DEAD\_CONFIDENCE\_FLOOR=0.2. Global sweep in idle.ts runs on first tick then hourly, gated on loreConfig().knowledge.enabled, wrapped in try/catch. Repeated directive, 34+ sessions.

<!-- lore:019efa21-0e50-7290-ae68-9231f88f690b -->
* **openai-codex ingress: codex flag, store:false enforcement, max\_output\_tokens suppression**: \`POST /v1/codex/responses\` → parseOpenAICodexRequest sets req.codex=true, captures Codex control fields into req.extras. store NOT captured — applyCodexResponsesDelta always forces store:false (ChatGPT rejects store:true). max\_output\_tokens deleted from upstream body (ChatGPT rejects it). Upstream URL https://chatgpt.com/backend-api/codex/responses. Gateway is stateless full-history proxy. buildCodexWorkerRequest (llm-adapter.ts:1169-1208) builds \`${target.url}/codex/responses\` with store:false, stream:false, NO max\_output\_tokens (ChatGPT Codex rejects it and enforces its own server-side output limits). Reasoning models put the answer in reasoning fields — never treat a present reasoning body as no-response; when an aggregator proxies a reasoning model through the Anthropic shape emitting only a thinking block, fall back to the thinking text rather than returning null.

<!-- lore:019fcee5-51bc-758d-8709-ea10e8c87e80 -->
* **opencode-lore plugin: applyLoreProviderConfig pins every provider baseURL**: The opencode-lore plugin redirects ALL provider traffic through the gateway: a process-wide fetch interceptor (installed once, preserves original auth/URL context) + \`applyLoreProviderConfig\` pinning EVERY provider's \`options.baseURL\` to \`${gatewayBase}/v1\` — not just Anthropic. Without it, opencode derives the Anthropic baseURL from env → \`http://host/messages\` (no \`/v1\`, gateway rejects); other providers have no baseURL env at all. Interceptor emits \`x-lore-provider\` from providerID and redirects e.g. \`chatgpt.com/backend-api/codex/responses → gateway\`. Module entry must export ONLY LorePlugin + default — the legacy plugin loader invokes every function export as a plugin and throws on non-function exports; helpers (\`applyLoreProviderConfig\`, \`probeGateway\`, \`surfaceGatewayUnavailable\`) live in internal.ts. Gateway discovery: LORE\_GATEWAY\_URL → port file → default ports 3207/5673. Startup banner goes through \`log\`, never raw stderr (TUI corruption).

<!-- lore:019eeb85-6440-7760-912a-30c1cdc31a9f -->
* **pipeline.ts inFlightBackground: tracks urgent distillation separately from limiter activeTasks**: pipeline.ts inFlightBackground + urgent distillation drain invariants: \`inFlightBackground\` (Set\<Promise>) tracks urgent distillation promises that bypass the background limiter. \`trackBackground(p)\` adds/removes on \`.finally()\`. Urgent distillation bypasses \`isBackgroundPaused()\` — degraded context for 10 min is worse than one API call. \`resetPipelineState\` non-fast path drains BOTH: \`drainBackground()\` (limiter's \`activeTasks\`) AND \`boundedSettle(\[...inFlightBackground], timeoutMs)\`. Missing either leaves a drain gap. \`boundedSettle()\` in background-limiter.ts: snapshots promises, races \`Promise.allSettled\` against a timeout that only resolves (never rejects); timer always cleared in \`finally\`. \`drainBackground\`: calls \`limiter.clearQueue()\` then \`boundedSettle(\[...activeTasks])\`. Queued-but-not-started tasks are never in \`activeTasks\`.

<!-- lore:019f04c9-26e4-794e-8d94-dd107b7bf454 -->
* **pipeline.ts prompt-delta system: surfaced-set, reanchor, cache-stability, deploy gate**: Three-layer delta system in \`pipeline.ts\`: 1. \*\*\`detectSurfacedMutations\`\*\* (line 1185): compares already-surfaced \`id:hash\` keys against current DB state — fires only on genuine content change or tombstone, never on per-turn ranking churn (churn-immunity property). 2. \*\*\`reanchorDeltaOnCompression\`\*\* (line 945): on a compressing turn, ALWAYS re-anchor (preserve \`content\` + \`mut\` signatures, rewrite only \`insertAt\`) — NEVER delete. Deleting wipes surfaced-set history, causing next append to re-derive full cumulative pin→DB wall (the regrowth churn #1013 trimmed but #1017 root-caused). 3. \*\*\`applySessionPromptDeltas\`\*\*: splices back-to-front so equal-position blocks replay in ascending-seq order; lower-index blocks never shifted by later splices. 🔴 Delta block must go BEFORE any assistant \`tool\_use\` — never between \`tool\_use\` and its \`tool\_result\`.

<!-- lore:019fcab7-8995-7268-8103-eaf1d23bdc39 -->
* **pipeline.ts single-envelope SSE invariant (Anthropic/Pi)**: Chose single response envelope over split/nested envelopes for recall streaming because Anthropic Messages (Pi) requires exactly one \`message\_start\` → \`message\_stop\` lifecycle. Split-envelope (marker + continuation) looked modular but emits multiple \`message\_start\`s and is rejected. Implementation: inline recall markers as text blocks, suppress continuation \`message\_start\`, emit only the final \`message\_stop\`.

<!-- lore:019fcdf4-5b89-7c76-99be-1ab6d05dd546 -->
* **pipeline.ts synthetic project resolution: stripSyntheticRoundTrips + refcheck probe**: Synthetic resolve round-trip (pipeline.ts ~7260-7344) must be stripped from the conversation via stripSyntheticRoundTrips(req) so the LLM never sees it AND it's excluded from temporal storage. The refcheck probe must never throw into the request path — wrapped in try/catch, NoopResolver on error/missing snapshot, failures logged as non-fatal warnings, still stamps the 24h gate. A path-less probe request must never create a project row for the gateway's cwd or an unattributed bucket — initIfNeeded(projectPath, config, pathResult.gitRemote) runs AFTER path correction. Provider-agnostic: applies to every protocol/client. sessionState.syntheticResolveKind is "read" or "shell"; probe stages advance readTried → shellTried; if still provisional after a read probe, re-eligible for shell probe on the same turn's injection phase. stripSyntheticRoundTrips called a second time (belt-and-suspenders).

<!-- lore:019fbea5-1e30-7a77-b92f-d6ebb227c8ee -->
* **queryDB visibility: always latest committed state**: Invariant: queries must always see the latest committed state (no stale reads). This guarantees correctness for eval harnesses and accounting. Allowing weaker isolation may look faster but causes heisenbugs in scoring and validation that depend on immediate consistency.

<!-- lore:019f9e12-55fa-7852-97ec-2c947b19e564 -->
* **readStorageMode / setStorageMode: monotonic latch and consistency**: Design principles for vector storage mode abstraction: (1) monotonic latch: once vec0 mode is observed, it NEVER falls back to blob mode — base embedding columns dropped after mode flip ensures no dropped-column reads. (2) setStorageMode consistency: the sticky read latch must agree with what was just written, preventing checkpoint/time-travel disagreements. (3) Callers must never reason about the 2×2 matrix (blob, blob-native, vec0, write-order) — the abstraction hides implementation details. (4) Safe fallback (unrecognized table\_type) must never assume vec0. (5) Resetting the storage mode latch ensures a fresh blob DB is never poisoned by a prior vec0 observation.

<!-- lore:019f9e12-5625-7f1b-ac14-bce720f527e5 -->
* **resolveTrustedRemote: disk remote always wins over client header**: resolveTrustedRemote: if disk has a git remote, ALWAYS use the on-disk remote value. A client-supplied x-lore-git-remote header can never override on-disk truth, preventing remote-param tampering. In local mode with no disk remote, drop the client remote entirely — prevents the git-remote magnet bug where a stale header incorrectly selects a remote.

<!-- lore:019f324d-a433-76ef-a7d3-c9acc21c6335 -->
* **scope\_keys.wrapped\_dek: first-write-wins immutability via key\_epoch guard trigger**: PR #1179 (migration 0012\_scope\_key\_immutable.sql) adds trigger \`guard\_scope\_key\_immutable\` on \`scope\_keys\` (BEFORE UPDATE): raises \`check\_violation\` (SQLSTATE 23514) when \`new.wrapped\_dek IS DISTINCT FROM old.wrapped\_dek AND new.key\_epoch <= old.key\_epoch\`. Metadata-only changes and higher-epoch DEK rotations are allowed; same-epoch DEK clobber is blocked. Rationale: DEK clobber is the catastrophic failure mode — it orphans already-encrypted ciphertext across devices. \`account\_escrow\` is NOT made immutable (must change on passphrase rotation) — only \`scope\_keys.wrapped\_dek\` gets the guard, since escrow clobber is recoverable (device-1 retains local identity) but DEK clobber is not. Trigger ordering matters: Postgres fires BEFORE UPDATE triggers alphabetically by name — \`scope\_keys\_guard\_immutable\` (0012) fires before \`scope\_keys\_row\_quota\`/\`scope\_keys\_set\_updated\_at\` (0010) since 'g' < 'r' < 's'.

<!-- lore:019eea5e-b325-720b-a9c1-1c526efc0096 -->
* **sync-data.ts seedOutbox: content-aware JS loop — 6 cases, cursor semantics, performance**: PR #868 rewrites \`seedOutbox()\` from single \`INSERT…SELECT\` to JS loop. Push cursor = \`Number(getKV('sync.push.'+table) ?? '0')\`. \`latestUnpushed\` = latest op with \`seq > cursor\`. Six cases: (a) no pending + hash==sync\_state → skip; (b) no pending + hash!=sync\_state → enqueue; (c) latest unpushed=upsert → skip; (d) latest unpushed=delete → enqueue trailing upsert; (e) stale upsert below cursor + content changed → enqueue (THE FIX); (f) stale upsert below cursor + content unchanged → skip. \`seq AUTOINCREMENT\` guarantees strict monotonicity. \`contentHash\` uses \`syncedColumns ∖ HASH\_EXCLUDE\`, sorted, SHA-256 truncated to 16 hex. 🔴 Wrap loop in \`withTransaction\` — N auto-commits cause ~23x slowdown. 🔴 \`seedOutbox\` is only ever reached via \`enableSync\`, never from within another transaction (code comment at line 498). PR #874 hoists \`columns(table)\` outside per-row loop; PR #878 memoizes globally via \`WeakMap\` keyed on \`db()\` instance — \`seedOutbox\` pre-warms cache for all tables, so subsequent \`pushOnce\` sees 0 PRAGMAs.

<!-- lore:019edfc9-c361-72f2-ada3-fc735def69f0 -->
* **sync.ts: local prune vs genuine delete — tombstone suppression required**: sync.ts: local prune vs genuine delete — tombstone suppression required. Hard invariant (🔴): only genuine user/curator-intent deletes generate tombstones and propagate to remote. Local TTL/size eviction (idle prune) must NOT enqueue a tombstone — run prune under a capture-suppressed context (analogous to \`withSyncApplying\`) that drops the DELETE-trigger tombstone and clears \`sync\_state\` only. \`reconcile\` must never see evicted rows as deletes. Evicted rows are re-pullable cache misses, not divergence. Remote is bounded by per-scope quota, not a TTL reaper.

<!-- lore:019edff0-4709-74cc-98eb-ad3bd46163c1 -->
* **sync.ts: push payload, pullOnly tables, and per-table cursor isolation**: sync.ts push payload: \`pushEntry()\` sends \`toRemoteRow(pickSyncColumns(table,row))\` + \`is\_deleted:false\` + \`content\_hash\`/\`revision\` (versioned only). \`scope\_id\` NEVER included — server fills via \`default auth.uid()\`. \`onConflict\` target post-A1: \`\["scope\_id",...idColumns(table)].join(",")\`. \`REMOTE\_ONLY\_COLS\` strips \`scope\_id\`,\`author\_id\`,\`content\_hash\`,\`revision\`,\`is\_deleted\` before local apply. Soft-delete push: \`.update({is\_deleted:true}).match(decomposeId(table,rowId))\`. \`pickSyncColumns\` strips local-only cols (e.g. \`knowledge.promoted\_at\`) to avoid PGRST204. \`SyncTableMeta.versioned\` flag (default true): strip \`content\_hash\`/\`revision\` when false — \`knowledge\_entity\_refs\` is the only non-versioned table; without this fix PGRST204 → 'transient' → push cursor stuck forever.

<!-- lore:019fa414-5edb-726e-b028-28b5b46905dd -->
* **system\[2] ltmSystem fully removed — ltmText, AnthropicCacheOptions.ltmSystem, contextLtm all deleted**: system\[2] is fully removed (Jul 27 2026, issue #1502 fix). All dead code excised: (1) \`ltmText\` variable declaration deleted from pipeline.ts; (2) \`ltmSystem?\` field removed from \`AnthropicCacheOptions\` in anthropic.ts; (3) \`contextLtm\` plumbing removed from \`buildAnthropicRequest\` (const, if-block, and concatenation path); (4) \`ltmParts\` arrays simplified from \`\[cache?.stableLtmSystem, cache?.ltmSystem].filter(Boolean)\` to \`\[cache?.stableLtmSystem].filter(Boolean)\` in OpenAI/Gemini branches; (5) \`ltmSystem: undefined\` override removed from OpenAI cacheOptions; (6) \`ltmSystem: ltmText\` removed from cacheOptions literal; (7) \`contextTokens\` simplified to \`setLtmTokens((stable?.tokenCount ?? 0), sessionID)\` since ltmText was always undefined; (8) all doc comments referencing ltmText/system\[2] updated. Tests passing \`ltmSystem\` in fixtures (anthropic-caching.test.ts, cache-segment-digest.test.ts) must be updated to remove the field.

<!-- lore:019fccdb-49eb-77b4-9e93-e7bc108067f3 -->
* **team invites: gateway passes intent, EF mints (no createTeamInvite in gateway)**: Chose EF-side invite minting over gateway-side creation because invite generation must be tied to the authoritative execution flow (EF) where permissions and side effects are enforced. The gateway only passes \`invite\_to\_scope\` and \`role\` so admins can introspect their own request once. Rejected gateway calling \`createTeamInvite\` as it looks simpler (single place), but would duplicate authority, risk divergence in permission checks, and break the single-source-of-truth model.

<!-- lore:019fbf47-a3a3-7aa5-8e43-0fe8f6f1a2d0 -->
* **Verifier-on-host / agent-in-Docker + env-leak isolation**: Chose host-side verifier + Docker-isolated agent over single-process execution because secrets (fact maps) must never be visible to agent code. Single runtime looks simpler but leaks facts via env/process; container boundary enforces separation and black-box tests.

<!-- lore:019fce4e-0474-70ce-bbb4-01688c54e89b -->
* **Worker call beta policy: context-1m stripped, oauth/thinking/cache betas preserved**: Worker beta header policy (\`buildOAuthWorkerHeaders\` in \`cch.ts:672-687\` + \`buildAnthropicWorkerRequest\` in \`llm-adapter.ts:712-818\`): 1. \`WORKER\_BETAS = oauth-2025-04-20,interleaved-thinking-2025-05-14,extended-cache-ttl-2025-04-11,prompt-caching-scope-2026-01-05\` (cch.ts:565-570). Does NOT include \`context-1m\`. 2. If client \`anthropic-beta\` header is sniffed, replay it — then immediately strip \`context-1m\` via \`applyModelBetaCapabilityFilter\` regardless of worker model capability. OAuth gate MUST survive stripping (stripping it would 401). 3. If stripping \`context-1m\` leaves no other betas, delete the \`anthropic-beta\` header entirely. 4. Helpers (llm-adapter.ts:115-164): \`hasLongContextBeta\` (case-insensitive \`/context-1m/i\`), \`stripBetaHeaders\` (regex \`LONG\_CONTEXT\_BETA\_RE = /^context-1m(?:-.\*)?$/\`), \`isBetaRelated400\` (\`/\bbeta\b/i && /(not\s+(yet\s+)?available|unsupported|not\senabled|invalid)/i\`). 5. Runtime 400-retry fallback (\`llm-adapter.ts:2182-2236\`) strips betas once on beta-related 400 — safety net for future betas, NOT the primary context-1m mechanism anymore.

<!-- lore:019fd7d9-bc4c-7eec-9522-f31dd1e6d1ae -->
* **Worker URL resolution: \_\_filename (CJS) vs import.meta.url (ESM)**: Worker URL resolution for \`new Worker(...)\` differs by module system: CJS bundles use \`\_\_filename\` (always defined); ESM bundles use \`import.meta.url\`. Wrong choice at the \`ensureWorker()\` site (\`packages/core/src/embedding.ts\` ~733-1127, uses worker\_threads) produces undefined paths and silent worker spawn failures. Test factory \`testWorkerFactory\` is never set in production — it is a deterministic fake worker so OOM-recovery lifecycle tests can run without a real ONNX runtime. WorkerInitData (\`embedding.ts:750-787\`) snapshots \`stderrSilenced: log.isStderrSilenced()\` from the main thread at spawn time.

<!-- lore:019fcdf4-5b3b-7299-ad23-82083a60c46f -->
* **worker-model.ts WORKER\_DEFAULTS: never cross-provider, never upgrade to full codex**: WORKER\_DEFAULTS (worker-model.ts:1000-1064) picks a cheaper worker model from the SAME provider only — "Never cross-provider" (line 1369). openai-codex → gpt-5.1-codex-mini, family="gpt-codex"; the alreadyCheap filter keeps "newest in family" from upgrading to full codex (~20x cheaper input). ChatGPT-backend short-circuit (1339-1366): a session routed to chatgpt.com/backend-api authenticates via ChatGPT OAuth JWT and serves ONLY the session's own live model — selecting a cheaper sibling 404s and poisons background work. Worker MUST reuse the session's exact live model (sessionProviderID/sessionModelID, NOT effectiveProvider/effectiveModelID — a cfg.model override naming a different model would also 404). Exempts sessionProviderID "openai-codex" (legitimately downgrades to mini on same endpoint). Keying on sessionProviderID prevents a cfg.model.providerID="openai" override from masking a real codex session.

<!-- lore:019fcee5-5240-76f4-bb4d-c35e89de55ff -->
* **worker-model.ts: ChatGPT-backend workers reuse session's exact model**: \`isChatGPTBackend(url)\` = hostname \`chatgpt.com\` or pathname includes \`/backend-api\`. The ChatGPT backend authenticates via a ChatGPT OAuth JWT and serves ONLY the session's own model — a sibling model (e.g. a cheaper gpt-5.4-mini from family selection) 404s, so a worker for such a session MUST reuse the session's exact model. It reports the same providerID ("openai") and protocol ("openai-responses") as a real api.openai.com API-key session — only host/path discriminates. Related traps: gpt-5.4-mini is /responses-only on Copilot and 400s on Chat Completions (worker falls back to gpt-5-mini for github-copilot); WORKER\_DEFAULTS: openai→gpt-5.6-luna (cheaper than sol), openai-codex→gpt-5.1-codex-mini.

### Decision

<!-- lore:019f43a4-4ab3-72a3-a2b6-6d1a1290548c -->
* **Adopted adversarial mutation-testing methodology:**: adopted adversarial mutation-testing methodology: for each of 4 identified risk areas (M1-M4),

<!-- lore:019f9393-21f5-7a6f-9218-d72cac34386f -->
* **Adopted key to default upstreams**: adopted key to default upstreams for the background auto-import path,

<!-- lore:019fa81f-1e27-77de-bdcd-5717dd166d12 -->
* **Adopted sid**: adopted sid for future Tier-1 fast path.

<!-- lore:019f5692-6771-7577-965f-da25f1e11f6d -->
* **Adopted three-commit strategy: commit 1 = tooling/config, commit 2 = mechanical reformat + safe autofixes, commit 3 = manual semantic (async-safety) fixes. (PR #1277 final commits: 0993b690 tooling, f11f2e4b reformat+autofix, a3486c5f**: Adopted three-commit strategy: commit 1 = tooling/config, commit 2 = mechanical reformat + safe autofixes, commit 3 = manual semantic (async-safety) fixes.

<!-- lore:019fcc59-eb27-70e7-9b6f-0c5553e5a832 -->
* **agent-timeout classified as valid scored outcome**: Chose to treat agent timeouts/runaways as VALID scored terminal outcomes over marking them as invalid runs because they reflect model capability (behavior under compaction) rather than infrastructure failure. Rejected prior approach (invalidate on non-zero exit/timeout) since it discards real progress and aborts campaigns. Implementation: driver sets \`terminalOutcome: "agent-timeout"\` only if there was agent activity; scorer preserves completed checkpoints and labels result PARTIAL, continuing the matrix.

<!-- lore:019fceb9-a966-7375-add6-9f9e69a9fefe -->
* **Bedrock Runtime passthrough — let upstream reject invalid auth (no SigV4 vs Bearer split)**: Chose transparent header passthrough (forward ALL Authorization headers unchanged, let upstream reject invalid SigV4 with 403) over per-auth-scheme branching for the Bedrock Runtime proxy. Bearer tokens (\`AWS\_BEARER\_TOKEN\_BEDROCK\`) are host-agnostic and work transparently at any destination. SigV4 (\`AWS4-HMAC-SHA256 ...\`) is host-specific — AWS SDK signs based on configured endpoint host, so SigV4 signed for the gateway URL won't validate at bedrock-runtime. Rejected the smart-split approach (parse Authorization header, strip SigV4, replace with Bearer): looks safer but introduces parsing complexity for an unsupported auth path, and opencode stores Bedrock auth as bearer tokens (\`auth.key\` → \`AWS\_BEARER\_TOKEN\_BEDROCK\`). Trade-off: callers using SigV4 will get 403 instead of friendly error — but this is accurate (no gateway workaround exists).

<!-- lore:019f9500-8717-7c1b-b527-755d6956c9c9 -->
* **Chose fuller/higher-quality path over apply-core-only first cut**: chose fuller/higher-quality path over apply-core-only first cut.

<!-- lore:019f3974-7c6a-7091-8602-b4ec0bb763a4 -->
* **Chose next task: knowledge\_meta seed-order alignment (self-contained, low-risk, completes value-ranked seed coherence work started with knowledge #1189 and entities #1194) — over #1191b (continuous top-N eviction)**: Completed value-ranked seed coherence work in sequence: #1189 (knowledge seed value-ranking) → PR #1194 (entity-graph seed: entities ranked by knowledge\_entity\_refs count DESC then recency; entity\_relations by recency; entity\_aliases by created\_at DESC) → PR #1196 (knowledge\_meta seed aligned to knowledge's ORDER BY: confidence DESC, COALESCE(last\_reinforced\_at, updated\_at) DESC, logical\_id tiebreak). Deferred #1191b (continuous top-N eviction for remote) as a separate larger/riskier task needing a design doc first (remote eviction, cap discovery, thrash guards, concurrency) — not suitable for autonomous same-session implementation. Follow-up issue #1197 filed for a known non-blocking edge case: a deleted high-confidence entry's orphaned knowledge\_meta row can outrank a live entry's meta row under the cap (self-healing, not data loss).

<!-- lore:019f821b-dbd6-74c8-b138-a378a7d3c1e3 -->
* **Chose PostgreSQL over MySQL for JSONB support**: Chose PostgreSQL over MySQL for JSONB support.

<!-- lore:019fcee0-1a34-77e3-831d-9e0dc28b42f0 -->
* **Chose root-served custom domain over byk**: chose root-served custom domain over byk.

<!-- lore:019f9505-2846-77a6-9d4d-01be80c9d0c6 -->
* **Decided to use a fresh isolated DB for the 0**: decided to use a fresh isolated DB for the 0.

<!-- lore:019f94bf-c91c-7256-a32b-06a0c50ac2eb -->
* **Decided to use a pty session to drive it interactively**: decided to use a pty session to drive it interactively.

<!-- lore:019fc92b-2db4-72fb-92f1-e7551c60ce07 -->
* **eval-cmd.ts deterministic --fact-seed via sha256(taskID:model)**: Chose deterministic \`--fact-seed\` derived as sha256(\`${taskID}:${model}\`) over random/constant seeds because paired Lore/no-Lore arms must share identical facts for fair A/B comparison and reproducibility. Rejected constant seed (collides across tasks/models) and implicit randomness (breaks pairing invariant). CLI must always pass \`--fact-seed\` to driver; driver enforces it for iterative tasks.

<!-- lore:019f821b-dc33-73a0-b54a-ebe01390e550 -->
* **Going with Bun**: Going with Bun because it ships a native SQLite driver; zero deps needed.

<!-- lore:019fd14d-dd94-740c-ac55-f936a3b1993c -->
* **Lint judge: self-contained budget + always log stderr (PR #1555)**: Decision (PR #1555, commit a984b56c, Aug 5 2026): the lint judge's token budget is self-contained (computed per judge, not shared/global) and it ALWAYS logs stderr. Rationale: a shared budget lets one runaway judge starve others; swallowed stderr hides real judge failures. This codifies the standing stderr directive for the lint path. Related and merged: the lint judge now routes through github-copilot rather than github-models (PR #1550, 3fa064e3) — the github-models route was dropped.

<!-- lore:019f942e-7965-79c8-a045-fb0aa0f68f3e -->
* **Migrated to consume it**: Migrated to consume it (no further detail retained).

<!-- lore:019f5b56-d717-7acf-82cd-ecfc75ca4871 -->
* **Migrated to TG): test failed at sync**: migrating to TG): test failed at sync.

<!-- lore:019fa04c-10ba-7f48-b187-86a060994529 -->
* **Migrated to v11 layout**: migrated to v11 layout

<!-- lore:019fc63b-8cf5-79dd-804b-db60444de116 -->
* **no-memory = full isolation (gateway + session + transform)**: Chose full isolation for \`no-memory\` over partial suppression because partial approaches (e.g., write-only \`x-lore-no-store\` or single-path guards) still leak temporal/distillation context and invalidate the control. Implementation: route via gateway, add \`x-lore-no-memory\`, gate ALL LTM injection via a shared predicate, and use a fresh session ID to prevent gradient/distillation reuse. Rejected partial suppression as it looks cheaper but produces contaminated baselines.

<!-- lore:019fbf47-a4dc-7d1e-b383-bb6a669e0469 -->
* **Pi contextWindow pinned via models.json override**: Chose per-run \`models.json\` override (contextWindow = capContext) over assuming model window because Pi’s internal window can differ, causing incorrect compaction thresholds. Assumed window looks simpler but leads to systematic bias.

<!-- lore:019fbf2e-bb39-7b6b-b409-8e460772bf05 -->
* **RealBPE token estimation (PR #1551): coreEstimateTokens at 4 pipeline.ts sites**: Token estimation migrated from the chars/3 heuristic to real BPE via coder/ai-tokenizer (PR #1551, commit d76e5fd4). In pipeline.ts, \`estimateTokens\` is imported aliased as \`coreEstimateTokens\` from @loreai/core; it replaces \`Math.ceil(formatted.length / 3)\` at 4 sites (~L7974, ~L8082, ~L8406, ~L8878). The ~L8878 site estimates \`JSON.stringify(modifiedReq.messages)\`. Adjacent cache rule (~L8406): always refresh the cache with freshly ranked entries — delete-then-set, not in-place overwrite.

<!-- lore:019f18a1-0f46-7d96-9aa5-151ac33868ec -->
* **Reverted to upstream sqlite-vec@0.1.9 from vendored 0.1.10-alpha.4 — AVX2 buys nothing on FLAT vec0**: Chose upstream \`sqlite-vec@0.1.9\` over vendored \`@loreai/sqlite-vec-vendored\` (0.1.10-alpha.4 with AVX2 + DiskANN + rescore flags) because FLAT vec0 cosine scan is memory-bandwidth-bound — 7-run pinned benchmark (106K vectors × 768-dim, single-core) showed 0.1ms p50 difference (dead heat). DiskANN rejected: partition keys unsupported, metadata filter columns unsupported, insert cost ~48ms/vec (106K backfill ≈ 85 min), recall@10 ~0.82 (approximate), cannot scope search to project/session. Vendored fork also introduced \`INSERT OR REPLACE\` vec0 support that 0.1.9 lacks, requiring a portable DELETE+INSERT upsert workaround. PR #1058 (\`25c9ea7e\`) reverts PR #1038. Upstream 0.1.9 linux-x64 prebuilt is pure scalar SSE2 baseline — zero AVX, zero cpuid dispatch — so AVX2 patch buys nothing. FLAT vec0 confirmed: inserts ~0.122ms/vec; exact recall (recall=1.0); supports partition keys + metadata columns.

<!-- lore:019fc75f-4181-7782-95f5-5d816db63b7b -->
* **run-matrix preflight fail-closed on missing worker artifacts**: Chose fail-closed preflight (build once + verify artifacts, then abort if still missing) over warning/best-effort because partial runs silently degrade (no embeddings) and produce invalid comparisons and telemetry noise. Ensures every matrix cell runs with correct gateway capabilities or not at all.

<!-- lore:019fc27c-8c86-7c2a-aed1-9ad0342e355d -->
* **run-matrix.mjs fail-fast on invalid cell**: Chose fail-fast execution over full-matrix completion because continuing after an invalid cell (auth expiry, dead backend) wastes compute and contaminates aggregates. Implement immediate post-cell validity check and abort the campaign on first invalid cell; previously the runner completed all cells and only then the scorer rejected invalids, mixing infra failures into results.

<!-- lore:019f4c06-ef9f-7603-bf46-5f1957887cf1 -->
* **Stale local refs cause rebases to silently drop commits**: adopted checkout under different path, NULL git\_remote); 4) convergeProjectsByRemote determinism + FK-safety (min-id winner, runs after pullOnce, outside any transaction); 5) git\_remote gate consistency between capture trigger and seedSelect (FK poison risk for remote-backed vs remote-less projects); 6) pull order — projects registered before knowledge/entities/temporal/distillations but after account\_escrow/scope\_keys (needs DEK first); 7) migration 0022 idempotency vs 0020 structure,

<!-- lore:019fc834-0866-7bad-beee-c50047f5611a -->
* **Stricli contract test: pin app.routes + rebase-in-place fix pattern (entity flags gap)**: Stricli contract test pattern: pin \`app.routes\` identity (\`toBe(app.routes)\`) + verify each route from argv.ts STRICLI\_ROUTES appears in \`app.routes\` and is NOT in LEGACY\_ROUTES. Test file: \`packages/gateway/test/cli-\<name>-contract.test.ts\`. Coverage: 8-10 tests per command, ~5s total run. PR #1570 (import, 11 flags) shipped 10 tests; PR #1572 (entity, 8 subcommands) shipped 8 tests. Fix-in-place pattern: rebase onto origin/main (\`git merge main\` then rebase/force-push) rather than close+recreate PR. Recurring CI-not-triggering on force-push requires rebase onto main HEAD. F-1/F-2/F-3 HIGH pattern (PR #1572): contract tests that mock legacy handler to print unconditionally (e.g. \`"json-mode ok"\` without checking \`flags.json\`) pass despite missing flag forwarding — verify legacy received the flag, not just envelope shape. Every forwarded-flag test must assert the legacy mock received \`values.\<flag> === true\`. See \[\[019fcde9-9f46-72c5-b9f0-3c9eb8bae098]] for short-alias invariants.

<!-- lore:019f821b-dc02-78a3-ad92-ad6c671702b6 -->
* **Switched from create-react-app to Next**: Switched from create-react-app to Next.

<!-- lore:019f869b-5a17-7ae0-a933-1993f8584217 -->
* **Switched from plan to build**: switched from plan to build.

### Gotcha

<!-- lore:019fd14d-dce5-702a-9f99-270ad4d9fe60 -->
* **.lore.md merge conflict: take 'ours', never hand-merge**: Trap: hand-merging \`.lore.md\` conflicts during branch/rebase looks like the careful choice. Fix: \`.lore.md\` is auto-managed by the background lore process, which re-applies the session's memory updates on its next cycle — take 'ours' (origin/main's version) and move on; a hand-merge is wasted effort and risks divergence. Confirmed Aug 5 2026 while branching \`feat/streaming-recall-aware\` off origin/main: stash pop yielded 60 conflict markers; taking ours left zero, and the background process re-applied session memory. Convention: always include \`.lore.md\` in stashes and commits (\`git add .\` covers it).

<!-- lore:019f1fde-f198-76a6-a8e1-1ba44ab8cf1d -->
* **backfillTemporalEmbeddings idle-gate: session-activity window starves backfill under sustained load**: Trap: gating temporal backfill on session activity (any session active within \`idleTimeoutSeconds \* 1000\` ms) looks correct — avoids competing with live traffic. Fix: on a busy machine with 5 active sessions and 142 turns in ~18 min, the gate never opens; cursor never advances during 8+ min monitoring window. The session-activity window is too coarse — it parks backfill even when the embed worker is idle between requests. Fix (PR #1111): gate on \`recallEmbedsInFlight() > 0\` instead. This is selective — backfill progresses during recall's sub-second gaps between requests. Under active multi-session load post-deploy: park/resume cycles seconds apart, ~16 msgs/min throughput confirmed in logs. Inherent limitation: sustained back-to-back recall with no gaps still starves backfill, but this is accurate (shared single worker) rather than a false signal.

<!-- lore:019f1c21-e737-76f1-94ad-d8e2e6db16b2 -->
* **backfillTemporalEmbeddings: distilled messages retain embeddings — full corpus scope, not just undistilled tail**: Trap: assuming distilled messages have their embeddings NULLed (stale JSDoc on \`embedTemporalMessage\` claims this) looks correct — distillation compresses old messages. Fix: \`markDistilled()\` only sets \`distilled=1\`; it never clears the embedding column. \`deleteEmbeddings\` is never called during distillation. Consequence: the temporal rechunk backfill must walk the ENTIRE embeddable corpus (220K+ messages on a real install), not just the ~34K undistilled tail — 6.5× larger scope. The stale JSDoc comment was corrected in PR #1087. Always verify by checking \`markDistilled()\` in temporal.ts — it is a simple UPDATE with no embedding side-effects.

<!-- lore:019fd191-7ef4-778e-a0ec-b870f281013b -->
* **bedrock-runtime modelId regex + test boundaries**: Trap: the modelId regex character class \`\[a-zA-Z0-9.\_:-]+\` looks complete after adding colon for AWS Bedrock version suffixes (e.g. \`anthropic.claude-opus-4-5-20251101-v1:0\`). Fix: regex enforces character class only, NOT colon position — colons are whitelisted freely (trailing/leading/double-colon all pass). Real boundary tests: empty modelId (class requires 1+), whitespace (space not in class), URL-encoded \`%3A\` (\`%\` not in class), trailing slash after verb (\`$\` anchor rejects), \`/v1/model/foo:converse-stream\` parsed as a single modelId with no \`/verb\` suffix (rejected). PR #1575 added the colon + initial tests; PR #1578 widened regex (no extra chars), corrected JSDoc, added 7 boundary tests. Both merged.

<!-- lore:019ef10f-5e1c-70ee-97c2-cd2e0836bd28 -->
* **Bedrock: atob() mojibakes multi-byte UTF-8 — use Buffer.from().toString('utf8')**: Trap: \`atob(base64)\` looks correct for decoding Bedrock event-stream \`bytes\` field. Fix: \`atob()\` yields a Latin-1 binary string — multi-byte UTF-8 sequences (accents, CJK, emoji, smart quotes) are corrupted. Proven: \`"café ☕ 你好"\` → \`"cafÃ© â ä½ å¥½"\`. Use \`Buffer.from(bodyJson.bytes, 'base64').toString('utf8')\` — \`Buffer\` is a global in Node.js and Bun, no import needed. Tests missed it because all \`chunkFrame\` calls in \`bedrock-stream.test.ts\` used ASCII-only text (\`"hi"\`, \`"Hello"\`, \`"split"\`). Fixed in \`bedrock-stream.ts\` (PR #898).

<!-- lore:019f9974-25d9-71b8-96f5-297b7fa4aea9 -->
* **Biome noParameterProperties rejects constructor parameter properties**: Trap: writing \`constructor(private readonly handle: FileHandle) {}\` looks idiomatic TS. Fix: Biome's \`noParameterProperties\` rule fails CI; declare explicit class fields and assign in constructor body. Seen 4 Biome errors at once in \`bspatch.ts\` (FileOldReader/MemoryOldReader) — biome reports every parameter-property slot, not just one. Quick audit before committing new reader/writer classes in \`src/lib/\*\*/\*.ts\`.

<!-- lore:019f93d7-911e-7a5e-bbc3-d9ef143726e4 -->
* **CACHE\_ANCHOR\_MIN\_MESSAGES=12 silently disables anchor for tailIdx 12-19**: CACHE\_ANCHOR\_MIN\_MESSAGES=12 silently disables anchor for tailIdx 12-19: the anchor's midpoint quantizes to \`Math.floor(tailIdx/CACHE\_ANCHOR\_STEP)\*CACHE\_ANCHOR\_STEP\`; for tailIdx 12-19 the midpoint quantizes to 0, anchorIdx=0 gets guard-skipped, so the anchor doesn't actually fire until tailIdx=20 (2×CACHE\_ANCHOR\_STEP=10). The constant was honest-looking but wrong. Fixed (PR #1471): declare CACHE\_ANCHOR\_STEP first, then \`CACHE\_ANCHOR\_MIN\_MESSAGES = 2 \* CACHE\_ANCHOR\_STEP\` so the constant matches the real engagement point. Rejected alternative: clamp anchorIdx to ≥1 — rejected because that places a near-useless anchor 6 messages deep. Regression test added at exact boundary (convo(19) no anchor, convo(20) anchor fires); mutation-verified — flooring anchorIdx alone did NOT break the test because the outer guard \`tailIdx >= CACHE\_ANCHOR\_MIN\_MESSAGES\` gates the whole block first.

<!-- lore:019fd322-34fc-7544-9eb8-e3e7ff04afcc -->
* **cache-stability.e2e.test.ts PR #1577 order-dependent flakiness**: Trap: 2 cache-stability.e2e.test.ts tests fail in full suite (raw-window start boundary march, #cch-resign-gate quoted billing-header sentinel) — appears caused by current change. Fix: these are pre-existing order-dependent flakiness from PR #1577 (recorded-fixture replay issues). They pass in isolation (17:57: 16/16) and in combined run with new tests (18:09: 26/26). Always verify flaky failures by running the failing file alone before attributing to recent changes; full-suite order effects can mask or amplify older bugs unrelated to current work.

<!-- lore:019fcc59-eb6b-79ba-8240-3d257c826515 -->
* **checkpoint-score.mjs rejects partial runs without terminalOutcome**: Trap: enforcing full c1–c5 checkpoints for every run looks like a strong invariant. But with timeouts after real progress, this incorrectly throws "incomplete checkpoint run" and discards valid work. Fix: allow partial only when \`terminalOutcome === "agent-timeout"\` and track \`expectedCheckpoints\`; otherwise still reject incomplete runs. This keeps infra failures invalid while scoring behavioral timeouts.

<!-- lore:019fa8e0-d827-7f90-8207-d8fe69c4d01c -->
* **Cloudflare CNAME proxy must be OFF and gh api pages PUT booleans must use JSON body**: GitHub Pages custom-domain traps: (1) Cloudflare CNAME proxy must be OFF (grey cloud) — GitHub Pages can't issue TLS cert while Cloudflare proxies the CNAME (orange cloud); HTTPS times out. Set CNAME grey-cloud so DNS resolves directly to GitHub Pages IPs (185.199.108-111.153). (2) \`gh api repos/\<owner>/\<repo>/pages -X PUT -f https\_enforced=true\` serializes booleans as strings, rejected with HTTP 422 'Invalid property /https\_enforced: "true" is not of type boolean'. Pass booleans/numbers via JSON body instead: \`gh api ... -X PUT --input - <<< '{"cname":"...","https\_enforced":true,"public":true}'\`. (3) GitHub Pages 404 may be stale cache (etag 6a63df58-24a3), not missing deploy — verify Pages config + gh-pages tree first, then wait up to ~10 min (cache-control: max-age=600); don't re-trigger deploy. Confirmed (binpatch.p.byk.im, 2026-07-28).

<!-- lore:019fc525-a65b-7858-913c-7649db419427 -->
* **Compaction response contract: cancel and summary are mutually exclusive**: Trap: handling \`{ cancel: true, summary: "..." }\` as a valid combined response seems defensive (tests even define "cancel wins"). In reality, the gateway NEVER returns both fields simultaneously, so code paths assuming coexistence are dead or misleading. Fix: treat \`cancel\` and \`summary\` as mutually exclusive states; simplify handling and avoid dual-branch logic that masks real bugs in cancellation flows.

<!-- lore:019ecab6-f928-7927-a39e-d601fb850af6 -->
* **computeMaxTokens: EMA-based floor ignores thinking budget, causing silent agent loop exit**: \`computeMaxTokens\` EMA floor ignores thinking budget → silent agent loop exit. \`computeMaxTokens\` (pipeline.ts) floors \`max\_tokens\` at \`max(8192, 3×outputEMA)\`. Anthropic counts \`max\_tokens\` as combined thinking+visible budget. Model consumes entire 8192 on reasoning → \`finish:"length"\` → no text/tool part → agent loop exits silently. Fix: pass \`thinkingBudget\`; set \`baseFloor = thinkingBudget + THINKING\_OUTPUT\_HEADROOM (8192)\`, clamped by \`modelOutput\`. Gate: \`if (!isCC)\` — CC clients excluded; opencode does NOT send \`x-claude-code-session-id\` so fix applies. EMA not corrupted by thinking truncation (Anthropic includes thinking tokens in output\_tokens).

<!-- lore:019f1e66-a002-7d12-872b-04e7900bc8c1 -->
* **copyBlobsToVec0: stale-dimension blobs abort cutover — dimension guard required**: Trap: copyBlobsToVec0: stale-dimension blobs abort cutover — dimension guard required. Fix: add length(embedding) = dim\*4 guard to all 4 INSERT queries; thread dim: number param. Skip-in-copy ≡ remove+recreate: base embedding column is dropped after copy; next backfill regenerates from source text via missingEmbeddingSql.

<!-- lore:019efb79-a067-7947-b900-e8fd56134b27 -->
* **cost-bulk-queries.test.ts: \`not.toContain('SCAN t')\` is fragile — misses covering scans**: 🔴 Directive (June 24 2026): use \`not.toMatch(/SCAN t(?! USING)/)\` to reject heap scans in SQLite EQP tests — never bare \`not.toContain("SCAN t")\`. Rationale: SQLite reports index-only walks as \`SCAN t USING COVERING INDEX …\` — bare contains false-fails on valid covering plans. Negative lookahead passes covering scans and kills true heap scans. Also: deliberate decision NOT to add \`(created\_at, session\_id)\` index — SQLite planner always prefers session-ordered \`idx\_temporal\_session\` / \`idx\_distillation\_session\` scan for token-sum, distillation, and recent-session aggregates; \`(created\_at, session\_id)\` index is pure write amplification.

<!-- lore:019f858d-4008-7fcd-8554-015db3988776 -->
* **Delta-patch (TRDIFF10) format, wire contract, and generation pipeline**: TRDIFF10 format: 32B header (magic + controlLen/diffLen/newSize as offtin i64) then zstd(control)|zstd(diff)|zstd(extra). SWAR diff-add on Uint32Array uses carry-less formula ((a&0x7f7f7f7f)+(b&0x7f7f7f7f))^((a^b)&0x80808080) — never port to BigUint64Array without widening masks. addDiffChunk requires byteOffset%4 alignment; misaligned falls back to byte-loop. parsePatchHeader newSize capped at 2GiB (MAX\_OUTPUT\_SIZE). FileOldReader cache-bypass and negative-oldpos zero-fill paths load-bearing. attemptDeltaUpgrade always returns full DeltaResult; final binary SHA-256 verified.

<!-- lore:019fd14d-dc9e-77de-b086-628cc0fb4dc2 -->
* **Detached worktree: local commits duplicate merged work — branch fresh off origin/main (git cherry lies)**: Trap: on a detached-HEAD worktree with local-only commits ahead of main, rebasing that history in-place onto origin/main looks like the clean path — it preserves context. But such offline commits often duplicate work already merged upstream under different SHAs. This session: 4 local commits; d5867a4a was content-equivalent to merged 3fa064e3 (PR #1550, same subject). Trap: \`git cherry\` reports rebased duplicates as '+' (not applied) because rebasing changes patch-ids — cherry cannot detect them. Detect via same-subject search in origin/main log + content comparison. Fix: \`git stash\` the uncommitted changes, create the feature branch fresh off origin/main tip, \`git stash pop\`, resolve conflicts, commit only the new work. Rejected rebase-in-place: it would re-apply stale/duplicate work into the PR. Convention: detached HEADs, fresh branches off main's current SHA, abandon/delete the worktree when done.

<!-- lore:019fbed7-2ad8-7274-8f6b-99566a794caa -->
* **driver.mjs auth.json leak on non-finally paths**: Trap: copying full auth.json into run dir seems needed for providers. Fix: include only required providers and always delete auth.json even on failures (finally/exit hook). Without this, credentials persist in artifacts.

<!-- lore:019fbeb3-36c8-79c0-ae9b-90a2531b8724 -->
* **driver.mjs hidden facts leak via LORE\_EVAL\_FACTS env**: Trap: exposing hidden eval facts via env (LORE\_EVAL\_FACTS) while importing agent-written code looks like a simple wiring shortcut. Fix: this enables exfiltration — agent code can read env and cheat. Hidden data must only be accessible to verifier, never the agent runtime.

<!-- lore:019fc3d7-7b36-7dd3-b71d-1ae10c4eb4d9 -->
* **driver.mjs MiniMax env mapping uses MINIMAX\_API\_KEY (not ANTHROPIC\_API\_KEY)**: Trap: mapping provider "minimax" to ANTHROPIC\_API\_KEY looks consistent with other pseudo-providers, but MiniMax requires MINIMAX\_API\_KEY. This causes Pi runs to exit with steps=0, tokens=0 and stderr "No API key found for minimax", often triggering fail-fast and invalidating the entire matrix. Fix: map minimax → MINIMAX\_API\_KEY in driver.mjs (authEntryForProvider + piProviderKeyEnv). Always run a single-turn Pi preflight (valid:true, non-zero steps/tokens) before launching the full matrix; this catches auth misconfigurations early.

<!-- lore:019fc27c-8cb9-7fd5-b936-437f21011d48 -->
* **findFactLeaks must exclude generated artifacts (\_\_pycache\_\_, .pyc, .git)**: findFactLeaks false positives from \_\_pycache\_\_/\*.pyc: scanning all changed files for fact strings flags byte sequences in generated artifacts (\`\_\_pycache\_\_/\*.pyc\`) as 'CONTAMINATED'. Fix: exclude \`.git\`, \`\_\_pycache\_\_\`, and \`.pyc\` (and similar generated paths) from leak scans; compare only source/artifact paths that models can influence.

<!-- lore:019f8b54-63ac-72eb-884b-284f8becbc6b -->
* **fossilize@0.10.1 reads SEA assets via entry.file, ignores entry.src**: Trap: aliasing a target-specific staged file to a shared SEA key via \`{file: "ort-manifest.json", src: "ort-manifest-\<target>.json"}\` looks correct — \`src\` suggests it's the read source. Fix: fossilize@0.10.1's asset-map builder uses \`entry.file\` for BOTH the SEA key AND the \`join(dir, file)\` read path; \`entry.src\` is never referenced anywhere in fossilize. CORRECT FIX (PR #1449, commit 0c9942b4, MERGE-approved on adversarial re-review): persist each target's content as \`ort-manifest-\<target>.json\`, then \`copyFileSync\` it onto the shared \`ort-manifest.json\` path immediately before that target's serial fossilize() call. Serial safety confirmed: fossilize's internal \`ze()\` fully awaits the sea-config write+read before returning (even the extra host-platform code-cache pass reads within the same awaited call), so no cross-target race is possible since \`runFossilize\`'s for-loop awaits each target fully before the next copy. Verified: clean staging + full 4-platform rebuild + \`--check-embeddings\` → \`ok dim=768\` on all targets.

<!-- lore:019f19db-f73c-74d1-865d-116cf01c0100 -->
* **gateway re-compression: protocol-only predicate trusts same-protocol cross-host routes — origin check required**: Trap: comparing only \`effectiveProtocol !== ingressProtocol\` to detect auto-cross-routing looks correct — protocol translation is the obvious signal. Fix: same-protocol re-routes to different hosts (e.g. bare openai client → \`deepseek-chat\` → \`api.deepseek.com\` ≠ \`api.openai.com\`) are also distrusted. The merged PR #1059 predicate was protocol-only; PR #1064 adds \`|| effectiveOrigin !== ingressOrigin\`. Combined predicate: \`autoCrossRouted = !hasUpstreamUrlOverride && !hasProviderOverride && (effectiveProtocol !== ingressProtocol || effectiveOrigin !== ingressOrigin)\`. Mutation A (remove origin clause) → 4 tests RED: 3 unit + 1 MockAgent e2e showing zstd replayed to foreign host.

<!-- lore:019fbf47-a447-739d-aa62-e000805f67b9 -->
* **gen-blob.mjs missing cwd causes ENOENT**: Trap: spawning gen-blob without cwd writes outputs to caller dir, which looks fine locally. Fix: set \`cwd: harnessDir\`; driver expects blobs there, otherwise ENOENT at runtime.

<!-- lore:019fcec2-3bca-7648-af75-9640c0d9b47a -->
* **generate-env-docs.ts: Env: marker must be on canonical JSDoc**: Trap: env-doc generator falls back to \`readDescriptionByNameProjectWide\` when no description found at use site — surfaces the FIRST JSDoc block in any file mentioning the var name, even unrelated files (e.g. bedrock-runtime.ts top comment replacing config.ts empty placeholder for LORE\_BEDROCK\_REGION). Fix: document env vars at the GatewayConfig interface declaration with explicit \`Env: LORE\_XXX\` marker on the JSDoc (matches upstreamAnthropic/upstreamOpenai convention at config.ts:45/47). \`readDescriptionAbove\` finds it at the use site, beats the project-wide fallback. Single-line \`//\` comments are NOT recognized as JSDoc.

<!-- lore:019fccdb-49b0-72ef-9a48-e1be31d663ff -->
* **getGitRemote is environment-dependent: mock it in tests; expect.anything rejects undefined**: Git-remote-derived values (getGitRemote()) are environment-dependent: CI runners have a remote (e.g. BYK/loreai) but local Jujutsu worktrees often return null/undefined, so tests asserting X-Lore-Git-Remote headers or using expect.anything() for repo-derived values fail locally or in CI depending on the machine. Fix: stub/mock getGitRemote() (vi.spyOn(...).mockReturnValue(null)) or make assertions conditional; never rely on process.cwd() having a real git repo. Avoid expect.anything() when undefined is a valid outcome — assert explicitly or use tolerating matchers (expect.any(Array) only when required).

<!-- lore:019fab63-c7f5-7c68-94a6-1b6cef2cca70 -->
* **gh api pulls query: no 'merged' field — use state/mergedAt/mergeCommit**: GitHub REST/GraphQL PR queries via \`gh api\` or \`gh pr view --json\` do not have a \`merged\` boolean field — requesting it errors 'unknown JSON field "merged"'. Trap: \`merged\` sounds like the obvious field name for merge status. Fix: use \`state\` (MERGED/OPEN/CLOSED), \`mergedAt\`, \`mergeCommit\`, \`mergeable\`, \`mergeStateStatus\`, \`headRefOid\` instead. Confirmed while checking PR #1521 auto-merge status.

<!-- lore:019fc359-5d62-72fd-98b2-bd271a35128b -->
* **gh pr merge --squash blocked by 'base branch policy' behind main**: Trap: \`gh pr merge --squash\` on a PR that's 1+ commits behind main returns 'Pull request is not mergeable: the base branch policy prohibits the merge' even though \`mergeable: MERGEABLE\`. Looks like a permission/admin error. Fix: BYK/loreai ruleset "default" (id 13281210) enforces an 'update' rule requiring branch up-to-date-before-merge. Rebase or merge main into the PR branch, then retry. Alternative: \`gh pr merge --admin --squash\` bypasses the policy check but still requires squash (the ruleset forbids merge-commit/rebase methods entirely).

<!-- lore:019fb2af-5cf1-78b8-810b-77f52d81e77e -->
* **git add . drags node\_modules symlinks into commit**: Trap: \`git add .\` after editing a package will sweep up \`node\_modules\` and \`packages/website/node\_modules\` symlinks (pnpm uses symlinks into the workspace virtual store) as new tracked files. Fix: before committing, run \`git status\` and \`git rm --cached node\_modules packages/website/node\_modules\` if they appear; verify \`.gitignore\` excludes them at the workspace root (it should). Trap recurs because the symlinks look like ordinary directories in \`ls\`. Always inspect \`git status\` for symlink entries before the first commit on a fresh branch.

<!-- lore:019eea52-a1f7-757a-b0ae-6b749bbcda18 -->
* **gradient.ts compression stages: rawFrac applied to full usable — escalating layers inflate window past l0cap**: Trap: COMPRESSION\_STAGES rawFrac (layer-2=0.5, layer-3=0.55) applied to full \`usable\` (~800K on 1M model) → layer-2 raw budget ~400K, larger than the ~200K l0cap that triggered compression AND larger than layer-1 window; escalating GREW the window until unrecoverable 400. Fix (PR #872, merged 2026-06-21T14:18:47Z, squash \`4d7910aa\`): introduce \`stageBudgetCeiling = maxLayer0Tokens > 0 ? Math.min(usable, maxLayer0Tokens) : usable\`; \`stageBudgetUsable = Math.min(usable, stageBudgetCeiling)\`; apply to escalated stages (layers 2+) only. Layer-1 base \`rawBudget = usable\*0.4\` must NOT be clamped. \`isEscalated = stageLayer > 1\`. Also clamp escalated-stage distilled budget. Confirmed in production logs: post-deploy layer-2 ratio dropped from ~0.499 (unclamped usable×0.5) to ~0.287–0.321 (clamped cap×0.5). M1–M4 mutant kills documented in test suite.

<!-- lore:019ef35a-01ce-7c91-a77a-e81e9f1f537b -->
* **isVerifierCall: bare \`ci\` keyword matches \`npm ci\` (clean install) — false positive corrupts knowledge confidence**: Trap: \`(?:run\s+)?(?:test|...|ci|...)\` in \`VERIFIER\_LEADING\` looks correct — \`ci\` is a valid script name. Fix: \`npm ci\` (clean install, not a verifier) matches the optional-run group → false positive → confidence penalty. Fix (PR #927): move \`ci\` and other script keywords (\`verify\`, \`validate\`, \`e2e\`, \`coverage\`, \`spec\`) into a dedicated branch requiring explicit \`run\`/\`exec\`/\`dlx\` prefix: \`(?:npm|pnpm|yarn|bun)\s+(?:run|exec|dlx)\s+(?:test|ci|verify|...)\b\`. Bare \`npm ci\` no longer matches; \`pnpm run ci\` still does. \`ci\` retained for \`make\`/\`just\`/\`task\`/\`rake\` targets (always explicit). Confirmed via 51-case empirical test suite in PR #927 adversarial review.

<!-- lore:019fc773-c0c5-747b-8565-34b3bd556b29 -->
* **jj conflicted bookmark name fails in revset/argument position**: Trap: referring to a conflicted bookmark by name in \`jj log -r \<name>\` or \`jj bookmark forget \<name>\` looks correct — jj normally resolves bookmark names. Fix: when a bookmark is in conflicted state (divergent local vs git-side), its name itself cannot be parsed as a revset/argument. Errors: \`Failed to parse revset: Syntax error\` (log) or \`Failed to parse name pattern: Invalid string expression\` (forget, especially with \`@origin\`). Workaround: use the commit ID directly — \`jj bookmark set \<commit-id>\` or look up the rev via \`jj log -r all() | grep\`. Affects \`jj bookmark forget \<name>@origin\` because \`@\` is parsed as a revset special character in the name pattern.

<!-- lore:019fccb7-cbcc-728c-b0fd-000cb685cc99 -->
* **jj conflicts are commit-state, not operation failures**: Trap: expecting \`jj\` operations (merge/rebase) to fail on conflicts like Git looks correct — most VCS tools abort and require manual resolution before proceeding. Fix: Jujutsu records conflicts inside the commit itself and still succeeds; the working copy advances with conflict markers to resolve later. Any workflow that waits for a "failed" operation to detect conflicts will silently miss them. Always explicitly check for conflicts in the working copy/commit state after jj operations, because success does NOT imply conflict-free.

<!-- lore:019f9395-afce-73ae-b33a-bf28e2c0b5fb -->
* **jj rebase/eval worktree stale-maintip + conflict resolution traps**: Trap: switching jj working-copy (\`jj edit\`) to a main-based fix commit while eval scripts are running in the same worktree looks safe — jj tracks branches independently. Fix: \`blob-sm.md\` and \`eval/live/\` files are gitignored on the eval branch (\`feat/eval-provider-agnostic-harness\`) but absent on main; switching working-copy deletes them from disk mid-run, killing any orchestrator with a \`cd\` into that directory (observed as instant Volta 'Could not determine current directory' failures across 2 parallel runs). Fix: always \`jj edit feat/eval-provider-agnostic-harness\` before running evals, and when pushing/committing an off-main fix concurrently with running evals, use \`jj git push -b \<bookmark>\` without ever switching the working copy away from the eval branch. Confirmed pattern still holds for validation runs on this branch (blog/N=3 cost validation, PR #1211): amend + push via \`jj git push\` works fine mid-eval as long as working-copy stays pinned to the eval branch.

<!-- lore:019fceb9-a9dd-786b-9516-7e8f242f6b35 -->
* **jj revset range syntax: use depth, not \`3..\` (ancestors(@-, 3))**: Trap: \`jj log -r "ancestors(@-, 3..)"\` fails with "Expected integer" at the revset position. The \`..\` range syntax is not valid inside \`ancestors()\`. Fix: use \`ancestors(@-, 3)\` — \`ancestors()\` takes an integer depth, not a range. Same pattern applies to \`descendants(@, 2)\`. The \`3..\` range syntax is only valid at the top level (\`jj log -r "3.."\`).

<!-- lore:019fd7d9-bbc3-7d5e-b3bb-ade852448963 -->
* **legacy-bridge.ts console.error shim captures embedding-worker noise**: Trap: \`legacy-bridge.ts\` (\`packages/gateway/src/cli/lib/legacy-bridge.ts\`) shims \`console.log\`, \`console.error\`, AND \`process.stdout.write\` into a single \`captured\[]\` array — and Node.js aliases \`console.warn\` to \`console.error\`. Embedding worker uses worker\_threads (inherits parent stdio) so worker \`console.warn\` (e.g. lines 252, 283, 771 of \`embedding-worker.ts\`) flows into the parent's shimmed \`console.error\` → lands in \`captured\[]\` next to the JSON envelope. Result: \`extractTrailingJsonObject\` can't find trailing JSON, falls back to \`{ output: data }\`, downstream \`report.mjs\` JSON.parse fails. Fix: tee \`console.error\` calls to real \`process.stderr.write\` (not just \`captured\[]\`). Add regression test verifying worker noise does NOT enter captured stream. Related to \[\[019fca24-2461-7c80-aa28-1eaf5f9891ca]] stderr capture directive.

<!-- lore:019f98cf-e495-7847-9227-ce6117a8a97e -->
* **malformed\_chain poison: transient HTTP failures must not classify as chain corruption**: Trap: catching a 5xx/timeout/connection-reset on chain-manifest fetch and treating it as \`malformed\_chain\` looks like a safe failure-handling improvement. Fix: transient HTTP failures MUST NOT trigger a \`malformed\_chain\` poison alert — that classification is reserved for genuinely unparseable/malformed bodies. User directive (Jul 25 2026): this is a permanent invariant of the chain-fetch code path. 5xx/timeout/connection-reset must fall through to the existing retry/reclassification path (chain-unavailable on persistent failure), never poison the chain marker. Confirmed in binpatch PR #7 review (Jul 27 2026): ghcrSource.resolveNightlyChain catches fetch errors per-tag setting fetchFailed=true, reports \`fetchFailed ? "network" : "malformed\_chain"\` if any manifest missing. Poisoned tag's manifest fetches fine (HTTP 200) and is caught by validateNightlyChain — fetch failure classified as "network" so it never triggers a false \`malformed\_chain\` poison alert.

<!-- lore:019fbed7-2b1b-773a-ba56-09158231e9bb -->
* **Pi window mismatch: reserveTokens can force perpetual compaction**: Trap: computing reserveTokens from --pi-window without aligning the model’s actual contextWindow looks correct. If reserveTokens > contextWindow, threshold becomes negative and Pi compacts every turn. Fix: cap reserveTokens to (contextWindow - safety) or override Pi’s contextWindow to match --pi-window, because compaction logic uses the model registry value.

<!-- lore:019fcab7-89ce-7545-9e3a-639149a31b32 -->
* **pipeline.ts accumulator drops deltas if start filtered**: Trap: filtering SSE events (e.g. via isForwardableAnthropicContentEvent) before \`accumulator.processEvent\` looks correct—only forward client-relevant events. But if \`content\_block\_start\` is filtered while \`content\_block\_delta/stop\` pass, the accumulator never initializes the block and silently drops content. Observed in \`packages/gateway/src/pipeline.ts\` (~L4774–4782, ~5109–5111) and exacerbated by providers like MiniMax with slight protocol deviations. Fix: ALWAYS feed the full unfiltered stream into the accumulator first, then filter only for downstream forwarding. Add logging on filtered events to detect divergence early.

<!-- lore:019fc2dc-7e2e-7e38-9cd6-d06645b973fd -->
* **pnpm test times out at 180s — bundle step in pretest hook**: Trap: \`pnpm test\` times out at 180000ms in the witty-wolf worktree. Fix: the \`pretest\` hook runs \`pnpm --filter @loreai/gateway run bundle\` which builds the gateway (esbuild) and exceeds the shell timeout. Invoke \`vitest run\` directly to bypass pretest. Verified in this session: 140 test files / 2916 tests pass in ~125s. Worktree path: \`/home/byk/.local/share/opencode/worktree/51d1015fd1f616acf9a5ffaad9331ec6b304586b/witty-wolf\`. Volta: Node 24.16.0, pnpm 10.28.0, vitest 4.1.8.

<!-- lore:019fceb9-a9a6-7326-a029-ef3d104a5dfa -->
* **pnpm typecheck must run from monorepo root — gateway package has no tsconfig.json**: Trap: running \`tsc --noEmit -p packages/gateway/tsconfig.json\` from the gateway package directory fails with TS5058 ("The specified path does not exist") because the gateway package does NOT have its own tsconfig.json. Same likely true for other workspace packages. Fix: run \`pnpm typecheck\` from the monorepo root — it picks up all workspace tsconfigs. Result for this repo: 5/6 projects (packages/core with tsconfig.eval.json, packages/gateway, packages/opencode, packages/pi) report "Done". packages/website excluded (no tests).

<!-- lore:019fd7cc-e989-741b-96e6-bb646b4ba331 -->
* **PR #1590 lint unparseable = embedding worker noise, not judge failure**: Trap: 20/20 lint unparseable results look like a judge or extractor bug, prompting log.notice fixes upstream of the JSON read. Aug 6 2026 finding: PR #1590 lint run 31116866927 surfaced an unreadable-result notice with stderr empty; root cause was embedding worker stdout noise corrupting \`/tmp/lore-ic.json\` BEFORE judge calls. PR #1587's first failed run (31098700547) hit the same root cause. Fix: investigate the file \`/tmp/lore-ic.json\` content + stdout/stderr split FIRST, before adding \`log.notice\` paths in the JSON parsing layer. Bug H in PR #1581 was necessary but insufficient.

<!-- lore:019ef645-249a-7099-9b79-a286babbb764 -->
* **pruneOversized uses clamped confidence — CRDT hysteresis leaves raw value > 1.0 unzeroed**: Trap: \`pruneOversized\` reads \`confidence\` from \`knowledge\_current\` (materialized, clamped to \[0,1]) and applies \`-confidence\` delta — looks correct for zeroing. Fix: CRDT deltas accumulate on \`base\_confidence\`; raw value can exceed 1.0 while materialized stays clamped at 1.0. Applying \`-1.0\` delta leaves raw > 0 → entry not actually pruned, GC fails silently. Fix: fetch raw unclamped value (\`base\_confidence + SUM(deltas)\`) and apply negative delta equal to that raw sum. Confirmed in PR #1126 fix commit \`36173a7\`.

<!-- lore:019eeb34-c8ab-7321-98ae-f8cbd3953ad4 -->
* **resetPipelineState: fire-and-forget background promises write to new harness DB — test flake trap**: resetPipelineState: two-phase teardown required — stop idle scheduler and drain background work BEFORE swapping db() singleton. Correct order: stopIdleScheduler() + null it → await drainBackground(5000) → await boundedSettle(\[...inFlightBackground], 5000) → llmClient.shutdown → swap/clear state. Double stopIdleScheduler is safe (fast path skips top block; later block is no-op if already nulled). Trap A: stopping after llmClient.shutdown lets idle ticks schedule new runBackground during drain. Trap B: fire-and-forget background promises (urgent/incremental distillation, saveSessionTracking INSERT OR IGNORE) write to new harness DB — creates phantom rows in next harness (~1/3639 flake, issue #885). Fast path skips drain; production shutdown uses fast to avoid Ctrl+C hang. \`boundedSettle\` is a shared helper in \`background-limiter.ts\`.

<!-- lore:019fcee5-5118-7f3f-8202-506014021495 -->
* **resolveWorkerProtocol collapse → ChatGPT-backend worker 404s (unfixed)**: Trap: routing a worker call for providerID "openai" through resolveWorkerProtocol("openai", "openai-responses") looks right — openai-responses collapses to "openai" (Chat Completions) as the simpler/cheaper worker protocol. Fix: a ChatGPT-backend session has providerID "openai" but its URL https://chatgpt.com/backend-api serves ONLY /backend-api/codex/responses; buildOpenAIChatCompletionsUrl appends /v1/chat/completions → 404 {"detail":"Not Found"} (14+ lore-distill worker 404s, session 0H5aCIGPVFfpBtvA, openai/gpt-5.6-terra, Aug 4 2026). The collapse never inspects the host; only providerID "openai-codex" routes to Responses. Unfixed as of Aug 4 2026; affects only background distillation/curation workers (non-blocking). Fix point: llm-adapter.ts:710 resolveWorkerProtocol — detect chatgpt.com/backend-api host and emit "openai-codex-responses".

<!-- lore:019fbf47-a4a8-700c-ad27-f6d790ef2dc9 -->
* **retry token accounting double-counts usage**: Trap: accumulating tokens before retry \`continue\` seems natural. Fix: move accumulation after retry decision so failed attempts don’t inflate totals; otherwise metrics are biased on transient errors.

<!-- lore:019fbeb3-3764-73af-b7ce-078c333a3b8d -->
* **run-matrix reproducibility and validation pitfalls**: run-matrix reproducibility and validation pitfalls: (1) Untracked blobs: generating blobs only-if-absent and not hashing them makes runs non-reproducible and can reuse stale inputs — always hash or deterministically regenerate. (2) Weak model validation: checking only string shape allows duplicates, wrong models, or missing required runtimes — validate exact required identities and coverage (Pi/OpenCode). (3) Output shape: dry-run/manifest is a top-level array, not an object — treating it like a keyed object causes runtime errors. Enforce strict schema + deterministic inputs.

<!-- lore:019fc75f-40f8-7efe-a756-54888f852407 -->
* **run-matrix.mjs stdout JSON corrupted by bundle logs**: Trap: writing bundle/preflight logs to stdout during \`--dry-run\` looks harmless, but the Python consumer uses \`json.load(stdout)\`, so any extra text corrupts parsing (JSONDecodeError). Fix: route ALL repair/build logs to stderr and keep stdout strictly JSON. This preserves machine-readability while retaining diagnostics.

<!-- lore:019f1e66-a04d-722c-822f-0cb794b9ebe5 -->
* **runStartupBackfill 'scheduled' log absent when only temporal backfill is pending**: Trap: absence of 'embedding backfill scheduled: N knowledge + M distillations pending' log looks like \`runStartupBackfill\` didn't run. Fix: the scheduled log fires ONLY if \`pendingKnowledge + pendingDistillations > 0\` — temporal backfill pending count is NOT included. Similarly, ~2,410 vec-missing distillations that are archived gen-0 are excluded by \`archived=0\` predicate, so they don't trigger the log. The final coverage log prefix is \`'embedding startup:'\` NOT \`'coverage:'\`. \`STARTUP\_BACKFILL\_DELAY\_MS = 2\_000\`. To confirm temporal backfill is running, check for \`lore:temporal\_rechunk.cursor\` KV key or the \`'temporal re-chunk: N messages to scan'\` log line (added in PR #1104).

<!-- lore:019fc92b-2dec-75b4-a5ea-b0e14be12bcf -->
* **score.mjs execSync shell injection via LORE\_DB path**: Trap: using \`execSync\` with inline shell interpolation (e.g., embedding paths like $LORE\_DB in a command string) looks convenient but invokes a shell—paths with spaces, quotes, or metacharacters can break execution or enable injection, often silently producing wrong results. Fix: use \`execFileSync\` (or equivalent) with argv (\`execFileSync("python3", \["-c", code, dbPath])\`) to avoid the shell entirely. Always pass dynamic values as arguments, never interpolate into a command string. Add regression tests with paths containing shell metacharacters and a guard forbidding \`execSync\` for such usage.

<!-- lore:019fa52a-735f-792b-b18f-9eb5af8e4574 -->
* **Security and token handling patterns**: Security-sensitive patterns: (1) WAL/SHM files inherit umask (0644) — chmod to 0o600 or set umask before opening DB. (2) provider\_token cached in SQLite with TTL, cleared on logout/account switch. (3) OAuth scope read:org for org membership, repo for private repo collaborators — repo only used during acquireGitHubProviderToken. (4) acquireGitHubProviderToken cache-hit fails if loadPersistedSession returns null — re-authenticate. (5) Hybrid OAuth race: rl.close() in finally causes unhandled rejection — handle or restructure. (6) enrichFunctionsError leaks Edge Function response body — redact sensitive fields. (7) Always extract error body via error.context.text() for FunctionsHttpError. (8) Authorize GitHub team discovery with caller's fresh provider\_token, never stale session.

<!-- lore:019fd16c-4aba-7a4c-8cba-d3abd2a29199 -->
* **Seer merge gate: NEUTRAL verdict + mergeStateStatus UNSTABLE diagnosis**: Trap: treating Seer Code Review conclusion NEUTRAL as 'no actionable findings' looks right — it's the standard verdict. Fix: the CheckRun conclusion and sentry-bot's inline review are independent; NEUTRAL can accompany a HIGH-severity inline comment on the head commit (PR #1577, ref 15721277/0 — duplicate response.created/completed leak). Always read sentry-bot's inline comments on the head commit before merging; any new finding ⇒ STOP and address, then re-run the gate. Merge-gate procedure (\[\[019fcec6-8df6-7350-bc6d-0a3c6fef7dc2]]): never merge on a single observation — schedule a time-bounded follow-up (~6-25 min apart) and merge only when the full gate (CI tests/lint/checks, binary smokes, Seer clean) is green on the latest HEAD.

<!-- lore:019f61b7-d498-7883-8c36-b1f4ab9d3d2e -->
* **SessionCostSnapshot inputTokens and outputTokens are required**: Trap: forgetting to initialize inputTokens and outputTokens in SessionCostSnapshot looks correct. Fix: always initialize with 0. User made required inputTokens and outputTokens fields in SessionCostSnapshot type and added test verifying round-trip.

<!-- lore:019fd322-3211-7b9e-a38c-754af94f14fa -->
* **singleFlightStableLtm cache-first check + latent 4-path budget anti-pattern**: Trap: checking \`stableLtmInFlight.get(sessionID)\` first in \`singleFlightStableLtm\` (pipeline.ts:2351) looks correct — single-flight is the whole point. Fix: cache lookup MUST happen FIRST, otherwise serial callers after settle re-run compute because the previous caller's \`finally\` block already deleted the session from \`stableLtmInFlight\`. The original code: in-flight check → cache check meant settled cached results were never reused. Reordering to cache-first made the regression test pass and matches the cache lifecycle. Test: \`packages/gateway/test/single-flight-stable-ltm.test.ts\` (PR #1580 review finding #1). PR #1580 merged 2026-08-05 (4f5c40e6); cold-post-restart sessions (e.g. 0H5aCIGPVFfpBtvA) no longer thrash DB on concurrent retries.

<!-- lore:019fd16c-4b29-7312-b4b5-b647298661fb -->
* **SSE test: String.match null + response.completed double-count**: Two traps when asserting on drained SSE text in vitest (e.g. openai-responses-recall-aware-stream.test.ts): (1) \`out.match(/re/g)\` returns null when nothing matches, so \`expect(null).toHaveLength(0)\` throws 'Target cannot be null or undefined' — always use \`(out.match(...) ?? \[])\`. (2) Counting \`out.match(/response\\.completed/g)\` double-counts ONE rebuilt event: buildCompleted emits the literal in both the \`event: response.completed\` wire line and the \`"type": "response.completed"\` field in the data payload. Count wire events with \`/^event: response\\.completed$/gm\`; raw SSE data payloads without a type field don't double-count, so a passing passthrough test can mask the trap.

<!-- lore:019fad86-edcb-7cb3-9182-c9691ca82bc8 -->
* **Starlight docs \`./\` relative links resolve to sibling 404s**: Trap: user-authored relative paths like \`./apply/\` or \`./security/\` in Starlight docs look correct — they're written as siblings. Fix: from a section page like \`/architecture/\`, \`./apply/\` resolves to \`/architecture/apply/\` (404), not \`/apply/\`. Use root-relative \`/apply/\` or \`../apply/\` depending on depth. Mass-converted 49 broken \`./\` refs across 14 binpatch doc files in PR #31. Starlight ALSO rewrites \`./\` paths to root-relative in rendered HTML (verified: href="./architecture/" and href="/architecture/" both appear in dist). Beware: hiding links via component override (PR #29) doesn't fix user-visible 404s — fix the actual paths.

<!-- lore:019fd18a-1e49-7a7d-b150-d0995aae5602 -->
* **streamResponsesRecallAware dead-code continue — break-before-continue, keep outer terminal check**: Trap: Seer flags the follow-up terminal break as unreachable, and removing the outer terminal check (pipeline.ts ~L5693-5701) looks like the clean fix — the in-branch suppression makes it appear dead. Fix (PR #1577, LOW 15721918): reorder the else-if suppression branch so terminal events (response.completed/done/incomplete/failed) are checked FIRST and break in-branch; only non-terminal lifecycle (response.created/in\_progress) hit the suppression continue. KEEP the outer terminal check: it remains genuinely reachable for a terminal event carrying output\_index (rare but protocol-possible) and provides the \[DONE]/parse-failure safety break. Non-message non-JSON fallback events (L5635-5641) are forwarded verbatim without this filtering — accepted low-severity leak.

<!-- lore:019eff67-7977-7ec5-bfc2-e55be71237ee -->
* **Symbol grep probe gotchas: toplevel+env scrubbing, exit-status trap**: Trap: running \`git grep\` from \`this.root\` (project root) looks correct — it's where the code lives. Fix: in a monorepo, \`this.root\` may be a subdirectory; grep from a subdir misses symbols defined elsewhere. Always \`cd $(git rev-parse --show-toplevel)\` first. Second trap: \`GIT\_DIR\`/\`GIT\_WORK\_TREE\` env vars inherited from the agent's shell override git's working-tree detection, causing grep to search the wrong tree. \`scrubbedGitEnv()\` strips both vars before every git subprocess. Both traps look safe because grep succeeds (exit 0) — it just silently searches the wrong scope.

<!-- lore:019fc344-c715-7963-ab66-48ccf80dbf4e -->
* **syncEntityRefs chunked INSERT (FK fallback + count overcount fix)**: Replaced per-row INSERT loop in syncEntityRefs (packages/core/src/entities.ts) with chunked multi-row INSERT. Trap A: \`INSERT OR IGNORE INTO … VALUES (…),(…),…\` looks like per-row conflict handling analogous to UNIQUE/NOT NULL/CHECK (which DO suppress per-row). Fix: SQLite's \`INSERT OR IGNORE\` does NOT suppress FOREIGN KEY violations per-row — one FK-violating row aborts the WHOLE multi-row statement. Trap B: unbounded batch exceeds SQLITE\_MAX\_VARIABLE\_NUMBER default 999 — \`knowledge\_entity\_refs\` uses 2 params/row, so chunk at 450 (mirrors batchLoadAliases/b at 900-param ceiling) to stay under the 999 limit. Fix: CHUNK=450, split linkedEntityIds, wrap each chunk in FK try/catch, fall back to per-row inserts on FK violation. Trap C (Seer 15653378): post-loop \`count = ids.length\` + \`affected.add(entityId)\` overcounts in fallback path — old per-row loop used \`count++\` inside try (only on success). Fix: track \`count\` and \`affected\` separately inside fallback loop; chunked path keeps length-based accounting. Schema: \`UNIQUE(knowledge\_id, entity\_id)\` with \`ON DELETE CASCADE\` to both parents. Fixed in PR #1553 (squash-merged commit 871e6c94, Aug 2 2026).

<!-- lore:019ef389-a52f-7427-9099-f8dea38e8caf -->
* **Testing gotchas: vitest alias mock mismatch + Date.now() flakiness**: Trap: \`vi.mock('@loreai/core', ...)\` to spy on exports like \`recordCacheUsage\` looks correct — it's the package specifier pipeline uses. Fix: vitest config aliases \`@loreai/core\` → \`packages/core/src\` at \`resolve.alias\` (top-level, NOT \`test.resolve.alias\` — wrong placement silently resolves to stale dist). The mock targets the specifier but pipeline resolves to the source path, creating a module identity mismatch — spy is never invoked (call count stays 0). No clean workaround for aliased barrel mocks; use e2e harness tests or file as tracked follow-up instead.

<!-- lore:019fc2da-1c80-7adf-9298-3430f2cbfef2 -->
* **Tool-loop final round must omit \`tools\`, not send empty array**: Tool-loop final round must omit \`tools\` (not empty array): sending \`tools: \[]\` in the final round looks equivalent to 'no tools', but some providers reject empty arrays or treat them differently, breaking compatibility. Fix: omit the \`tools\` field entirely in the final round; contract tests assert it must be \`undefined\`.

<!-- lore:019fbf47-a478-7710-b29f-449872359d81 -->
* **trace-extract includes \*-curate.json as turns**: Trap: treating all session JSON files as turns seems correct. Fix: exclude \`\*-curate.json\`; they default ti=0 and sort before real turns, corrupting behavioral traces and metrics.

<!-- lore:019fceb9-a8ee-744e-8abd-23b1a0a7eab7 -->
* **undici MockAgent traps: sync replies, body buffering, Host/Uint8Array, Bun external**: undici MockAgent replies are synchronous + Bun bundle external trap: undici must stay external in the Bun ESM bundle (undici@7 hangs on streaming reads under Bun; it is lazily imported only on the Node path in fetch.ts, Bun uses native fetch) — enforce via type-only import. MockAgent reply callbacks are synchronous and cannot drain a Web ReadableStream; proxies forwarding streaming bodies via \`duplex:"half"\` get "fetch failed" 500s — buffer via \`await req.arrayBuffer()\` before forwarding. Related traps: Host header is auto-generated from URL, never in \`opts.headers\`; \`opts.body\` normalizes to Uint8Array (not Buffer) — decode with \`Buffer.from(opts.body).toString("utf8")\`; \`upstreamFetch\` passes an explicit dispatcher, so \`setGlobalDispatcher\` can't intercept — use \`setUpstreamDispatcherForTest(mock)\` (fetch.ts:63). Pattern: packages/gateway/test/recompress-scope.test.ts.

<!-- lore:019f93cf-fd90-7584-b451-a3297eead735 -->
* **v76 tool\_calls.input\_paths\_json: BOTH-columns crash + visitor body loss**: v76 tool\_calls.input\_paths\_json issues: drop orphan input\_path when both columns present (or restore try/catch), fix visitor to handle CompoundLists (avoid body loss), and correct discriminator to 'SingleQuoted'.

<!-- lore:019f1cc8-b6eb-7d3f-9be2-2cf81848d519 -->
* **Vacuous seam test: single-message transform input never reaches loadDistillationsCached (Layer 0 early return)**: Trap: writing an end-to-end seam test for \`prewarmDistillationSnapshot\` with a single small message looks correct — it exercises the prewarm→transform path. Fix: \`transformInner\` early-returns at Layer 0 when all messages fit, so \`loadDistillationsCached\` is never called. Single-message inputs with empty \`parts\` (0 tokens) always take the Layer 0 path. To force the compaction path where \`loadDistillationsCached\` is actually called, build a large input (~60K chars across ~20 user+assistant messages) that exceeds the Layer 0 ceiling. Verify non-vacuousness by mutating \`loadDistillationsCached\` to always force cache-miss — seam test must fail.

<!-- lore:019f43a4-4ea3-752b-bd99-f2ef59d93db6 -->
* **vectorSearchDistillations is not project-scoped — hydration query must filter by project\_id**: Trap: calling \`vectorSearchDistillations(queryVec, limit)\` and hydrating results looks correct — it mirrors \`vectorSearchTemporal\`. Fix: \`vectorSearchDistillations\` takes no project param; results include distillations from ALL projects. \`vectorSearchTemporal(queryVec, pid, limit)\` is correctly project-scoped. Any code path that calls \`vectorSearchDistillations\` must filter the hydration query with \`WHERE archived = 0 AND project\_id = ?\`, over-fetching (\`limit \* 4\`) and capping at \`limit\` after filtering to avoid multi-project starvation. Seer caught this as a CRITICAL cross-project data leak in PR #1228 review. Follow-up: add a natively project-scoped distillation vector search function.

<!-- lore:019fce4e-0500-770e-9c03-d9bbadc1e471 -->
* **worker-health mock resets in beforeEach — recordWorkerFailure pollution (PR #1574)**: Trap: \`recordWorkerFailure\`/\`markWorkerPaused\` mocks are shared across tests in \`packages/gateway/test/llm-adapter.test.ts\`. A failing test that calls \`recordWorkerFailure(sessionID, workerID, reason)\` pollutes the next test's \`expect(recordWorkerFailure).not.toHaveBeenCalled()\` assertion. Symptom: tests pass individually but fail in sequence — observed Aug 4 2026: "a model needing BOTH a beta strip AND a temperature strip recovers" failure bled into "retries once without the thinking param" test (same \`sess-both\` sessionID). Fix: every describe block that mocks \`recordWorkerFailure\`/\`markWorkerPaused\` MUST \`vi.mocked(recordWorkerFailure).mockClear()\` + \`vi.mocked(markWorkerPaused).mockClear()\` in \`beforeEach\`. PR #1574 added these clears to BOTH new \`worker beta stripping (#1571)\` block AND existing \`worker thinking disabled for Anthropic Claude models\` block (which was missing them). Codified convention: worker-health uses \`\_resetForTest()\`, worker-model uses \`resetWorkerModelState()\`, background tests use \`resetBackgroundLimiter()\`. Same pattern in \`sync-security.integration.test.ts\`.

### Pattern

<!-- lore:019fc344-c695-733c-bc1b-a2346e22c4c2 -->
* **Adversarial PR review: TDD + check Seer inline comments before merge**: PR review protocol (from quality/REVIEW.md + 39 sessions): (1) read diff, (2) read full changed function with surrounding context, (3) read new test file in full, (4) verify new test CORRECTLY FAILS on pre-fix code (stash or \`git checkout main -- \<file>\` into tmp/worktree), (5) run new tests + adjacent suite, (6) verify working tree unmodified end (hash). Output: per-point verdict (PASS/FAIL/CONCERN/MUST-FIX) with file:line evidence, final MERGE/DO-NOT-MERGE. Never write "LGTM" without structure. Highest-risk surface: skip/early-return branches left untested by new test (verified via probe in PR #1552 — empty-skip path needs canary test that guard is exercised; canary passes on BOTH broken and fixed code — confirms guard fires, not exact count regression). Two-pass mandatory for non-trivial: (1) adversarial/correctness + (2) security/pentest as separate subagents. Aug 2 2026: \*\*Seer NEUTRAL status does NOT mean no inline review comments\*\* — PR #1552 had 2 inline Seer comments (LOW chunking fallback, MEDIUM SQLite param limit) that were missed pre-merge. Always fetch Seer/Codecov/PR Preview inline comments before executing merge command, regardless of overall status check verdict.

<!-- lore:019fd18a-1e81-7291-8696-298d3fbfaf53 -->
* **Adversarial review subagent empty task\_result — fall back to manual review**: Steps: 1. Launch a code-type background subagent on the full PR diff (strict read-only) — per merge-gate convention. 2. If task\_result comes back empty, relaunch once with a fresh task — 'completed' with an empty result is a tool failure, not a clean verdict. 3. If the second run is also empty, abandon subagent relaunches and perform the adversarial review manually in the foreground with full change context (observed twice, Aug 5 2026, PR #1577). Gotchas: - 'completed' + empty task\_result looks like a clean pass; merging on it skips the gate's adversarial step entirely. - Don't keep cycling subagents — two empties is the signal to go manual. Verify: - \[ ] PR merge gate includes an adversarial review outcome (subagent result or documented manual review). - \[ ] Reviewed diff covers behavior changes, not just exports.

<!-- lore:019f1e66-a06c-7c58-b5ec-6d7cb21eddc3 -->
* **backfillTemporalEmbeddings: per-row checkpoint + backlog/progress logging (PR #1104)**: PR #1104 (\`fix/temporal-rechunk-checkpoint\`) changes two behaviors in \`backfillTemporalEmbeddings\`: 1. \*\*Per-row checkpoint\*\*: \`setKV(TEMPORAL\_RECHUNK\_CURSOR\_KEY, retryFrom ?? cursor)\` moved INSIDE the row loop (was per-page). Crash mid-pass now resumes from last completed row, not start of last page. \`retryFrom ?? cursor\` uses nullish (not \`||\`) — when first row fails, \`retryFrom = ''\` and \`'' ?? cursor\` → \`''\`, correctly resuming at \`id > ''\`. 2. \*\*Backlog + heartbeat logging\*\*: upfront \`SELECT COUNT(\*)\` logs \`'temporal re-chunk: N messages to scan (resuming)'\`; \`PROGRESS\_INTERVAL\_MS = 30\_000\` replaces count-based \`PROGRESS\_INTERVAL = 1000\`; heartbeat shows \`processed re-chunked, scanned scanned…\`. Mutations: per-row checkpoint test and backlog-log test both killed independently. Merged \`25a04e22\` at 2026-07-01T17:30:31Z.

<!-- lore:019f6047-5672-73ac-a80b-521adbebedce -->
* **Codex-worker cache accounting**: When fixing codex-worker cache accounting, always verify test suite non-vacuousness and apply fixes to parseResponsesWorkerResponse.

<!-- lore:019fd191-7f32-7f86-bc12-de0cc20030bd -->
* **Follow-up PR: jj new main + reapply only deltas**: When original PR has been squash-merged to main but follow-up fixes are needed: do NOT extend the original PR's commit. Procedure: (1) \`jj new main\` creates an empty change on top of main's squash-merge SHA; (2) re-apply ONLY the follow-up deltas (e.g. regex widening, JSDoc fix, new tests, regenerated docs); (3) \`jj describe\` + commit + push; (4) open follow-up PR. Rejected alternatives: \`jj split\` the original @ into base + follow-up (more steps, requires verifying split correctness); resetting @ to squash-merged SHA and reapplying (same effect, more steps).

<!-- lore:019f1cc8-b65e-7a6e-97ae-f48d1a7d542b -->
* **forProjectOffloaded / entitiesForSessionOffloaded: timeout must fall back to in-process scan, never return \[]**: All offloaded variants of frozen-baseline builders (\`ltm.forProjectOffloaded\`, \`entities.forProjectOffloaded\`, \`entities.entitiesForSessionOffloaded\`) must fall back to the synchronous in-process scan on \`READ\_JOB\_TIMED\_OUT\` — never return \`\[]\`. Trap: returning \`\[]\` on timeout looks safe (same as \`forSession\` degrade). Fix: unlike \`forSession\` (reruns every turn), these results are frozen into \`stableLtmCache\` + \`saveSessionTracking\` for the entire session lifetime. A spurious empty baseline permanently breaks the knowledge catalog for that session. \`forSession\` can safely return \`\[]\` because it reruns every turn. Mutation test: force timeout branch to return \`\[]\` → timeout test must fail.

<!-- lore:019fcee5-51f5-7333-a7c9-0f81712567ea -->
* **llm-adapter worker retry: embedded 2xx error envelopes + deferred capability learning**: Worker request handling (llm-adapter.ts ~2090): (1) a 2xx whose body is a provider error envelope (e.g. OpenRouter surfaces an upstream timeout as 200 \`{error:{code:504}}\`) is NOT a usable completion — \`extractBodyErrorCode\` routes TRANSIENT\_CODES into the same retry/backoff ladder as HTTP-level transients (issue #899); embedded 429 trips the circuit breaker once (background work to that provider pauses). (2) Deferred learning: \`markTemperatureUnsupported\`/\`markThinkingUnsupported\` fire ONLY on a 2xx AFTER the offending param was stripped — a regex false-positive on an unrelated 400 can never permanently mislabel a capable model. (3) Some providers (ChatGPT/Copilot/Codex, DeepSeek) return SSE even when \`stream:false\` was sent, sometimes without \`text/event-stream\` content-type — \`looksLikeSSE\` sniffs; run through the protocol accumulator (merges every chunk), never JSON.parse.

<!-- lore:019eec48-01c5-78bd-b7f9-6cd8b7ddfada -->
* **Lore gateway deploy procedure on production host**: 🔴 Deploy procedure (Burak Yigit Kaya directive): \`git stash save && git pull && git stash pop && pnpm install && pnpm build && pnpm --filter @loreai/gateway run bundle && sudo service opencode restart && journalctl -u opencode -f\`. Plugin file at \`~/.config/opencode/plugins/lore.ts\` contains only \`export { LorePlugin as default } from "opencode-lore";\`. opencode-lore resolves from \`/home/byk/.config/opencode/node\_modules/opencode-lore\` (active built repo) — NOT the cached version at \`~/.cache/opencode/packages/opencode-lore@\*/\`.

<!-- lore:019f14f5-5edb-7bd1-bc31-9b10062ce1a3 -->
* **Pi real-runtime e2e: console spy must be installed BEFORE resourceLoader.reload()**: Trap: installing \`console.\*\` spies after \`resourceLoader.reload()\` looks correct — the test body runs after setup. Fix: Pi's extension factory executes during \`reload()\`, so any \`console.\*\` calls in the factory are missed by late-installed spies. Additionally, Vitest intercepts \`console.\*\` away from \`process.stdout.write\`, so \`process.stdout/stderr.write\` spies also miss console output. Both traps look safe because the test passes — it just silently misses the mutation. Fix: install direct \`console.\*\` method spies BEFORE \`resourceLoader.reload()\` so the entire reload + \`createAgentSession\` + prompt window is captured. Mutation A (inject \`console.info("pi: ...")\` at line 99 of index.ts) confirmed this kills the markers test when spies are correctly placed.

<!-- lore:019fc3e1-1388-76c3-a403-fecce5604154 -->
* **PR gate before eval matrix launch (CI test + Seer clean)**: Only launch full eval matrices after PR head passes all REQUIRED CI checks AND has no unresolved Seer inline comments. In this flow: even with Seer=NEUTRAL and all other checks green, the run is BLOCKED while the required \`test\` job is IN\_PROGRESS. Rationale: launching early looks safe (most signals are green) but risks benchmarking on a broken head or rerunning after CI failure, wasting 180-cell runs and breaking comparability. Gate strictly on \`test=SUCCESS\` + zero inline findings, then start from a fresh durable root.

<!-- lore:019f147e-7b0d-7ed0-aef5-bb2b8c0bc065 -->
* **standard.site publish script: safety invariants for orphan cleanup**: Three invariants in \`publish-standard-site.mjs\` orphan cleanup (PR #1043, also ported to byk.github.io PR #58): 1. \*\*Upserts-first ordering\*\*: all \`putRecord\` calls complete before any \`deleteRecord\` — a mid-run failure never strands the blog without live records. 2. \*\*Empty-manifest guard\*\*: if \`manifest.documents.length === 0\`, skip orphan cleanup entirely with \`console.warn\` — a broken/empty build cannot wipe the whole collection. 3. \*\*Pagination termination\*\*: \`listAllRecords\` uses dual-condition stop — (a) stop when batch length is 0 even if PDS returns a cursor (PDS returns trailing cursor on last page — confirmed live), (b) stop when cursor is null/undefined. 4. \*\*DID cross-check\*\*: if \`session.did !== recordDid\`, script refuses to publish. 5. \*\*\`validate: false\`\*\* on \`putRecord\` — PDS doesn't host \`site.standard.\*\` lexicons. 6. \*\*Blob uploads\*\*: blobs are content-addressed (CID) → idempotent re-runs; best-effort; size cap \`MAX\_BLOB\_BYTES = 1\_000\_000\`; Astro-optimized webp variants always under 1MB.

<!-- lore:019f9e34-ffd5-7daa-bdf6-5a9531bf91e1 -->
* **Verification and testing practices**: Before declaring a fix complete: (1) run mutation tests and verify mutant killed (rebuild workspace package first); (2) verify invariants against test assertions with file:line citations; (3) confirm test coverage actually exercises the code path under review; (4) capture raw SSE wire output and verify envelope structure; (5) independently verify off-by-one and protocol-path assumptions; (6) run repo-wide symbol search for schema changes; (7) rename tests to encode invariants as literal phrases (never/always). Use throwaway worktrees for isolation.

<!-- lore:019fcb4b-758a-77f2-bfe3-2160557356f0 -->
* **Zero memory\_\* calls classification (dead backend vs behavior)**: When a run has zero \`memory\_\*\` tool calls, classify before scoring. Trap: scoring 0% by default looks correct. Fix: split cases—(A) dead backend (MCP not up / no tools exposed) → mark INVALID and exclude; (B) healthy backend but model never uses it → VALID, score 0%. This avoids conflating infra failure with model behavior.

### Preference

<!-- lore:019fd743-0338-7bdc-a7e5-977978b55cbe -->
* **Always capture user invariants and directives as durable facts with red-triangle markers and rationale**: Across sessions, the user repeatedly states load-bearing invariants (e.g., 'a failure here must never block a PR', 'tooling failures must NEVER fail the build', 'a non-2xx warmup must be NEUTRAL', 'models.dev outage must never silently re-break workers', 'budget is a floor, never a charge'). Each is captured with a 🔴 marker including the verbatim quote, the file/location it applies to, and the empirical rationale (PR numbers, time budgets, control corpus results). When reviewing or modifying code that touches these invariants, an assistant should: (1) record the invariant verbatim, (2) cite the source location, (3) preserve the rationale, and (4) never silently change behavior that violates the invariant. Treat these as durable constraints, not suggestions.

<!-- lore:019fd6b3-ce32-7fa5-a12c-b1c9735b5f5b -->
* **Always check inline review comments (including resolved Seer findings) before any merge action**: Before merging any PR, the user requires re-fetching and addressing ALL inline review comments — including those from automated reviewers (e.g. Seer/Sentry bots) even if marked resolved. Never rely solely on CI status. The user explicitly enforces lore \[019fb858] and related directives \[019fb95e, 019f6022]: findings must be surfaced and addressed (always 0 outstanding) before merge. When a missed finding is discovered post-merge, the user immediately creates a fast-follow branch to restore the lost safeguard (e.g. PR #1586 restoring the per-provider guard weakened in PR #1582). The user also requires probing recalled notes for precise technical details before acting, pushing back on reviewer directives that extrapolate beyond actual intent.

<!-- lore:019fd810-d84b-7f55-9200-90077eb2705b -->
* **Always cite exact file:line and quoted code when referencing implementation details**: Across all 12 sessions, the user references implementation by precise file path and line number (e.g., worker-model.ts:1359-1366, config.ts:578-589), quotes the actual code or comment text, and anchors behavioral rules to that exact location. When making claims about behavior, they cite the source: 'worker-model.ts:1369: "Determine if the session model is expensive enough to warrant a cheaper worker from the SAME provider. Never cross-provider."' or 'config.ts:580 — provider openai-codex default url = https://chatgpt.com/backend-api, protocol openai-responses.' Follow this pattern: when discussing code, always cite file:line, quote the relevant comment or code, and treat those citations as the authoritative source. Never paraphrase a rule without grounding it to a specific location.

<!-- lore:019fd465-7e58-7682-90f5-2731c825acb3 -->
* **Always cite line numbers and verbatim comments when documenting codebase invariants**: Across all 13 observations, the user consistently anchors every finding, directive, or assertion to a specific file path plus line range (e.g. \`llm-adapter.ts:691-696\`, \`llm-adapter.ts:1453-1525\`, \`translate/openai-responses.ts:374-393\`) and often quotes the exact source comment verbatim as the rationale. New directives are framed as comments pulled from code (e.g. "never from the tiny raw one", "no foreign-key-to-anthropic.com hazard", "Copilot always dispatches Bearer."). When stating rules or invariants, prefer the wording already in the codebase over paraphrasing. When proposing edits, name the file:line range where the change lands. This grounds every claim in checkable evidence rather than abstract description.

<!-- lore:019fd14d-dc60-7648-a6f0-528e54e79fe5 -->
* **Always commit work, then open a PR; never edit directly on main**: Directive (Burak Yigit Kaya, Aug 5 2026, triple emphatic): 'Always always always commit your work. Then create a PR if applicable.' Reinforced Aug 6 2026: NEVER apply fix edits directly to \`main\` working tree — always branch off \`origin/main\` (e.g. \`feat/\<slug>\`), re-apply staged changes, then commit + push + open PR. Working on \`main\` and stashing/popping to switch branches is a workflow deviation; user explicitly switched to a feature branch after edits landed on \`main\`. Standing workflow mandate: after committing, open a PR whenever appropriate. Before committing: format → lint → typecheck → tests on the current base, stage with \`git add .\`, and check for a stray opencode.json in repo root.

<!-- lore:019fd49d-d00e-7038-91d8-fdaa2518a7cf -->
* **Always conduct read-only adversarial PR reviews with mutation probes in /tmp copies**: When reviewing PRs, the user insists on a strictly READ-ONLY adversarial review with these invariants: (1) worktree under /tmp/opencode, never modify the main tree, verify file hashes via sha256 if needed; (2) mutation testing done by copying source files to /tmp and reverting via \`git checkout origin/main -- \<file>\`, not by editing in place; (3) review structured into numbered scrutiny buckets covering TDD failing-first verification, defensive-guard tightness, prefix matching, cache stability, cross-provider safety, header/auth, etc.; (4) each bucket produces PASS/CONCERN findings with concrete mutation probes (e.g., dropping a branch, renaming a field, broadening a prefix) demonstrating what would/could regress; (5) final verdict is one of SHIP / SHIP-WITH-FOLLOWUPS / REQUEST-CHANGES. Assistants should treat this protocol as mandatory for any review request in this repo.

<!-- lore:019fd7d9-bc13-7306-8de9-9b4e88524399 -->
* **Always emit logs on stderr; keep stdout clean for data**: Directive (Burak Yigit Kaya, Aug 6 2026): all log/diagnostic messages MUST go to stderr; stdout stays clean for structured data (JSON envelopes, etc.). Root cause: embedding worker's \`console.warn\` (aliased to \`console.error\` in Node.js) was being captured into the same stdout stream as the JSON result, corrupting output. Coverage stats must always log to stderr so the problem is visible (\`packages/core/src\` lines 2733/2868). Reinforces the stderr capture directive \[\[019fca24-2461-7c80-aa28-1eaf5f9891ca]] — the new layer is that the JSON envelope itself must never share the stream with diagnostics.

<!-- lore:019fd506-5fe1-7fd4-8170-819595046e88 -->
* **Always gate PR merges on 0 IN\_PROGRESS + 0 FAILURES, then squash-merge with --auto (fall back to --admin)**: Across all sessions, the user repeatedly enforces a strict merge gate before any PR is merged: recheck CI status, parse statusCheckRollup for IN\_PROGRESS and FAILURES, and only proceed to squash-merge when BOTH are zero. If checks are still running, schedule a 600s (or 300s) follow-up recheck and do NOT investigate yet. When ready, merge with \`gh pr merge --auto --squash\`. If --auto fails with 'base branch policy prohibits the merge', retry with --admin to bypass the ruleset. Always verify the merge commit, confirm origin/main advanced, then clean up by deleting the feature branch and the worktree. Inline PR review comments must be checked before merging.

<!-- lore:019fd7b0-fbf1-7e9a-b224-481bcadb5b2f -->
* **Always investigate test failures by checking environmental artifacts (stray opencode.json, missing .git) before treating them as regressions**: When test suites fail, the user consistently probes whether failures stem from worktree/environmental issues rather than code regressions before assuming the code is broken. Repeatedly observed patterns: (1) stray \`opencode.json\` files appear at worktree root and must be moved to \`/tmp\` and verified absent via \`ls\` before re-running; (2) jj worktrees lacking \`.git\` remotes cause header-based test failures that pass in real checkouts and CI — these are documented as known environmental limitations, not regressions; (3) the assistant is expected to read PTY output with pattern searches to identify root cause rather than blindly retrying. Before declaring a regression, always: ls for stray config files, inspect failure messages for environment-specific assertions (git remotes, file paths under ~/.local), and compare pass counts against the prior baseline.

<!-- lore:019fd474-554f-71e3-b316-52c98566d0f5 -->
* **Always isolate CI failures into separate fast-follow PRs**: When CI fails on a PR due to an issue unrelated to the PR's own changes, the user opens a separate fast-follow PR from a fresh worktree off origin/main rather than amending the failing fix into the original PR. Following lore \[019fab42] ('avoid smuggling CI changes into the fix'), the user: (1) verifies the failure is pre-existing (not introduced by the current diff), (2) creates a new branch off origin/main, (3) applies only the minimum fix, (4) runs the full pre-merge gate, (5) pushes as a distinct PR. The original PR remains focused on its own change. Use \`oxfmt --write \<file>\` to fix format drift; do not attempt manual sed/regex edits that can introduce new errors.

<!-- lore:019fcceb-4444-7a31-a7ac-7209c14fb18d -->
* **Always isolate tests from the real environment and avoid side effects**: Always isolate tests from the real environment and avoid side effects: tests must run in fully sandboxed environments that never touch real machine state or repository. Redirect HOME, XDG\_DATA\_HOME, XDG\_CONFIG\_HOME to temp dirs; avoid writes to process.cwd(); prevent stray config files (like opencode.json) from polluting the worktree. Mutations only in throwaway dirs or worktrees, with precise cleanup that never removes files the test didn't create. Ensure strict isolation, deterministic setup/teardown, and zero impact on real configs or tracked files. Includes removing shared/global config artifacts that could interfere with tests or CI, verifying isolation between test cases, and preventing hidden state from affecting results.

<!-- lore:019fd7da-106b-7872-878c-9df48979e554 -->
* **Always log a clean, low-risk fix for worker-health and PR it, while flagging deeper issues separately**: When a bug appears in the worker-health / OpenAI / background-workers path, the user's consistent pattern is: scope the change to a shippable, minimal fix (often a \`lore doctor\` warning, status message, or auth-failure record path), commit + lint + typecheck + push as a PR, and report the remaining/likely-root-cause issues (e.g., OpenAI API-key permissions, stale session auth) to the maintainer as a separate flag — rather than bundling speculative fixes into the PR. Treat the adapter/worker-health module as the single owner of transport-failure attribution (always call recordWorkerFailure/recordWorkerSuccess). Before merging, always run \`tsc --noEmit\` and lint in packages/gateway.

<!-- lore:019fc7b3-1577-7fc7-8213-b051cbee4edb -->
* **Always log stderr + tee stdout in CLI bridge**: Directive: always capture and log stderr when any process completes—never silently swallow it. This applies across CI runs, judge pipelines, linting, verifier spawns, and local executions. Silent stderr can make runs appear clean while hiding real failures (e.g., auth issues, misconfigurations, regressions). Always surface stderr alongside stdout for diagnosability.

<!-- lore:019fd47d-f551-7938-90ae-918a71babef3 -->
* **Always poll CI with timed follow-ups and gate merges on no failures**: When monitoring PR CI status, the user repeatedly schedules short follow-up re-checks (300s or 600s) via scheduled tasks rather than blocking or polling inline. The decision rule applied consistently: if mergeStateStatus=BLOCKED, mergeable=MERGEABLE, reviewDecision=empty, and checks are IN\_PROGRESS with no FAILURES → schedule another follow-up. Only when checks complete with zero failures → proceed to squash-merge. Hard guard: NEVER merge if any check is FAILURE. Additionally, ALWAYS check inline review comments on the PR before merging. Apply this gating logic uniformly across all PRs.

<!-- lore:019fd4f4-65bf-7154-8d24-ef11b3d664e1 -->
* **Always produce adversarial PR reviews with numbered PASS/FAIL/CONCERNS, file:line evidence, and a SHIP verdict before merging**: When CI clears (0 IN\_PROGRESS + 0 FAIL) on a PR, the user launches a read-only adversarial review via explore subagent. The review must follow a fixed structure: per-file numbered PASS/FAIL/CONCERN/NIT verdicts with file:line evidence, a final SHIP/SHIP-WITH-FOLLOWUPS/BLOCK verdict, and explicit verification of routing/protocol correctness, config precedence expressions, and stale-reference scans. Concerns are partitioned into in-scope (blocker) vs out-of-scope/latent (follow-up). TDD failing-first is empirically verified by reverting fixes/tests and confirming tests break. External references in commit messages (issues, PRs, docs) are fact-checked. After SHIP-WITH-FOLLOWUPS verdict, the user squash-merges per established convention and tracks follow-up cleanup. Apply this template whenever reviewing PRs in this project.

<!-- lore:019fd6d4-a51e-7e08-b0e8-ef5d6193e181 -->
* **Always run adversarial read-only code reviews with empirical mutation probes before merging PRs**: The user triggers a structured adversarial review process on PRs that pass CI (0 FAIL) before any merge action. Reviews follow a documented multi-bucket scrutiny pattern (TDD failing-first verification, defensive guard tightness, cross-provider safety, cache stability, header/auth, prefix scope, etc.). The reviewer must empirically verify test sensitivity by copying source files to /tmp and reverting targeted lines, then re-running tests to confirm failing-first behavior. Mutation probes systematically target implementation branches (e.g., dropping a guard, broadening a prefix, removing a check) to surface dead code or untested branches. Verdicts are categorical (SHIP, SHIP-WITH-FOLLOWUPS, FAIL) with concerns categorized as in-scope vs latent/pre-existing. Inline review comments on predecessor PRs are treated as authoritative — missed findings trigger fast-follow fix PRs rather than being dismissed.

<!-- lore:019fd7f6-8dac-7a0d-b3db-d7dc435746fa -->
* **Always run full test suite plus lint after changes to verify no regressions**: After any code change (fix, feature, or test addition), the user expects: (1) run the relevant test suite via Vitest and confirm all tests pass with timing reported, (2) check for regressions across related packages (gateway, core, embedding, vec-store), (3) run lint (Biome/eslint) on changed files, (4) only then proceed to stage/commit. The user values regression-safety over speed and expects the assistant to chain these verification steps before declaring work complete. Reports should include pass counts, duration, and explicit 'no regressions' confirmation.

<!-- lore:019fd728-8b1f-7b70-b92f-8018920319fc -->
* **Always surface stderr and treat lint/CI failures as advisory, never blocking**: User consistently enforces two related invariants for the semantic-linter and CI tooling: (1) stderr from CLI runs must always be surfaced as a collapsible group in CI output — on success too, not just failure, because useful diagnostics (models.dev pre-warm status, invariant-seed timing, judge-budget logs) get masked otherwise; (2) lint check never fails a build. Any non-zero exit code other than gate-mode intended failure (rc=2 in gate mode) is a TOOLING failure which must NEVER fail the build — log it and pass. Follow-up: invariant-check reporting UNSTABLE alone is acceptable/advisory for merging; merge can proceed if test/Seer/Sync pass even if invariant-check is UNSTABLE.

<!-- lore:019fd774-5cf0-767d-b3b2-db314f94e1a5 -->
* **Always treat semantic lint as advisory-only tooling that must never fail the build**: Across all sessions the user consistently enforces and articulates the same invariant: the \`lore lint\` / semantic-linter workflow is ADVISORY ONLY. Specifically: (1) any non-zero exit code from the CLI in advisory mode is a TOOLING failure and must NEVER fail the build — log it and pass; only \`rc=2\` with \`LORE\_IC\_GATE=true\` is an intentional gate failure. (2) \`continue-on-error: true\` on the lint job is non-negotiable. (3) Stderr from the CLI run must always be surfaced as a collapsible group (even on success) so diagnostics like models.dev pre-warm status, invariant-seed timing, judge-budget logs, and unparseable warnings are visible — previously hidden stderr masked useful signal. (4) When investigating unparseable/parse-failure runs, compare across runs to distinguish between root causes (e.g., embedding worker stdout pollution vs. judge model returning prose) and probe the actual model contract before concluding the model is the culprit.

<!-- lore:019fd81c-0886-79e6-b465-bb4c531065cb -->
* **Always verify external infrastructure status before triggering retries or workarounds**: When CI failures appear infrastructure-related (e.g., GitHub Actions 'Service Unavailable' flakes), the user expects the assistant to first check githubstatus.com to confirm whether an active outage is in progress. During active outages, the assistant should NOT trigger reruns, use --admin bypass, or invoke any aggressive remediation — instead, schedule follow-ups with longer intervals (900s) and wait. Only when GitHub Status returns to Operational/Recovering should the assistant trigger \`gh run rerun --failed\` and resume normal retry cadence (300s). The --admin bypass is reserved for recovered-but-not-yet-cleared states, never during active major outages.

<!-- lore:019fd77f-2fac-7bbb-a7b0-a42e0a0731ba -->
* **Always verify routing/parser behavior via bundle inspection + live probes before proposing fixes**: When investigating CI failures or behavioral bugs, the user repeatedly: (1) inspects minified bundles to verify routing logic, protocol resolution, and parser branches actually match expectations (e.g., resolveWorkerProtocol, buildOpenAIResponsesUrl, accumulateResponsesSSEStream); (2) runs direct HTTP probes against upstream endpoints (github-copilot, openai) to capture exact request/response shapes, error bodies, and SSE event sequences; (3) saves probe artifacts to /tmp/opencode/\<descriptive-name>/ for reproducibility; (4) only after ground-truth is established, proposes minimal-risk fixes (often 1-10 lines) like LORE\_DEBUG=1 toggle or defensive parser hardening. Pattern: evidence-before-fix, with concrete file:line citations and SHA-pinned commits. Never propose speculative fixes without bundle/probe verification.

<!-- lore:019fcd2a-2067-7a68-a397-015d5929ffd0 -->
* **Auth tests: fixtures only, never real credentials**: Directive (Burak Yigit Kaya): auth tests must use fixtures and never the machine’s real credentials. Real creds seem like higher-fidelity tests but introduce nondeterminism, secret leakage, and CI coupling. Always stub providers and use fixture configs within isolated temp dirs.

<!-- lore:019fcb4b-75c2-7272-8e54-59518e4ca4b8 -->
* **Completion criterion: stable vector count defines done**: Directive (Burak Yigit Kaya): completion is a settled, non-growing vector count; a match may never arrive. If full coverage isn’t reached within the cap, surface it loudly. Waiting for exact matches looks intuitive but can stall forever; stability provides a deterministic stop condition and exposes partial coverage.

<!-- lore:019fd7cb-fd9e-7b17-8988-69afa13c11df -->
* **Distinguish code bugs from infrastructure flakes when triaging CI failures**: When CI checks fail, the user expects thorough root-cause analysis to separate actual code regressions from transient infrastructure issues (action download 503s, timeouts, runner flakes) before deciding whether to merge or fix. Apply the project's standing policy that the advisory semantic linter NEVER blocks builds — tooling failures are not findings. Investigate: timing windows, stderr vs stdout routing, env vars, cache state (CACHE\_HIT), bundle artifacts, and prior run history. Trace embedded worker stdout pollution into result JSON files (e.g. /tmp/lore-ic.json corruption) as a recurring bug class distinct from judge-unparseable failures. Once root cause is established as infra-only, proceed with merge/decisions; only treat findings as actionable when they're reproducible in code.

<!-- lore:019fc9bd-7fa1-7ea6-af8d-bc0295e06cbb -->
* **Eval files are \*.eval.ts (TypeScript-only)**: Directive: evaluation files are always written in TypeScript with the \`\*.eval.ts\` convention. This standardizes tooling, typing, and execution paths. Alternatives like JS or mixed extensions were implicitly rejected because they introduce inconsistency in build/test pipelines and reduce type safety in eval logic.

<!-- lore:019fcb4b-74bf-7225-8fee-326b27db3db6 -->
* **Eval harness: never abort on curate-log parse failure**: Directive (Burak Yigit Kaya): curate-log parsing must NEVER abort a run. Parsing looks like a safe gate for data integrity, but it converts non-critical log issues into false invalidations, hiding real model behavior. Fix: treat parse errors as soft failures—log and continue; scoring/validity must rely on primary artifacts (result.json, session traces), not auxiliary logs.

<!-- lore:019fc9bd-803a-77ce-84fc-d35b67833c08 -->
* **Eval invariant: 'never reached for the store' is target behavior**: Directive: the eval explicitly tests that the agent never reaches for the store. This is a core success condition, not a side effect. Allowing store access might look like a harmless optimization, but it contaminates the signal—tests must validate reasoning from provided context only.

<!-- lore:019fc9bd-80ce-72c3-bb8c-91471210ea2d -->
* **Eval runtime uses isolated SQLite DB only (no real gateway)**: Directive: evals run against their own SQLite database and NEVER touch a real user gateway or production DB. Using real services may seem more realistic but introduces nondeterminism, side effects, and data leakage; isolation ensures reproducibility and safety.

<!-- lore:019fcab7-8956-7867-8b39-3c7efb4426bc -->
* **Git workflow: git add . then commit, no stray opencode.json, .lore.md auto-managed**: Git workflow (Burak Yigit Kaya): ALWAYS run \`git add .\` before committing — partial staging looks safer but omits related changes (tests, fixtures, config), breaking CI or non-reproducible commits. Before committing: format → lint → typecheck → tests on the current base; check for a stray \`opencode.json\` (untracked opencode config in repo root — BYK/loreai has none; it contaminates cache/auth tests). \`.lore.md\` is auto-managed by the background lore process: on stash-pop conflicts resolve to 'ours' (origin/main), never hand-edit — it re-applies session memory on the next cycle; its regenerated changes are committed via \`git add .\` like everything else.

<!-- lore:019fc52b-ba3c-7d2c-a45f-775a46a713c4 -->
* **Knowledge-delta closer isolation invariant**: Directive: the knowledge-delta closer ('\*🧠 Refreshed memory\*') must ALWAYS be a separate assistant message and NEVER coalesced with real content; it is always followed by a user-turn separator. Coalescing looks cleaner but breaks parsing and turn accounting.

<!-- lore:019fd829-0c21-78df-887d-ccb823896641 -->
* **Never bypass CI with --admin during active GitHub infrastructure outages**: When a PR's CI failure is diagnosed as a GitHub Actions infrastructure flake (Service Unavailable) during an active GitHub Status Major Outage, the user expects the assistant to wait through the outage and reschedule rechecks rather than use the \`--admin\` flag to bypass the ruleset's required-status check. The \`--admin\` bypass strategy is reserved for a recovered-but-not-yet-cleared state. During active outages: poll GitHub Status for Major Outage status, reschedule follow-ups in ~900s intervals, and do not trigger reruns that are likely to fail again. Only after GitHub Status returns to Operational/Recovering should the assistant trigger \`gh run rerun \<id> --failed\` and reschedule a shorter (~300s) recheck for the rerun to clear. This pattern combines the user's general rule of never merging with FAILURE (lore 019fb858) with an explicit escalation rule against \`--admin\` bypass during active outages.

<!-- lore:019fcee5-52e5-7863-9f9a-8d414b4e71a4 -->
* **openai-responses worker adapter: per-provider guard + endpoint split by model**: Directive (Burak Yigit Kaya, Aug 4 2026): \`ProviderHeaderTimeoutError\`-class failures (response headers not received within the client timeout) must ALWAYS be retried — retrying is mandatory, not best-effort. The opencode client ladder is RETRY\_INITIAL\_DELAY=2000ms × factor 2, capped at RETRY\_MAX\_DELAY\_NO\_HEADERS=30000ms. Any fix that drops the request after a header timeout instead of retrying violates this. PR #1580 (merged 2026-08-05T18:30:34Z as 4f5c40e6) addressed the cold-post-restart session 0H5aCIGPVFfpBtvA 10s timeout via two mechanisms: (1) \`earlyFlushStreamingResponse\` (pipeline.ts ~L10937) wraps handleConversationTurn so 200 + text/event-stream headers arrive in ≤10s for openai-responses protocol — keepalive enqueues synchronously before heavy work. (2) \`singleFlightStableLtm\` (pipeline.ts:2351) shares one LTM compute across concurrent retries via cache-first check, preventing DB thrashing. Scope: opencode's OPENAI\_HEADER\_TIMEOUT\_DEFAULT=10\_000 (provider.ts:35) is openai-provider-scoped only; anthropic/gemini/openai Chat Completions clients are not wrapped.

<!-- lore:019fd822-03ac-7063-883b-45ccfd1ba8f2 -->
* **Polling PR CI with 300s cadence until mergeable**: User repeatedly monitors PR status in BYK/loreai using \`gh pr view\` with statusCheckRollup, waiting in 300s intervals when checks are IN\_PROGRESS. Decision logic: if BLOCKED but no FAILURES and only IN\_PROGRESS checks remain, schedule another 300s follow-up; once all checks terminal (no IN\_PROGRESS, no FAILURES) and Seer Code Review passed, proceed to adversarial review before merging; any FAILURE → do NOT merge. Always enumerates each check's terminal state, checks inline comments for Seer findings, and explicitly states readiness before acting. Treats BLOCKED + MERGEABLE + empty reviewDecision as still-pending state requiring continued polling rather than immediate action.

<!-- lore:019fce4e-04c6-78d9-b302-f15b7a89e6f6 -->
* **PR squashing: use --use-destination-message (nano fails non-TTY), verify commit target**: Directive (Aug 4 2026, observed during PR #1571 squash onto PR #1574): when squashing jj commits via \`jj squash\`, ALWAYS pass \`--use-destination-message\` because interactive \`nano\` exits with status 1 in non-TTY agent environments and aborts the squash. Trap: \`jj describe -m \<new-msg>\` AFTER squash targets the wrong commit if @ has been replaced by an empty squashed child. Observed: describe accidentally rewrote the empty @ commit instead of @- (squashed commit with all changes). Symptom: both commits show the same first line via \`jj log -r 'all() & ancestors(@)'\`. Fix: (1) \`jj undo\` to revert accidental describe, (2) \`jj describe -r @- -m "..."\` to target the actual change commit. After re-describe, jj may move the bookmark sideways via rebase — bookmark \`\[move sideways from X to Y]\` log line is expected, force-push \`jj git push -b \<name>\` still works. Final squashed commit ID \`09740c5f\`, change ID preserved through re-describe.

<!-- lore:019fcc59-ec09-7794-b695-00e229ed7694 -->
* **Prefer dataclasses over bare dicts**: Preference (Burak Yigit Kaya): use dataclasses instead of bare dicts. Dicts look flexible but lead to implicit schemas and brittle call sites; dataclasses make contracts explicit and safer to evolve.

<!-- lore:019fd7ea-126b-7f3d-950c-1026528c034f -->
* **Prefer root-cause fixes over narrow patches**: When presented with a bug in domain logic (e.g. \`resolveWorkerProtocol\`), the user consistently opts for the broader, cleaner architectural fix at the root cause rather than a narrow workaround. The assistant should: (1) surface both the narrow patch and the cleaner root-cause option when a fix has multiple viable scopes, (2) implement the fix at the root location (e.g. \`resolveWorkerProtocol\` in \`packages/gateway/src/llm-adapter.ts\`) rather than at downstream sites, and (3) avoid scattering special-case logic — instead, generalize the helper so the existing ChatGPT short-circuit logic in \`worker-model.ts\` (lines 971/1055/1185) can be replaced by a shared \`isChatGPTBackend\` helper rather than duplicated checks.

<!-- lore:019fd6e3-f33f-7fbd-b9ab-cf3944ea7f97 -->
* **Recheck CI on a 600s schedule until green, then squash-merge per lore policy**: After pushing fixes or format corrections, the user schedules a delayed follow-up (typically 600s / 10 minutes) to re-check PR CI status. The decision policy (lore \[019fb96c]) is: 0 IN\_PROGRESS + 0 FAIL → squash-merge immediately; only IN\_PROGRESS (no failures) → reschedule another 600s; any FAIL → investigate before merging. Once CI reaches OK + mergeable=MERGEABLE + mergeState=CLEAN and Seer review verdict is SHIP (\[019fb858]), the user runs \`gh pr merge --squash --admin\` and then performs post-merge cleanup (\[019fb976]) such as deleting the head branch and re-basing dependent PRs. Follow-ups should always reference the relevant lore IDs in re-prompt criteria.

<!-- lore:019fd4e7-6428-7a67-aab4-a088f97bd0d7 -->
* **Request adversarial read-only PR reviews with structured verdict format**: When reviewing PRs, the user consistently requests a READ-ONLY adversarial review (no file mutations; copies allowed only under /tmp/opencode/\<pr>-mutation-probe-\*). The user expects numbered PASS/FAIL/CONCERN verdicts citing \`file\_path:line\_number\`, organized under explicit scrutiny buckets tailored to the change's risk surface, and ending with a final ship recommendation (SHIP / SHIP-WITH-FOLLOWUPS / BLOCK). Reviews must surface regressions, header-scoping invariants, race conditions, model-routing edge cases, and convention adherence. The user often pairs this with deep context-dumping (commit SHAs, CI status, prior PR lineage) and expects the reviewer to verify against CHANGELOG entries, worker-model.ts registrations, and workflow precedence rules.

<!-- lore:019fd2f3-b040-74b8-ad98-75d5697acbe6 -->
* **Request strict adversarial code reviews that hunt for real defects, not rubber-stamps**: Across repeated PR review sessions in the loreai monorepo, the user consistently: 1. Explicitly frames reviews as ADVERSARIAL and read-only, stating NOT a rubber-stamp and noting prior reviewers have found real defects on every PR in the series. 2. Defines scope tightly: specific PR number, branch/HEAD SHA, base branch, remote, worktree path, and exact files in diff. 3. Lists required review areas as a checklist (protocol/event correctness, error paths, race safety, dead code, test-quality gaps like loose substring assertions or missing regression coverage). 4. Demands a numbered findings report with severity (HIGH/MED/LOW), file:line citations, root-cause analysis, and explicit real-defect vs non-issue verdicts per finding. 5. Permits only read-only operations (no edits, no commits, no state-mutating test runs); mutation testing and \`git diff\` are fair game. 6. Treats test gaps as first-class defects (e.g., missing regression coverage for core invariants, loose \`toContain\` assertions). Follow this pattern: when asked to review a PR, assume adversarial intent, enumerate required review dimensions up front, cite line numbers, and never rubber-stamp.

<!-- lore:019fd4dc-60c7-71ab-8f95-434e5fc1753c -->
* **Reschedule CI rechecks when only IN\_PROGRESS checks remain, with no failures**: When monitoring PR CI status, the user follows a strict merge-gate policy: if there are 0 FAILURES and only IN\_PROGRESS checks remaining, do NOT proceed to merge or adversarial review — reschedule another follow-up recheck (~300–600s later). Only advance to adversarial review and squash-merge once the gate condition (0 IN\_PROGRESS + 0 FAILURES) is met. The user consistently logs the recheck decision with the gate lore reference (e.g., \[019fb96c]), records the follow-up id, and re-states the next recheck plan. This applies to any PR awaiting merge while CI runs.

<!-- lore:019fd7f6-dfba-7f17-a918-ae74852b6fa3 -->
* **Schedule follow-up rechecks at fixed intervals when CI is still running**: When monitoring CI on a PR and the status shows IN\_PROGRESS or QUEUED checks with no failures, the user schedules a recheck follow-up rather than polling or acting immediately. Typical cadence is 300s (5 min) for active changes/actionlint reruns and 600s (10 min) for longer pipelines like smoke tests. Decision logic follows lore references \[019fb96c]/\[019fb976]: 0 FAIL + only IN\_PROGRESS → reschedule; 1+ FAIL → investigate (including distinguishing real failures from GitHub Actions infrastructure flakes like 'Service Unavailable'); all green → proceed to next step (adversarial review, squash-merge, or bypass with \`--admin\`). Always bake the next-step decision rules into the scheduled prompt so the follow-up can act autonomously.

<!-- lore:019fc9bd-7ff9-7b5c-bfd6-454171dc46f6 -->
* **Seed tasks must not include future run values**: Directive: static task files and seed projects must NEVER contain values from future runs. This preserves causal isolation and prevents leakage. Including future values looks like helpful determinism but invalidates the eval by turning it into a lookup instead of a capability test.

<!-- lore:019fcab7-8a07-7801-8d33-a9adca7a00e2 -->
* **Streaming invariant: always feed inner accumulator**: Directive (Burak Yigit Kaya): always feed the inner accumulator with the full event stream — it tracks the authoritative state. Skipping or partially feeding it looks like an optimization but causes divergence (missing starts → dropped deltas/stops) and silent data loss. Applies to ALL streaming paths: Anthropic \`inner.processEvent\` runs unconditionally before any recall suppression; Responses \`applyResponsesEvent(state, event, parsed)\` must run for every event INCLUDING suppressed recall-item events, so the internal state stays complete for the final postResponse rebuild (\`streamResponsesRecallAware\`, reinforced Aug 4 2026).

<!-- lore:019fbecc-846c-73a7-a46d-0918a5ee209d -->
* **Stricli typed-wrapper invariants + follow-mode bypass + vacuous-mock trap**: Stricli typed-wrapper invariants from PR #1573 Phase 3C review (data.ts): (1) typed wrappers MUST delegate I/O to legacy via runLegacyAndCollect. (2) Short-flag aliases go on parameters.aliases at root, NOT on individual flag specs. (3) json:boolean must be declared in flags interface (entity.ts/data.ts pattern); buildOutputCommand reads it via AugmentedFlags. (4) Every flag the legacy handler reads MUST be declared in typed schema AND forwarded — missing flags break variadic-positional fallback. PR #1573 caught 4 missing destructive flags: --dry-run (cmdMove/cmdSplit/cmdConsolidate/cmdReground), --no-children (cmdMove/cmdMoveRemote), --no-backup (cmdSplit), --min-confidence (cmdSplit) → exit 20. (5) Kebab-case flags need BOTH kebab + camelCase forwarding. (6) Forwarding tests must cover ALL declared flags, not 3 of 9. (7) cli-data-contract.test.ts variants without all 13 data flags hide schema gaps — extend forwarding + help-text assertions. (8) Phase 3C.1 MERGED (see related: \[\[019fcde9-9f61-798a-b182-4d07c0224cb9]] streaming fast path unlocked by same runLegacyAndCollect pattern).

<!-- lore:019fd4a7-fefb-752a-a858-e3905ba14829 -->
* **Thoroughly catalog function signatures, call sites, and protocol-specific dispatch logic before making changes**: Before modifying \`buildWorkerRequest\` or related worker-routing logic in \`packages/gateway/src/llm-adapter.ts\`, the user expects a comprehensive inventory of: (1) the function's exact signature with all parameters and their types, (2) all call sites with line numbers, (3) the protocol-dispatch switch and each translator's quirks (e.g., Codex omits maxTokens, Vertex uses placeholder bearer, Gemini uses partsText). They consistently track protocol-specific edge cases — credential handling, retry paths, system-prompt normalization, candidate blocking — across sessions. When adding a new provider, follow the existing pattern: add a case to both \`buildWorkerRequest\` and \`parseWorkerResponse\` switches, place new builders near related ones (e.g., Gemini near OpenAI). Always preserve the auth-error retry loop's rebuild via \`buildWorkerRequest\` with \`activeCred\`.

<!-- lore:019fd7be-20bb-7230-8964-772cd26726dc -->
* **Verify failing tests are pre-existing before treating as branch regressions**: When test failures appear in CI or local runs during a feature branch, the user consistently investigates whether they are pre-existing on the base branch before assuming the branch caused them. Pattern: (1) check failure details and isolate to the specific file/test, (2) run the same test file on \`origin/main\` HEAD to compare, (3) identify root cause (e.g., stray \`opencode.json\` polluting config-reading tests, env-var leakage from \`ANTHROPIC\_API\_KEY\`), (4) confirm failures are stable across full suite + isolation runs, and (5) only then proceed without trying to fix unrelated pre-existing failures. Apply this when any test fails during branch work: bisect to origin/main before assigning blame to the current changes.

<!-- lore:019fd810-5dca-7a01-9dc6-26f6af56dc6e -->
* **Verify GitHub infrastructure status before troubleshooting CI failures**: When CI failures on PRs appear to be infrastructure-related (e.g., 'Service Unavailable', CANCELLED, repeated flakes), the user wants the assistant to first verify whether GitHub itself is having an outage by checking https://githubstatus.com before pursuing code-level diagnosis or risky workarounds. During an active outage, defer investigation and avoid risk-increasing actions like \`--admin\` merge bypass; instead, schedule follow-up rechecks with intervals tuned to recovery state (e.g., 900s if still degraded, 300s if Actions recovered but queue not cleared). On recovery, re-run failed jobs and recheck via \`gh pr view\` to confirm readiness.

<!-- lore:019fd737-ede3-7e31-9dd6-39aa0387071a -->
* **Verify logger gating before treating absence of log output as failure signal**: Before concluding a log-bearing operation failed because no log line appeared, the user requires verifying the actual logging semantics: which log levels are emitted to stderr, whether they're gated by an env flag (e.g., LORE\_DEBUG), and whether the absence reflects a real failure or a deliberate suppression. In the current session, the user rejected the prior 'pre-warm failed' theory after discovering \`log.info\`/\`log.warn\` are debug-gated while \`log.notice\`/\`log.error\` and direct \`console.\*\`/\`process.stderr.write\` are not. Real signal must come from non-debug-gated channels (notice/error/console/stderr) — never assume missing debug logs indicate failure.

<!-- lore:019fd7fe-4674-70cd-ba6d-c30e6ed0fe0e -->
* **Verify with typecheck before committing and pushing**: Before committing, pushing, or creating a PR, the user expects a clean \`tsc --noEmit\` typecheck on the affected package (typically \`@loreai/gateway\`). The workflow is: implement fix → run typecheck → confirm pass → commit → push → open PR. The typecheck result is treated as a gate; passing it is documented in observations alongside the commit hash and PR link. When changes are uncommitted or a branch switch occurs, typecheck is re-run after staging to confirm the working tree still typechecks before proceeding to commit/push.

### Reference

<!-- lore:019fc2dc-7ed5-792b-b112-79719048f196 -->
* **Sentry dashboard widget dimensions (full-width table = own row)**: Reference (Sentry dashboard widget layout, 2026-08-02): full-width \`table\` widget always takes its own row (width 6, category common). Other widget widths/heights: \`big\_number\` (2×1, 3/row), \`line\`/\`area\`/\`bar\`/\`stacked\_area\`/\`top\_n\`/\`categorical\_bar\`/\`text\`/\`details\`/\`wheel\`/\`rage\_and\_dead\_clicks\`/\`server\_tree\`/\`agents\_traces\_table\` (3×2, 2/row). A 6-column grid total. Use this when composing dashboard layouts via \`sentry dashboard\` API calls.
