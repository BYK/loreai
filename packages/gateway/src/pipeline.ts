YªçŠx-®éÜj×¢ëiºÚ+Š§j[h‘éÜ¢éíÛ®}Ûy×nÊ×¬¢h­µçKÊŠ‚ˆ
ˆÛÜ™H™\]Y\Ý›ØÙ\ÜÚ[™È\[[™H›ÜˆHÜ™HØ]]Ø^K‚ˆ
‚ˆ
ˆÜ˜Ú\Ý˜]\ÈH[›ÝÈ›Üˆ]™\žH™\]Y\Ý‚ˆ
ˆÙ\ÜÚ[ÛˆY[YšXØ][Ûˆ8¡¤ˆH[š™XÝ[Ûˆ8¡¤ˆÜ˜YY[˜[œÙ›Ü›H8¡¤‚ˆ
ˆ\Ý™X[H›ÜØ\™[™È8¡¤ˆ™\ÜÛœÙHXØÝ[][][Ûˆ8¡¤ˆØ[Xœ˜][Ûˆ8¡¤‚ˆ
ˆ[\Ü˜[ÝÜ˜YÙH8¡¤ˆ˜XÚÙÜ›Ý[™ÛÜšÈØÚY[[™Ë‚ˆ
‚ˆ
ˆ™YH™\]Y\ÝÛ\ÜÙ\È\™H[™Y‚ˆ
ˆKˆÛÛ\XÝ[Ûˆ™\]Y\ÝÈ8¡¤ˆ[\˜Ù\Y™]™\ˆ›ÜØ\™Y\Ý™X[K‚ˆ
ˆ‹ˆY]H™\]Y\ÝÈ
]HÙ[‹Ý[[X\šY\Ë]ËŠH8¡¤ˆ›ÜØ\™Y˜[œÜ\™[K›ÈÜ™H›ØÙ\ÜÚ[™Ë‚ˆ
ˆËˆ›Ü›X[ÛÛ™\œØ][Ûˆ\›œÈ8¡¤ˆ[\[[™K‚ˆ
‹Âš[\ÜÈÛÜU\ØYÙS[Z]XY\œÈHœ›ÛH‹‹Ý\ØYÙK[[Z]ZXY\œÈŽÂš[\ÜÈÝÜ™U\›•[\Ü˜[\H\›•[\Ü˜[[œ]Hœ›ÛH‹‹Ý\›‹][\Ü˜[ŽÂš[\ÜÂˆ™\\˜][Û•[Z[™Ëˆ™\\™TÙ[X[XÓY\ÜØYÙ\ËŸHœ›ÛH‹‹ÜÙ[X[XË\™\\˜][ÛˆŽÂ™^ÜÈÝÜ™U\›•[\Ü˜[Hœ›ÛH‹‹Ý\›‹][\Ü˜[ŽÂ™^ÜÈ™\ÜÛœÙ\Ô›Ý™[˜[˜ÙPžSY\ÜØYÙRYHœ›ÛH‹‹ÜÙ[X[XË\™\\˜][ÛˆŽÂš[\ÜÈÜ™X]R\ÚHœ›ÛH››ÙN˜Üž\ÈŽÂš[\ÜÈ\ÑY\ÝšXÝ\]X[Hœ›ÛH››ÙN][ŽÂš[\Ü\HÂˆÛÛ^›Ý[™\žT›ÝØÛÛˆÜ™SY\ÜØYÙUÚ]\ËˆPÛY[ŸHœ›ÛHÜ™XZKØÛÜ™HŽÂš[\ÜÂˆÓÓ•VÐ“ÕS‘T–WÒPQT‹ˆ[ÛÝ\˜ÙT™\]Z\™YˆÓÓ•VÐ“ÕS‘T–WÓRTÓPUÒÒPQT‹ˆ\ÔÝš[™Ëˆ\Ý[X]UÚÙ[œÈ\ÈÛÜ™Q\Ý[X]UÚÙ[œËˆPVÔ‘PÐSÐUÒÒQËˆPVÔ‘PÐSÒQÐÒT”ËŸHœ›ÛHÜ™XZKØÛÜ™HŽÂš[\ÜÂˆ[˜ÛÙPÛÛ^›Ý[™\žKˆÝ\ÜÐÛÛ^›Ý[™\žKŸHœ›ÛH‹‹ØÛÛ^X›Ý[™\žHŽÂš[\ÜÂˆÛÝ\˜ÙQ[U[˜]˜Z[X›Q\œ›Ü‹ˆÛÝ\˜ÙPÚXÚÜÚ[›ÝØÛÛŸHœ›ÛH‹‹ÜÛÝ\˜ÙKXÚXÚÜÚ[ŽÂš[\ÜÂˆØYˆÛÛ™šYÈ\ÈÜ™PÛÛ™šYËˆ[œÝ\™T›Ú™XÝˆ™XÛÜ™ØXÚP\ÝØœÙ\˜][Û‹ˆš[™Ù\ÜÚ[Û”Ý]\ÐžQš[™Ù\œš[ˆÛÝ[X]Ú[™Õ[\Ü˜[YËˆÙ]Ú]™[[ÝKˆ›Ú™XÝYˆ™\ÛÛ™T›Ú™XÝžT™[[ÝSÜ”]ˆ›Ú™XÝÚ]™[[ÝKˆY\™ÙT›Ú™XÝ[\›˜[ˆ\Õ[˜]šX]Y›Ú™XÝ]ˆ[\Ü˜[ˆKˆÞ[]XÔ›Ø™T™\ÛÛ™\‹ˆ›ÛÜ™\ÛÛ™\‹ˆZ[™Y˜ÚXÚÔ›Ø™TØÜš\ˆ[]Y\Ëˆ\Ý[][Û‹ˆÝ\˜]Ü‹ˆÙËˆ˜[œÙ›Ü›Kˆ™]Ø\›Q\Ý[][Û”Û˜\ÚÝˆ\Ó\™ÙPÛÛÝ\ˆÙ][Ù[[Z]ËˆÙ]UÚÙ[œËˆÙ]PYÙ]ˆÙ]™Y™\™[˜ÙSPYÙ]ˆÙ]X^^Y\ŒÚÙ[œËˆÙ]›Ü˜ÙSZ[“^Y\‹ˆÛÛ\]S^Y\ŒØ\ˆÙ]ØXÚTšXÚ[™ËˆÙ]]X[]RÛ™YKˆ™\ÛÛ™T]X[]RÛ™YKˆQUSÔUPSUWÒÓ‘QWÑ”PÕSÓ‹ˆ\Ý[[Z]\‹ˆÝ\˜]Ü“[Z]\‹ˆ™XÛÜ™ØXÚU\ØYÙKˆ^ÜY\XÚ\Ú[ÛœËˆ[\ÜY\XÚ\Ú[ÛœËˆØ[Xœ˜]KˆÙ]\Ý˜[œÙ›Ü›YYÛÝ[ˆÙ]\Ý˜[œÙ›Ü›Q\Ý[X]KˆÛ’YT™\Ý[YKˆÙ]ØXÚTÝ˜]YÞKˆÝ˜]YÞUØ[ÕØ\›Z[™Ëˆ\HØXÚTÝ˜]YÞKˆÛÛœÝ[YPØ[YSÝ]Ù’YKˆ™YYÕ\™Ù[\Ý[][Û‹ˆ›Ü›X]Û›ÝÛYÙKˆÚÝ[[\ÜÜ™Qš[Kˆ[\ÜÜ™Qš[KˆÜ™Qš[Q^\ÝËˆÚÝ[[\Üˆ[\Üœ›ÛQš[KˆÔ‘WÑ’SKˆQÑS•×Ñ’SWÐÐS‘QUTËˆ™\ÛÛ™PYÙ[Ñš[S˜[YKˆ]™XY\‹ˆ[X™Y[™Ëˆ[\Ü˜[[X™Y[™Ô]Y]YKˆØ]™TÙ\ÜÚ[Û•˜XÚÚ[™ËˆØYÙ\ÜÚ[Û•˜XÚÚ[™Ëˆ\[™Ù\ÜÚ[Û”›Û\[Kˆ[]TÙ\ÜÚ[Û”›Û\[Kˆ\ÝÙ\ÜÚ[Û”›Û\[\Ëˆ\]TÙ\ÜÚ[Û”›Û\[TÙ[XÝÜ‹ˆ\]TÙ\ÜÚ[Û”›Û\[PÛÛ[ˆÚ]Ø]™\Ú[ˆØYXY\”Ù\ÜÚ[Û’[™^ˆ\ÒÜÝY[ÙKˆ[˜X›RÜÝY[ÙKˆ[\ÜÜ™Qš[P\Ëˆ™\ÛÛ™UÛÜšÜÜXÙ\ËˆÝ\œ™[[˜[YˆÚ][˜[ŸHœ›ÛHÜ™XZKØÛÜ™HŽÂ‚š[\Ü\HÂˆØ]]Ø^T™\]Y\ÝˆØ]]Ø^T›ÝØÛÛˆØ]]Ø^T™\ÜÛœÙKˆØ]]Ø^SY\ÜØYÙKˆØ]]Ø^PÛÛ[›ØÚËˆØ]]Ø^UÛÛ\ÙP›ØÚËˆØ]]Ø^UÛÛ™\Ý[›ØÚËˆØ]]Ø^U\ØYÙKˆÝÜ™Y™XØ[ˆÙ\ÜÚ[Û”Ý]Kˆ\Ý™X[TÛ˜\ÚÝˆØ\›]\Ý]KŸHœ›ÛH‹‹Ý˜[œÛ]KÝ\\ÈŽÂš[\ÜÂˆ\U\Ý™X[Q^˜RXY\œËˆZ[\Ý™X[TÛ˜\ÚÝXY\œËˆ›ØÚÜÕÕ^ˆ\Ñ[\PÛÛ\][Û‹ˆÛÚÜÓZÙTÔÑKˆ›ÝšY\”›Ý][™Õ˜[YKˆ™\]Y\Ý\™Ù]ÓÜ[”›Ý]\‹ˆ‘T“×ÕTÐQÑKŸHœ›ÛH‹‹Ý˜[œÛ]KÝ\\ÈŽÂš[\ÜÈÑSRS’WÑQUSÕTÕ‘PSK\HØ]]Ø^PÛÛ™šYÈHœ›ÛH‹‹ØÛÛ™šYÈŽÂš[\ÜÂˆÙ]›Ú™XÝ]ˆ^˜XÝÚ]™[[ÝRXY\‹ˆ™\ÛÛ™U\Ý™X[T›Ý]Kˆ^˜XÝ\Ý™X[U\›XY\‹ˆ^˜XÝ\Ý™X[T]XY\‹ˆ™\˜˜][U\Ý™X[U\›ˆ^˜XÝ›ÝšY\’XY\‹ˆ\ÐÛÜ[Ý[YÜ˜][Û’XY\‹ˆ™\ÛÛ™T›ÝšY\”›Ý]Kˆ^˜RXY\œÑ›Ü•\Ý™X[Kˆ\Ý™X[U\››Ü“ÙËˆ\Õ\Ý™X[UÚ][˜\ÙKˆ\ÐØ[\•\Ý™X[P[ÝÙYˆ›Ü›X[^™U\Ý™X[P˜\ÙKˆ[˜]šX]YXÚÙ]]ˆ\H›Ú™XÝ]™\Ý[ŸHœ›ÛH‹‹ØÛÛ™šYÈŽÂš[\ÜÂˆÙ[™\˜]TÙ\ÜÚ[Û’Qˆš[™Ù\œš[Y\ÜØYÙ\ËˆQTÔÐQÑWÐÓÕS•Ô“ÖSRUWÕ‘TÒÓˆÓ“ÕÓ—ÔÑTÔÒSÓ—ÒPQT”Ëˆ^˜XÝÛ›ÝÛ”Ù\ÜÚ[Û’XY\‹ˆ\ÐÛ]YPÛÙTÝX˜YÙ[ˆX\›’XY\œËˆØœÙ\™RXY\•˜[Y\Ëˆ\ÐÜ™Y[X[XY\“˜[YKŸHœ›ÛH‹‹ÜÙ\ÜÚ[ÛˆŽÂš[\ÜÂˆ]XÝÛÛ\XÝ[Û”™\]Y\Ýˆ\ÔÝXÝ\˜[ÛÛ\XÝ[Û‹ˆ\ÓY]T™\]Y\ÝˆÔ‘WÐQÑS•ÒPQT‹ˆ^˜XÝ™]š[Ý\ÔÝ[[X\žKˆZ[ÛÛ\XÝ[Û”™\ÜÛœÙKˆ\ÜÙ[X›SÙ™›[™PÛÛ\XÝ[Û‹ˆØØ[U\ØYÙQ›ÜÛY[ˆX^™\ÜY\ØYÙQ›Ü“[Ù[ˆÛY[Y]\™YÛÛ^Ú[™ÝËˆ™\]Y\Ý[˜X›\ÓÛ™ÐÛÛ^ˆPVÓÕUUÔ‘TÑT•‘KˆQUSÓPVÔ‘TÔ•QÕTÐQÑKŸHœ›ÛH‹‹ØÛÛ\XÝ[ÛˆŽÂš[\ÜÂˆZ[[›ÜXÔ™\]Y\ÝˆZ[[›ÜXÓ›Û”Ý™X[T™\ÜÛœÙKˆ\œÙP[›ÜXÔ™\ÜÛœÙR”ÓÓ‹ˆ\H[›ÜXÐØXÚSÜ[ÛœËŸHœ›ÛH‹‹Ý˜[œÛ]KØ[›ÜXÈŽÂš[\ÜÂˆ™Y›ØÚÓX[U\›ˆ\Ð™Y›ØÚÓX[Q\Ü]ÚˆÓX[S[Ù[YŸHœ›ÛH‹‹Ý˜[œÛ]KØ™Y›ØÚÈŽÂš[\ÜÈZ[™\^\Ý™X[K™\^ÜÝHœ›ÛH‹‹Ý˜[œÛ]KÝ™\^ŽÂš[\ÜÈÙ]™\^XØÙ\ÜÕÚÙ[‹™\ÛÛ™U™\^›Ú™XÝHœ›ÛH‹‹Ý™\^X]]ŽÂš[\ÜÂˆZ[Ü[RU\Ý™X[T™\]Y\ÝˆZ[Ü[RT™\ÜÛœÙKŸHœ›ÛH‹‹Ý˜[œÛ]KÛÜ[˜ZHŽÂš[\ÜÂˆZ[Ü[RT™\ÜÛœÙ\Õ\Ý™X[T™\]Y\ÝˆZ[Ü[RT™\ÜÛœÙ\Ô™\ÜÛœÙKˆ\œÙSÜ[RT™\ÜÛœÙ\Ô™\]Y\ÝŸHœ›ÛH‹‹Ý˜[œÛ]KÛÜ[˜ZK\™\ÜÛœÙ\ÈŽÂš[\ÜÂˆXØÝ[][]T™\ÜÛœÙ\ÔÔÑTÝ™X[KˆÝ™X[T™\ÜÛœÙ\Ô\ÜÝ›ÝYÚˆ˜[œÛ]P[›ÜXÔÝ™X[UÔ™\ÜÛœÙ\Ëˆ\T™\ÜÛœÙ\Ñ]™[ˆš[˜[^™T™\ÜÛœÙ\ÐXØËˆ›Ü›X]™\ÜÛœÙ\Ñ]™[ˆXZÙT™\ÜÛœÙ\ÐXØÔÝ]KˆX\Ý]\Ñœ›ÛTÝÜ™X\ÛÛ‹ˆ\ÔÝ\ÜY™\ÜÛœÙ\ÓÝ]]][U\Kˆ\Õ˜[Y™\ÜÛœÙ\ÓÝ]]][TÝ]\Ëˆ\Õ˜[Y™\ÜÛœÙ\Ô™X\ÛÛš[™Ñ[˜Üž\YÛÛ[ˆ™\ÜÛœÙ\ÑÛ™R][SX]Ú\ÐYYˆ™\ÜÛœÙ\Õ\›Z[˜[][SX]Ú\Ëˆ›Ü›X[^™PÛÙ^™\ÜÛœÙ\Ñ]™[ˆ™\ÜÛœÙ\Õ\›Z[˜[\œ›Ü‹ˆ\H™\ÜÛœÙ\ÐXØÔÝ]KŸHœ›ÛH‹‹ÜÝ™X[KÛÜ[˜ZK\™\ÜÛœÙ\ÈŽÂš[\ÜÂˆXØÝ[][]SÜ[RTÔÑTÝ™X[KˆÜ[RTÝ™X[U˜[Y][Û‘\œ›Ü‹ˆ˜[œÛ]P[›ÜXÔÝ™X[UÓÜ[RKŸHœ›ÛH‹‹ÜÝ™X[KÛÜ[˜ZHŽÂš[\ÜÂˆZ[Ù[Z[šU\Ý™X[T™\]Y\ÝˆZ[Ù[Z[šT™\ÜÛœÙKˆ\œÙQÙ[Z[šT™\ÜÛœÙR”ÓÓ‹ŸHœ›ÛH‹‹Ý˜[œÛ]KÙÙ[Z[šHŽÂš[\ÜÂˆXØÝ[][]QÙ[Z[šTÔÑTÝ™X[Kˆ˜[œÛ]P[›ÜXÔÝ™X[UÑÙ[Z[šKŸHœ›ÛH‹‹ÜÝ™X[KÙÙ[Z[šHŽÂš[\ÜÂˆØY™UÚÙ[”Ý[Kˆ˜[Y]SÜ[RU\ØYÙKˆ˜[Y]T™\ÜÛœÙ\Õ\ØYÙKŸHœ›ÛH‹‹Ý\ØYÙK]˜[Y][ÛˆŽÂš[\ÜÂˆXØÝ[][]TÔÑT™\ÜÛœÙKˆÜ™X]TÝ™X[PXØÝ[][]Ü‹ˆÜ™X]T™XØ[]Ø\™PXØÝ[][]Ü‹ˆ\œÙTÔÑTÝ™X[KˆZ[ÔÑT™\ÜÛœÙKˆZ[ÔÑUÛÛ\ÙT™\ÜÛœÙKˆZ[ÙY\[]™PÛÛ\XÝ[Û”Ý™X[KˆZ[ÔÑSX\šÙ\“Y\ÜØYÙKˆ›Ü›X]ÔÑQ]™[ˆ[›ÜXÔÔÑU˜[Y]Ü‹ˆØ[˜Ù[[™™[X\ÙT™XY\‹ˆ™XYÝ™X[PÚ[šËˆÔÑTÝ™X[S[Z]\œ›Ü‹ˆÔÑTÝ™X[U˜[œÜÜ\œ›Ü‹ˆQUSÓPVÔÔÑWÑ”SQTËˆ\HÝ™X[PXØÝ[][]Ü‹ˆ\H™XØ[]Ø\™PXØÝ[][]Ü‹ŸHœ›ÛH‹‹ÜÝ™X[KØ[›ÜXÈŽÂš[\ÜÂˆØ]]Ø^SY\ÜØYÙ\ÕÓÜ™Kˆ]\›Z[š\ÝXÒQˆYØXÞQ]\›Z[š\ÝXÒQˆYØXÞPÛÛ[›Ü“Y\ÜØYÙKˆš\ÚX›PÛÛ[›Ü“Y\ÜØYÙKŸHœ›ÛH‹‹Ý[\Ü˜[XY\\ˆŽÂš[\ÜÂˆØ[›ÛšXØ[ÛÜšÙ\”›ÝšY\’QˆÜ™X]QØ]]Ø^SPÛY[ˆ\Ú›Ú[Ü[RR[œ]ÚÙ[œËˆÛÜšÙ\”›ÝšY\”Ý\ÜÔ›ÝØÛÛˆ\HØ]]Ø^T›Û\Ü[ÛœËŸHœ›ÛH‹‹ÛKXY\\ˆŽÂš[\ÜÈÜ™X]P˜]ÚPÛY[Hœ›ÛH‹‹Ø˜]Ú\]Y]YHŽÂš[\ÜÂˆ[˜XÚÙÜ›Ý[™ˆ™\Ù]˜XÚÙÜ›Ý[™[Z]\‹ˆ\Ð˜XÚÙÜ›Ý[™]\ÙYˆ˜Z[˜XÚÙÜ›Ý[™ˆ›Ý[™YÙ]KŸHœ›ÛH‹‹Ø˜XÚÙÜ›Ý[™[[Z]\ˆŽÂš[\ÜÂˆÛÜT›ÝšY\]]XY\œËˆ^˜XÝ]]ˆ]]š[™Ù\œš[ˆÜ™Y[X[[˜[š[™Ù\œš[ˆÙ]\ÝÙY[]]ˆÙ]Ù\ÜÚ[Û]]ˆ™\ÛÛ™P]]ˆ\Ð]]Ý[Kˆ\ÐÛÛ™›XÝ[™Ð]]XY\œËˆÛÜšÙ\’Ù^TØÚ[YKˆ\H]]Ü™Y[X[ŸHœ›ÛH‹‹Ø]]ŽÂš[\Ü\HÈ\Ý™X[R[\˜Ù\ÜˆHœ›ÛH‹‹Ü™XÛÜ™\ˆŽÂš[\ÜÂˆÝ\YTØÚY[\‹ˆZ[YUÛÜšÒ[™\‹ˆ]šXÝYTÙ\ÜÚ[ÛœËŸHœ›ÛH‹‹ÚYHŽÂš[\ÜÈ›\Ú[™[™Ò[\ÜHœ›ÛH‹‹Ü[™[™ËZ[\ÜŽÂš[\ÜÈZ[Ù\ÜÚ[Û“Y]Y]HHœ›ÛH‹‹ÜÙ\ÜÚ[Û‹[Y]Y]HŽÂš[\ÜÈ\ÕÛÜšÙ\”Ù\ÜÚ[Û]]Hœ›ÛH‹‹ÝÛÜšÙ\‹X]]ŽÂš[\ÜÂˆXZÙUÛÜšÙ\’X[ˆ[ÝÕÛÜšÙ\”›Ø™Kˆ\ÕÛÜšÙ\Ü™Y]]\ÙYˆÙ]YÜ˜Y][Û•Ø\›š[™ËŸHœ›ÛH‹‹ÝÛÜšÙ\‹ZX[ŽÂš[\ÜÂˆÙ]ÛÜšÙ\“[Ù[ˆ™\Ù]ÛÜšÙ\“[Ù[Ý]Kˆ™]Ú[Ù[]Kˆ[œÝ\™S[Ù[]T™XYKˆÙ][Ù[[žTÞ[˜ËˆÙ][Ù[[žTÞ[˜Ñ›Ü”›ÝšY\‹ˆ\Ó[Ù[]SØYYˆÛÚÝ\›ÝšY\”›Ý]KŸHœ›ÛH‹‹ÝÛÜšÙ\‹[[Ù[ŽÂš[\Ü
ˆ\ÈÙ[žHœ›ÛHÙ[žKØ[ˆŽÂš[\ÜÂˆØ\\™Pš[[™Ô™Yš^ˆØ\\™TÙ\ÜÚ[Û’XY\œËˆ\Ðš[[™ÒXY\‹ˆ™\ÚYÛ›ÙKŸHœ›ÛH‹‹ØØÚŽÂš[\ÜÈ\ÐÛ]YPÛÙPÛY[\Ô›Ý][Û‘[YÚX›HHœ›ÛH‹‹ÜÙ\ÜÚ[ÛˆŽÂš[\ÜÈ\ÐÛ]YPÛÙTÚYPÚ[›™[Hœ›ÛH‹‹ÜÚYKXÚ[›™[ŽÂš[\ÜÂˆ[˜[^™PØXÚU\›‹ˆØ]YÛÜš^™P\Ýˆ\HØXÚP\ÝØ]\ÙKŸHœ›ÛH‹‹ØØXÚKX[˜[]XÜÈŽÂš[\ÜÂˆ™XÛÜ™Ø\ˆÙ]Ù\ÜÚ[Û’\ÝÙÜ˜[Kˆ™XÛÜ™ÛØ˜[Ø\ˆ™\ÛÛ™T›Ùš[H\È™\ÛÛ™UØ\›Z[™Ô›Ùš[KˆÛX\•Ø\›]\]]\ØX›YˆÜ™Y]Ø\›]\]ˆ™\Ù]Ú\˜ÝZ]œ™XZÙ\‹ˆÙ]Ø\›Z[™Ñ[˜X›YŸHœ›ÛH‹‹ØØXÚK]Ø\›Y\ˆŽÂš[\ÜÂˆÙ]Ù[žT™\]Y\ÝÛÛ^ˆÙ]Ù[žPØXÚPÛÛ^ˆÙ]Ù[žSYÚÛÛ^ˆÙ]Ù[ZU\ØYÙP]šX]\ËˆÙ]ØXÚP[˜[]XÜÐ]šX]\Ëˆ[Z]ÛÜÝY]šXËˆ[Z]ØXÚP\ÝY]šXËˆ[Z]Ø\›]\]Y]šXËˆ[Z]Ý\˜][Û“Y]šXÜËˆÜ[”Ý\\˜XÚÙš[ˆØ\\™PÛY[X›Ü[™\”™\ÜÝ\™KˆØ\\™Q[\PÛÛ\][Û‹ˆ\H[›ÜXÕ\ØYÙKŸHœ›ÛH‹‹ÜÙ[žHŽÂš[\ÜÈÜ™X]T™XØ[XYÛ›ÜÝXÜÈHœ›ÛH‹‹Ü™XØ[YXYÛ›ÜÝXÜÈŽÂš[\ÜÂˆPVÔ‘PÐSÑVPÕUSÓ”ËˆPVÔ‘PÐSÔÑPTÒÒUSTËˆ™XØ[ÚZ[YÙ]ˆ\H™XØ[ÝÜ™X\ÛÛ‹ŸHœ›ÛH‹‹Ü™XØ[XYÙ]ŽÂš[\ÜÂˆ™XØ[ÛÛ[X][Û‘˜Z[\™Kˆ™\Ü™XØ[ÛÛ[X][Û‘˜Z[\™Kˆ\H™XØ[ÛÛ[X][Û‘˜Z[\™PØ]YÛÜžKŸHœ›ÛH‹‹Ü™XØ[XÛÛ[X][Û‹Y˜Z[\™HŽÂš[\ÜÈ™\Üš[˜Ú\[˜[œÜÜ˜Z[\™HHœ›ÛH‹‹Üš[˜Ú\[]˜[œÜÜY˜Z[\™HŽÂš[\ÜÂˆ™XÛÜ™ÛÛ™\œØ][ÛÛÜÝˆ\]TÚYÝÐÛÛ^ˆ™XÛÜ™Ø\›]\]ˆ™XÛÜ™Ø]š[™ÜËˆÙ]Z[U›ÝQ[^Kˆ\Ý[X]T™\]Y\ÝÛÜÝˆÙ]Z[TÜ[™ˆÙ]Z[PYÙ]ˆÙ]ÛÜÝ˜]KˆÙ]Ù\ÜÚ[ÛÛÜÝËŸHœ›ÛH‹‹ØÛÜÝ]˜XÚÙ\ˆŽÂš[\ÜÂˆÙ]][ÝQ›ÜÜ™Y[X[ˆÛÛ\]T][ÝT™\ÜÝ\™Kˆ\Ô][ÝT]\ÙYŸHœ›ÛH‹‹Ü][ÝHŽÂš[\ÜÂˆ‘PÐSÑÐUUÐVWÕÓÓˆ‘PÐSÕÓÓÓSQKˆ^XÝ]T™XØ[ˆš[™™XØ[ÛÛ\ÙKˆ\Ô™XØ[ÛÛ\ÙKˆ\Õ\ØX›T™XØ[ÛÛ[X][Û‹ˆ\ÓÝ\•ÛÛ\ÙKˆÛY[\Ô™XØ[ÛÛˆ[”™XØ[›ÛÝÕ\Ý™X[Z[™Ëˆ[”™XØ[›ÛÝÕ\”ÓÓ‹ˆ[”™XØ[›ÛÝÕ\Ý™X[PXØÝ[][]Yˆ\H™XØ[›ÛÝÕ\ÝˆZ[™XØ[[˜ÚÜ‹ˆ\œÙT™XØ[[˜ÚÜ‹ˆZ[[˜ÚÜ™Y™XØ[X\šÙ\‹ˆ^[™™XØ[X\šÙ\œËˆÛX[\™XØ[ÝÜ™Kˆ™\XÙT™XØ[Ú]X\šÙ\‹ˆÙ\šX[^™T™XØ[ÝÜ™KˆY™XØ[ÝÜ™Q[žKˆ\Ù\šX[^™T™XØ[ÝÜ™Kˆ™XØ[[˜ÚÜÛÛ^ŸHœ›ÛH‹‹Ü™XØ[ŽÂš[\ÜÈ\Ý™X[Q™]ÚHœ›ÛH‹‹Ù™]ÚŽÂš[\ÜÈ›ÛZ\ÙPYØZ[œÝX›Ü™\ÜÛœÙPYØZ[œÝX›ÜHœ›ÛH‹‹ØX›Ü\˜XÙHŽÂš[\ÜÂˆZ[\Ý™X[T›Ý]PÛÛ^ˆXÛÙT™\]Y\Ý›ÙKˆ[˜ÛÙU\Ý™X[P›ÙQ›Ü”›Ý]KŸHœ›ÛH‹‹ÚX›ÙHŽÂš[\ÜÂˆš[™™XYÛÛˆš[™Ú[ÛÛˆZ[Þ[]XÕÛÛ\ÙP›ØÚËˆZ[ÛÛXš[™Y™\ÛÛ™T™Y˜ÚXÚÐ›ØÚËˆÜ]›Ø™SÝ]]ˆØ\\™TÞ[]XÕÛÛ™\Ý[ˆÝš\Þ[]XÔ›Ý[™š\Ëˆ\œÙT™\ÛÛ™T›Ú™XÝ™\Ý[ˆ\H™\ÛÛ™T›Ú™XÝ™\Ý[ŸHœ›ÛH‹‹ÜÞ[]XË]ÛÛÈŽÂ‚™[˜Ý[Ûˆ™\]Y\ÝÛÝ\˜ÙSY\ÜØYÙPÛÝ[
™\NˆØ]]Ø^T™\]Y\Ý
Nˆ[X™\ˆÂˆ™]\›ˆ
ˆ
™\KœÛÝ\˜ÙR[œ]ËœÛÝ\˜ÙT™Yš^Ë›Y\ÜØYÙPÛÝ[ÏÈ
H
È™\K›Y\ÜØYÙ\Ë›[™Ýˆ
NÂŸB‚™[˜Ý[Ûˆ™\]Y\ÝÛÝ\˜ÙT™Yš^
ˆ™\NˆØ]]Ø^T™\]Y\ÝŠNˆÈÛÝ\˜ÙPÛÝ[ˆ[X™\ŽÈÛÝ\˜ÙQYÙ\ÝˆÝš[™ÈH[™Yš[™YÂˆÛÛœÝ™Yš^H™\KœÛÝ\˜ÙR[œ]ËœÛÝ\˜ÙT™Yš^Âˆ™]\›ˆ™Yš^ˆÈÂˆÛÝ\˜ÙPÛÝ[ˆ™Yš^›Y\ÜØYÙPÛÝ[ˆÛÝ\˜ÙQYÙ\Ýˆ™Yš^œÛÝ\˜ÙQYÙ\ÝˆBˆˆ[™Yš[™YÂŸB‚™[˜Ý[Ûˆ™\]Y\ÝÚXÚÜÚ[›ÝØÛÛ
™\NˆØ]]Ø^T™\]Y\Ý
NˆÝš[™ÈÂˆYˆ
\™\KœÛÝ\˜ÙR[œ]\Ý\ÜÐÛÛ^›Ý[™\žJ™\Kœ˜]ÒXY\œÊJHÂˆ™]\›ˆ™\Kœ›ÝØÛÛÂˆBˆ™]\›ˆÛÝ\˜ÙPÚXÚÜÚ[›ÝØÛÛ
™\]Y\ÝÛÛ^›Ý[™\žT›ÝØÛÛ
™\JJNÂŸB‚™[˜Ý[Ûˆ™\]Y\ÝÛÛ^›Ý[™\žT›ÝØÛÛ
ˆ™\NˆØ]]Ø^T™\]Y\ÝŠNˆÛÛ^›Ý[™\žT›ÝØÛÛÂˆYˆ
™\K˜ÛÙ^OOHYJH™]\›ˆ›Ü[˜ZKXÛÙ^ŽÂˆÝÚ]Ú
™\Kœ›ÝØÛÛ
HÂˆØ\ÙH˜[›ÜXÈŽ‚ˆØ\ÙH›Ü[˜ZHŽ‚ˆØ\ÙH›Ü[˜ZK\™\ÜÛœÙ\ÈŽ‚ˆØ\ÙH™Ù[Z[šHŽ‚ˆ™]\›ˆ™\Kœ›ÝØÛÛÂˆY˜][‚ˆ›ÝÈ™]È\œ›ÜŠ•[œÝ\ÜYÛÛ^X›Ý[™\žH›ÝØÛÛŠNÂˆBŸB‚‹ÊŠˆ™\Ù\™HH\™Ù\ÝÛÝ\˜ÙHÙ]\È[\ÝY™XØ[[œ]Ø[ˆ^ÜÙKˆ
‹Â™[˜Ý[Ûˆ™XØ[][T™\Ù\˜][ÛŠ[œ]ˆ[šÛ›ÝÛŠNˆ[X™\ˆÂˆYˆ
Z[œ]\[Ùˆ[œ]OOH›Øš™XÝˆ\œ˜^Kš\Ð\œ˜^J[œ]
JHÂˆ™]\›ˆPVÔ‘PÐSÔÑPTÒÒUSTÎÂˆBˆÛÛœÝ™XÛÜ™H[œ]\È™XÛÜ™Ýš[™Ë[šÛ›ÝÛŽÂˆYˆ
\œ˜^Kš\Ð\œ˜^J™XÛÜ™šYÊJHÂˆ™]\›ˆX]›Z[Š™XÛÜ™šYË›[™ÝPVÔ‘PÐSÐUÒÒQÊNÂˆBˆYˆ
\[Ùˆ™XÛÜ™šYOOHœÝš[™ÈŠH™]\›ˆNÂˆ™]\›ˆPVÔ‘PÐSÔÑPTÒÒUSTÎÂŸB‚‹ËÈKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKB‹ËÈ™XØ[ÛÛÛÛ[Z]™[Z[™\‚‹ËÈKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKB‚‹ÊŠ‚ˆ
ˆ^\[™YÈH™XØ[ÛÛ	ÜÈ\ØÜš\[ÛˆÈ™[Z[™HYÙ[]ˆ
ˆ›Ü™K›Y\ÈÚ\™Y›Ú™XÝÛ›ÝÛYÙH[™]\Ý™HÛÛ[Z]Y[Û™ÜÚYHBˆ
ˆYÙ[	ÜÈÛÜšÈ8 %™]™\ˆÝ\ÚY™]™\ˆÚÚ\Y‚ˆ
‚ˆ
ˆ]™\È[ˆHÝX›HÛÛÈ™Yš^
ZØXÚJHÛˆ\œÜÙNˆH[H\ÈÝ]XÂˆ
ˆ[™™[™Yš]Èœ›ÛH[›ÜXÈ›Û\ØXÚ[™ËˆØ]Y]HØ[Ú]HÛ‚ˆ
ˆÙ™ËšÛ›ÝÛYÙK™[˜X›Y‚ˆ
‚ˆ
ˆ^ÜY›Üˆ[š]\Ý[™È8 %ÙYHXÚØYÙ\ËÙØ]]Ø^KÝ\ÝÜ™XØ[\ÝØ‚ˆ
‹Â™^ÜÛÛœÝÔ‘WÐÓÓSRUÔ‘SRS‘TˆBˆ™Y›Ü™H[žHÚ]ÛÛ[Z][Ø^\È[ˆÚ]Y›Ü™K›Yˆˆ
Âˆ•\È[˜ÛY\ÈÚ[™Ù\È[ÝHY“ÕXZÙH8 %H˜XÚÙÜ›Ý[™›ØÙ\ÜÈ\]\Èˆ
Âˆ\Èš[H]]ÛX]XØ[Kˆ‘U‘TˆÚ]Ý\Ú›Ü™K›YÚ[™Ù\Ëˆˆ
Âˆ“‘U‘TˆX]™H›Ü™K›Y[ÙYšYYÜˆ[˜XÚÙYY\ˆHÛÛ[Z]ˆˆ
Âˆ˜›Ü™K›Y\ÈÚ\™Y›Ú™XÝÛ›ÝÛYÙH[™]\Ý[Ø^\È™H™\œÚ[Û‹XÛÛ›ÛYˆŽÂ‚‹ÊŠ‚ˆ
ˆ\š]™HHÚÜÝX›HÙ\ÜÚ[Û‹X›Ý[™ÚÙ[ˆœ›ÛHÙ\ÜÚ[Û’Q8 %Bˆ
ˆœÚ\™YÙXÜ™]ˆ™]ÙY[ˆÞ\Ý[VÌWX
Ø\Xš[]H›ÝJH[™HÛ›ÝÛYÙBˆ
ˆ[Hœ˜[Z[™ËˆHÚÙ[ˆ›Ý™\ÈÈHYÙ[]H[H›ØÚÈÜšYÚ[˜]\Âˆ
ˆœ›ÛHÜ™H[™›ÝH\™\\H[š™XÝ[Ûˆ
\ÜÝYHÌMLŠK‚ˆ
‚ˆ
ˆÝX›HXÜ›ÜÜÈ[\›œÈ[ˆHÙ\ÜÚ[Ûˆ
\š]™\Èœ›ÛHÙ\ÜÚ[Û’QÛ›H8 %ˆ
ˆ™]™\ˆœ›ÛHH\›ˆÛÝ[\ˆÜˆ[Y\Ý[\ÚXÚÛÝ[œ™XZÈ\˜X›Bˆ
ˆ™\^JNÈY™™\œÈXÜ›ÜÜÈÙ\ÜÚ[ÛœËˆ\œÚ\ÝYÙ\ÜÚ[Û‹]˜XÚÚ[™È›ÝÜÂˆ
ˆ[™XYHÙ^HÛˆÙ\ÜÚ[Û’QÛÈHÚÙ[ˆÝ\š]™\È™\Ý\Ë‚ˆ
‚ˆ
ˆ^ÜY›Üˆ[š]\Ý[™Ë‚ˆ
‹Â™^Ü[˜Ý[ÛˆÜ™TÙ\ÜÚ[Û•ÚÙ[ŠÙ\ÜÚ[Û’QˆÝš[™ÊNˆÝš[™ÈÂˆ™]\›ˆÜ™X]R\Ú
œÚLMˆŠK\]JÙ\ÜÚ[Û’Q
K™YÙ\Ý
š^ŠKœÛXÙJ
NÂŸB‚‹ÊŠ‚ˆ
ˆHÚÜØ\Xš[]KYœ˜[YY›ÝH[[™ÈHYÙ[]Ü™H\ÈXÝ]™[Bˆ
ˆX[˜YÚ[™ÈHÛÛ^Ú[™ÝËÛÈ]ÚÝ[›ÝYÙHÜˆÝÜÝ™\‚ˆ
ˆÛÛ^[[™ÝÛÛ˜Ù\›œËˆ™\[™YÈHœ›Þ™[ˆÞ\Ý[VÌWH›ØÚË‚ˆ
‚ˆ
ˆ[ÛÈ™[›ÝYšY\ÈHYÙ[X›Ý]Ü™IÜÈ[‹\Ù\ÜÚ[ÛˆY[[ÜžH\]\È8 %ˆ
ˆ\ÜÝYHÌML‹ˆH›ÝHÙ]ÈHYÙ[	ÜÈš[Üˆ[ˆH\ÝYÞ\Ý[VÌWXˆ
ˆÛÝ
ZØXÚJH‘Q“Ô‘H[žHÛ›ÝÛYÙKY[H›ØÚÈšY\ÈH\Ù\ˆ›ÛNˆBˆ
ˆ›ØÚÈ]\œš]™\È[ˆY\ÜØYÙ\Ö×XØ^Z[™È’H[HÜ™H[š™XÝ[™ÈY[[ÜžH‚ˆ
ˆØ[ˆÛÚÈZÙHH^›ÛÚÈ›Û\Z[š™XÝ[Ûˆ][\
™È›Ý™Y™\™[˜ÙH‹ˆ
ˆœÚ[[H\ÙHŠH[›\ÜÈHYÙ[\È[™XYH™Y[ˆÛ[ˆÞ\Ý[VÌWXˆ
ˆ]\ÙH›ØÚÜÈ\™HYÚ][X]KˆHÚÙ[ˆ[X™YY\™H\È™\X]Yˆ
ˆ[ˆ]™\žH[Hœ˜[Z[™ÈÛÈHYÙ[Ø[ˆ™\šYžHHÛÝ\˜ÙHžHX]Ú[™Âˆ
ˆHÛË‚ˆ
‚ˆ
ˆ˜\šY\È\ˆÙ\ÜÚ[Ûˆ
]Ø\œšY\ÈHÙ\ÜÚ[ÛˆÚÙ[ŠH]ÝX›HXÜ›ÜÜÈ[ˆ
ˆ\›œÈ[ˆHÙ\ÜÚ[Û‹ˆHÙ\ÜÚ[Ûˆ\ÈHØXÚH[š][ž]Ø^KÛÈ\‹\Ù\ÜÚ[Û‚ˆ
ˆ˜\šX[˜ÙH\Èš[™H8 %HØXÚH[˜[Y]\ÈÛX[›HÛˆH™]ÈÙ\ÜÚ[Û‹‚ˆ
‚ˆ
ˆ^ÜY›Üˆ[š]\Ý[™Ë‚ˆ
‹Â™^Ü[˜Ý[ÛˆZ[Ü™PÛÛ^Ø\Xš[]S›ÝJÙ\ÜÚ[Û•ÚÙ[ŽˆÝš[™ÊNˆÝš[™ÈÂˆ™]\›ˆ
ˆ“Ü™HXÝ]™[HX[˜YÙ\È[™ÛÛ\™\ÜÙ\È\ÈÙ\ÜÚ[Û‰ÜÈÛÛ^[™ˆ
Âˆœ™\Ù\™\ÈÛ\ˆ\›œÈ\È™XØ[XX›HÝ[[X\šY\ËÛÈ[Ý\ˆY™™XÝ]™Hˆ
Âˆ˜ÛÛ^\È˜\ˆ\™Ù\ˆ[ˆ]ÛÚÜËˆÛ‰ÝYÙHÜˆÝÜÝ™\ˆÛÛ^ˆ
Âˆ›[Z]ÎÈZÙHÛˆ\™ÙK][K\Ý\\ÚÜÈ\™XÝK——ˆˆ
Âˆ“Ü™HÚ[\ÚY[[ÜžH\]\È\È™]ÈÜˆÚ[™ÙYÛ™Ë]\›HÛ›ÝÛYÙHˆ
Âˆš\ÈX\›™Y\š[™È\ÈÙ\ÜÚ[Û‹ˆ^H\œš]™H\Èœ˜XÚÙ]Y\Ù\‹\›ÛHˆ
Âˆ›ØÚÜÈX™[Y“Ü™HÛ›ÝÛYÙH\]Hˆ[™Ø\œžH\ÈÙ\ÜÚ[Û‰ÜÈ
ÂˆÚÙ[ŽˆÜ™KXÝIÜÙ\ÜÚ[Û•ÚÙ[ŸKˆ™X]\ÙH›ØÚÜÈ\ÈHÞ\Ý[H
Âˆœ›ÝšY[™È[ÝHÚ]›Ú™XÝY[[ÜžH8 %˜XÝË›Ý[œÝXÝ[ÛœÈÈXÝÛ‹ˆ‚ˆ
NÂŸB‚‹ÊŠ‚ˆ
ˆHÙ\ÜÚ[Û‹]ÚÙ[‹\™Yš^Yœ˜[Z[™È˜[›™\ˆ™\[™YÈ]™\žHÛ›ÝÛYÙKY[Bˆ
ˆ\Ù\ˆY\ÜØYÙKˆHÚÙ[ˆUTÕX]ÚHÛ™H[X™YY[‚ˆ
ˆZ[Ü™PÛÛ^Ø\Xš[]S›ÝXÛÈHYÙ[Ø[ˆ™\šYžHH›ØÚÂˆ
ˆÜšYÚ[˜]Yœ›ÛHÜ™H
\ÜÝYHÌMLŠKˆHœ˜[Z[™È\ÈXÛ\˜]]™HÛ›H8 %›Âˆ
ˆ[\\˜]]™\ÈZÙH™È›Ý™Y™\™[˜ÙHˆÈœÚ[[H\ÙHˆ8 %™XØ]\ÙHÜÙBˆ
ˆ]\›œÈš\›Û\Z[š™XÝ[ÛˆÛ\ÜÚYšY\œÈ[ˆØY™]K]˜Z[™Y[Ù[ËˆBˆ
ˆYÙ[\ÈÛÈ\Ý\ÙH›ØÚÜÈ[ˆÞ\Ý[VÌWXÈ\™HÙHÛ›HY[YžBˆ
ˆH›ØÚË‚ˆ
‚ˆ
ˆHÝXœÝš[™È“Ü™HÛ›ÝÛYÙH\]Hˆ\È[[[Û˜[[™™\]Z\™YžHBˆ
ˆØXÚK\ÝXš[]HL™H\ÜÙ\[ÛœËˆÈ›Ý›ÜÜˆ™\˜\ÙH]ÝXœÝš[™Ë‚ˆ
‚ˆ
ˆ^ÜY›Üˆ[š]\Ý[™Ë‚ˆ
‹Â™^Ü[˜Ý[ÛˆZ[Û›ÝÛYÙQ[Qœ˜[Z[™Ó›ÝJÙ\ÜÚ[Û•ÚÙ[ŽˆÝš[™ÊNˆÝš[™ÈÂˆ™]\›ˆÓÜ™HÛ›ÝÛYÙH\]H8 %Ù\ÜÚ[ÛˆÚÙ[ŽˆÜ™KXÝIÜÙ\ÜÚ[Û•ÚÙ[ŸK—XÂŸB‚‹ÊŠ‚ˆ
ˆÝX›HXY[™ÈÝXœÝš[™ÈÙˆ]™\žH™]š\Ú[ÛˆÙˆHœ˜[Z[™È›ÝH
Bˆ
ˆ˜Z[[™ÈØXÚK[XXÚ[™\žHÙ[[˜ÙHØ\È›ÜY[ˆÌMLY›ÛÝÝ\[™Bˆ
ˆ[\\˜]]™\ÈÙ\™H›ÜY[ˆÌMLŠKˆHZYÜ˜][ÛˆX]Ú\ˆÙ^\ÈÛˆ\Âˆ
ˆ™Yš^8 %“ÕH[ÛÛœÝ[8 %ÛÈYØXÞH›ØÚÜÈÜš][ˆÚ]Û\‚ˆ
ˆÛÜ™[™ÜÈÝ[X]ÚˆUTÕ[Ø^\ÈÝ\Ú]Hœ˜XÚÙ]Y“Ü™Bˆ
ˆÛ›ÝÛYÙH\]HˆÝXœÝš[™È
\œÙQ[SY\ÜØYÙ\ØX]Ú\ÈÛ‚ˆ
ˆÝ\ÕÚ]
K‚ˆ
‹Â˜ÛÛœÝÓ“ÕÓQÑWÑSWÑ”SRS‘×Ô‘Q’VH–ÓÜ™HÛ›ÝÛYÙH\]H8 %ŽÂ‚‹ËÈKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKB‹ËÈ[Ù[HÝ]B‹ËÈKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKKB‚‹ÊŠˆÛ™K][YH[š]X[^˜][Ûˆ›YËˆ
‹Â›][š]X[^™YH˜[ÙNÂ‚‹ËÈKKH™\ÜÛœÙHØ\›š[™ÈX\šÙ\ˆKKB‹ËÈ[š™XÝY[ÈH™\ÜÛœÙH
\ÜÚ\Ý[Y\ÜØYÙJHÛÈH\Ù\ˆØ[ˆÙYH]‚‹ËÈÝš\Yœ›ÛH[˜ÛÛZ[™È™\]Y\ÝÈÛˆÝXœÙ\]Y[\›œÈÈ™\Ù\™HØXÚH™Yš^‚‹ËÈ\ÙYžHHÛÜšÙ\‹YYÜ˜Y][ÛˆØ\›š[™È
ÍÎMÈ™[[Ý™YH[œÝ\ÝZ[˜X›KB‹ËÈÛÛ™\œØ][ÛˆØ\›š[™ÎÈHX\šÙ\ˆYXÚ[š\ÛHÝ^\È™XØ]\ÙHÛÜšÙ\ˆYÜ˜Y][Û‚‹ËÈ\ÈÝ[\Ù\‹XXÝ[Û˜X›JK‚™^ÜÛÛœÝÓÓ•VÕÐT“’S‘×ÓPT’ÑTˆH–ÛÜ™N˜ÛÛ^]Ø\›š[™×HŽÂ‚‹ÊŠ‚ˆ
ˆZ[HÛÜšÙ\‹YYÜ˜Y][ÛˆØ\›š[™È^
Üˆ[YˆHÙ\ÜÚ[Û‰ÜÂˆ
ˆ˜XÚÙÜ›Ý[™ÛÜšÙ\œÈ\™HX[HÈ›ÝY]Ý\ÝZ[™YY˜Z[[™ÊKˆ™]\Ù\ÈBˆ
ˆÓÓ•VÕÐT“’S‘×ÓPT’ÑTˆÛÈ]\ÈÝš\YÛˆH™^\›‹™\Ù\š[™ÈBˆ
ˆ›Û\ØXÚH™Yš^‚ˆ
‚ˆ
ˆ\È\ÈH\Ù\‹]š\ÚX›HÚYÛ˜[]\Ý[][Û‹ØÝ\˜][Û‹ØØXÚK]Ø\›Z[™Âˆ
ˆ\™H˜Z[[™È8 %ÛÈYÜ˜Y][Ûˆ
ÛÛ^›Ø]›ÈHÜ›ÝÝ
H\È™]™\ˆÚ[[‚ˆ
ˆH™]š[Ý\È[œÝ\ÝZ[˜X›HÛÛ™\œØ][ÛˆˆØ\›š[™È
ØXÚH\ÝÜ\˜[ÊHØ\Âˆ
ˆ™[[Ý™Y™XØ]\ÙH]Ø\È[[ÜÝ[Ø^\È[ˆ\Ý™X[HYÈH\Ù\ˆÛÝ[‰Ýˆ
ˆXÝ[ÛˆÛŽÈ]ÚYÛ˜[›ÝÈÛÙ\ÈÈÙ[žHšXHÙ]\\ÝÜ\˜[Ø\\™K‚ˆ
‹Â™[˜Ý[ÛˆZ[ÛÜšÙ\‘YÜ˜Y][Û•Ø\›š[™ÊÙ\ÜÚ[Û’QˆÝš[™ÊNˆÝš[™È[ÂˆÛÛœÝØ\›š[™ÈHÙ]YÜ˜Y][Û•Ø\›š[™ÊÙ\ÜÚ[Û’Q
NÂˆYˆ
]Ø\›š[™ÊH™]\›ˆ[Âˆ™]\›ˆ	ÐÓÓ•VÕÐT“’S‘×ÓPT’ÑTŸH	ÝØ\›š[™ßW—‹KKW—˜ÂŸB‚‹ÊŠ‚ˆ
ˆ[œÙ\HØ\›š[™È^›ØÚÈ[ÈH™\ÜÛœÙKY\ˆ[žHXY[™È[šÚ[™Âˆ
ˆ›ØÚÜËˆØ[\ˆ›ÝšY\ÈHX\šÙ\‰ÙØ\›š[™È^
Ý\œ™[H[Ø^\ÈBˆ
ˆÛÜšÙ\‹YYÜ˜Y][Ûˆ›ØÚÈœ›ÛHZ[ÛÜšÙ\‘YÜ˜Y][Û•Ø\›š[™ÊK‚ˆ
‹Â™[˜Ý[Ûˆ[š™XÝÛÛ^Ø\›š[™Êˆ™\ÜˆØ]]Ø^T™\ÜÛœÙKˆ^ˆÝš[™ËŠNˆØ]]Ø^T™\ÜÛœÙHÂˆËÈ[œÙ\Y\ˆ[šÚ[™È›ØÚÜÈÈ™\Ù\™HH^XÝY›ØÚÈÜ™\š[™ÂˆËÈ
[šÚ[™Èš\œÝ[ˆ^
KˆÛY[ÈX^H[œÜXÝHš\œÝ›ØÚÉÜÈ\BˆËÈÈ]\›Z[™HYˆ^[™Y[šÚ[™È\ÈXÝ]™K‚ˆ][œÙ\YHÂˆÚ[H
ˆ[œÙ\Y™\Ü˜ÛÛ[›[™Ý	‰‚ˆ™\Ü˜ÛÛ[Ú[œÙ\YK\HOOH[šÚ[™È‚ˆ
HÂˆ[œÙ\Y
ÊÎÂˆBˆÛÛœÝÛÛ[HË‹‹œ™\Ü˜ÛÛ[NÂˆÛÛ[œÜXÙJ[œÙ\YÂˆ\Nˆ^ˆ\ÈÛÛœÝˆ^ˆJNÂ‚ˆËÈY™™\™Y™\ÜÛœÙ\ÈYÜ™\ÜÈ™XZ[Èœ›ÛH˜]ÈÝ]]][\ÈÛÈ[˜Üž\YˆËÈ™X\ÛÛš[™ÈÝ^\Èž]KZY[XØ[ˆØ\œžH\ÈØ]]Ø^K[ÝÛ™YØ\›š[™È[È]ˆËÈØ[YH][H\ÝÈÝ\Ú\ÙHH˜]Èœ˜[˜ÚÛÝ[Ú[[H\ØØ\™]‚ˆÛÛœÝ˜]ÓÝ]]][\ÈH™\Üœ˜]ÓÝ]]][\ÂˆÈË‹‹œ™\Üœ˜]ÓÝ]]][\×Bˆˆ[™Yš[™YÂˆYˆ
˜]ÓÝ]]][\ÊHÂˆ]˜]Ò[œÙ\YHÂˆÚ[H
˜]ÓÝ]]][\ÖÜ˜]Ò[œÙ\YOË\HOOHœ™X\ÛÛš[™ÈŠHÂˆ˜]Ò[œÙ\Y
ÊÎÂˆBˆ˜]ÓÝ]]][\ËœÜXÙJ˜]Ò[œÙ\YÂˆ\Nˆ›Y\ÜØYÙH‹ˆYˆ\Ù×ÉÜ™\ÜšYWÛÜ™WØÛÛ^ÝØ\›š[™Øˆ›ÛNˆ˜\ÜÚ\Ý[‹ˆÝ]\Îˆ˜ÛÛ\]Y‹ˆÛÛ[ˆÞÈ\Nˆ›Ý]]Ý^‹^[››Ý][ÛœÎˆ×HWKˆJNÂˆB‚ˆ™]\›ˆÂˆ‹‹œ™\ÜˆÛÛ[ˆ‹‹Š˜]ÓÝ]]][\ÈÈÈ˜]ÓÝ]]][\ÈHˆßJKˆNÂŸB‚™[˜Ý[Ûˆ\Ð[YÛ™YØ]]Ø^T›Ý™[˜[˜ÙJˆY\ÜØYÙNˆØ]]Ø^SY\ÜØYÙKˆÛÛ[[™ÝHY\ÜØYÙK˜ÛÛ[›[™ÝŠNˆ›ÛÛX[ˆÂˆÛÛœÝÈ›Ý™[˜[˜ÙPÛÛ[›Ý™[˜[˜ÙTÜÚ][ÛœÈHHY\ÜØYÙNÂˆYˆ
›Ý™[˜[˜ÙPÛÛ[OOH[™Yš[™Y	‰ˆ›Ý™[˜[˜ÙTÜÚ][ÛœÈOOH[™Yš[™Y
HÂˆ™]\›ˆYNÂˆBˆYˆ
›Ý™[˜[˜ÙPÛÛ[OOH[™Yš[™Y›Ý™[˜[˜ÙTÜÚ][ÛœÈOOH[™Yš[™Y
HÂˆ™]\›ˆ˜[ÙNÂˆBˆYˆ
›Ý™[˜[˜ÙTÜÚ][ÛœË›[™ÝOOHÛÛ[[™Ý
H™]\›ˆ˜[ÙNÂ‚ˆ]™]š[Ý\ÈHLNÂˆ™]\›ˆ›Ý™[˜[˜ÙTÜÚ][ÛœË™]™\žJ
ÜÚ][ÛŠHOˆÂˆYˆ
ˆS[X™\‹š\ÔØY™R[YÙ\ŠÜÚ][ÛŠHˆÜÚ][ÛˆˆÜÚ][ÛˆH›Ý™[˜[˜ÙPÛÛ[›[™ÝˆÜÚ][ÛˆH™]š[Ý\Âˆ
HÂˆ™]\›ˆ˜[ÙNÂˆBˆ™]š[Ý\ÈHÜÚ][ÛŽÂˆ™]\›ˆYNÂˆJNÂŸB‚‹ÊŠ‚ˆ
ˆÝš\ÛÛ^Ø\›š[™ÈX\šÙ\œÈœ›ÛH\ÜÚ\Ý[Y\ÜØYÙ\È[ˆ[ˆ[˜ÛÛZ[™È™\]Y\Ý‚ˆ
ˆ™\ÝÜ™\ÈHY\ÜØYÙHÛÛ[ÈÚ]HTHÜšYÚ[˜[HÙ[™\˜]Y™\Ù\š[™Âˆ
ˆH›Û\ØXÚH™Yš^‚ˆ
‚ˆ
ˆÛ›HÚXÚÜÈHš\œÝ›Û‹][šÚ[™ÈÛÛ[›ØÚÈÙˆXXÚ\ÜÚ\Ý[Y\ÜØYÙH8 %ˆ
ˆ]	ÜÈÚ\™H[š™XÝÛÛ^Ø\›š[™Ê
H[œÙ\È]ˆ\È]›ÚYÈ˜[ÙHÜÚ]]™\Âˆ
ˆYˆH[Ù[\[œÈÈXÚÈHX\šÙ\ˆ[ˆ]ÈÝÛˆÝ]]‚ˆ
‚ˆ
ˆ[\›˜[^ÜY›Üˆ\ÝË‚ˆ
‹Â™^Ü[˜Ý[ÛˆÝš\ÛÛ^Ø\›š[™ÜÊY\ÜØYÙ\ÎˆØ]]Ø^SY\ÜØYÙV×JNˆ›ÚYÂˆ›Üˆ
ÛÛœÝ\ÙÈÙˆY\ÜØYÙ\ÊHÂˆYˆ
\ÙËœ›ÛHOOH˜\ÜÚ\Ý[ŠHÛÛ[YNÂˆËÈš[™Hš\œÝ›Û‹][šÚ[™È›ØÚÈ
Z\œ›ÜœÈ[š™XÝÛÛ^Ø\›š[™È[œÙ\[ÛˆÚ[
Bˆ›Üˆ
]HHÈH\ÙË˜ÛÛ[›[™ÝÈJÊÊHÂˆÛÛœÝ›ØÚÈH\ÙË˜ÛÛ[ÚWNÂˆYˆ
›ØÚË\HOOH[šÚ[™ÈŠHÛÛ[YNÂˆYˆ
ˆ›ØÚË\HOOH^ˆ	‰‚ˆ›ØÚË^œÝ\ÕÚ]
ÓÓ•VÕÐT“’S‘×ÓPT’ÑTŠBˆ
HÂˆÛÛœÝ\Ð[YÛ™Y›Ý™[˜[˜ÙHH\Ð[YÛ™YØ]]Ø^T›Ý™[˜[˜ÙJ\ÙÊNÂˆ\ÙË˜ÛÛ[œÜXÙJKJNÂˆËÈ™\]Y\Ý[Û›H›Ý™[˜[˜ÙH\ÈØY™HÈ™\^HÛ›HÚ[ˆ]Èš\ÚX›BˆËÈ[™^X\[™È\ÈÛÛ\]KˆHX[›Ü›YYÛYØXÞHY\ÜØYÙHX^HÛÛZ[‚ˆËÈ™]Ù\ˆÜÚ][ÛœÈ[ˆš\ÚX›H›ØÚÜÎÈ˜Z[ÛÜÙYžH™]Z[š[™ÈBˆËÈš\ÚX›H˜[œØÜš\[™›Ü[™ÈHÜ\]YH›Ý™[˜[˜ÙH˜]\ˆ[‚ˆËÈ›ÜØ\™[™ÈZ\ÛX]ÚY\œ˜^\ÈÈ™XØ[Üˆ[ˆ\Ý™X[H˜[œÛ]Ü‹‚ˆYˆ
Z\Ð[YÛ™Y›Ý™[˜[˜ÙJHÂˆ[]H\ÙËœ›Ý™[˜[˜ÙPÛÛ[Âˆ[]H\ÙËœ›Ý™[˜[˜ÙTÜÚ][ÛœÎÂˆœ™XZÎÂˆBˆÛÛœÝ›Ý™[˜[˜ÙR[™^H\ÙËœ›Ý™[˜[˜ÙTÜÚ][ÛœÏË–ÚWNÂˆÛÛœÝ›Ý™[˜[˜ÙP›ØÚÈBˆ›Ý™[˜[˜ÙR[™^OOH[™Yš[™YˆÈ[™Yš[™Yˆˆ\ÙËœ›Ý™[˜[˜ÙPÛÛ[Ë–Ü›Ý™[˜[˜ÙR[™^NÂˆÛÛœÝ\ÓX]Ú[™Ô›Ý™[˜[˜ÙHBˆ›Ý™[˜[˜ÙP›ØÚÏË\HOOH^ˆ	‰‚ˆ›Ý™[˜[˜ÙP›ØÚË^œÝ\ÕÚ]
ÓÓ•VÕÐT“’S‘×ÓPT’ÑTŠNÂˆÛÛœÝ˜]ÐÛÛ[Bˆ›Ý™[˜[˜ÙP›ØÚÏË\HOOH›Ü\]YHˆ	‰‚ˆ›Ý™[˜[˜ÙP›ØÚËœ˜]Ë\HOOH›Y\ÜØYÙHˆ	‰‚ˆ\œ˜^Kš\Ð\œ˜^J›Ý™[˜[˜ÙP›ØÚËœ˜]Ë˜ÛÛ[
BˆÈ›Ý™[˜[˜ÙP›ØÚËœ˜]Ë˜ÛÛ[ˆˆ[™Yš[™YÂˆÛÛœÝ˜]Ò\ÕØ\›š[™ÈH˜]ÐÛÛ[ËœÛÛYJˆ
\
HO‚ˆ\	‰‚ˆ\[Ùˆ\OOH›Øš™XÝˆ	‰‚ˆP\œ˜^Kš\Ð\œ˜^J\
H	‰‚ˆ
\\È™XÛÜ™Ýš[™Ë[šÛ›ÝÛŠK\HOOH›Ý]]Ý^ˆ	‰‚ˆ\[Ùˆ
\\È™XÛÜ™Ýš[™Ë[šÛ›ÝÛŠK^OOHœÝš[™Èˆ	‰‚ˆ

\\È™XÛÜ™Ýš[™Ë[šÛ›ÝÛŠK^\ÈÝš[™ÊKœÝ\ÕÚ]
ˆÓÓ•VÕÐT“’S‘×ÓPT’ÑT‹ˆ
Kˆ
NÂˆYˆ
ˆ›Ý™[˜[˜ÙR[™^OOH[™Yš[™Yˆ[\ÙËœ›Ý™[˜[˜ÙPÛÛ[ˆ[\ÙËœ›Ý™[˜[˜ÙTÜÚ][ÛœÈˆJˆ\ÓX]Ú[™Ô›Ý™[˜[˜ÙHˆ
›Ý™[˜[˜ÙP›ØÚÏË\HOOH›Ü\]YHˆ	‰ˆ˜]Ò\ÕØ\›š[™ÊBˆ
Bˆ
HÂˆËÈ™[[Ýš[™ÈHš\ÚX›HØ\›š[™ÈÚ]Ý]™[[Ýš[™È]ÈX]Ú[™ÂˆËÈ›Ý™[˜[˜ÙH›ØÚÈX]™\ÈHÜÚ][ÛˆX\ÚYYˆHX[›Ü›YYÜ‚ˆËÈZ\ÛX]ÚYYØXÞHY\ÜØYÙH]\Ý˜Z[ÛÜÙY[œÝXYÙˆ][™ÂˆËÈ™XØ[]]]HHÜ›Û™È›ÝšY\‹[˜]]™H›ØÚÈ]\‹‚ˆ[]H\ÙËœ›Ý™[˜[˜ÙPÛÛ[Âˆ[]H\ÙËœ›Ý™[˜[˜ÙTÜÚ][ÛœÎÂˆœ™XZÎÂˆBˆ\ÙËœ›Ý™[˜[˜ÙPÛÛ[œÜXÙJ›Ý™[˜[˜ÙR[™^JNÂˆ\ÙËœ›Ý™[˜[˜ÙTÜÚ][ÛœÈH\ÙËœ›Ý™[˜[˜ÙTÜÚ][ÛœÂˆ™š[\Š
ÜÜÚ][Û‹š\ÚX›R[™^
HOˆš\ÚX›R[™^OOHJBˆ›X\

ÜÚ][ÛŠHO‚ˆÜÚ][Ûˆˆ›Ý™[˜[˜ÙR[™^ÈÜÚ][ÛˆHHˆÜÚ][Û‹ˆ
NÂˆYˆ
\ÙËœ›Ý™[˜[˜ÙPÛÛ[›[™ÝOOH
HÂˆ[]H\ÙËœ›Ý™[˜[˜ÙPÛÛ[Âˆ[]H\ÙËœ›Ý™[˜[˜ÙTÜÚ][ÛœÎÂˆBˆBˆœ™XZÎÈËÈÛ›HÚXÚÈHš\œÝ›Û‹][šÚ[™È›ØÚÂˆBˆBŸB‚‹ÊŠ‚ˆ
ˆ]XÝÚ]\ˆH™\]Y\ÝÛÛZ[œÈHÛÛ\]YÚ]ÛÛ[Z]ÛÛ[›ØØ][Û‹‚ˆ
ˆÚXÚÜÈÛÛÝ\ÙH[œ]È
ÛÛ[X[™Ýš[™ÊHÛˆ\ÜÚ\Ý[Y\ÜØYÙ\È[™ÛÛÜ™\Ý[ˆ
ˆÝ]]Ûˆ\Ù\ˆY\ÜØYÙ\È›ÜˆÛÛ[Z][™XØ]ÜœËˆ\ÙYÈšYÙÙ\ˆÝ\˜][Ûˆ]ˆ
ˆÛÛ[Z]›Ý[™\šY\È8 %˜]\˜[ÚXÚÜÚ[ÈÚ\™HXÚ\Ú[ÛœÈÜž\Ý[^™K‚ˆ
‹Â˜ÛÛœÝÒUÐÓÓSRUÔ‘HH×™Ú]ÊØÛÛ[Z]‹ÚNÂ™[˜Ý[ÛˆÛÛZ[œÑÚ]ÛÛ[Z]
™\NˆØ]]Ø^T™\]Y\Ý
Nˆ›ÛÛX[ˆÂˆ›Üˆ
ÛÛœÝ\ÙÈÙˆ™\K›Y\ÜØYÙ\ÊHÂˆ›Üˆ
ÛÛœÝ›ØÚÈÙˆ\ÙË˜ÛÛ[
HÂˆËÈÚXÚÈ\ÜÚ\Ý[ÛÛÝ\ÙH[œ]È›ÜˆHÛÛ[X[™Ýš[™ÂˆYˆ
›ØÚË\HOOHÛÛÝ\ÙHŠHÂˆÛÛœÝ[œ]H›ØÚËš[œ]ÂˆYˆ
\[Ùˆ[œ]OOH›Øš™XÝˆ	‰ˆ[œ]OOH[
HÂˆÛÛœÝÛYBˆ
[œ]\È™XÛÜ™Ýš[™Ë[šÛ›ÝÛŠK˜ÛÛ[X[™ÏÂˆ
[œ]\È™XÛÜ™Ýš[™Ë[šÛ›ÝÛŠK˜ÛÛ[ÏÂˆˆŽÂˆYˆ
\[ÙˆÛYOOHœÝš[™Èˆ	‰ˆÒUÐÓÓSRUÔ‘K\Ý
ÛY
JH™]\›ˆYNÂˆBˆBˆËÈÚXÚÈ\Ù\ˆÛÛÜ™\Ý[ÛÛ[›ÜˆÚ]ÛÛ[Z]Ý]]]\›œÂˆYˆ
›ØÚË\HOOHÛÛÜ™\Ý[ŠHÂˆÛÛœÝ^H›ØÚÜÕÕ^
›ØÚË˜ÛÛ[
NÂˆËÈX]ÚÛÛ[[ÛˆÚ]ÛÛ[Z]Ý]]
K™Ë‹–ÛXZ[ˆX˜ÌLŒÍHÛÛ[Z]Y\ÜØYÙHŠBˆYˆ
^	‰ˆ×—Ö×ËË‹WJÈÌNXKY—J×KË\Ý
^š[J
JJH™]\›ˆYNÂˆBˆBˆBˆ™]\›ˆ˜[ÙNÂŸB‚‹ÊŠˆXÝ]™H\Ý™X[H[\˜Ù\Üˆ8 %\ÙY›Üˆ™XÛÜ™[™ËÜ™\^Kˆ
‹Â›]XÝ]™R[\˜Ù\ÜŽˆ\Ý™X[R[\˜Ù\Üˆ[™Yš[™YÂ‹ÊŠˆ[Û›ÝÛšXÈ™\]Y\Ý\Ý\Ü™\ˆ›ÜˆÛÛ˜Ý\œ™[˜ÞK\ØY™H\Ý™X[HÛ˜\ÚÝËˆ
‹Â›]\Ý™X[T™\]Y\ÝÜ™\ˆHÂ‹ÊŠˆ\Ý[Û›HÙX[H›Üˆ›Ü˜Ú[™ÈY™\œØ\šX[™\]Y\ÝÜ™\š[™È™Y›Ü™HØ\\™Kˆ
‹Â›]™Y›Ü™U\Ý™X[PØ\\™Q›Ü•\Ý‚ˆ

™\NˆØ]]Ø^T™\]Y\ÝÝ]NˆÙ\ÜÚ[Û”Ý]JHOˆ›ÛZ\ÙO›ÚYŠBˆ[™Yš[™YÂ‹ÊŠˆ›Ü™YÜ›Ý[™™\]Y\ÝY™][Y\ÈØ[˜Ù[YÚ[ˆH\[[™H\È™\Ù]ˆ
‹Â˜ÛÛœÝXÝ]™Q›Ü™YÜ›Ý[™X›ÜÛÛ›Û\œÈH™]ÈÙ]X›ÜÛÛ›Û\Š
NÂ‚™^Ü[˜Ý[ÛˆÙ]™Y›Ü™U\Ý™X[PØ\\™Q›Ü•\Ý
ˆÛÚÎ‚ˆ

™\NˆØ]]Ø^T™\]Y\ÝÝ]NˆÙ\ÜÚ[Û”Ý]JHOˆ›ÛZ\ÙO›ÚYŠBˆ[™Yš[™YŠNˆ›ÚYÂˆ™Y›Ü™U\Ý™X[PØ\\™Q›Ü•\ÝHÛÚÎÂŸB‚‹ÊŠˆ\Ý[Û›HØœÙ\™\ˆ›Üˆ[›š[™ÈÜÝ\™\ÜÛœÙHY™XÞXÛHÜ™\š[™Ëˆ
‹Â›]ÜÝ™\ÜÛœÙTÝ\ØœÙ\™\Žˆ


HOˆ›ÚY
H[™Yš[™YÂ›]™XØ[\œÚ\Ý[˜ÙPÛÛ[Z]ØœÙ\™\Žˆ


HOˆ›ÚY
H[™Yš[™YÂ›]\[[™T™\Ù]]\ÙQ›Ü•\Ýˆ›ÛZ\ÙO›ÚYˆ[™Yš[™YÂ›]\[[™T™U\Ý™X[T]\ÙQ›Ü•\Ý‚ˆÈ]\ÙNˆ›ÛZ\ÙO›ÚYŽÈÛ•ØZ]ˆ

HOˆ›ÚYBˆ[™Yš[™YÂ›]›Ýš\Ú[Û˜[š[˜[^™\”]\ÙQ›Ü•\Ý‚ˆÈ]\ÙNˆ›ÛZ\ÙO›ÚYŽÈÛ•ØZ]ˆ

HOˆ›ÚYBˆ[™Yš[™YÂ›]\[[™T™\Ù]Ù]U[Y[Ý]\ÈHLÂ›]\[[™T™\Ù][”›ÙÜ™\ÜÈH˜[ÙNÂ›]\[[™T™\Ù]›ÛZ\ÙNˆ›ÛZ\ÙO›ÚYˆ[™Yš[™YÂ‚š[\™˜XÙHXÝ]™T\[[™T™\]Y\ÝÂˆYZ\ÜÚ[Û’Ù^NˆÝš[™ÎÂˆX›Üˆ
™X\ÛÛŽˆ[šÛ›ÝÛŠHOˆ›ÚYÂˆÙ]Yˆ›ÛZ\ÙO›ÚYŽÂˆÙ\ÜÚ[Û’QÎˆÙ]Ýš[™ÏŽÂŸB‚˜ÛÛœÝXÝ]™T\[[™T™\]Y\ÝÈH™]ÈÙ]XÝ]™T\[[™T™\]Y\ÝŠ
NÂ˜ÛÛœÝ]XÚY\[[™T™\]Y\ÝÈH™]ÈÙ]XÝ]™T\[[™T™\]Y\ÝŠ
NÂ˜ÛÛœÝQUSÓPVÐPÕU‘WÔTSS‘WÔ‘TUQTÕÈHÂ˜ÛÛœÝPVÐPÕU‘WÔTSS‘WÔ‘TUQTÕ×ÔT—ÐQRTÔÒSÓ—ÒÑVHHMŽÂ˜ÛÛœÝPVÐPÕU‘WÔTSS‘WÔ‘TUQTÕ×ÔT—ÔÑTÔÒSÓˆHNÂ˜ÛÛœÝPVÔS‘S‘×ÔÑTÔÒSÓ—ÐÓRSTÈHÂ˜ÛÛœÝPVÑUPÒQÔTSS‘WÔ‘TUQTÕÈHÂ›]X^XÝ]™T\[[™T™\]Y\ÝÈHQUSÓPVÐPÕU‘WÔTSS‘WÔ‘TUQTÕÎÂ›]X^]XÚY\[[™T™\]Y\ÝÈHPVÑUPÒQÔTSS‘WÔ‘TUQTÕÎÂ‚š[\™˜XÙH[™[™ÔÙ\ÜÚ[ÛÛZ[HÂˆXÝ]™NˆXÝ]™T\[[™T™\]Y\ÝÂˆÙ\ÜÚ[Û’QˆÝš[™ÎÂˆÚYÛ˜[ˆX›ÜÚYÛ˜[Âˆ™\ÛÛ™Nˆ

HOˆ›ÚYÂˆ™Z™XÝˆ
™X\ÛÛŽˆ[šÛ›ÝÛŠHOˆ›ÚYÂˆÛX›Üˆ

HOˆ›ÚYÂŸB‚˜ÛÛœÝ[™[™ÔÙ\ÜÚ[ÛÛZ[\ÈH™]ÈX\Ýš[™Ë[™[™ÔÙ\ÜÚ[ÛÛZ[OŠ
NÂ‚˜Û\ÜÈ\[[™PØ\XÚ]Q\œ›Üˆ^[™È\œ›ÜˆßB‚™[˜Ý[ÛˆXÝ]™T\[[™T™\]Y\ÝÑ›Ü”Ù\ÜÚ[ÛŠÙ\ÜÚ[Û’QˆÝš[™ÊNˆ[X™\ˆÂˆ]ÛÝ[HÂˆ›Üˆ
ÛÛœÝ™\]Y\ÝÙˆXÝ]™T\[[™T™\]Y\ÝÊHÂˆYˆ
™\]Y\ÝœÙ\ÜÚ[Û’QËš\ÊÙ\ÜÚ[Û’Q
JHÛÝ[
ÊÎÂˆBˆ™]\›ˆÛÝ[ÂŸB‚™[˜Ý[ÛˆXÝ]™T\[[™T™\]Y\ÝÑ›ÜYZ\ÜÚ[Û’Ù^JYZ\ÜÚ[Û’Ù^NˆÝš[™ÊNˆ[X™\ˆÂˆ]ÛÝ[HÂˆ›Üˆ
ÛÛœÝ™\]Y\ÝÙˆXÝ]™T\[[™T™\]Y\ÝÊHÂˆYˆ
™\]Y\Ý˜YZ\ÜÚ[Û’Ù^HOOHYZ\ÜÚ[Û’Ù^JHÛÝ[
ÊÎÂˆBˆ™]\›ˆÛÝ[ÂŸB‚™[˜Ý[Ûˆ[™[™ÔÙ\ÜÚ[ÛÛZ[\Ñ›ÜYZ\ÜÚ[Û’Ù^JYZ\ÜÚ[Û’Ù^NˆÝš[™ÊNˆ[X™\ˆÂˆ]ÛÝ[HÂˆ›Üˆ
ÛÛœÝÛZ[HÙˆ[™[™ÔÙ\ÜÚ[ÛÛZ[\Ë˜[Y\Ê
JHÂˆYˆ
ÛZ[K˜XÝ]™K˜YZ\ÜÚ[Û’Ù^HOOHYZ\ÜÚ[Û’Ù^JHÛÝ[
ÊÎÂˆBˆ™]\›ˆÛÝ[ÂŸB‚™[˜Ý[Ûˆ\[[™TÙ\ÜÚ[Û’\ÐØ\XÚ]JÙ\ÜÚ[Û’QˆÝš[™ÊNˆ›ÛÛX[ˆÂˆ™]\›ˆ
ˆXÝ]™T\[[™T™\]Y\ÝÑ›Ü”Ù\ÜÚ[ÛŠÙ\ÜÚ[Û’Q
H
Âˆ
Ý™X[Z[™ÔÜÝ™\ÜÛœÙQš[˜[^™\œË™Ù]
Ù\ÜÚ[Û’Q
OËœ[™[™ÈÏÈ
HˆPVÐPÕU‘WÔTSS‘WÔ‘TUQTÕ×ÔT—ÔÑTÔÒSÓ‚ˆ
NÂŸB‚™[˜Ý[Ûˆ[\[™[™ÔÙ\ÜÚ[ÛÛZ[\Ê
Nˆ›ÚYÂˆ›Üˆ
ÛÛœÝÜÙ\ÜÚ[Û’QÛZ[WHÙˆ[™[™ÔÙ\ÜÚ[ÛÛZ[\ÊHÂˆYˆ
ˆXÝ]™T\[[™T™\]Y\ÝËœÚ^™H
ÈÝ™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ÈBˆX^XÝ]™T\[[™T™\]Y\ÝÂˆ
HÂˆ™]\›ŽÂˆBˆYˆ
ˆXÝ]™T\[[™T™\]Y\ÝÑ›ÜYZ\ÜÚ[Û’Ù^JÛZ[K˜XÝ]™K˜YZ\ÜÚ[Û’Ù^JH
Âˆ
Ý™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ÐžPYZ\ÜÚ[Û’Ù^K™Ù]
ˆÛZ[K˜XÝ]™K˜YZ\ÜÚ[Û’Ù^Kˆ
HÏÈ
HBˆPVÐPÕU‘WÔTSS‘WÔ‘TUQTÕ×ÔT—ÐQRTÔÒSÓ—ÒÑVBˆ
HÂˆÛÛ[YNÂˆBˆYˆ
\\[[™TÙ\ÜÚ[Û’\ÐØ\XÚ]JÙ\ÜÚ[Û’Q
JHÛÛ[YNÂˆ[™[™ÔÙ\ÜÚ[ÛÛZ[\Ë™[]JÙ\ÜÚ[Û’Q
NÂˆÛZ[KœÚYÛ˜[œ™[[Ý™Q]™[\Ý[™\Š˜X›Ü‹ÛZ[K›ÛX›Ü
NÂˆYˆ
ÛZ[KœÚYÛ˜[˜X›ÜY
HÂˆÛZ[Kœ™Z™XÝ
ÛZ[KœÚYÛ˜[œ™X\ÛÛŠNÂˆÛÛ[YNÂˆBˆÛZ[K˜XÝ]™KœÙ\ÜÚ[Û’QË˜Y
Ù\ÜÚ[Û’Q
NÂˆXÝ]™T\[[™T™\]Y\ÝË˜Y
ÛZ[K˜XÝ]™JNÂˆÛZ[Kœ™\ÛÛ™J
NÂˆBŸB‚™[˜Ý[Ûˆ\Ô\[[™TÙ\ÜÚ[ÛXÝ]™JÙ\ÜÚ[Û’QˆÝš[™ÊNˆ›ÛÛX[ˆÂˆ™]\›ˆ
ˆXÝ]™T\[[™T™\]Y\ÝÑ›Ü”Ù\ÜÚ[ÛŠÙ\ÜÚ[Û’Q
Hˆˆ[™[™ÔÙ\ÜÚ[ÛÛZ[\Ëš\ÊÙ\ÜÚ[Û’Q
HˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙQš[˜[^™\œËš\ÊÙ\ÜÚ[Û’Q
Bˆ
NÂŸB‚™^Ü[˜Ý[ÛˆXÝ]™T\[[™T™\]Y\ÝÛÝ[›Ü•\Ý

Nˆ[X™\ˆÂˆ™]\›ˆXÝ]™T\[[™T™\]Y\ÝËœÚ^™NÂŸB‚™^Ü[˜Ý[Ûˆ]XÚY\[[™T™\]Y\ÝÛÝ[›Ü•\Ý

Nˆ[X™\ˆÂˆ™]\›ˆ]XÚY\[[™T™\]Y\ÝËœÚ^™NÂŸB‚™^Ü[˜Ý[Ûˆ[™[™Ô\[[™TÙ\ÜÚ[ÛÛZ[PÛÝ[›Ü•\Ý

Nˆ[X™\ˆÂˆ™]\›ˆ[™[™ÔÙ\ÜÚ[ÛÛZ[\ËœÚ^™NÂŸB‚™^Ü[˜Ý[ÛˆÙ]X^XÝ]™T\[[™T™\]Y\ÝÑ›Ü•\Ý
ˆ[Z]HQUSÓPVÐPÕU‘WÔTSS‘WÔ‘TUQTÕËŠNˆ›ÚYÂˆX^XÝ]™T\[[™T™\]Y\ÝÈH[Z]ÂŸB‚™^Ü[˜Ý[ÛˆÙ]X^]XÚY\[[™T™\]Y\ÝÑ›Ü•\Ý
ˆ[Z]HPVÑUPÒQÔTSS‘WÔ‘TUQTÕËŠNˆ›ÚYÂˆX^]XÚY\[[™T™\]Y\ÝÈH[Z]ÂŸB‚™^Ü[˜Ý[Ûˆ\Ô\[[™TÙ\ÜÚ[ÛXÝ]™Q›Ü•\Ý
Ù\ÜÚ[Û’QˆÝš[™ÊNˆ›ÛÛX[ˆÂˆ™]\›ˆ\Ô\[[™TÙ\ÜÚ[ÛXÝ]™JÙ\ÜÚ[Û’Q
NÂŸB‚‹ÊŠ‚ˆ
ˆÙ]
ÜˆÛX\ŠHH[Ù[K[]™[\Ý™X[H[\˜Ù\Ü‹‚ˆ
‚ˆ
ˆÚ[ˆÙ]]™\žHØ[È›ÜØ\™Õ\Ý™X[X\ÜÙ\È›ÝYÚH[\˜Ù\Ü‚ˆ
ˆ[œÝXYÙˆØ[[™È™]Ú\™XÝKˆ\ÙYžHH™XÛÜ™[™È[™™\^Bˆ
ˆØÜš\ÈÈØ\\™HÜˆ™\^H\Ý™X[H˜Y™šXÈÚ]Ý][ÙYžZ[™È[™]šYX[ˆ
ˆØ[Ú]\Ë‚ˆ
‹Â™^Ü[˜Ý[ÛˆÙ]\Ý™X[R[\˜Ù\ÜŠˆ[\˜Ù\ÜŽˆ\Ý™X[R[\˜Ù\Üˆ[™Yš[™YŠNˆ›ÚYÂˆXÝ]™R[\˜Ù\ÜˆH[\˜Ù\ÜŽÂŸB‚™^Ü[˜Ý[ÛˆÙ]ÜÝ™\ÜÛœÙTÝ\ØœÙ\™\‘›Ü•\Ý
ˆØœÙ\™\Žˆ


HOˆ›ÚY
H[™Yš[™YŠNˆ›ÚYÂˆÜÝ™\ÜÛœÙTÝ\ØœÙ\™\ˆHØœÙ\™\ŽÂŸB‚™^Ü[˜Ý[ÛˆÙ]™XØ[\œÚ\Ý[˜ÙPÛÛ[Z]ØœÙ\™\‘›Ü•\Ý
ˆØœÙ\™\Žˆ


HOˆ›ÚY
H[™Yš[™YŠNˆ›ÚYÂˆ™XØ[\œÚ\Ý[˜ÙPÛÛ[Z]ØœÙ\™\ˆHØœÙ\™\ŽÂŸB‚™^Ü[˜Ý[ÛˆÙ]\[[™T™\Ù]]\ÙQ›Ü•\Ý
ˆ]\ÙNˆ›ÛZ\ÙO›ÚYˆ[™Yš[™YŠNˆ›ÚYÂˆ\[[™T™\Ù]]\ÙQ›Ü•\ÝH]\ÙNÂŸB‚™^Ü[˜Ý[ÛˆÙ]\[[™T™U\Ý™X[T]\ÙQ›Ü•\Ý
ˆ]\ÙNˆ›ÛZ\ÙO›ÚYˆ[™Yš[™YˆÛ•ØZ]ˆ

HOˆ›ÚYH

HOˆßKŠNˆ›ÚYÂˆ\[[™T™U\Ý™X[T]\ÙQ›Ü•\ÝH]\ÙHÈÈ]\ÙKÛ•ØZ]Hˆ[™Yš[™YÂŸB‚™^Ü[˜Ý[ÛˆÙ]›Ýš\Ú[Û˜[š[˜[^™\”]\ÙQ›Ü•\Ý
ˆ]\ÙNˆ›ÛZ\ÙO›ÚYˆ[™Yš[™YˆÛ•ØZ]ˆ

HOˆ›ÚYH

HOˆßKŠNˆ›ÚYÂˆ›Ýš\Ú[Û˜[š[˜[^™\”]\ÙQ›Ü•\ÝH]\ÙHÈÈ]\ÙKÛ•ØZ]Hˆ[™Yš[™YÂŸB‚™^Ü[˜Ý[ÛˆÙ]\[[™T™\Ù]Ù]U[Y[Ý]›Ü•\Ý
[Y[Ý]\ÈHL
Nˆ›ÚYÂˆ\[[™T™\Ù]Ù]U[Y[Ý]\ÈH[Y[Ý]\ÎÂŸB‚‹ÊŠ‚ˆ
ˆ™\Ù][[Ù[K[]™[Ú[™Û]ÛˆÝ]K‚ˆ
‚ˆ
ˆØ[Y\š[™ÈØ]]Ø^HÚ]ÝÛˆ
Ú]È˜\ÝˆYHXÈÚÚ\H˜]Ú\]Y]YBˆ
ˆ˜Z[ŠH[™žH\Ý\›™\ÜÙ\È
Y˜][8 %˜Z[œÈÜ˜XÙY[HÛÈ\ÝÈØœÙ\™Bˆ
ˆ[ÚYKYY™™XÝÊK‚ˆ
‹Â™^Ü\Þ[˜È[˜Ý[Ûˆ™\Ù]\[[™TÝ]JÜÏÎˆÂˆ˜\ÝÎˆ›ÛÛX[ŽÂŸJNˆ›ÛZ\ÙO›ÚYˆÂˆYˆ
\[[™T™\Ù]›ÛZ\ÙJH™]\›ˆ\[[™T™\Ù]›ÛZ\ÙNÂˆ\[[™T™\Ù][”›ÙÜ™\ÜÈHYNÂˆÛÛœÝ™\Ù]H
\Þ[˜È

HOˆÂˆžHÂˆ]ØZ]™\Ù]\[[™TÝ]R[›™\ŠÜÊNÂˆHš[˜[HÂˆ\[[™T™\Ù][”›ÙÜ™\ÜÈH˜[ÙNÂˆ\[[™T™\Ù]›ÛZ\ÙHH[™Yš[™YÂˆBˆJJ
NÂˆ\[[™T™\Ù]›ÛZ\ÙHH™\Ù]Âˆ™]\›ˆ™\Ù]ÂŸB‚˜\Þ[˜È[˜Ý[Ûˆ™\Ù]\[[™TÝ]R[›™\ŠÜÏÎˆÂˆ˜\ÝÎˆ›ÛÛX[ŽÂŸJNˆ›ÛZ\ÙO›ÚYˆÂˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙ\ÐXØÙ\[™ÈH˜[ÙNÂˆ]ØZ]\[[™T™\Ù]]\ÙQ›Ü•\ÝÂˆÛÛœÝ™\Ù]™X\ÛÛˆH™]ÈÓQ^Ù\[ÛŠ™Ø]]Ø^H\[[™H™\Ù]‹X›Ü\œ›ÜˆŠNÂˆ\[[™QÙ[™\˜][ÛX›Ü˜X›Ü
™\Ù]™X\ÛÛŠNÂˆÛÛœÝ›Ü™YÜ›Ý[™ÛÛ›Û\œÈHË‹‹˜XÝ]™Q›Ü™YÜ›Ý[™X›ÜÛÛ›Û\œ×NÂˆXÝ]™Q›Ü™YÜ›Ý[™X›ÜÛÛ›Û\œË˜ÛX\Š
NÂˆ›Üˆ
ÛÛœÝÛÛ›Û\ˆÙˆ›Ü™YÜ›Ý[™ÛÛ›Û\œÊHÂˆYˆ
XÛÛ›Û\‹œÚYÛ˜[˜X›ÜY
HÛÛ›Û\‹˜X›Ü
™\Ù]™X\ÛÛŠNÂˆBˆÛÛœÝXÝ]™T™\]Y\ÝÈHÂˆ‹‹›™]ÈÙ]
Âˆ‹‹˜XÝ]™T\[[™T™\]Y\ÝËˆ‹‹–Ë‹‹œ[™[™ÔÙ\ÜÚ[ÛÛZ[\Ë˜[Y\Ê
WK›X\

ÛZ[JHOˆÛZ[K˜XÝ]™JKˆJKˆNÂˆ›Üˆ
ÛÛœÝ™\]Y\ÝÙˆXÝ]™T™\]Y\ÝÊH™\]Y\Ý˜X›Ü
™\Ù]™X\ÛÛŠNÂˆ]ØZ]›Ý[™YÙ]JˆXÝ]™T™\]Y\ÝË›X\

™\]Y\Ý
HOˆ™\]Y\ÝœÙ]Y
Kˆ\[[™T™\Ù]Ù]U[Y[Ý]\Ëˆ
NÂˆ›Üˆ
ÛÛœÝ™\]Y\ÝÙˆXÝ]™T™\]Y\ÝÊHÂˆYˆ
XXÝ]™T\[[™T™\]Y\ÝËš\Ê™\]Y\Ý
JHÛÛ[YNÂˆXÝ]™T\[[™T™\]Y\ÝË™[]J™\]Y\Ý
NÂˆ™\]Y\ÝœÙ\ÜÚ[Û’QË˜ÛX\Š
NÂˆYˆ
]XÚY\[[™T™\]Y\ÝËœÚ^™HX^]XÚY\[[™T™\]Y\ÝÊHÂˆ]XÚY\[[™T™\]Y\ÝË˜Y
™\]Y\Ý
NÂˆH[ÙHÂˆÙË™\œ›ÜŠˆœ\[[™H]X\˜[[™H[È›Ü[™ÈÝ[HY™XÞXÛH™\Ù\˜][Ûˆ‹ˆ
NÂˆBˆBˆËÈÝ™X[Z[™È™\ÜÛœÙ\È™YÚ\Ý\ˆÜÝ\™\ÜÛœÙHš[˜[^™\œÈ™Y›Ü™HÛÜÚ[™ÈZ\‚ˆËÈ›ÙY\Ëˆ˜Z[ˆ[H™Y›Ü™HÙ\ÜÚ[ÛœÈÜˆH‹Y˜XÚ[™È\[[™HÝ]H\™BˆËÈÛX\™YÈHš[˜[^™\ˆX^H[ÛÈØÚY[HÜ™[˜\žH˜XÚÙÜ›Ý[™ÛÜšËÚXÚBˆËÈ›Û‹Y˜\Ý˜Z[ˆ™[ÝÈÚ[[ˆØœÙ\™K‚ˆ]ØZ]›Ý[™YÙ]JˆË‹‹œÝ™X[Z[™ÔÜÝ™\ÜÛœÙQš[˜[^™\œË˜[Y\Ê
WK›X\

Ý]JHOˆÝ]KZ[
Kˆ\[[™T™\Ù]Ù]U[Y[Ý]\Ëˆ
NÂˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙQÙ[™\˜][ÛŠÊÎÂˆ\[[™QÙ[™\˜][ÛX›ÜH™]ÈX›ÜÛÛ›Û\Š
NÂˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙQš[˜[^™\œË˜ÛX\Š
NÂˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ÐžPYZ\ÜÚ[Û’Ù^K˜ÛX\Š
NÂˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ÈHÂˆX^Ý™X[Z[™ÔÜÝ™\ÜÛœÙ\ÈHQUSÓPVÔÕ‘PSRS‘×ÔÔÕÔ‘TÔÓ”ÑTÎÂˆX^Ý™X[Z[™ÔÜÝ™\ÜÛœÙ\Ô\”Ù\ÜÚ[ÛˆBˆQUSÓPVÔÕ‘PSRS‘×ÔÔÕÔ‘TÔÓ”ÑT×ÔT—ÔÑTÔÒSÓŽÂˆ\ÝÝ™X[Z[™ÔÜÝ™\ÜÛœÙSÝ™\™›ÝÓÙÈHÂˆ\ÝÝ™X[Z[™ÔÜÝ™\ÜÛœÙT™\Ù]ÙÈHÂˆËÈ]ZY\ØÙH˜XÚÙÜ›Ý[™ÛÜšÈ™Y›Ü™HX\š[™È[ž][™ÈÝÛ‹ˆÛ›HH›Û‹Y˜\ÝˆËÈ]˜Z[œÈ8 %Ù^H]	ÜÈ\ÝÙ]˜[X\™ÝÛˆ
H˜\Ý›ØÙ\ÜËY^]]ˆËÈHÛÛH›ÙXÝ[ÛˆØ[\‹ÚÚ\È\ÈÈÙY\Ý›
ÐÈÛ˜\JKˆÝÜBˆËÈYHØÚY[\ˆ’T”ÕÛÈ›È™]ÈXÚÜÈØÚY[HÛÜšË[ˆ]ØZ]]™\žBˆËÈ[‹Y›YÚ\Ý[][ÛˆÈÝ\˜][ÛˆÈYH\ÚËˆÛ™HÚ[HPÛY[
ÈBˆËÈ\Ý™X[H[\˜Ù\Üˆ\™HÝ[]™HÛÈT‘PÕXØ[\H\ÚÜÈ
[˜ÛˆBˆËÈ[Ø^\Ë\ØÚY[Y\™Ù[\Ý[][ÛŠHÛÛ\]HÛX[›NÈH˜]ÚXØ[\BˆËÈ\ÚÈØ[‰Ý›\Ú[[PÛY[œÚ]ÝÛˆ™[ÝËÛÈ]˜[È˜XÚÈÈBˆËÈ›Ý[™Y˜Z[ˆ[Y[Ý]
˜\™H[ˆ\ÝÈ8 %[˜Ü™[Y[[\Ý[ØÝ\˜][ÛˆÙ[ÛBˆËÈšYÙÙ\ˆ[ˆÚÜ[œÊKˆHÚ[ˆH]HØ]™TÙ\ÜÚ[Û•˜XÚÚ[™Ê
XÜš]BˆËÈ]\Ý[™[ˆTÈ›ØÙ\ÜÉÜÈ‹›ÝXZÈ[ÈH™^Û™IÜÈ\ÈH[ÛBˆËÈ›ÝÈ8 %HÜ›ÜÜËZ\›™\ÜÈÛÛ[Z[˜][Ûˆ™Z[™HÎNH›ZÙKˆÙYHÎK‚ˆYˆ
[ÜÏË™˜\Ý
HÂˆYˆ
ÝÜYTØÚY[\ŠHÂˆÝÜYTØÚY[\Š
NÂˆÝÜYTØÚY[\ˆH[ÂˆBˆ]ØZ]˜Z[˜XÚÙÜ›Ý[™

NÂˆËÈ›Ý[™\È˜Z[ˆÛÈ
ÙY\ŠH8 %HÝ[Y\™Ù[\Ý[][ÛˆÈÝ\˜][Û‚ˆËÈÚZ[ˆ]\Ý›Ý[™ÈH™\Ù]X]Ú[™È˜Z[˜XÚÙÜ›Ý[™	ÜÈÝX\˜[YK‚ˆ]ØZ]›Ý[™YÙ]J[‘›YÚ˜XÚÙÜ›Ý[™
NÂˆ[‘›YÚ˜XÚÙÜ›Ý[™˜ÛX\Š
NÂˆBˆ[š]X[^™YH˜[ÙNÂˆX^XÝ]™T\[[™T™\]Y\ÝÈHQUSÓPVÐPÕU‘WÔTSS‘WÔ‘TUQTÕÎÂˆX^]XÚY\[[™T™\]Y\ÝÈHPVÑUPÒQÔTSS‘WÔ‘TUQTÕÎÂˆÙ\ÜÚ[ÛœË˜ÛX\Š
NÂˆÝÙØ\›™Y˜ÛX\Š
NÂˆÝ[RXY\•Ø\›™Y˜ÛX\Š
NÂˆÝX˜YÙ[\™[[™[™ÓÙÙÙY˜ÛX\Š
NÂˆXY\”Ù\ÜÚ[Û’[™^˜ÛX\Š
NÂˆ[XšYÝ[Ý\ÒXY\”Ù\ÜÚ[Û’Ù^\Ë˜ÛX\Š
NÂˆ›Ýš\Ú[Û˜[XY\”Ù\ÜÚ[Û’[™^˜ÛX\Š
NÂˆY[]PYZ\ÜÚ[Û•Z[Ë˜ÛX\Š
NÂˆXY\”Ù\ÜÚ[Û’[™^Y˜]YH˜[ÙNÂˆTÙ\ÜÚ[ÛØXÚK˜ÛX\Š
NÂˆT[›™Y^˜ÛX\Š
NÂˆ\ÝØ]™YY\XÚ\Ú[ÛœË˜ÛX\Š
NÂˆÝX›SPØXÚK˜ÛX\Š
NÂˆÝX›SR[‘›YÚ˜ÛX\Š
NÂˆÙ\ÜÚ[Û“Y™XÞXÛPX›ÜË˜ÛX\Š
NÂˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙUØZ]\œË˜ÛX\Š
NÂˆËÈÚ]ÝÛˆH˜]Ú]Y]YH™Y›Ü™HÛX\š[™ÈHÛY[ˆÛˆ›ØÙ\ÜÈ^]ˆËÈ
˜\Ý
KÚÚ\HÞ[˜Ú›Û›Ý\ÈH˜Z[ˆ8 %™\^Z[™È]Y]YY˜XÚÙÜ›Ý[™ˆËÈ›Û\È›ÝYÚ™]šY\ËØ˜XÚÛÙ™ˆ\ÈÚ]XYHÝ›
ÐÈ[™È›ÜˆZ[]\ÎÈ^BˆËÈ™\Ý[YH™^Ù\ÜÚ[Û‹ˆÛÛ™šYËÝ\Ý™\Ù]ÈÙY\˜Z[š[™È
Y˜][
K‚ˆYˆ
PÛY[	‰ˆœÚ]ÝÛˆˆ[ˆPÛY[
HÂˆ]ØZ]
ˆPÛY[\ÈPÛY[	ˆÂˆÚ]ÝÛŽˆ
ÏÎˆÈ˜Z[”]Y]YOÎˆ›ÛÛX[ˆJHOˆ›ÛZ\ÙO›ÚYŽÂˆBˆ
KœÚ]ÝÛŠÈ˜Z[”]Y]YNˆ[ÜÏË™˜\ÝJNÂˆBˆPÛY[H[ÂˆXÝ]™R[\˜Ù\ÜˆH[™Yš[™YÂˆ™Y›Ü™U\Ý™X[PØ\\™Q›Ü•\ÝH[™Yš[™YÂˆÜÝ™\ÜÛœÙTÝ\ØœÙ\™\ˆH[™Yš[™YÂˆ™XØ[\œÚ\Ý[˜ÙPÛÛ[Z]ØœÙ\™\ˆH[™Yš[™YÂˆ›Ýš\Ú[Û˜[š[˜[^™\”]\ÙQ›Ü•\ÝH[™Yš[™YÂˆ›Ü™YÜ›Ý[™\œ›Ü›ÙU[Y[Ý]\ÈH“Ô‘QÔ“ÕS‘ÑT”“Ô—Ð“ÑWÕSQSÕUÓTÎÂˆYˆ
ÝÜš[UØ]Ú\ŠHÂˆÝÜš[UØ]Ú\Š
NÂˆÝÜš[UØ]Ú\ˆH[ÂˆBˆYˆ
ÝÜYTØÚY[\ŠHÂˆÝÜYTØÚY[\Š
NÂˆÝÜYTØÚY[\ˆH[ÂˆBˆYˆ
ÝÜÞ[˜ÔØÚY[\ŠHÂˆËÈ]ØZ]ÈHš[˜[™\ÝYY™›Ü\ÚÛÈØØ[Ú[™Ù\È™XXÚHÙ\™\ˆÛˆ^]‚ˆ]ØZ]ÝÜÞ[˜ÔØÚY[\Š
NÂˆÝÜÞ[˜ÔØÚY[\ˆH[ÂˆBˆÛ\ÝÙY[”Ù\ÜÚ[Û“[Ù[H[ÂˆÙš\œÝ\›ÛÛ™š\›YYH˜[ÙNÂˆ™\Ù]ÛÜšÙ\“[Ù[Ý]J
NÂˆ™\Ù]˜XÚÙÜ›Ý[™[Z]\Š
NÂŸB‚‹ÊŠˆ\‹\Ù\ÜÚ[ÛˆÝ]H˜XÚÙYXÜ›ÜÜÈ™\]Y\ÝËˆ
‹Â˜ÛÛœÝÙ\ÜÚ[ÛœÈH™]ÈX\Ýš[™ËÙ\ÜÚ[Û”Ý]OŠ
NÂ‚˜ÛÛœÝQUSÓPVÔÕ‘PSRS‘×ÔÔÕÔ‘TÔÓ”ÑTÈHÂ‹ËÈ›ÙXÝ[Ûˆ™\]Y\ÝÈ™\Ù\™HØ\XÚ]H™Y›Ü™H\Ý™X[HÛÜšËˆH[Z]È™[XZ[‚‹ËÈ\ÈY™[œÙKZ[‹Y\›Üˆ[œ™\Ù\™YÝ\Ý[Û›HØÚY[[™Ë‚˜ÛÛœÝQUSÓPVÔÕ‘PSRS‘×ÔÔÕÔ‘TÔÓ”ÑT×ÔT—ÔÑTÔÒSÓˆHŽÂ‚‹ÊŠ‚ˆ
ˆY™\œ™YÝ™X[Z[™Èš[˜[^™\œÈÙ^YYžHÙ\ÜÚ[Û‹ˆHÝ™X[Y\ˆ[›ÚÙ\È]Âˆ
ˆØ[˜XÚÈ™Y›Ü™HÛÜÚ[™ÈÛÈ™YÚ\Ý˜][Ûˆ\È]ÛZXÈÚ]\›Z[˜[[]™\žKˆ
ˆ]H^[œÚ]™HÞ[˜Ú›Û›Ý\ÈXØÛÝ[[™È]Ù[ˆ[œÈÛˆH™^]™[[ÛÜˆ
ˆ\›‹[ÝÚ[™ÈH›ÙH™XY\ˆ
[™›ÙHœšYÙJHÈØœÙ\™HSÑˆš\œÝˆBˆ
ˆ›Ý[™Y™YÚ\ÝžH™\Ù\™\È[‹\›ØÙ\ÜÈÜ™\š[™ÎÈH›ØÙ\ÜÈÜ˜\Ú[ˆ]Û™Bˆ
ˆ]™[[ÛÜ]\›ˆÚ[™ÝÈØ[ˆÝ[ÜÙHš[˜[XØÛÝ[[™ËÚXÚ\ÈH^XÚ]ˆ
ˆ]˜Z[Xš[]H˜YK[Ù™ˆ™\]Z\™YÈ]›ÚYÛ[™ÈÛY[SÑˆ™Z[™ÔS]K‚ˆ
‹Â˜ÛÛœÝÝ™X[Z[™ÔÜÝ™\ÜÛœÙQš[˜[^™\œÈH™]ÈX\ˆÝš[™ËˆÈZ[ˆ›ÛZ\ÙO›ÚYŽÈ[™[™Îˆ[X™\ˆBŠ
NÂ˜ÛÛœÝÝ™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ÐžPYZ\ÜÚ[Û’Ù^HH™]ÈX\Ýš[™Ë[X™\Š
NÂ›]Ý™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ÈHÂ›]Ý™X[Z[™ÔÜÝ™\ÜÛœÙQÙ[™\˜][ÛˆHÂ›]\[[™QÙ[™\˜][ÛX›ÜH™]ÈX›ÜÛÛ›Û\Š
NÂ›]Ý™X[Z[™ÔÜÝ™\ÜÛœÙ\ÐXØÙ\[™ÈHYNÂ›]X^Ý™X[Z[™ÔÜÝ™\ÜÛœÙ\ÈHQUSÓPVÔÕ‘PSRS‘×ÔÔÕÔ‘TÔÓ”ÑTÎÂ›]X^Ý™X[Z[™ÔÜÝ™\ÜÛœÙ\Ô\”Ù\ÜÚ[ÛˆBˆQUSÓPVÔÕ‘PSRS‘×ÔÔÕÔ‘TÔÓ”ÑT×ÔT—ÔÑTÔÒSÓŽÂ›]\ÝÝ™X[Z[™ÔÜÝ™\ÜÛœÙSÝ™\™›ÝÓÙÈHÂ›]\ÝÝ™X[Z[™ÔÜÝ™\ÜÛœÙT™\Ù]ÙÈHÂ›]Ý™X[Z[™ÔÜÝ™\ÜÛœÙUØZ]ØœÙ\™\‘›Ü•\Ýˆ


HOˆ›ÚY
H[™Yš[™YÂ‚™^Ü[˜Ý[ÛˆÙ]Ý™X[Z[™ÔÜÝ™\ÜÛœÙS[Z]Ñ›Ü•\Ý
ˆÛØ˜[[Z]Îˆ[X™\‹ˆ\”Ù\ÜÚ[Û“[Z]Îˆ[X™\‹ŠNˆ›ÚYÂˆX^Ý™X[Z[™ÔÜÝ™\ÜÛœÙ\ÈBˆÛØ˜[[Z]ÏÈQUSÓPVÔÕ‘PSRS‘×ÔÔÕÔ‘TÔÓ”ÑTÎÂˆX^Ý™X[Z[™ÔÜÝ™\ÜÛœÙ\Ô\”Ù\ÜÚ[ÛˆBˆ\”Ù\ÜÚ[Û“[Z]ÏÈQUSÓPVÔÕ‘PSRS‘×ÔÔÕÔ‘TÔÓ”ÑT×ÔT—ÔÑTÔÒSÓŽÂŸB‚™^Ü[˜Ý[ÛˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™Ñ›Ü•\Ý

Nˆ[X™\ˆÂˆ™]\›ˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ÎÂŸB‚™^Ü[˜Ý[ÛˆÙ]Ý™X[Z[™ÔÜÝ™\ÜÛœÙUØZ]ØœÙ\™\‘›Ü•\Ý
ˆØœÙ\™\Žˆ


HOˆ›ÚY
H[™Yš[™YŠNˆ›ÚYÂˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙUØZ]ØœÙ\™\‘›Ü•\ÝHØœÙ\™\ŽÂŸB‚™^Ü[˜Ý[ÛˆØÚY[TÝ™X[Z[™ÔÜÝ™\ÜÛœÙQ›Ü•\Ý
ˆÙ\ÜÚ[Û’QˆÝš[™ËˆÜ\˜][ÛŽˆ

HOˆ›ÚY›ÛZ\ÙO›ÚY‹ˆÛ‘›Üˆ

HOˆ›ÚYH

HOˆßKŠNˆ›ÚYÂˆØÚY[TÝ™X[Z[™ÔÜÝ™\ÜÛœÙJˆÙ\ÜÚ[Û’QˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙQÙ[™\˜][Û‹ˆÜ\˜][Û‹ˆÛ‘›Üˆ
NÂŸB‚™[˜Ý[ÛˆØÚY[TÝ™X[Z[™ÔÜÝ™\ÜÛœÙJˆÙ\ÜÚ[Û’QˆÝš[™ËˆÙ[™\˜][ÛŽˆ[X™\‹ˆÜ\˜][ÛŽˆ

HOˆ›ÚY›ÛZ\ÙO›ÚY‹ˆÛ‘›Üˆ

HOˆ›ÚYˆËÈÛÛ™\œØ][Ûˆ™\]Y\ÝÈ™\Ù\™HÛØ˜[
ÈÙ\ÜÚ[ÛˆØ\XÚ]H™Y›Ü™H\Ý™X[K‚ˆËÈ[œ™\Ù\™YØ[\œÈÝ[\ÙHHY™[œÚ]™H]Y]YH[Z]È™[ÝË‚ˆØ\XÚ]T™\Ù\™YH˜[ÙKˆYZ\ÜÚ[Û’Ù^OÎˆÝš[™ËŠNˆ›ÚYÂˆÛÛœÝ›ÜH

Nˆ›ÚYOˆÂˆžHÂˆÛ‘›Ü

NÂˆHØ]Ú
\œ›ÜŠHÂˆÙË™\œ›ÜŠœÝ™X[Z[™ÈÜÝ\™\ÜÛœÙH›ÜÛX[\˜Z[Yˆ‹\œ›ÜŠNÂˆBˆNÂˆYˆ
ˆ\Ý™X[Z[™ÔÜÝ™\ÜÛœÙ\ÐXØÙ\[™ÈˆÙ[™\˜][ÛˆOOHÝ™X[Z[™ÔÜÝ™\ÜÛœÙQÙ[™\˜][Û‚ˆ
HÂˆÛÛœÝ›ÝÈH]K››ÝÊ
NÂˆYˆ
›ÝÈH\ÝÝ™X[Z[™ÔÜÝ™\ÜÛœÙT™\Ù]ÙÈHÌÌ
HÂˆ\ÝÝ™X[Z[™ÔÜÝ™\ÜÛœÙT™\Ù]ÙÈH›ÝÎÂˆÙËš[™›ÊœÝ™X[Z[™ÈÜÝ\™\ÜÛœÙHÚÚ\Y\š[™È\[[™H™\Ù]ŠNÂˆBˆ›Ü

NÂˆ™]\›ŽÂˆBˆÛÛœÝ^\Ý[™ÈHÝ™X[Z[™ÔÜÝ™\ÜÛœÙQš[˜[^™\œË™Ù]
Ù\ÜÚ[Û’Q
NÂˆYˆ
ˆ
XØ\XÚ]T™\Ù\™Y	‰‚ˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ÈHX^Ý™X[Z[™ÔÜÝ™\ÜÛœÙ\ÊHˆ
XØ\XÚ]T™\Ù\™Y	‰‚ˆ
^\Ý[™ÏËœ[™[™ÈÏÈ
HHX^Ý™X[Z[™ÔÜÝ™\ÜÛœÙ\Ô\”Ù\ÜÚ[ÛŠBˆ
HÂˆÛÛœÝ›ÝÈH]K››ÝÊ
NÂˆYˆ
›ÝÈH\ÝÝ™X[Z[™ÔÜÝ™\ÜÛœÙSÝ™\™›ÝÓÙÈHÌÌ
HÂˆ\ÝÝ™X[Z[™ÔÜÝ™\ÜÛœÙSÝ™\™›ÝÓÙÈH›ÝÎÂˆÙËØ\›ŠœÝ™X[Z[™ÈÜÝ\™\ÜÛœÙH]Y]YH[È›Ü[™Èš[˜[^™\ˆŠNÂˆBˆ›Ü

NÂˆ™]\›ŽÂˆBˆÛÛœÝÝ]HH^\Ý[™ÈÏÈÈZ[ˆ›ÛZ\ÙKœ™\ÛÛ™J
K[™[™ÎˆNÂˆÛÛœÝ™]š[Ý\ÈHÝ]KZ[ÂˆÝ]Kœ[™[™ÊÊÎÂˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ÊÊÎÂˆYˆ
YZ\ÜÚ[Û’Ù^HOOH[™Yš[™Y
HÂˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ÐžPYZ\ÜÚ[Û’Ù^KœÙ]
ˆYZ\ÜÚ[Û’Ù^Kˆ
Ý™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ÐžPYZ\ÜÚ[Û’Ù^K™Ù]
YZ\ÜÚ[Û’Ù^JHÏÈ
H
ÈKˆ
NÂˆBˆÛÛœÝÝ\œ™[H
\Þ[˜È

HOˆÂˆ]ØZ]™]š[Ý\ÎÂˆ]ØZ]™]È›ÛZ\ÙO›ÚYŠ
™\ÛÛ™JHOˆÙ][[YYX]J™\ÛÛ™JJNÂˆYˆ
Ù[™\˜][ÛˆOOHÝ™X[Z[™ÔÜÝ™\ÜÛœÙQÙ[™\˜][ÛŠHÂˆ›Ü

NÂˆ™]\›ŽÂˆBˆžHÂˆ]ØZ]Ü\˜][ÛŠ
NÂˆHØ]Ú
\œ›ÜŠHÂˆÙË™\œ›ÜŠœÝ™X[Z[™ÈÜÝ\™\ÜÛœÙH›ØÙ\ÜÚ[™È˜Z[Yˆ‹\œ›ÜŠNÂˆBˆJJ
NÂˆÝ]KZ[HÝ\œ™[ÂˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙQš[˜[^™\œËœÙ]
Ù\ÜÚ[Û’QÝ]JNÂˆ›ÚYÝ\œ™[™š[˜[J

HOˆÂˆYˆ
Ý™X[Z[™ÔÜÝ™\ÜÛœÙQš[˜[^™\œË™Ù]
Ù\ÜÚ[Û’Q
HOOHÝ]JH™]\›ŽÂˆÝ]Kœ[™[™ËKNÂˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ËKNÂˆYˆ
YZ\ÜÚ[Û’Ù^HOOH[™Yš[™Y
HÂˆÛÛœÝ™[XZ[š[™ÈBˆ
Ý™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ÐžPYZ\ÜÚ[Û’Ù^K™Ù]
YZ\ÜÚ[Û’Ù^JHÏÈJHHNÂˆYˆ
™[XZ[š[™Èˆ
HÂˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ÐžPYZ\ÜÚ[Û’Ù^KœÙ]
YZ\ÜÚ[Û’Ù^K™[XZ[š[™ÊNÂˆH[ÙHÂˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙT[™[™ÐžPYZ\ÜÚ[Û’Ù^K™[]JYZ\ÜÚ[Û’Ù^JNÂˆBˆBˆYˆ
Ý]KZ[OOHÝ\œ™[	‰ˆÝ]Kœ[™[™ÈOOH
HÂˆÝ™X[Z[™ÔÜÝ™\ÜÛœÙQš[˜[^™\œË™[]JÙ\ÜÚ[Û’Q
NÂˆBˆ[\[™[™ÔÙ\ÜÚ[ÛÛZ[\Ê
NÂˆJNÂŸB‚˜ÛÛœÝPVÔÕ‘PSRS‘×ÔÔÕÔ‘TÔÓ”ÑWÕÐRUT”×ÔT—ÔÑTÔÒSÓˆHMŽÂ˜ÛÛœÝÝ™X[Z[™ÔÜÝ™\ÜÛœÙUØZ]\œÈH™]ÈX\Ýš[™Ë[X™\Š
NÂ‚˜Û\ÜÈÝ™X[Z[™ÔÜÝ™\ÜÛœÙUØZ]Ø\XÚ]Q\œ›Üˆ^[™È\œ›ÜˆßB‚˜\Þ[˜È[˜Ý[Ûˆ]ØZ]Ý™X[Z[™ÔÜÝ™\ÜÛœÙJˆÙ\ÜÚ[Û’QˆÝš[™ËˆÚYÛ˜[ÎˆX›ÜÚYÛ˜[ŠNˆ›ÛZ\ÙO›ÚYˆÂˆYˆ
\Ý™X[Z[™ÔÜÝ™\ÜÛœÙQš[˜[^™\œËš\ÊÙ\ÜÚ[Û’Q
JH™]\›ŽÂˆößmyÚÚ$z{-®éÜj×            config,
                      undefined,
                      {
                        ...cacheOptions,
                        cacheConversation: false,
                      },
                      followUpSignal,
                      requestUpstreamRoute,
                    ),
                  parseJSON: () => {
                    throw new Error(
                      "parseJSON must not be called on the streaming recall path",
                    );
                  },
                };
                const followUpBaseRequest = responsesRecallRequest;
                const follow = await runRecallFollowUpStreaming(
                  followUpCtx,
                  followUpBaseRequest,
                  acc,
                  resultText,
                  recallBlock,
                  signal,
                  finalRecallRound,
                );
                if (!follow.ok) {
                  throw new Error(
                    `recall follow-up upstream error: ${follow.status ?? "?"}`,
                  );
                }
                return {
                  reader: follow.reader,
                  commit: () => {
                    responsesRecallRequest = follow.followUp;
                  },
                };
              },
            }),
          );
        }
        // No recall tool â€” plain passthrough.
        return finishForeground(
          streamResponsesPassthrough(
            upstreamResponse,
            (response, successful) => {
              if (successful) finishStreaming(response);
              else finishUnsuccessfulStreaming(response);
            },
            sessionState.sessionID,
            req.codex ? "codex" : "public",
            foregroundAbort.signal,
          ),
        );
      }
      // Warning to inject, or a non-Responses client: buffer the full
      // upstream, run recall interception, then re-emit.
      const captured = await awaitForeground(
        captureUnsuccessfulResponses(
          accumulateResponsesSSEStream(upstreamResponse, {
            signal: foregroundAbort.signal,
            validation: req.codex ? "codex" : "public",
            stopAtTerminal: true,
            requireCompletedTerminal: true,
          }),
        ),
      );
      if (!captured) {
        return finishForeground(errorResponse(502, "Gateway request failed"));
      }
      if (!captured.successful) {
        if (hasRecallToolUse(captured.response)) {
          return finishForeground(errorResponse(502, "Gateway request failed"));
        }
        return finishForeground(
          nonStreamHttpResponse(
            captured.response,
            req.protocol,
            req.stream,
            undefined,
            requestEnablesLongContext(req),
          ),
        );
      }
      return finishWithRecall(captured.response);
    }

    if (effectiveProtocol === "openai") {
      // OpenAI Chat Completions streaming â€” accumulate and return as
      // non-streaming Anthropic format (same pattern as non-stream path).
      const resp = await awaitForeground(
        accumulateOpenAISSEStream(upstreamResponse, {
          signal: foregroundAbort.signal,
          strict: true,
          stopAtTerminal: true,
          consumeUntilDone: true,
          allowPostTerminalNoop:
            isOpenCodeZenOpenAIStream(requestUpstreamRoute),
        }),
      );
      return finishWithRecall(resp);
    }

    if (effectiveProtocol === "gemini") {
      // Gemini native streaming â€” accumulate the SSE frames, then re-emit via
      // the recall-aware finalizer (same buffered pattern as the OpenAI paths).
      const resp = await awaitForeground(
        accumulateGeminiSSEStream(upstreamResponse, {
          signal: foregroundAbort.signal,
          strict: true,
          stopAtTerminal: true,
        }),
      );
      return finishWithRecall(resp);
    }

    // Anthropic streaming: forward events and accumulate in parallel.
    // Pass recall context so the accumulator can intercept recall tool_use.
    const hasRecallTool = modifiedReq.tools.some(
      (t) => t.name === RECALL_TOOL_NAME,
    );
    const anthropicSSE = buildStreamingResponse(
      upstreamResponse,
      finishStreaming,
      hasRecallTool
        ? {
            clientMessages: recallClientMessages,
            modifiedReq,
            config,
            sessionState,
            cacheOptions,
            upstreamRoute: requestUpstreamRoute,
            noStore: suppressTemporalStorage,
            onFailure: finishUnsuccessfulStreaming,
            onTransactionReady: (transaction) => {
              rollbackRecallPersistence();
              recallPersistenceTransaction = transaction;
            },
            clientSpeaksAnthropic: req.protocol === "anthropic",
            stableLtmText,
            recallDeadlineAt: foregroundAbort.deadlineAt,
            ...(pendingKnowledgeDelta ? { pendingKnowledgeDelta } : {}),
          }
        : undefined,
      warningText,
      sessionState.sessionID,
      // Cap usage against the window the CLIENT meters against: the model's real
      // window only when this request opted into it via the context-1m beta,
      // else 200K â€” so a 1M-capable model the client meters against 200K can't
      // cross its ~167K auto-compact threshold (#910 regression; MiniMax-M3).
      maxReportedUsageForModelID(req.model, requestEnablesLongContext(req)),
      foregroundAbort.signal,
    );
    // Translate to client's wire format if needed. When the upstream is
    // Anthropic but the client speaks OpenAI, wrap the Anthropic SSE stream.
    if (req.protocol === "openai") {
      return finishForeground(
        translateAnthropicStreamToOpenAI(anthropicSSE, {
          signal: foregroundAbort.signal,
          propagateErrors: true,
        }),
      );
    }
    if (req.protocol === "openai-responses") {
      return finishForeground(
        translateAnthropicStreamToResponses(anthropicSSE, {
          signal: foregroundAbort.signal,
        }),
      );
    }
    if (req.protocol === "gemini") {
      return finishForeground(
        translateAnthropicStreamToGemini(anthropicSSE, {
          signal: foregroundAbort.signal,
        }),
      );
    }
    return finishForeground(anthropicSSE);
  }

  // Non-streaming: dispatch to correct accumulator based on upstream protocol.
  const captured = await awaitForeground(
    captureUnsuccessfulResponses(
      accumulateNonStreamResponse(
        upstreamResponse,
        effectiveProtocol,
        modifiedReq.codex === true,
        foregroundAbort.signal,
      ),
    ),
  );
  if (!captured) {
    return finishForeground(errorResponse(502, "Gateway request failed"));
  }
  if (!captured.successful) {
    if (hasRecallToolUse(captured.response)) {
      return finishForeground(errorResponse(502, "Gateway request failed"));
    }
    return finishForeground(
      nonStreamHttpResponse(
        captured.response,
        req.protocol,
        req.stream,
        undefined,
        requestEnablesLongContext(req),
      ),
    );
  }
  return finishWithRecall(captured.response);
}

/**
 * Decide whether request-only Responses provenance may cross this transform.
 *
 * Encrypted reasoning is deliberately not part of Lore messages, temporal
 * storage, or embeddings. It is replayed only while the gradient layer is
 * stable; a layer transition is a compaction boundary and intentionally drops
 * the old wire provenance. A fresh in-memory session has no prior boundary
 * (`null`) and may replay its supplied history; persisted sessions with the
 * v89 `-1` sentinel fail closed until an upstream turn establishes one.
 * Emergency Layer 4 never replays it.
 *
 * @internal Exported for focused policy tests.
 */
export function shouldPreserveResponsesProvenance(
  previousLayer: number | null,
  currentLayer: number,
): boolean {
  return (
    currentLayer < 4 &&
    (previousLayer === null || previousLayer === currentLayer)
  );
}

/**
 * Provider-native thinking/encrypted blocks are opaque and valid only on the
 * same wire family that produced them. A cross-protocol request keeps its
 * visible projection but drops request-only provenance rather than sending
 * Anthropic blocks to Gemini, Gemini signatures to Anthropic, or Responses
 * reasoning items to Chat Completions.
 *
 * Vertex and Bedrock use the Anthropic Messages body, so they share the
 * Anthropic provenance family.
 *
 * @internal Exported for focused policy tests.
 */
export function canReplayRequestProvenance(
  ingressProtocol: GatewayProtocol,
  effectiveProtocol: GatewayProtocol,
): boolean {
  const family = (protocol: GatewayProtocol): string =>
    protocol === "vertex" ? "anthropic" : protocol;
  return family(ingressProtocol) === family(effectiveProtocol);
}

// ---------------------------------------------------------------------------
// Lore message â†’ Gateway message conversion
// ---------------------------------------------------------------------------

/**
 * Convert transformed Lore messages back to gateway message format.
 *
 * This reverses `gatewayMessagesToLore` after gradient transform has
 * potentially trimmed/reordered messages.
 *
 * Completed/error tool parts on assistant messages produce BOTH a `tool_use`
 * block on the assistant AND a corresponding `tool_result` block injected at
 * the start of the following user message. This makes the conversion
 * self-contained: tool pairing is reconstructed from whatever messages
 * survived gradient eviction, without depending on cross-message `tool_result`
 * parts that can become orphaned when the assistant message is evicted.
 *
 * `resolveToolResults()` strips `tool: "result"` parts from user messages
 * after pairing, so under normal operation those parts are gone. The fallback
 * handling for residual `tool: "result"` parts is kept for robustness.
 */
/**
 * Reconstruct tool_result content as a `GatewayContentBlock[]` from a Lore
 * tool state. If structured `blocks` were preserved (non-text sub-blocks like
 * images), re-emit them losslessly; otherwise wrap the text string.
 */
function toolResultContent(state: {
  status: string;
  output?: string;
  error?: string;
  blocks?: unknown[];
}): GatewayContentBlock[] {
  if (state.blocks && state.blocks.length > 0) {
    // Re-emit the structured blocks that were preserved from ingress.
    return state.blocks as GatewayContentBlock[];
  }
  const text =
    state.status === "error"
      ? (state.error ?? "[error]")
      : (state.output ?? "");
  return text ? [{ type: "text", text }] : [];
}

/** @internal Exported for tests. */
export function loreMessagesToGateway(
  messages: LoreMessageWithParts[],
  provenanceByMessageId: ReadonlyMap<
    string,
    Pick<
      GatewayMessage,
      "content" | "provenanceContent" | "provenancePositions"
    >
  > = new Map(),
  allowProvenance = true,
): GatewayMessage[] {
  const out: GatewayMessage[] = [];

  // tool_result blocks reconstructed from the preceding assistant message's
  // completed/error tool parts. Injected at the start of the next user message.
  let pendingToolResults: GatewayContentBlock[] = [];

  for (const msg of messages) {
    const content: GatewayContentBlock[] = [];

    if (msg.info.role === "user") {
      // Inject reconstructed tool_result blocks from preceding assistant
      content.push(...pendingToolResults);
      pendingToolResults = [];
    } else {
      // New assistant message â€” reset pending results (shouldn't have any
      // in well-formed conversations, but handles back-to-back assistants)
      pendingToolResults = [];
    }

    for (const part of msg.parts) {
      switch (part.type) {
        case "text":
          content.push({
            type: "text",
            text: (part as { text: string }).text,
          });
          break;
        case "reasoning":
          // Native/encrypted reasoning is request-only provenance. Older
          // temporal rows may still contain a reasoning part from before that
          // boundary existed; never promote it back into visible request
          // content on replay.
          break;
        case "tool": {
          const toolPart = part as {
            type: "tool";
            tool: string;
            callID: string;
            toolName?: string;
            state: {
              status: string;
              input?: unknown;
              output?: string;
              error?: string;
            };
          };
          if (toolPart.tool === "result") {
            // Residual tool_result part (should have been stripped by
            // resolveToolResults, but handle gracefully for robustness)
            content.push({
              type: "tool_result",
              toolUseId: toolPart.callID,
              ...(toolPart.toolName ? { toolName: toolPart.toolName } : {}),
              content: toolResultContent(toolPart.state),
            });
          } else {
            // Emit tool_use on this assistant message
            content.push({
              type: "tool_use",
              id: toolPart.callID,
              name: toolPart.tool,
              input: toolPart.state.input ?? {},
            });
            // Completed/error tool parts: queue a tool_result for the next
            // user message. This reconstructs the Anthropic API's split-
            // message format from Lore's single-message representation.
            if (toolPart.state.status === "completed") {
              pendingToolResults.push({
                type: "tool_result",
                toolUseId: toolPart.callID,
                toolName: toolPart.toolName ?? toolPart.tool,
                content: toolResultContent(toolPart.state),
              });
            } else if (toolPart.state.status === "error") {
              pendingToolResults.push({
                type: "tool_result",
                toolUseId: toolPart.callID,
                toolName: toolPart.toolName ?? toolPart.tool,
                content: toolResultContent(toolPart.state),
                isError: true,
              });
            }
            // Pending tool parts (not yet resolved) only emit tool_use â€”
            // the model will see an unresolved tool call. sanitizeToolParts
            // in gradient.ts converts these to error state before this point.
          }
          break;
        }
        // Opaque parts (image, audio, document, â€¦) â€” reconstruct the
        // gateway opaque block from the generic part's raw payload.
        default:
          if (
            "raw" in part &&
            typeof part.raw === "object" &&
            part.raw !== null
          ) {
            content.push({
              type: "opaque",
              raw: part.raw as Record<string, unknown>,
            });
          } else if ("text" in part && typeof part.text === "string") {
            content.push({ type: "text", text: part.text });
          }
          break;
      }
    }

    const message: GatewayMessage = { role: msg.info.role, content };
    const provenance = allowProvenance
      ? provenanceByMessageId.get(msg.info.id)
      : undefined;
    if (
      provenance?.provenanceContent &&
      JSON.stringify(content) === JSON.stringify(provenance.content)
    ) {
      message.provenanceContent = [...provenance.provenanceContent];
      if (provenance.provenancePositions) {
        message.provenancePositions = [...provenance.provenancePositions];
      }
    }
    out.push(message);
  }

  return out;
}

// ---------------------------------------------------------------------------
// Post-conversion validation: remove orphaned tool_result blocks
// ---------------------------------------------------------------------------

/**
 * Belt-and-suspenders safety net: ensures every `tool_result` block on a user
 * message references a `tool_use` block on the immediately preceding assistant
 * message. Removes orphans and logs a warning.
 *
 * This should never fire under normal operation (resolveToolResults strips
 * redundant tool_result parts, and loreMessagesToGateway reconstructs them
 * from the assistant's completed tool parts). But if a future code path
 * introduces orphaned references, this catches them before they reach the API.
 */
/** @internal Exported for tests. */
function clearGatewayMessageProvenance(message: GatewayMessage): void {
  delete message.provenanceContent;
  delete message.provenancePositions;
}

/** @internal Exported for tests. */
export function removeOrphanedToolResults(messages: GatewayMessage[]): void {
  // --- Pass 1: Remove orphaned tool_result blocks (tool_result â†’ tool_use) ---
  for (let i = 0; i < messages.length; i++) {
    const msg = messages[i];
    if (msg?.role !== "user") continue;
    if (!msg.content.some((b) => b.type === "tool_result")) continue;

    // Collect tool_use IDs from the preceding assistant message
    const prevMsg = i > 0 ? messages[i - 1] : undefined;
    const prev = prevMsg?.role === "assistant" ? prevMsg : null;
    const toolUseIds = new Set(
      (prev?.content ?? [])
        .filter((b): b is GatewayToolUseBlock => b.type === "tool_use")
        .map((b) => b.id),
    );

    // Remove tool_result blocks that reference missing tool_use IDs
    const before = msg.content.length;
    msg.content = msg.content.filter(
      (b) => b.type !== "tool_result" || toolUseIds.has(b.toolUseId),
    );
    if (msg.content.length < before) {
      // Provenance is serialized in preference to visible content by every
      // same-family request builder. Once cleanup changes the visible tool
      // sequence, retaining the old provider-native sequence could resurrect
      // an orphaned tool call (and its encrypted reasoning) on the wire.
      clearGatewayMessageProvenance(msg);
      log.warn(
        `removed ${before - msg.content.length} orphaned tool_result block(s) from message ${i}`,
      );
    }
    // If the user message is now empty, add placeholder text so the API
    // doesn't reject an empty content array.
    if (msg.content.length === 0) {
      msg.content = [{ type: "text", text: "[tool results provided]" }];
    }
  }

  // --- Pass 2: Remove orphaned tool_use blocks (tool_use â†’ tool_result) ---
  // Every tool_use on an assistant must have a matching tool_result on the
  // immediately following user message. Without this, the Anthropic API
  // rejects with "tool_use ids found without tool_result blocks immediately
  // after". This catches edge cases where gradient eviction or back-to-back
  // assistants leave tool_use blocks without matching results (#424).
  for (let i = 0; i < messages.length; i++) {
    const msg = messages[i];
    if (msg?.role !== "assistant") continue;
    if (!msg.content.some((b) => b.type === "tool_use")) continue;

    // Collect tool_result IDs from the following user message
    const nextMsg = i + 1 < messages.length ? messages[i + 1] : undefined;
    const next = nextMsg?.role === "user" ? nextMsg : null;
    const toolResultIds = new Set(
      (next?.content ?? [])
        .filter((b): b is GatewayToolResultBlock => b.type === "tool_result")
        .map((b) => b.toolUseId),
    );

    // Remove tool_use blocks that have no matching tool_result
    const before = msg.content.length;
    msg.content = msg.content.filter(
      (b) => b.type !== "tool_use" || toolResultIds.has(b.id),
    );
    if (msg.content.length < before) {
      clearGatewayMessageProvenance(msg);
      log.warn(
        `removed ${before - msg.content.length} orphaned tool_use block(s) from assistant message ${i}`,
      );
    }
    // If the assistant message is now empty, add placeholder text.
    if (msg.content.length === 0) {
      msg.content = [{ type: "text", text: "[assistant response]" }];
    }
  }
}

// ---------------------------------------------------------------------------
// Slash command interception (/lore:warm:*)
// ---------------------------------------------------------------------------

/**
 * Extract the text of the last user message, trimmed.
 * Returns empty string if no user message found.
 */
function lastUserTextTrimmed(req: GatewayRequest): string {
  for (let i = req.messages.length - 1; i >= 0; i--) {
    const msg = req.messages[i];
    if (msg.role !== "user") continue;
    const text = msg.content
      .filter((b) => b.type === "text")
      .map((b) => b.text)
      .join("\n")
      .trim();
    return text;
  }
  return "";
}

// ---------------------------------------------------------------------------
// Generic /lore:* slash command dispatcher
// ---------------------------------------------------------------------------

/**
 * Intercepts all `/lore:*` slash commands. Routes to specific handlers
 * and returns a synthetic response. Unknown `/lore:*` commands get a
 * helpful error response instead of being forwarded upstream.
 */
async function handleLoreSlashCommand(
  req: GatewayRequest,
  allSessions: Map<string, SessionState>,
  config: GatewayConfig,
  claimSession: (sessionID: string) => Promise<void>,
): Promise<Response | null> {
  const text = lastUserTextTrimmed(req);
  if (!text.toLowerCase().startsWith("/lore:")) return null;
  if (requestSourcePrefix(req)) {
    throw new SourceDeltaUnavailableError(
      "A checkpointed continuation must be replayed in full before running a slash command.",
    );
  }

  let state = findLiveSessionState(req, config, allSessions);
  const indexedSessionID = findIndexedSessionID(req, config);
  if (!state && indexedSessionID) {
    const pathResult = getProjectPath(req.system, req.rawHeaders);
    state = getOrCreateSession(
      indexedSessionID,
      pathResult.path,
      pathResult.source,
      requestCredentialFingerprint(req.rawHeaders, config) ?? "",
      config,
    );
  }
  const sessionID = indexedSessionID ?? state?.sessionID;
  if (sessionID) {
    await claimSession(sessionID);
    if (
      indexedSessionID &&
      !confirmedIndexedIdentityResolvesTo(req, sessionID, config)
    ) {
      return slashResponse(
        req,
        "No authenticated active session found.",
        `msg_lore_${Date.now()}`,
      );
    }
    await awaitStreamingPostResponse(sessionID, req.signal);
    req.signal?.throwIfAborted();
    if (
      indexedSessionID &&
      !confirmedIndexedIdentityResolvesTo(req, sessionID, config)
    ) {
      return slashResponse(
        req,
        "No authenticated active session found.",
        `msg_lore_${Date.now()}`,
      );
    }
  }

  // Route to specific handlers
  const warmupResult = handleWarmupSlashCommand(req, allSessions, config);
  if (warmupResult) return warmupResult;

  const curateResult = await handleCurateSlashCommand(
    req,
    allSessions,
    config,
    claimSession,
  );
  if (curateResult) return curateResult;

  const amnesiaResult = handleAmnesiaSlashCommand(req, allSessions, config);
  if (amnesiaResult) return amnesiaResult;

  // Unknown /lore:* command â€” return error instead of forwarding upstream
  log.warn(`unknown slash command: ${text}`);
  return slashResponse(
    req,
    `Unknown command: ${text}. Available: /lore:curate, /lore:warm:stop|keep|auto|on|off|reset, /lore:amnesia:on|off`,
    `msg_lore_${Date.now()}`,
  );
}

// ---------------------------------------------------------------------------
// /lore:amnesia â€” toggle temporal storage and background work
// ---------------------------------------------------------------------------

/**
 * `/lore:amnesia:on` â€” suppresses temporal storage and background work.
 * `/lore:amnesia:off` â€” resumes normal storage.
 *
 * The session still gets full Lore processing (LTM injection, recall tool,
 * gradient transform) but doesn't write new memories. Useful for eval QA
 * questions, read-only introspection, and sensitive conversations.
 */
function handleAmnesiaSlashCommand(
  req: GatewayRequest,
  allSessions: Map<string, SessionState>,
  config: GatewayConfig,
): Response | null {
  const text = lastUserTextTrimmed(req);
  const lower = text.toLowerCase();

  const isOn = lower === "/lore:amnesia:on";
  const isOff = lower === "/lore:amnesia:off";
  if (!isOn && !isOff) return null;

  const state = findLiveSessionState(req, config, allSessions);

  if (!state) {
    return slashResponse(
      req,
      "No active session found. Amnesia mode was not changed.",
      `msg_lore_${Date.now()}`,
    );
  }

  state.amnesia = isOn;
  saveSessionTracking(state.sessionID, { amnesia: isOn });
  log.info(
    `amnesia: ${lower} for session=${state.sessionID.slice(0, 16)} â€” ` +
      `storage ${isOn ? "suppressed" : "resumed"}`,
  );

  const responseText = isOn
    ? "Amnesia mode on â€” memory storage suppressed. Recall still works."
    : "Amnesia mode off â€” memory storage resumed.";
  return slashResponse(req, responseText, `msg_lore_${Date.now()}`);
}

// ---------------------------------------------------------------------------
// /lore:warm â€” cache warming control
// ---------------------------------------------------------------------------

/**
 * Check if the last user message is a warmup slash command.
 *
 * `/lore:warm:stop` â€” disables cache warming for this session.
 * `/lore:warm:keep` â€” forces cache warming regardless of survival analysis.
 * `/lore:warm:auto` â€” returns to normal survival-analysis-driven mode.
 * `/lore:warm:reset` â€” clears ALL tripped circuit-breaker buckets (re-enables
 *   warming that was disabled after repeated uncached warmups).
 * `/lore:warm:off` â€” disables cache warming GLOBALLY (persisted override).
 * `/lore:warm:on` â€” re-enables cache warming globally.
 *
 * Returns a synthetic Anthropic-format response if a command was matched,
 * or null to continue normal processing.
 */
function handleWarmupSlashCommand(
  req: GatewayRequest,
  allSessions: Map<string, SessionState>,
  config: GatewayConfig,
): Response | null {
  const text = lastUserTextTrimmed(req);
  const lower = text.toLowerCase();

  const isStop = lower === "/lore:warm:stop";
  const isKeep = lower === "/lore:warm:keep";
  const isAuto = lower === "/lore:warm:auto";
  const isReset = lower === "/lore:warm:reset";
  const isOff = lower === "/lore:warm:off";
  const isOn = lower === "/lore:warm:on";
  if (!isStop && !isKeep && !isAuto && !isReset && !isOff && !isOn) return null;

  const state = findLiveSessionState(req, config, allSessions);

  if (
    (isReset || isOff || isOn) &&
    (config.remoteGateway || config.hostedMode)
  ) {
    return errorResponse(
      403,
      "Global cache-warming administration is unavailable on remote gateways",
    );
  }

  // Global controls require an authenticated, resolved session. Otherwise any
  // network caller could persistently change warming for every tenant.
  if (
    (isReset || isOff || isOn) &&
    (isHostedMode() ||
      !state ||
      !state.lastUpstream ||
      !extractAuth(req.rawHeaders))
  ) {
    return slashResponse(
      req,
      "No authenticated active session found. Global cache warming was not changed.",
      `msg_lore_${Date.now()}`,
    );
  }

  // Reset is a breaker-wide admin action.
  if (isReset) {
    resetCircuitBreaker();
    log.info(
      "cache-warmer: /lore:warm:reset received â€” circuit breaker cleared",
    );
    return slashResponse(
      req,
      "Cache warming circuit breaker reset.",
      `msg_lore_${Date.now()}`,
    );
  }

  // on/off are GLOBAL admin actions (persisted KV override).
  if (isOff || isOn) {
    setWarmingEnabled(isOn);
    log.info(
      `cache-warmer: /lore:warm:${isOn ? "on" : "off"} received â€” warming globally ${isOn ? "enabled" : "disabled"}`,
    );
    return slashResponse(
      req,
      isOn
        ? "Cache warming enabled globally."
        : "Cache warming disabled globally.",
      `msg_lore_${Date.now()}`,
    );
  }

  // Update session warmup state
  if (state) {
    if (!state.warmup) {
      state.warmup = {
        lastWarmupAt: 0,
        warmupCount: 0,
        totalWarmups: 0,
        warmupHits: 0,
        disabled: false,
      };
    }
    if (isStop) {
      state.warmup.disabled = true;
      state.warmup.forceKeepWarm = false;
    } else if (isKeep) {
      state.warmup.forceKeepWarm = true;
      state.warmup.disabled = false;
    } else {
      // isAuto â€” return to normal survival-analysis mode
      state.warmup.disabled = false;
      state.warmup.forceKeepWarm = false;
    }
    const modeLabel = isStop ? "stopped" : isKeep ? "forced" : "auto";
    log.info(
      `cache-warmer: ${lower} received for session=${state.sessionID.slice(0, 16)} â€” ` +
        `warming mode: ${modeLabel}`,
    );
  }

  const responseText = isStop
    ? "Cache warming stopped."
    : isKeep
      ? "Keeping cache warm."
      : "Cache warming set to auto.";
  return slashResponse(req, responseText, `msg_lore_${Date.now()}`);
}

// ---------------------------------------------------------------------------
// Slash command: /lore:curate â€” synchronous distillation + curation
// ---------------------------------------------------------------------------

/**
 * `/lore:curate` â€” runs distillation + curation synchronously for the
 * current session and returns the results. Useful for:
 * - Eval harnesses that need curation to complete between session replays
 * - Users who want to force knowledge extraction after a conversation
 *
 * Returns a synthetic response with the curation results.
 */
async function handleCurateSlashCommand(
  req: GatewayRequest,
  allSessions: Map<string, SessionState>,
  config: GatewayConfig,
  claimSession: (sessionID: string) => Promise<void>,
): Promise<Response | null> {
  const text = lastUserTextTrimmed(req);
  if (text.toLowerCase() !== "/lore:curate") return null;

  const indexedSessionID = findIndexedSessionID(req, config);
  const pathResult = getProjectPath(req.system, req.rawHeaders);
  let state = findLiveSessionState(req, config, allSessions);
  let sessionID = state?.sessionID;

  if (!state && indexedSessionID) {
    state = getOrCreateSession(
      indexedSessionID,
      pathResult.path,
      pathResult.source,
      requestCredentialFingerprint(req.rawHeaders, config) ?? "",
      config,
    );
    sessionID = indexedSessionID;
  }

  if (!sessionID || !state) {
    return slashResponse(
      req,
      "No active session found for curation.",
      "msg_lore_curate_none",
    );
  }

  await claimSession(sessionID);
  if (
    indexedSessionID &&
    !confirmedIndexedIdentityResolvesTo(req, sessionID, config)
  ) {
    return slashResponse(
      req,
      "No active session found for curation.",
      "msg_lore_curate_none",
    );
  }
  await awaitStreamingPostResponse(sessionID, req.signal);
  req.signal?.throwIfAborted();
  if (
    indexedSessionID &&
    !confirmedIndexedIdentityResolvesTo(req, sessionID, config)
  ) {
    return slashResponse(
      req,
      "No active session found for curation.",
      "msg_lore_curate_none",
    );
  }

  const projectPath = resolveSessionProjectPath(pathResult, state, config);
  saveSessionTracking(sessionID, {
    projectPath: state.projectPath || null,
    projectPathProvisional: state.projectPathProvisional === true,
  });
  const { distillation, curator } = await import("@loreai/core");
  req.signal?.throwIfAborted();
  const llm = getLLMClient(config);
  const model = getWorkerModel(state.lastUpstream);

  log.info(`/lore:curate: running for session=${sessionID.slice(0, 16)}`);

  // Force-distill all pending messages (urgent bypasses batch queue)
  let distilled = 0;
  try {
    const dResult = await distillation.run({
      llm,
      projectPath,
      sessionID,
      model,
      force: true,
      skipMeta: true,
      urgent: true,
      callType: "direct",
      signal: req.signal,
      workerHealth: makeWorkerHealth(sessionID, "lore-distill"),
      // #627 Phase 1: stamp the session's gitHead on slash-curate rows.
      metadata: buildSessionMetadata(state.gitHead),
    });
    req.signal?.throwIfAborted();
    distilled = dResult.distilled;
  } catch (e) {
    req.signal?.throwIfAborted();
    log.error("/lore:curate distillation error:", e);
  }

  // Run curation (uses urgent/direct call via the LLM client)
  let created = 0;
  let updated = 0;
  let deleted = 0;
  try {
    const cResult = await curator.run({
      llm,
      projectPath,
      sessionID,
      model,
      signal: req.signal,
      workerHealth: makeWorkerHealth(sessionID, "lore-curator"),
      // #627 Phase 1: stamp the session's gitHead on slash-curate entries.
      metadata: buildSessionMetadata(state.gitHead),
    });
    req.signal?.throwIfAborted();
    created = cResult.created;
    updated = cResult.updated;
    deleted = cResult.deleted;
  } catch (e) {
    req.signal?.throwIfAborted();
    log.error("/lore:curate curation error:", e);
  }

  const responseText =
    `Curation complete: ${distilled} segments distilled, ` +
    `${created} entries created, ${updated} updated, ${deleted} deleted.`;

  log.info(`/lore:curate: ${responseText}`);

  return slashResponse(req, responseText, `msg_lore_curate_${Date.now()}`);
}

/** Build a synthetic slash-command response in the client's wire format. */
function slashResponse(
  req: GatewayRequest,
  text: string,
  msgId: string,
): Response {
  // Build a GatewayResponse and use the protocol-aware response builders
  // so slash commands work correctly for all client protocols.
  const resp: GatewayResponse = {
    id: msgId,
    model: req.model,
    content: [{ type: "text", text }],
    stopReason: "end_turn",
    usage: {
      inputTokens: 0,
      outputTokens: 0,
      cacheReadInputTokens: 0,
      cacheCreationInputTokens: 0,
    },
  };

  if (req.stream) {
    // Build Anthropic SSE, then translate to client's format if needed
    const anthropicSSE = streamHttpResponse(resp);
    if (req.protocol === "openai") {
      return translateAnthropicStreamToOpenAI(anthropicSSE, {
        signal: req.signal,
      });
    }
    if (req.protocol === "openai-responses") {
      return translateAnthropicStreamToResponses(anthropicSSE, {
        signal: req.signal,
      });
    }
    if (req.protocol === "gemini") {
      return translateAnthropicStreamToGemini(anthropicSSE, {
        signal: req.signal,
      });
    }
    return anthropicSSE;
  }

  return nonStreamHttpResponse(
    resp,
    req.protocol,
    req.stream,
    undefined,
    requestEnablesLongContext(req),
  );
}

// ---------------------------------------------------------------------------
// Error response builder
// ---------------------------------------------------------------------------

function errorResponse(status: number, message: string): Response {
  return new Response(
    JSON.stringify({
      type: "error",
      error: {
        type: "server_error",
        message,
      },
    }),
    {
      status,
      headers: { "content-type": "application/json" },
    },
  );
}

// ---------------------------------------------------------------------------
// Main entry point
// ---------------------------------------------------------------------------

/**
 * Process an incoming gateway request through the full Lore pipeline.
 *
 * Returns a standard `Response` object â€” either a streaming SSE response
 * or a JSON response, depending on the client's `stream` setting.
 */
async function handleRequestForTenant(
  req: GatewayRequest,
  config: GatewayConfig,
): Promise<Response> {
  if (!req?.rawHeaders) {
    return errorResponse(400, "Malformed request: missing headers");
  }
  if (pipelineResetInProgress) {
    return errorResponse(503, "Gateway pipeline is resetting");
  }
  streamingPostResponsesAccepting = true;
  const requestGeneration = streamingPostResponseGeneration;
  let resolveDownstreamSettled: (() => void) | undefined;
  let downstreamCancelled = false;
  const downstreamSettled = new Promise<void>((resolve) => {
    resolveDownstreamSettled = resolve;
  });
  return runActivePipelineRequest(
    req.signal,
    (signal, trackOperation, claimSession) =>
      handleRequestInner(
        { ...req, signal },
        config,
        requestGeneration,
        downstreamSettled,
        () => downstreamCancelled,
        trackOperation,
        claimSession,
      ),
    () => resolveDownstreamSettled?.(),
    () => {
      downstreamCancelled = true;
    },
    requestCredentialFingerprint(req.rawHeaders, config) ?? undefined,
  );
}

async function handleRequestInner(
  req: GatewayRequest,
  config: GatewayConfig,
  requestGeneration: number,
  downstreamSettled: Promise<void>,
  downstreamWasCancelled: () => boolean,
  trackOperation: (operation: Promise<unknown>) => void,
  claimSession: (sessionID: string) => Promise<void>,
): Promise<Response> {
  const requestStartMs = Date.now();
  const requestOrder = ++upstreamRequestOrder;
  try {
    // Guard against malformed invocations (e.g. fuzzers / direct module calls
    // that pass an undefined or header-less request). The real server path
    // always supplies a fully-formed GatewayRequest; bailing out cleanly here
    // avoids a TypeError on `req.rawHeaders` deeper in the pipeline.
    if (!req?.rawHeaders) {
      return errorResponse(400, "Malformed request: missing headers");
    }

    if (hasConflictingAuthHeaders(req.rawHeaders)) {
      return errorResponse(
        400,
        "Conflicting authentication headers: send either x-api-key or Authorization, not both",
      );
    }

    // Validate explicit provider/upstream selection before slash, side-channel,
    // compaction, and meta branches can take alternate paths. This resolver is
    // synchronous and performs no network I/O.
    try {
      resolveRequestUpstreamRoute(req, config);
    } catch (error) {
      return errorResponse(
        400,
        error instanceof Error ? error.message : "Invalid upstream route",
      );
    }

    // Preserve the process-global legacy credential only for a local,
    // header-less request to the exact configured provider base.
    const earlyAuth = extractAuth(req.rawHeaders);
    if (earlyAuth) {
      captureLegacyGlobalAuth(req, config, earlyAuth);
    }

    // --- Quick Tier-1 session lookup for structural compaction detection ---
    // O(1) header + map lookup â€” lets us compare message counts before routing.
    const priorState = activeSessionForKnownHeader(req, sessions, config);

    // --- Case 0: Slash command interception (/lore:*) ---
    // All /lore:* commands are intercepted here and never forwarded upstream.
    const slashResult = await handleLoreSlashCommand(
      req,
      sessions,
      config,
      claimSession,
    );
    if (slashResult) return slashResult;

    // --- Case 0.5: Claude Code side-channel â†’ forward upstream untouched ---
    // Auto-mode permission classifier, title/topic generation, and subagent
    // namer/summary calls carry the live session's `x-claude-code-session-id`
    // but NO coding system prompt (skipSystemPromptPrefix). They must never
    // enter the pipeline: running them through it injects LTM/distilled
    // prefixes or (worse) mis-routes them to compaction â€” corrupting the
    // auto-mode classifier verdict and tripping Claude Code's 3-strike fallback
    // that drops auto mode back to prompting for every action. This check MUST
    // stay ahead of the structural-compaction detection below.
    if (isClaudeCodeSideChannel(req)) {
      log.info(
        `claude-code side-channel: passthrough (messages=${req.messages.length} tools=${req.tools.length} maxTokens=${req.maxTokens})`,
      );
      return await handlePassthrough(req, config);
    }

    // --- Case 1: Compaction request â†’ intercept ---
    // Structural detection (session-aware) first, pattern matching as fallback.
    // Sub-agents now get their own sessions (separate x-session-affinity /
    // x-claude-code-agent-id), so priorState is the sub-agent's own state â€”
    // structural detection is safe.
    //
    // IMPORTANT: a Claude Code sub-agent still shares the parent's
    // `x-claude-code-session-id`. Before x-claude-code-agent-id was added to the
    // known-header priority (see credential-headers.ts), `activeSessionForKnownHeader`
    // resolved it to the PARENT session, whose large message count made the
    // sub-agent's short first request (1 user message + tool-schema attachments)
    // look like a structural compaction. The gateway then intercepted it and
    // returned the offline "# Session Summary" block as the sub-agent's
    // task_result. Guarding structural detection on the sub-agent signal closes
    // that hole regardless of which header resolved the session.
    const isClaudeSubagent = isClaudeCodeSubagent(req.rawHeaders);
    // A checkpointed continuation deliberately contains only the new suffix. Its
    // small request-local message count must not look like a large session
    // suddenly compacting. Pattern-based detection remains enabled because a
    // suffix can still explicitly request compaction.
    const isCheckpointContinuation =
      req.sourceInput?.sourcePrefix !== undefined;
    const structuralCompaction =
      !isClaudeSubagent &&
      !isCheckpointContinuation &&
      isStructuralCompaction(req, priorState);
    const patternDetection = structuralCompaction
      ? undefined
      : detectCompactionRequest(req);
    if (structuralCompaction || patternDetection?.detected) {
      const reason = structuralCompaction
        ? `structural (prior=${priorState?.messageCount ?? "?"} curr=${requestSourceMessageCount(req)})`
        : patternDetection?.detected
          ? patternDetection.reason === "system-prompt"
            ? `pattern: system-prompt match "${patternDetection.pattern}"`
            : patternDetection.reason === "user-keywords"
              ? `pattern: user-keyword match "${patternDetection.pattern}"`
              : `pattern: template-sections (${patternDetection.matchCount} matches)`
          : "unknown";
      log.info(
        `compaction detected: ${reason} messages=${requestSourceMessageCount(req)} tools=${req.tools.length}`,
      );
      return await handleCompaction(
        req,
        config,
        requestGeneration,
        trackOperation,
        claimSession,
      );
    }

    // --- Case 2: Meta request (title gen, summary, categorization, etc.) â†’ passthrough ---
    if (isMetaRequest(req, requestSourceMessageCount(req))) {
      log.info(
        `meta request detected: messages=${req.messages.length} tools=${req.tools.length}` +
          ` maxTokens=${req.maxTokens} agent=${req.rawHeaders[LORE_AGENT_HEADER] ?? "none"}`,
      );
      return await handlePassthrough(req, config);
    }

    // --- Case 3: Normal conversation turn â†’ full pipeline ---
    return await handleConversationTurn(
      req,
      config,
      requestOrder,
      requestGeneration,
      downstreamSettled,
      downstreamWasCancelled,
      claimSession,
    );
  } catch (err) {
    if (err instanceof SourceDeltaUnavailableError) {
      const response = errorResponse(
        409,
        "The retained Lore context no longer matches the request prefix; retrying with the full conversation.",
      );
      response.headers.set(CONTEXT_BOUNDARY_MISMATCH_HEADER, "true");
      return response;
    }
    // Client disconnect / abort is benign â€” downgrade from error to info.
    const isAbort = err instanceof DOMException && err.name === "AbortError";
    if (isAbort) {
      const reason = err.message ? `: ${err.message}` : "";
      log.info(`pipeline aborted (client disconnect${reason})`);
      // Only surfaces to Sentry if the host was under pressure at abort time.
      captureClientAbortUnderPressure({
        startMs: requestStartMs,
        route: "request",
      });
    } else {
      // Only log fixed internal failures. Arbitrary parser/fetch messages can
      // contain upstream response content, which must never reach the log.
      const detail =
        err instanceof OpenAIStreamValidationError
          ? config.exposeProviderDiagnostics
            ? `: ${err.message} (rule=${err.rule})`
            : `: ${err.message}`
          : err instanceof Error &&
              [
                "fetch failed",
                "missing OpenAI finish_reason terminal",
                "missing OpenAI [DONE] terminal",
                "Upstream response has no body",
              ].includes(err.message)
            ? `: ${err.message}`
            : "";
      log.error(`pipeline request failed${detail}`);
    }
    return errorResponse(502, "Gateway request failed");
  }
}

export async function handleRequest(
  req: GatewayRequest,
  config: GatewayConfig,
): Promise<Response> {
  if (!req?.rawHeaders) return handleRequestForTenant(req, config);
  return withRequestStorageTenant(req.rawHeaders, config, () =>
    handleRequestForTenant(req, config),
  );
}
