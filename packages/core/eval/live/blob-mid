# Legacy Warehouse API v2 — Full Endpoint Specification

This document specifies every endpoint. Implementations MUST conform exactly.

### orders.endpoint_1 (op #1)

**Path:** `/v2/orders/resource_1/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_1 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_1 (op #2)

**Path:** `/v2/inventory/resource_1/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_1 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_1 (op #3)

**Path:** `/v2/shipping/resource_1/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_1 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_1 (op #4)

**Path:** `/v2/billing/resource_1/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_1 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_1 (op #5)

**Path:** `/v2/returns/resource_1/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_1 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_1 (op #6)

**Path:** `/v2/pricing/resource_1/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_1 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_1 (op #7)

**Path:** `/v2/catalog/resource_1/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_1 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_1 (op #8)

**Path:** `/v2/auth/resource_1/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_1 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_1 (op #9)

**Path:** `/v2/audit/resource_1/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_1 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_1 (op #10)

**Path:** `/v2/webhooks/resource_1/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_1 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_1 (op #11)

**Path:** `/v2/reporting/resource_1/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_1 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_1 (op #12)

**Path:** `/v2/tax/resource_1/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_1 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_2 (op #13)

**Path:** `/v2/orders/resource_2/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_2 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_2 (op #14)

**Path:** `/v2/inventory/resource_2/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_2 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_2 (op #15)

**Path:** `/v2/shipping/resource_2/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_2 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_2 (op #16)

**Path:** `/v2/billing/resource_2/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_2 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_2 (op #17)

**Path:** `/v2/returns/resource_2/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_2 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_2 (op #18)

**Path:** `/v2/pricing/resource_2/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_2 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_2 (op #19)

**Path:** `/v2/catalog/resource_2/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_2 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_2 (op #20)

**Path:** `/v2/auth/resource_2/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_2 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_2 (op #21)

**Path:** `/v2/audit/resource_2/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_2 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_2 (op #22)

**Path:** `/v2/webhooks/resource_2/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_2 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_2 (op #23)

**Path:** `/v2/reporting/resource_2/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_2 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_2 (op #24)

**Path:** `/v2/tax/resource_2/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_2 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_3 (op #25)

**Path:** `/v2/orders/resource_3/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_3 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_3 (op #26)

**Path:** `/v2/inventory/resource_3/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_3 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_3 (op #27)

**Path:** `/v2/shipping/resource_3/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_3 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_3 (op #28)

**Path:** `/v2/billing/resource_3/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_3 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_3 (op #29)

**Path:** `/v2/returns/resource_3/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_3 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_3 (op #30)

**Path:** `/v2/pricing/resource_3/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_3 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_3 (op #31)

**Path:** `/v2/catalog/resource_3/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_3 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_3 (op #32)

**Path:** `/v2/auth/resource_3/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_3 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_3 (op #33)

**Path:** `/v2/audit/resource_3/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_3 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_3 (op #34)

**Path:** `/v2/webhooks/resource_3/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_3 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_3 (op #35)

**Path:** `/v2/reporting/resource_3/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_3 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_3 (op #36)

**Path:** `/v2/tax/resource_3/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_3 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_4 (op #37)

**Path:** `/v2/orders/resource_4/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_4 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_4 (op #38)

**Path:** `/v2/inventory/resource_4/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_4 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_4 (op #39)

**Path:** `/v2/shipping/resource_4/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_4 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_4 (op #40)

**Path:** `/v2/billing/resource_4/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_4 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_4 (op #41)

**Path:** `/v2/returns/resource_4/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_4 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_4 (op #42)

**Path:** `/v2/pricing/resource_4/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_4 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_4 (op #43)

**Path:** `/v2/catalog/resource_4/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_4 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_4 (op #44)

**Path:** `/v2/auth/resource_4/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_4 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_4 (op #45)

**Path:** `/v2/audit/resource_4/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_4 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_4 (op #46)

**Path:** `/v2/webhooks/resource_4/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_4 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_4 (op #47)

**Path:** `/v2/reporting/resource_4/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_4 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_4 (op #48)

**Path:** `/v2/tax/resource_4/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_4 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_5 (op #49)

**Path:** `/v2/orders/resource_5/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_5 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_5 (op #50)

**Path:** `/v2/inventory/resource_5/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_5 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_5 (op #51)

**Path:** `/v2/shipping/resource_5/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_5 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_5 (op #52)

**Path:** `/v2/billing/resource_5/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_5 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_5 (op #53)

**Path:** `/v2/returns/resource_5/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_5 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_5 (op #54)

**Path:** `/v2/pricing/resource_5/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_5 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_5 (op #55)

**Path:** `/v2/catalog/resource_5/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_5 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_5 (op #56)

**Path:** `/v2/auth/resource_5/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_5 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_5 (op #57)

**Path:** `/v2/audit/resource_5/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_5 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_5 (op #58)

**Path:** `/v2/webhooks/resource_5/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_5 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_5 (op #59)

**Path:** `/v2/reporting/resource_5/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_5 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_5 (op #60)

**Path:** `/v2/tax/resource_5/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_5 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_6 (op #61)

**Path:** `/v2/orders/resource_6/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_6 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_6 (op #62)

**Path:** `/v2/inventory/resource_6/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_6 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_6 (op #63)

**Path:** `/v2/shipping/resource_6/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_6 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_6 (op #64)

**Path:** `/v2/billing/resource_6/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_6 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_6 (op #65)

**Path:** `/v2/returns/resource_6/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_6 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_6 (op #66)

**Path:** `/v2/pricing/resource_6/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_6 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_6 (op #67)

**Path:** `/v2/catalog/resource_6/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_6 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_6 (op #68)

**Path:** `/v2/auth/resource_6/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_6 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_6 (op #69)

**Path:** `/v2/audit/resource_6/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_6 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_6 (op #70)

**Path:** `/v2/webhooks/resource_6/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_6 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_6 (op #71)

**Path:** `/v2/reporting/resource_6/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_6 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_6 (op #72)

**Path:** `/v2/tax/resource_6/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_6 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_7 (op #73)

**Path:** `/v2/orders/resource_7/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_7 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_7 (op #74)

**Path:** `/v2/inventory/resource_7/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_7 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_7 (op #75)

**Path:** `/v2/shipping/resource_7/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_7 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_7 (op #76)

**Path:** `/v2/billing/resource_7/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_7 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_7 (op #77)

**Path:** `/v2/returns/resource_7/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_7 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_7 (op #78)

**Path:** `/v2/pricing/resource_7/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_7 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_7 (op #79)

**Path:** `/v2/catalog/resource_7/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_7 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_7 (op #80)

**Path:** `/v2/auth/resource_7/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_7 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_7 (op #81)

**Path:** `/v2/audit/resource_7/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_7 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_7 (op #82)

**Path:** `/v2/webhooks/resource_7/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_7 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_7 (op #83)

**Path:** `/v2/reporting/resource_7/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_7 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_7 (op #84)

**Path:** `/v2/tax/resource_7/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_7 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_8 (op #85)

**Path:** `/v2/orders/resource_8/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_8 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_8 (op #86)

**Path:** `/v2/inventory/resource_8/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_8 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_8 (op #87)

**Path:** `/v2/shipping/resource_8/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_8 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_8 (op #88)

**Path:** `/v2/billing/resource_8/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_8 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_8 (op #89)

**Path:** `/v2/returns/resource_8/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_8 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_8 (op #90)

**Path:** `/v2/pricing/resource_8/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_8 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_8 (op #91)

**Path:** `/v2/catalog/resource_8/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_8 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_8 (op #92)

**Path:** `/v2/auth/resource_8/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_8 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_8 (op #93)

**Path:** `/v2/audit/resource_8/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_8 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_8 (op #94)

**Path:** `/v2/webhooks/resource_8/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_8 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_8 (op #95)

**Path:** `/v2/reporting/resource_8/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_8 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_8 (op #96)

**Path:** `/v2/tax/resource_8/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_8 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_9 (op #97)

**Path:** `/v2/orders/resource_9/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_9 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_9 (op #98)

**Path:** `/v2/inventory/resource_9/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_9 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_9 (op #99)

**Path:** `/v2/shipping/resource_9/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_9 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_9 (op #100)

**Path:** `/v2/billing/resource_9/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_9 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_9 (op #101)

**Path:** `/v2/returns/resource_9/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_9 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_9 (op #102)

**Path:** `/v2/pricing/resource_9/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_9 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_9 (op #103)

**Path:** `/v2/catalog/resource_9/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_9 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_9 (op #104)

**Path:** `/v2/auth/resource_9/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_9 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_9 (op #105)

**Path:** `/v2/audit/resource_9/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_9 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_9 (op #106)

**Path:** `/v2/webhooks/resource_9/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_9 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_9 (op #107)

**Path:** `/v2/reporting/resource_9/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_9 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_9 (op #108)

**Path:** `/v2/tax/resource_9/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_9 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_10 (op #109)

**Path:** `/v2/orders/resource_10/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_10 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_10 (op #110)

**Path:** `/v2/inventory/resource_10/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_10 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_10 (op #111)

**Path:** `/v2/shipping/resource_10/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_10 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_10 (op #112)

**Path:** `/v2/billing/resource_10/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_10 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_10 (op #113)

**Path:** `/v2/returns/resource_10/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_10 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_10 (op #114)

**Path:** `/v2/pricing/resource_10/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_10 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_10 (op #115)

**Path:** `/v2/catalog/resource_10/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_10 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_10 (op #116)

**Path:** `/v2/auth/resource_10/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_10 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_10 (op #117)

**Path:** `/v2/audit/resource_10/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_10 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_10 (op #118)

**Path:** `/v2/webhooks/resource_10/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_10 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_10 (op #119)

**Path:** `/v2/reporting/resource_10/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_10 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_10 (op #120)

**Path:** `/v2/tax/resource_10/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_10 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_11 (op #121)

**Path:** `/v2/orders/resource_11/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_11 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_11 (op #122)

**Path:** `/v2/inventory/resource_11/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_11 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_11 (op #123)

**Path:** `/v2/shipping/resource_11/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_11 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_11 (op #124)

**Path:** `/v2/billing/resource_11/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_11 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_11 (op #125)

**Path:** `/v2/returns/resource_11/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_11 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_11 (op #126)

**Path:** `/v2/pricing/resource_11/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_11 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_11 (op #127)

**Path:** `/v2/catalog/resource_11/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_11 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_11 (op #128)

**Path:** `/v2/auth/resource_11/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_11 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_11 (op #129)

**Path:** `/v2/audit/resource_11/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_11 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_11 (op #130)

**Path:** `/v2/webhooks/resource_11/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_11 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_11 (op #131)

**Path:** `/v2/reporting/resource_11/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_11 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_11 (op #132)

**Path:** `/v2/tax/resource_11/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_11 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_12 (op #133)

**Path:** `/v2/orders/resource_12/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_12 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_12 (op #134)

**Path:** `/v2/inventory/resource_12/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_12 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_12 (op #135)

**Path:** `/v2/shipping/resource_12/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_12 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_12 (op #136)

**Path:** `/v2/billing/resource_12/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_12 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_12 (op #137)

**Path:** `/v2/returns/resource_12/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_12 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_12 (op #138)

**Path:** `/v2/pricing/resource_12/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_12 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_12 (op #139)

**Path:** `/v2/catalog/resource_12/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_12 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_12 (op #140)

**Path:** `/v2/auth/resource_12/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_12 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_12 (op #141)

**Path:** `/v2/audit/resource_12/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_12 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_12 (op #142)

**Path:** `/v2/webhooks/resource_12/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_12 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_12 (op #143)

**Path:** `/v2/reporting/resource_12/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_12 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_12 (op #144)

**Path:** `/v2/tax/resource_12/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_12 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_13 (op #145)

**Path:** `/v2/orders/resource_13/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_13 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_13 (op #146)

**Path:** `/v2/inventory/resource_13/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_13 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_13 (op #147)

**Path:** `/v2/shipping/resource_13/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_13 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_13 (op #148)

**Path:** `/v2/billing/resource_13/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_13 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_13 (op #149)

**Path:** `/v2/returns/resource_13/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_13 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_13 (op #150)

**Path:** `/v2/pricing/resource_13/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_13 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_13 (op #151)

**Path:** `/v2/catalog/resource_13/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_13 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_13 (op #152)

**Path:** `/v2/auth/resource_13/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_13 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_13 (op #153)

**Path:** `/v2/audit/resource_13/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_13 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_13 (op #154)

**Path:** `/v2/webhooks/resource_13/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_13 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_13 (op #155)

**Path:** `/v2/reporting/resource_13/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_13 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_13 (op #156)

**Path:** `/v2/tax/resource_13/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_13 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_14 (op #157)

**Path:** `/v2/orders/resource_14/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_14 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_14 (op #158)

**Path:** `/v2/inventory/resource_14/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_14 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_14 (op #159)

**Path:** `/v2/shipping/resource_14/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_14 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_14 (op #160)

**Path:** `/v2/billing/resource_14/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_14 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_14 (op #161)

**Path:** `/v2/returns/resource_14/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_14 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_14 (op #162)

**Path:** `/v2/pricing/resource_14/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_14 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_14 (op #163)

**Path:** `/v2/catalog/resource_14/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_14 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_14 (op #164)

**Path:** `/v2/auth/resource_14/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_14 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_14 (op #165)

**Path:** `/v2/audit/resource_14/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_14 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_14 (op #166)

**Path:** `/v2/webhooks/resource_14/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_14 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_14 (op #167)

**Path:** `/v2/reporting/resource_14/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_14 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_14 (op #168)

**Path:** `/v2/tax/resource_14/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_14 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_15 (op #169)

**Path:** `/v2/orders/resource_15/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_15 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_15 (op #170)

**Path:** `/v2/inventory/resource_15/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_15 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_15 (op #171)

**Path:** `/v2/shipping/resource_15/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_15 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_15 (op #172)

**Path:** `/v2/billing/resource_15/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_15 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_15 (op #173)

**Path:** `/v2/returns/resource_15/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_15 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_15 (op #174)

**Path:** `/v2/pricing/resource_15/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_15 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_15 (op #175)

**Path:** `/v2/catalog/resource_15/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_15 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_15 (op #176)

**Path:** `/v2/auth/resource_15/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_15 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_15 (op #177)

**Path:** `/v2/audit/resource_15/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_15 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_15 (op #178)

**Path:** `/v2/webhooks/resource_15/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_15 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_15 (op #179)

**Path:** `/v2/reporting/resource_15/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_15 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_15 (op #180)

**Path:** `/v2/tax/resource_15/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_15 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_16 (op #181)

**Path:** `/v2/orders/resource_16/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_16 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_16 (op #182)

**Path:** `/v2/inventory/resource_16/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_16 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_16 (op #183)

**Path:** `/v2/shipping/resource_16/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_16 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_16 (op #184)

**Path:** `/v2/billing/resource_16/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_16 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_16 (op #185)

**Path:** `/v2/returns/resource_16/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_16 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_16 (op #186)

**Path:** `/v2/pricing/resource_16/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_16 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_16 (op #187)

**Path:** `/v2/catalog/resource_16/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_16 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_16 (op #188)

**Path:** `/v2/auth/resource_16/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_16 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_16 (op #189)

**Path:** `/v2/audit/resource_16/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_16 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_16 (op #190)

**Path:** `/v2/webhooks/resource_16/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_16 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_16 (op #191)

**Path:** `/v2/reporting/resource_16/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_16 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_16 (op #192)

**Path:** `/v2/tax/resource_16/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_16 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_17 (op #193)

**Path:** `/v2/orders/resource_17/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_17 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_17 (op #194)

**Path:** `/v2/inventory/resource_17/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_17 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_17 (op #195)

**Path:** `/v2/shipping/resource_17/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_17 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_17 (op #196)

**Path:** `/v2/billing/resource_17/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_17 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_17 (op #197)

**Path:** `/v2/returns/resource_17/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_17 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_17 (op #198)

**Path:** `/v2/pricing/resource_17/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_17 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_17 (op #199)

**Path:** `/v2/catalog/resource_17/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_17 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_17 (op #200)

**Path:** `/v2/auth/resource_17/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_17 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_17 (op #201)

**Path:** `/v2/audit/resource_17/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_17 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_17 (op #202)

**Path:** `/v2/webhooks/resource_17/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_17 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_17 (op #203)

**Path:** `/v2/reporting/resource_17/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_17 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_17 (op #204)

**Path:** `/v2/tax/resource_17/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_17 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_18 (op #205)

**Path:** `/v2/orders/resource_18/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_18 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_18 (op #206)

**Path:** `/v2/inventory/resource_18/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_18 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_18 (op #207)

**Path:** `/v2/shipping/resource_18/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_18 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_18 (op #208)

**Path:** `/v2/billing/resource_18/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_18 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_18 (op #209)

**Path:** `/v2/returns/resource_18/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_18 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_18 (op #210)

**Path:** `/v2/pricing/resource_18/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_18 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_18 (op #211)

**Path:** `/v2/catalog/resource_18/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_18 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_18 (op #212)

**Path:** `/v2/auth/resource_18/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_18 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_18 (op #213)

**Path:** `/v2/audit/resource_18/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_18 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_18 (op #214)

**Path:** `/v2/webhooks/resource_18/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_18 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_18 (op #215)

**Path:** `/v2/reporting/resource_18/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_18 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_18 (op #216)

**Path:** `/v2/tax/resource_18/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_18 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_19 (op #217)

**Path:** `/v2/orders/resource_19/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_19 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_19 (op #218)

**Path:** `/v2/inventory/resource_19/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_19 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_19 (op #219)

**Path:** `/v2/shipping/resource_19/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_19 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_19 (op #220)

**Path:** `/v2/billing/resource_19/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_19 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_19 (op #221)

**Path:** `/v2/returns/resource_19/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_19 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_19 (op #222)

**Path:** `/v2/pricing/resource_19/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_19 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_19 (op #223)

**Path:** `/v2/catalog/resource_19/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_19 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_19 (op #224)

**Path:** `/v2/auth/resource_19/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_19 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_19 (op #225)

**Path:** `/v2/audit/resource_19/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_19 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_19 (op #226)

**Path:** `/v2/webhooks/resource_19/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_19 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_19 (op #227)

**Path:** `/v2/reporting/resource_19/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_19 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_19 (op #228)

**Path:** `/v2/tax/resource_19/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_19 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_20 (op #229)

**Path:** `/v2/orders/resource_20/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_20 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_20 (op #230)

**Path:** `/v2/inventory/resource_20/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_20 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_20 (op #231)

**Path:** `/v2/shipping/resource_20/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_20 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_20 (op #232)

**Path:** `/v2/billing/resource_20/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_20 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_20 (op #233)

**Path:** `/v2/returns/resource_20/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_20 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_20 (op #234)

**Path:** `/v2/pricing/resource_20/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_20 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_20 (op #235)

**Path:** `/v2/catalog/resource_20/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_20 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_20 (op #236)

**Path:** `/v2/auth/resource_20/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_20 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_20 (op #237)

**Path:** `/v2/audit/resource_20/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_20 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_20 (op #238)

**Path:** `/v2/webhooks/resource_20/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_20 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_20 (op #239)

**Path:** `/v2/reporting/resource_20/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_20 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_20 (op #240)

**Path:** `/v2/tax/resource_20/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_20 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_21 (op #241)

**Path:** `/v2/orders/resource_21/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_21 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_21 (op #242)

**Path:** `/v2/inventory/resource_21/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_21 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_21 (op #243)

**Path:** `/v2/shipping/resource_21/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_21 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_21 (op #244)

**Path:** `/v2/billing/resource_21/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_21 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_21 (op #245)

**Path:** `/v2/returns/resource_21/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_21 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_21 (op #246)

**Path:** `/v2/pricing/resource_21/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_21 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_21 (op #247)

**Path:** `/v2/catalog/resource_21/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_21 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_21 (op #248)

**Path:** `/v2/auth/resource_21/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_21 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_21 (op #249)

**Path:** `/v2/audit/resource_21/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_21 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_21 (op #250)

**Path:** `/v2/webhooks/resource_21/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_21 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_21 (op #251)

**Path:** `/v2/reporting/resource_21/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_21 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_21 (op #252)

**Path:** `/v2/tax/resource_21/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_21 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_22 (op #253)

**Path:** `/v2/orders/resource_22/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_22 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_22 (op #254)

**Path:** `/v2/inventory/resource_22/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_22 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_22 (op #255)

**Path:** `/v2/shipping/resource_22/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_22 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_22 (op #256)

**Path:** `/v2/billing/resource_22/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_22 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_22 (op #257)

**Path:** `/v2/returns/resource_22/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_22 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_22 (op #258)

**Path:** `/v2/pricing/resource_22/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_22 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_22 (op #259)

**Path:** `/v2/catalog/resource_22/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_22 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_22 (op #260)

**Path:** `/v2/auth/resource_22/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_22 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_22 (op #261)

**Path:** `/v2/audit/resource_22/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_22 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_22 (op #262)

**Path:** `/v2/webhooks/resource_22/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_22 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_22 (op #263)

**Path:** `/v2/reporting/resource_22/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_22 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_22 (op #264)

**Path:** `/v2/tax/resource_22/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_22 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_23 (op #265)

**Path:** `/v2/orders/resource_23/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_23 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_23 (op #266)

**Path:** `/v2/inventory/resource_23/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_23 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_23 (op #267)

**Path:** `/v2/shipping/resource_23/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_23 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_23 (op #268)

**Path:** `/v2/billing/resource_23/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_23 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_23 (op #269)

**Path:** `/v2/returns/resource_23/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_23 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_23 (op #270)

**Path:** `/v2/pricing/resource_23/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_23 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_23 (op #271)

**Path:** `/v2/catalog/resource_23/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_23 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_23 (op #272)

**Path:** `/v2/auth/resource_23/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_23 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_23 (op #273)

**Path:** `/v2/audit/resource_23/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_23 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_23 (op #274)

**Path:** `/v2/webhooks/resource_23/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_23 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_23 (op #275)

**Path:** `/v2/reporting/resource_23/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_23 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_23 (op #276)

**Path:** `/v2/tax/resource_23/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_23 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_24 (op #277)

**Path:** `/v2/orders/resource_24/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_24 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_24 (op #278)

**Path:** `/v2/inventory/resource_24/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_24 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_24 (op #279)

**Path:** `/v2/shipping/resource_24/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_24 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_24 (op #280)

**Path:** `/v2/billing/resource_24/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_24 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_24 (op #281)

**Path:** `/v2/returns/resource_24/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_24 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_24 (op #282)

**Path:** `/v2/pricing/resource_24/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_24 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_24 (op #283)

**Path:** `/v2/catalog/resource_24/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_24 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_24 (op #284)

**Path:** `/v2/auth/resource_24/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_24 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_24 (op #285)

**Path:** `/v2/audit/resource_24/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_24 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_24 (op #286)

**Path:** `/v2/webhooks/resource_24/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_24 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_24 (op #287)

**Path:** `/v2/reporting/resource_24/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_24 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_24 (op #288)

**Path:** `/v2/tax/resource_24/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_24 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_25 (op #289)

**Path:** `/v2/orders/resource_25/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_25 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_25 (op #290)

**Path:** `/v2/inventory/resource_25/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_25 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_25 (op #291)

**Path:** `/v2/shipping/resource_25/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_25 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_25 (op #292)

**Path:** `/v2/billing/resource_25/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_25 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_25 (op #293)

**Path:** `/v2/returns/resource_25/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_25 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_25 (op #294)

**Path:** `/v2/pricing/resource_25/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_25 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_25 (op #295)

**Path:** `/v2/catalog/resource_25/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_25 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_25 (op #296)

**Path:** `/v2/auth/resource_25/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_25 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_25 (op #297)

**Path:** `/v2/audit/resource_25/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_25 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_25 (op #298)

**Path:** `/v2/webhooks/resource_25/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_25 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_25 (op #299)

**Path:** `/v2/reporting/resource_25/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_25 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_25 (op #300)

**Path:** `/v2/tax/resource_25/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_25 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_26 (op #301)

**Path:** `/v2/orders/resource_26/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_26 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_26 (op #302)

**Path:** `/v2/inventory/resource_26/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_26 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_26 (op #303)

**Path:** `/v2/shipping/resource_26/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_26 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_26 (op #304)

**Path:** `/v2/billing/resource_26/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_26 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_26 (op #305)

**Path:** `/v2/returns/resource_26/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_26 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_26 (op #306)

**Path:** `/v2/pricing/resource_26/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_26 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_26 (op #307)

**Path:** `/v2/catalog/resource_26/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_26 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_26 (op #308)

**Path:** `/v2/auth/resource_26/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_26 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_26 (op #309)

**Path:** `/v2/audit/resource_26/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_26 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_26 (op #310)

**Path:** `/v2/webhooks/resource_26/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_26 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_26 (op #311)

**Path:** `/v2/reporting/resource_26/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_26 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_26 (op #312)

**Path:** `/v2/tax/resource_26/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_26 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_27 (op #313)

**Path:** `/v2/orders/resource_27/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_27 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_27 (op #314)

**Path:** `/v2/inventory/resource_27/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_27 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_27 (op #315)

**Path:** `/v2/shipping/resource_27/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_27 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_27 (op #316)

**Path:** `/v2/billing/resource_27/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_27 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_27 (op #317)

**Path:** `/v2/returns/resource_27/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_27 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_27 (op #318)

**Path:** `/v2/pricing/resource_27/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_27 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_27 (op #319)

**Path:** `/v2/catalog/resource_27/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_27 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_27 (op #320)

**Path:** `/v2/auth/resource_27/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_27 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_27 (op #321)

**Path:** `/v2/audit/resource_27/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_27 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_27 (op #322)

**Path:** `/v2/webhooks/resource_27/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_27 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_27 (op #323)

**Path:** `/v2/reporting/resource_27/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_27 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_27 (op #324)

**Path:** `/v2/tax/resource_27/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_27 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_28 (op #325)

**Path:** `/v2/orders/resource_28/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_28 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_28 (op #326)

**Path:** `/v2/inventory/resource_28/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_28 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_28 (op #327)

**Path:** `/v2/shipping/resource_28/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_28 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_28 (op #328)

**Path:** `/v2/billing/resource_28/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_28 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_28 (op #329)

**Path:** `/v2/returns/resource_28/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_28 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### pricing.endpoint_28 (op #330)

**Path:** `/v2/pricing/resource_28/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the pricing resource_28 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the pricing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### catalog.endpoint_28 (op #331)

**Path:** `/v2/catalog/resource_28/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the catalog resource_28 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the catalog schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### auth.endpoint_28 (op #332)

**Path:** `/v2/auth/resource_28/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the auth resource_28 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the auth schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### audit.endpoint_28 (op #333)

**Path:** `/v2/audit/resource_28/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the audit resource_28 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the audit schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### webhooks.endpoint_28 (op #334)

**Path:** `/v2/webhooks/resource_28/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the webhooks resource_28 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the webhooks schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### reporting.endpoint_28 (op #335)

**Path:** `/v2/reporting/resource_28/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the reporting resource_28 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the reporting schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### tax.endpoint_28 (op #336)

**Path:** `/v2/tax/resource_28/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the tax resource_28 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the tax schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### orders.endpoint_29 (op #337)

**Path:** `/v2/orders/resource_29/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the orders resource_29 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the orders schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### inventory.endpoint_29 (op #338)

**Path:** `/v2/inventory/resource_29/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the inventory resource_29 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the inventory schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### shipping.endpoint_29 (op #339)

**Path:** `/v2/shipping/resource_29/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the shipping resource_29 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the shipping schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### billing.endpoint_29 (op #340)

**Path:** `/v2/billing/resource_29/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the billing resource_29 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the billing schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.

### returns.endpoint_29 (op #341)

**Path:** `/v2/returns/resource_29/{id}`
**Methods:** GET, POST, PUT, PATCH, DELETE

Request validation: the returns resource_29 payload must include a stable client request-id for idempotency, an integer cents amount for any monetary field, and integer quantities. All timestamps are UTC ISO-8601 and produced by the shared clock helper. The handler validates the payload against the returns schema, enforces the per-resource invariants, writes an audit row capturing actor/action/at, and returns the persisted representation. On retry with the same request-id the operation is idempotent and returns the original result without duplicating side effects. Concurrent writers are serialized per-row; readers observe a consistent snapshot. Error responses use the shared error envelope with a machine-readable code and a human-readable message. Rate limits apply per client per minute. Pagination is keyset-based on (created_at, id). Soft-deletes set a tombstone and are excluded from default listings but retained for audit and returns processing.
